---
title: "2026-09-23 · System Programming · 보정 STT"
course: system_programming
date: 2026-09-23
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
date_confidence: user_confirmed
---

사용자가 2026-09-23 수업으로 확인한 녹음입니다. 파일의 날짜 메타데이터를 독립 확인한 것은 아닙니다.

명확한 전문용어 오류를 보정한 공개 STT 사본입니다. 불명확한 말은 원문 표현 또는 `[[unclear: …]]`로 남겼으며, 전체 음성의 완전 복원이나 누락 없음이 확인된 자료는 아닙니다. 부정확하거나 뜻이 통하지 않는 발화는 검증된 개념 설명과 구분해 읽으세요.

개인정보 검토를 거친 공개용 사본이며 시간·화자 순서를 유지했습니다. 교수자·수강생의 예시, 의견과 추측은 검증된 사실이나 현재 공식 규정과 구분해 읽으세요.

보정 원본 SHA-256: `b40efab78ff42f063367e1f9d0f0fdefb4e04d3d28f31979def34ee47866adbd`

````text
00:09 화자 1
Thank you.

00:31 화자 2
Okay.

00:50 화자 1
So Thank you.

01:32 화자 2
Okay, let's get started. So we will have a one hour lecture followed by assignment number two, an explanation of the assignment number two by CA. So today's topic is about memory abstraction. We are starting, we are done with the file system and then we are moving into memory abstraction, virtual memory. And then as a first step, we.

02:06 화자 1
What?

02:08 화자 2
Are and what memory is. So some examples. Okay, I just updated Reboot. And there was memory pointers. We reviewed again about memory allocation, data types, alignment, and composite data structures like arrays, structures, and final So where do functions and variables go? So which memory address do they take up? So look at the code. It's not the entire code.

02:54 화자 1
Okay.

02:55 화자 2
So we have global variables, right? Global ints, global array of size of n, and its macro here. And then pointer, global pointer. Then a structure that has two fields. The first one is m. The semaphore, we have a semaphore to use for locking. So if multiple processor threads are sharing the same variable, you may want to sometimes lock the semaphore. And then shared int. So why is things here shared int? Because we want to run two processes.

03:43 화자 1
You.

03:43 화자 2
That will share this variable together. I'm going to try to implement it. Then there's a function whose name is foo, then there's a function whose name is bar, and then main function. And then inside main function we have local variables, local array, local k, k local v. And then, what you want is, we want to print out your address Not just variables, but just the function itself. Function itself has address. Function eventually, right, function is placed in memory. Function is part of code. And then what you actually do is that you go to the database and then read one instruction at a time. And then execute it. That's it.

04:42 화자 2
So we, what is the first thing? So percent d is PID. PID is process ID. The program that you're running is called process. This process is unique integer ID on the machine, on the OS. The ID is an identified process. And then, to to r function thread. And then, yeah, we can apply & operator to get the address. So we want to print out what the address of foo, in memory address foo, r and v. Actually, even without an ampersand, it should work.

05:36 화자 2
So when you use function name, foo is a function, a type -detected function, but then when you actually use it, it decays to a function. It's like array. So you have array name, right? But when you use it, that's the address of the first element. That's the decay. Or radically typecasted. So actually, ampersand is not needed, but when you use ampersand, this is actually function, okay, functions address, function. And then, you know this, right, the global int, global array, global pointers, the addresses, and then all local variables, that global PTR, global PTR point is solved. And then we share the variable. Share, what is share? Share is a pointer to a structure.

06:37 화자 2
Size of shared is 8 bytes. But the size of star shared is what? Size of all these, right? And then what? What do you want is we want the address of shared int. Okay? Remember variable inside structure. When you run this on an older little store that does not have address space layout randomization. What do you want? So address space layout randomization, in short, ASLR. That changes the virtual memory address every time you run it. But the older Linux doesn't do that. Or you can actually disable it.

07:36 화자 2
Then every time you run it, you actually see the same addresses each time you run it. The address is fixed. But then with ASLR, so the text slide actually has ASLR. the addresses I think are similar but if you look at the higher address part, you see? If you look at, yeah, you guys add in it It doesn't have that EA, right? Why? Why do you use ASLR? Because if you know that a circuit function or some variables always has fixed address, then it's vulnerable to attack.

08:34 화자 2
I saw it does not provide anything, but then if you find some other vulnerability, you can use it to call that function. If there is no code flow, maybe you can override it to return address to the specific permission that you want. Of course, if you have the source code, then you know exactly what happens. So that can be exploited to launch the attack. they don't actually introduce ASLR. So whenever you rerun it, actual virtual memory address changes each time. So that, such a fact.

09:18 화자 2
Anyway, so this, the output, This is not very surprising because all of that, yes, we are running this program on 64 -bit Linux environment. So all addresses are 64 -bit. But then, we haven't shown the entire code yet, but then this shared structure, which is shared by multiple processes. Two processes here. So the leftmost number is PID, process ID. So same process ID, the same code. So we see two programs are running at the same time concurrently.

10:15 화자 2
And then, if you see here, int is a global variable that is not shared. So even if you run another process of the same program, They have different memory numbers. Global int for process 1 and global int for process 2 are completely different. Even if you increment global int itself, it doesn't increment the other global int on another process. That makes sense, right? Because each process is independent. But then, we can do IPC inter-process communication, so you can have shared memory section. So you can have shared memory together with multiple processes.

11:09 화자 2
Then, if you increment the, for example, shared int variable in this structure, then that is repeatable to the other process. That's what we want to say here. Okay? So here what's interesting is that even if the memory addresses itself are the same So the global int, this one, this one, same right? Even if the memory addresses itself are the same That's virtual memory. That has a meaning only inside one process. So the memory address, for example, 10 in one process, memory address 10 in another process, they are different. They have different physical.

12:08 화자 2
So even if these are the same, they are different. But then, shared int, they have the same address, right? If you look at the number, if you increment the number 1 by 1, it is actually consistent over two processes. If you see here, it was 0 and 1, 2. So the process is 7, 4, right? Had the little 2, but then next time, 7, 5 runs is still 1 because this guy and this guy are independent. They are not sharing. But then you can hide the shared data.

13:08 화자 2
Alright. Let's move on. Variables and memory. Actually we will get back to the source code next lecture. So, in order to define a variable, you have a type and a name, right? Let's see. int counter. So, counter is integer variable. The compiler allocates memory somewhere in the process' memory space to hold a variable. The size of allocated memory is size of type. Size of int form is reserved, allocated for the variable counter. It creates a label. name that allows us to conveniently reference this data. For example, if you have A5, B7, and C.

14:01 화자 2
So we have three variables, A address 16, B address 20, C address 24, that are allocated by compiler. Usually continuous, but not guaranteed. If they are a, b, c, r, continuous, right? Then usually they are sequential. address is sequential but not guaranteed. and then actually when you use A here, you read the memory, address 16. you go to the memory 16 and this is A is 4 bytes, right? If you read 4 bytes from there, and then do the operation. So typically you read up to the register and then do the operation. So all the operations, right, typically done using the registers. You cannot do memory to memory operations directly.

15:07 화자 2
Where is the LK11? So we have three global variables, but that one is constant variable, constant in the B itself. We cannot change over time. The foo, what is a foo? It's a function. Foo is not data. The content of foo, when you compile it, right, that goes to the code, right? So if you look at the layer, we actually revisit this layer later, or the error. the memory layer of the processor. Then it is divided into many segments or sections. Then some portion of it that contains only read -only data.

16:05 화자 2
So read -only data. Read -only part, that is text, the same as code. Text section, text segment, code section, same thing. And that contains the instructions. utilize a variable are all data read only data so global variables like b it's read only that is typically assigned here or if you have like let's say printf hello world it's string it's string is what read only printf, double quotes, and a hello world. This string itself is a string literal, a string constant, and it does not change over time.

17:04 화자 2
So it's placed here. And then there's a part that I read over it. So readable and writeable. By the way, the memory itself is the same. So this is just a conceptual understanding. But the D -RAM itself, the functionality of D -RAM is the same. You can write with it and write. It's not, yeah, they're broken, but we're talking about the RAM. Rather than just memory or D -RAM. That's the memory that we run the program on. And then the physical memory itself does not understand anything about this. This is completely loaded with one, completely OS mind. But then we just logically, it should not change. If this is changing, that's a problem. That's why we set it here.

18:01 화자 2
And then when you try to access, try to write this area, What happens? It's bad. We promise not to do that, right? And then someone tries to change the memory there. Although physically you can change it, but then logically you cannot change it. So the operating system will see the access. And then with the help of CPU, it can catch this access and then kill the process. So, data. So read /write data sections. Readable as well as writeable. And then global variables like A and C are placed here. And then A, so there are two sections, data section and BSS section.

18:57 화자 2
Data section contains all the variables, global variables with initializer. For example, A has initializer 5, right? C does not have initializer, right? And C is in BSS. Okay? The reason why there is a division between data and BSS is because you can optimize BSS. Let's say, it is just one variable, don't worry. But let's say you have a huge array that's not initialized yet. Then you don't have to have the data in the file itself. Actually, this one, some of the numbers should be inside the executable tip. How do you implement the initializing? So when you compile it, you have an executable a .app, right?

19:54 화자 2
So the a, someone has to remember that a has an initialized file, b has an initialized set, right? Then it has to be written. to the pilot's sub, right? The executor. C, it does not have the initial value. So global variables, you need to only worry about global variables. Why? Because local variables are initialized as you go. So you don't have to worry about it. But then global variables, the number should be The initialize should be written here, but the BSS, it doesn't have to be. Then you can save the disk space. But this is not, because we are living in the world of terabytes of storage. But it used to be, the earlier program environment wasn't very good.

20:52 화자 2
The software was very expensive too, but they wanted to save me some of the amount. with BSS. And then, when you call malloc, if you dynamically malloc the memory, memory is typically located Then there's a brk. That's the end of the heap memory. So typically, small memory allocation. If you allocate 4 bytes, 8 bytes, right? And that is here. If you allocate 1 megabyte, if you allocate 1 gigabyte, that's actually located in some other places. It's also called here. And then you can map libraries.

21:47 화자 2
Also, if you look at the path of the memory address, that is interesting. Function runs whenever you want a function, you need to store the local variables, right? And those are stored in this stack memory. Stack grows from higher memory address to lower memory address in Intel CPU. Let's say this is CCC. So I explained it in detail, but we will actually probably have another chance to explain it in more detail. So we have global variables, local variables, and then dynamically allocate the data. The N is that it calls null. It combines. And then that's placed in here. So it is just a snake.

22:48 화자 2
Again, from the perspective here, every place is the same. What about the function parameters? So how are the function parameters, where are the function parameters stored? The same as local variables. They are stored in the stack. Let's move on to pointers. Variable names are aliases to memory addresses. Eventually, if you don't remember addresses, you can just use it directly. But there are people, we don't want to remember the address. So it's the same as IP address. We don't want to remember IP address for www .cda .com. So we have name. Same thing. and you do not know what the address is directly, pointers are explicit memory addresses.

23:44 화자 2
So you have a variable that contains addresses. If you need to allocate memory, dynamically or want to know where memory and memory is, you can use the pointer. Pointers are used to refer to arrays. Actually, this is the review again. We have abc, ap, port2, ap. I'll allocate abc of the memory. then address is 16, 20, 24, AP points to 8, 8, 8 itself, 8 itself has address of 28, but then AP value itself is what? Value is 16. Then APP can point to the pointer. APP has the address of APP's address. What is the address of the address?

24:43 화자 2
The address is 28, so the value of APP is 28. Then APP's address is 28. dynamic arrays A is address c1a4. so ffff c1a4. then we call So it allocates how many bytes? 40 kilobytes, right? 4? So I can make this 4 bytes and then 1024 kilobytes. And then it allocates memory and then it returns the first byte address of allocated memory. That A is affiliated with data. So what? That is 565500.

25:42 화자 2
That is allocated memory is first byte address. I'm not going to raise my fingers to open confusion between the two winners. I think we all know this though. P, we are allocated by flowlines. Then P is a pointer to the data of P, 512. Actually, P0 refers to the first element of array P. And then M P0 over P something. And also the first element of P and its eigenvalue P. So, static Typically allocated arrays are also treated as countries of wealth, high wealth, So, buf is an array of size 512. buf is pointing to the array of data of buf 512. buf 0 refers to the first element, and buf 0 is the same as buf, it points to the first element.

26:45 화자 2
Handling of arrays and dynamically allocated arrays versus statically allocated arrays. So let's look at the main function. The buff is initialized grid with three characters, 80. So if you do this, what is the buff? Zero. Character 80. What is buf[3]? It's not defined. Raise your hand. It's not defined. Let's say I want to print out the value of bof0123. Is it a random value or is it a random value? is some fixed value. Random value or regular? Fixed value or regular? Okay, what value?

27:45 화자 2
- Null. - Null. Zero, zero. Null is a pointer. Zero. So when the... this is the initialization, right? And then it's not enough to fill, 5 kilobytes, right? Remaining bytes are filled with 0. Not random. So we print out buf is sum and the buf0 character and then buf0 address. And then of course this guy, this guy should be the same and then buf0 is a. What is p? p we allocate by 12 bytes. And then 0 wants to be initialized in a specific way, a, b, c. What about p3? Is it random or is it 0? Random or general?

28:44 화자 2
0? Not 0, it's random. If you want to make it 0, you need to call calloc. Then you have P N P0, say P0, you have A. And then when you're done with memory, then you should call free, otherwise memory will be leaked. But then, if you finish the process, OS will keep track of all these resources that are not allocated, deallocated, and that OS is responsible for releasing all those images. All the OS does, didn't do that. So I probably don't have memory for that.

29:40 화자 2
So it did create a problem, but that Unix does care. What about 3 FD off side of the board? So we want to read up to 512 bytes from the file descriptor FD. Then, what is this? Is this good? FD, buff, instead of size of buff, we use 512 times character. So we specify the size of the array. So can we replace the width with size of buff? He said no. Why? Size of buff itself is not an array. Buff is a pointer, so size of buff is just 8 bytes.

30:40 화자 2
What about this? Another thing is that instead of using barefoot numbers, it's a good practice to use define. It involves both sides. That's good. What about single character? I want to reach a single character. Then, buf is just character type, not pointer type. So you should, the second parameter should be a pointer type. You should pass the address, right? So you should use ampersand. But even if you don't use ampersand, it will still compile. But GCC 800 will not. elements, but that GCC, okay? So if you want to, you don't want it. Why?

31:35 화자 2
Because buf might have a valid address, and that C compiler assumes you are expert. No, you don't. You don't. So it must work, right? So it provides a nice code. Alignment. Integer data is typically 1 byte, 2 bytes, 4 bytes. Character is 1 byte. Short is 2 bytes. Integer is 4 bytes. Then long is 8 bytes. Data values. It has data values in the process.

32:17 화자 2
14 .48 mostly 4x28 bytes float or double 10x16 bytes this used to be the case and it still supports Unless you need it, you don't need to use the devices, right? Then, vector data types, arrays and structures. No aggregate types such as arrays or structures. So, array is just the multiple elements of the same type. Structure can have multiple heterogeneous types of data. So, what's the size of type?

33:16 화자 2
So if you look at character short int long, at least you need to know, say 64 -int. So this is a little bit complex. It also reflects history. But then let's look at the characteristics. It's just easy. What might, regardless of platform, be short int also feel much more better. C standard does not say int should be format. No, int size should be larger than short size. As long as that is true, it conforms to C. It's very simple. But then in real implementation, we have to show int. these are fixed across all platforms. Long used to be 4 bytes when the CPU was 30 bits.

34:15 화자 2
Or even CPU with 64 bits, if your OS is 32 bits, or your program is comparable to 32 -bit function call, then long -sized work. But then we are living in 64 -bit OS, right? So it's 8 bytes. Long, long. was there because in 32 -bit OS, I wanted to use 8 bytes in the future. So they introduced long long. But then, even in 64 -bit, it's long long, so you don't use it in 64 -bit. So long is okay. But then if you actually look at the C runtime libraries, there is some function that uses long long. So it's confusing, right?

35:14 화자 2
But you don't have to introduce log1. In your code, it is just log1. But then, if your code has to run regardless of the platform, so you need to write one code that has to run on 32 bits CPU as well, then maybe you can use log1. But don't worry too much about it. Float double, these are standard. IEEE 754 specification. 4 bytes, 8 bytes. Regardless of the... Regardless of the platforms, long double. Confused. In the earlier history of CPU, CPU only supported integer operations. And then in order to support floating point operations, you need to have a separate processor or coprocessor.

36:13 화자 2
But that's combined together these days. That's called x87. And then that guy used to use 8 bits for floating point or 10 bytes for floating point. So if you wanted to use the original space Because a double is only 8 bytes, right? If you want to use entire 10 bytes, you could use long double. But then it takes up 10 bytes, but then the actual number is 10 bytes, but then 6 bytes are padding. So if you see the sign, it's 16 bytes. Or the alignment will talk about later in this. Don't worry about this, OK? So this is a historic effect.

37:11 화자 2
And then if you do see this type later in the fifth wave, you can look it up. But what should be memorized, what should be internalized? Character short, int, long, and float double. And then pointer, pointer size is okay, the address size, 32 bit, 64 bit, and then there is a 16 byte integer, that's non -standard, it's implemented in software, So yeah, these are less and less popular. It's still available. If you want to use it, you can use it. But don't worry too much about it. Alignment data. Rules, what is alignment? Rules of, rules regarding the location of data.

38:04 화자 2
The first byte address The rules regarding the location of data in the memory of a computer system. Required of some machines, advised on IA33, 32, and 64. Also advised on x86-64. What do you mean by advised? If you do not or bike lane, right?

38:39 화자 2
There'd be skills But that's not efficient because memory is accessed in the unit of certain size and then if you go beyond that boundary then many complications may arise For example, an atomic write is not possible because you can do two writes instead of one write the memory alignment does not match. And then, yes, because this is an extra instruction, it slows down. So it's good, actually, it's good to keep this one for performance, for atomicity, for other things.

39:37 화자 2
There are some CPUs like MIPS, I'm not sure if you've heard of MIPS. If you look at the electronic devices, they're not using Intel CPUs. They're using very small, very efficient, power efficient chip called MIPS. MIPS required. So if it does not follow this alignment, it just crashes. MIPS and then SPARC, older ARM, recent one is okay. But that also Power, have you heard of Power? Power used to be, you're using Apple, right? Power was the main incentive for Apple. But they switched to Intel in 2005.

40:32 화자 2
[개인적인 근무·인턴 경험을 언급한 부분 비공개] Power required is the line. So that's why the sound of machines, okay. The device on, I... Another thing is that it's also, not just the CPU, so that's a physical thing, right? Access to memory. But then, when you compile the binary, it has all the assembly language code. So, assembly language code, They have their own role for passing the parameters or allocating the local memory. So each OS has their own convention.

41:29 화자 2
So Linux, 32 -bit Linux, 64 -bit Linux, Windows are all different. They are mostly similar, but there are some differences. So focus on x86 64 -bit Linux. I'm not assuming that you're memorizing all of this. Basic rule, primitive data type requires k bytes. If the data type requires k bytes, address must be k -byte -aligned, which means that the first byte address of the variable should be divisible by k. If it's integer, the address should be divisible by 4. If it's a double, the first byte address should be divisible by 8. Okay? Easy, right? K-byte aligned, address divisible by K. Example, for my application, it must be a located address divisible by 4. 0, 4, 8, 12, 16.

42:29 화자 2
These are right allocation that follows the alignment rule. Equal address like this. motivation for aligning data memory access by chunks of 4 bytes to 8 bytes. Inefficient loader for data is in multiple word context. It's possible from the point of view of logic software, but then it's not automatic. It requires multiple instructions. It's not efficient. Then also we learn virtual memory later, but then it may span over multiple pages. That's very good. Then it needs to translate the virtual memory address to physical memory address twice.

43:27 화자 2
So we will actually see that later. Okay, 64 -bit, x86-64 data alignment convention. This rule, again, primitive data type of K bytes should be K bytes aligned. There's one exception, which is long double, which is 10 bytes, size of 10 bytes, but then it's 16 bytes aligned. Why? Because 16 bytes are what? The peg. Okay, but don't worry about this exception. We're not going to use long, long double. So Linux and Windows have the same rule. 1 byte, 2 byte, 4 byte, 8 small, 4 byte, and then 8 byte long. Long, long, long, star, double. And then 16 byte.

44:25 화자 2
This is an old story, 32 bits OS. Again same basic rule and 10 byte primitive type, 4 byte aligned. Long double, long double, long double. 4 byte aligned. long double, 4-byte aligned in Linux and Windows. And 8-byte primitive types. 8-byte primitive types in long long, right? That's 4-byte aligned on Linux, but 8-byte aligned on Windows. Very complex. One more, it's much better. You can look it up. So you just remember there's some complexity here. So if you do not conform to this, you cannot call the already compiled library because they assume this.

45:24 화자 2
Otherwise it will break. Okay, let's look at this code. What does it do? So in the main function, it prints out the title, and then it prints out information about each type of character, short, blah blah, right? And then what is the info character? So go to info definition. It has structure. Inside one structure, it has two fields One is dummy, the other is data And data has a type of what? Vista character churn, etc. T is a cabinet. And then S is a variable, maybe, of this structure.

46:17 화자 2
And then it brings up what three things, type name, #t. #t makes this parameter So you print out the type name first, T. And then size T, size T, the size of T, size of, character size of, short, et cetera. And then what is OFS? Look at OFS. OFS has the parameter V in the defined language. And then what it does is that it extract the address of the second field and then minus the V, so V is what? S, so here the entire structure structure's address is what?

47:15 화자 2
The address of the first field so that is the what? The address of dummy and address of data difference difference of address data and address data do you understand depending on different type so if you use a macro you can substantially reduce the amount of code for or very repeated pattern but yes it's difficult to recognize it, understand it but then if you just follow all at once it's not too hard So here let's say info character is extended to lines.

48:09 화자 2
You have this structure Okay, dummy and character and print, three things. One is character string itself, the size of character, and then difference between two fields address. Address of two fields, okay. So why do we do this? Because inside one structure you have one byte character, dummy. But then next, field is different type. It can have a character, but then you can have a long type. But then if you follow the alignment rule, the long's address should be divisible by 8 in 64 -bit OS.

49:08 화자 2
Then there should be some gap between characters and this guy. 7 might give a difference. So that's why I want to see what it's like. So you can compile this into two binaries. One is that follows a 64 -bit OS. Another thing is that follows 32 -bit OS. So -m64 means, so if you give a -m64, you compile it into 64-bit binary. So you don't actually give the parameter, by default, -m32, you compile it into binary compatible for running in the OS 32 Linux. Let's look at the 64 -bit. Result. Character size 1, alignment 1.

50:07 화자 2
And the short, size 2, the alignment is 2. So it's not just padding, there's a difference between the dummy and data. Dummy includes the 1x2. character size as well. Two bytes. Integer aligns 4 bytes, long 8, 8 bytes, float 4 bytes, double 8, 8 bytes, long double 668, pointer 8, 8. Very simple, right? But if you look at 32 -bit version, characters are all bytes. And the short, character short Long. So character int, short int, same thing. Long is 4 bytes in 32 bits OS. Alignment should be also 4 bytes. Long, long. The size of it is 8 bytes.

51:07 화자 2
But then its alignment rule is 4 bytes. Okay? So it's more kind of efficient in the, if you compare it to 64 -bit version, because 64 -bit will just use 7 bytes unused. Here only 3 bytes unused. Long double, 12 bytes, 4 bytes, a line like this, anything that's larger than 8 is 4 bytes. Alignment rules are held by a compiler and assembly programmer. Location of each variable, encoding to the alignment rules, and gaps inserted as needed. Also, we composite data structures, structs, arrays. Unions actually share the same scanning approach.

52:04 화자 2
Compiler knows target architecture, generates layout that is compatible with the architecture. ABI application -minded, ABI stands for application -minded. As programmers, we don't need to worry too much about data alignments. However, once we go to the system assembly level, sometimes it's needed. Another program, so we have a global variable section, right? So we have a bunch of different variable types. And then we also print out info. They print out info I, size of I, address of I, and number. So what does it do?

53:00 화자 2
So we just printed out the address so we see whether this align with the rule actually applies. Okay, so we have a lot of different variables, i, c, s, all these, and then they are defined sequentially, and i takes up blah blah four zero address, okay, without padding, and c will, if you define c right after i, c will take up next byte, right?

53:36 화자 3
Or four.

53:38 화자 2
So because integer i takes up the address.

53:44 화자 3
4 -0 -4 -1 -4 -2 4 -0 -4 -1 -4 -2 -4 -3.

53:53 화자 2
These four bytes are used. It can be used by another variable. So C, when you have a character C, you will think of that kind. Short is 2 bytes. Alignment is 2 bytes. So one byte, you cannot use next byte C. So there is a one byte difference. So it will start to use from 4 /6 instead of 4 /5.

54:26 화자 2
So it's a size 8 bytes And then already the next, the 4 /8 is aligned to the 8 So you can use the memory address without it Okay, like this, C2, C3, F So we'll talk about array structure unions here. So this is an array. We have a type, a name, and the number of array elements. Then one size of model is the size of T. And the entire array is the size of name.

55:25 화자 2
Or N times ST. Remember, layout is very simple. Each guy picks up just a s_T bytes. It's possible to get sub -zero, but then it may be as big as like seven bytes. And what's this start address key? Remember.

56:23 화자 2
So you start address x, and then you add the space taken up by the previous, all previous fields. That's it. the size of the entire structure, and then you compute, you sum the entire space, right? Sum of the space taken by each individual field. Last padding, interesting is the last padding, the last padding, the pad N, is chosen such that the size of the structure is the next multiple of the biggest alignment requirement of any of its kind. If this comes in handy, when you declare a new range, if you follow this, then it's easy to build the next of the element.

57:22 화자 2
Here we have structure, the whole of the int i and character c and then that's the array. We have a and then array size n. So then we look at a1's address. A1's address, so the entire structure is How many bytes does the structure take up? Double it, 4 bytes, 8 bytes, right? There are 2 ints, right? 8 bytes. 16? But they can't finish one line. It's not the same thing. The rule is that you need to pad the large member according to the large member. Large member is double. So 7 bytes should be padded. It will take up 24 bytes instead of 17 bytes.

58:23 화자 2
Union will share the same script. They share the same concept memory region containing all members MI and property of all members are located offset 0 and overlapping. Union alignment, maximum alignment requirement of any... And then alignment means that, yes, obviously, right, you will take, you use the largest size field. Then, that will be the size of the union. Address of k-th member, same, same location. Size is maximum. Always the individual field size. Okay. Roughly testing. Actually, you'll notice, but we'll quickly review it. Caller is a function that calls a function.

59:22 화자 2
Callee is the function that is being called. And then you can pass a request to be called in two ways. Passed by value or passed by a reference. Typical is passed by value, When you pass an array, you do not copy the entire array. You pass only the reference. - Which one? - Passed by value, yeah, pass copy of the value to the callee, but patient to arguments. Inside the callee function is local, it's not visible, transferable to caller, right? Scalar, scalar, types are passed by value. We do this, then pass by reference, then we're passing a copy of the value. The copy of the value of the memory address we're passing to the callee. What if the argument affects the original value of the caller? So what typical example is that you pass the address, or you pass the derivative.

01:00:25 화자 2
But if you actually look at the assembly language code, so A is a pointer, so then we dereference A. So assume that A addresses in RDI is one of the 64 -bit registers. It has the address of A. And then, parentheses, it means that that's the dereference. You go there and read the content of this A. And then, store it to EAX. So what it does is that EAX now has value of star A. Okay? And then you multiply by two, but compiler automatically to addition. And ex2, ex2, ex2, the same as 2 times EAX.

01:01:24 화자 2
And then you store store the value of EAX to RDI. Okay? It does not change RDI itself, right? It changes the value to the memory location, pointed by RDI. That's the static score. Arrays are always passed by reference, makes sense. We know that arrays are treated as pointers. Arrays are accessed via memory, addressed visually and separately. So here, A is passed, and then we try to use A as an array. This is impossible. And then how do you implement this 2 thing? 2 is implemented as offset.

01:02:17 화자 2
So, 2 means from A, if you know the address of A, what is A2's address? A plus 8, right? 8 by 2. 8 by 2 is represented like this. This is an offset. So this thing is evaluated as RDI plus 8. And then use this address as the reference pointer. Go there and fetch the number. Command line parameters. Command line parameters are passed to main. It's a number of command line parameters and then command line parameters are written. Then argv[0] is the program itself. So argc must be larianic of 2, at least 1.

01:03:22 화자 2
argv is the array of pointers to characters. That's it. Variables and memory data types and alignments are machine-specific. Programming is mapped to machine data types. Not just machine, but also OS is involved. Why? Because we build up compiled libraries and find this role. Alignment requirements are taken care of by the compiler. Composite data types only exist in high-level languages. The machine only knows scalar data types. The constant data types are mapped to the contiguous blocks of memory. Offset of fields, computing, and management.

01:04:16 화자 2
Variables are labels to the actual memory addresses because you don't want to use variable memory addresses. So we use variable name. Pointers are variables that contain memory addresses. Copy to passing, pass by value, copy, you pass the copied data, pass by reference, you pass the memory address of the data.

01:04:39 화자 4
Okay, that's it and then we'll move to assignment number two session. Thank you.

01:05:01 화자 1
Thank you. Thank you. Thank you.

01:06:06 화자 1
Thank you.

01:06:20 화자 4
Today I will introduce two features about input and output in system programming It is a neural implement of a program called Dirtree or Dirtree tree. Dirtree takes a directory and visits the files and subdirectories inside a recursive mode. It prints the entries in a sorted order. Together we get information about each entry. It also prints statistics for each directory. And if you give multiple directories, it prints total statistics at the end. There are many detailed rules and edge cases, so please read the readme file carefully. So, why are we implementing this program? The main goal is to practice several basic system programming concepts. You will learn how to traverse a file system, how to read file information, and how to format output correctly.

01:07:17 화자 4
Also, you will implement a small pattern matching engine in C language. So this level also gives you some basic experience with regular expression like hit matching. There are three main options. The -d option says the maximum traversal depth and the -f option gives a pattern. Okay. used to filter your file names. You can use -d and -f options together, or you can use neither of them. Also, you may give more than one root directory. If you do not give any directory, Dirtree uses the current directory in the top. The tree has three main steps. The first is, it traverses the directory tree recursively. Second is sort and print the entries.

01:08:16 화자 4
This must come before files and entries are sorted by name. You should ignore the special entries and The skeleton code already provides the dirent_compare function, which you can use for sorting. Finally, after processing the target directory, target directory, it prints a summary line. If the command contains multiple target directories, it also prints the total statistics at the end. For every level in the directory tree, you should add two spaces of indentation. The output contains several columns and the positions of these columns are fixed, so alignment is important.

01:09:12 화자 4
If a path or summary becomes too long for each column, you should truncate it and end with three dots. So please follow the exact formatting rules in the readme. Skip the details for this column. After printing the entries of one target directory, you also need to print a summary line. As you can see in the example, each target directory has a header at the beginning and a footer at the end. The summary line is printed near the bottom and contains statistics for that directory. There is one small detail that is easy to miss. You need to use the correct singular or plural form, or So make sure to Check this carefully. Here is one more type of summary line.

01:10:08 화자 4
If the user gives multiple target directories on the command line, you need to print aggregate statistics at the end. These statistics combine the results from all of the target directories. This slide shows the exact column layout. You can see that the output is designed around fixed column positions. I recommend using the print format array provided in the skeleton code. And please also check the detailed format specification in the readme because your output will be compared with the expected output during the grading. Now let's look at the first main option, the -d. It limits how deep the program traverses the directory tree.

01:11:03 화자 4
The root directory itself is depth 0, and its direct children are depth 1, their children are depth 2, and so on. Only entries whose depth is within the given limit should be printed and traversed. If the option is not given, the default maximum depth is 20. Here are some examples. With -d1, we show the root and the entries directly inside. We do not go deeper than that. With -d2, we can go one level further, so entries at depth 2 are also included. The important point is to count the target directory itself as depth 2. Same as depth3.

01:12:01 화자 4
And there are a few additional rules for the depth option. The valid depth values are from 1 to 20. And if the option is not given, use 20 as the default. You do not need to support a depth greater than 20. The skeleton code provides the MAX_DEPTH macro for this, so you can use that macro. Also, if an entry is skipped because it is deeper than the allowed depth, it must not be included in either the directory summary or the aggregate statistics. The second option is -f the file name filtering. This option filters entries using a simple pattern matching. The pattern is applied to both file and directory names.

01:12:54 화자 4
The result of the pattern matching decides whether an entry is printed with its full information and whether it is counted in the statistics. In this example, the pattern is used to select names related to .txt. There are several important matching rules. First, matching is This means that the pattern can match any substring of the file name. For example, the file name 'abc' matches a pattern 'b'. Second, the matching is case -sensitive, so lowercase 'a' and uppercase 'a' are different. Finally, you match only the base name, not the full path. So in this example, only 'def' is considered for matching. This part is very important.

01:13:52 화자 4
If an entry does not match, normally you do not read it and do not count it to statistics. However, there is an important exception for directories. Even when a directory itself does not match, you still have to traverse it because one of its descendants may match. If a descendant matches, you print the non -matching parent directory only as a name, without the detailed information. This lets the output show how you reach the matched Your pattern matching engine should support three special features question mark, Star, and parentheses.

01:14:44 화자 4
a question mark matches exactly one arbitrary character, and a Star means zero or more repetitions of the immediately preceding character or group. Parentheses create a group, so several characters can be treated as one unit. In the example of a(bc)*d, the group BC may appear zero or more times, so this can match strings such as AD -ABCD or ABC -BCD. Skip the examples. You also need to detect invalid patterns. First, the entire pattern cannot be empty, and a group inside parentheses also cannot be empty.

01:15:40 화자 4
Second, a star must follow a valid character or a group. So, star is invalid, and a *b is also invalid. Finally, parentheses must be balanced and correctly ordered. But there are some complicated cases that you do not need to support. For example, you do not need to handle a lot of stars inside a group and nested groups. These cases will not be considered in the grading. So please focus on the pattern syntax. When your program finds an invalid error, it should print exactly the error message shown here. The message must go to standard error rather than standard output. The skeleton code provides a panic function. So you can simply use that function.

01:16:37 화자 4
Other kinds of errors do not need special handling for Github and will not be included in the grading. This slide gives you a hint for implementing the matching logic. This pseudocode only shows a simpler version that only supports the star, so it is not the full solution. The outer function tries the pattern from different starting positions in the string. This is how partial matching can work. Then, sub function checks whether the pattern matches from one specific position. When it sees a star, it can first try zero repetitions. If that fails, it can consume one occurrence and try again.

01:17:34 화자 4
This recursive structure is one possible way to implement the method. And you will also need to extend the idea to support question marks and groups from this. Handouts are escaped. So please read the readme file carefully. There are two important restrictions. First, do not use an external pattern matching library such as regex.h. The point of this slide is to implement the simple parametric viewset. Second, you do not use scandir function to list directory entries. You may read the documentation of these functions to understand the idea, but your implementation must not use them. The standard library functions from the previous slide are allowed.

01:18:33 화자 4
So please check this slide. Before asking a question about the assignment, please check the FAQ section first. The FAQ includes answers to questions that students often ask in previous semesters. There are several small rules and edge cases in this lab, so reading the FAQ can save you a lot of time.

01:18:59 화자 1
Thank you.

01:18:59 화자 4
Of course, if your question is not answered there, you can still ask us during the Q &A session or eTL. The deadline is Friday, October 9th at 9pm and your submission directory must be named like this structure. Please check the directory and file names carefully before submitting. Finally, the grading rules are included in the last slide. Please check the slide carefully. So, any questions about the lecture?

01:19:52 화자 4
This slide is same to last semester's too but we will check the code, your solutions and previous semester's students' solutions so please put it in your site.

01:20:17 화자 1
Thank you.

01:20:20 화자 4
If there's no questions, we'll end here.

01:20:29 화자 1
Thank you.
````
