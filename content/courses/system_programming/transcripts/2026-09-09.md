---
title: "2026-09-09 · System Programming · 보정 STT"
course: system_programming
date: 2026-09-09
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
---

강의 녹음을 바탕으로 보정한 STT의 공개용 사본입니다. 음성인식 오류와 불명확한 발화가 남아 있을 수 있습니다. 수강생 이름·연락처와 일부 개인 자기소개를 가렸고, 타임스탬프·화자 순서는 보존했습니다.

원문 보정본 SHA-256: `9eeffcc32f83010642c5d6279d1f0063bc2d0143c799ad932debb775ffd8f186`

`[[unclear]]` 등 표시는 원문에 남은 불확실성입니다. 코드·수식과 발화 표기를 그대로 읽을 수 있도록 본문을 텍스트로 제공합니다.

````text
00:01 화자 1
No.
[[unclear: possible missing speech before the lecture starts]]

01:22 화자 2
Okay, let's get started. So we learned using an array name as a pointer. The array name and a pointer are closely related. So when you use the array name itself, typically it indicates its first element. Because there are exceptions here: when you use sizeof(array), it computes the number of bytes needed for the whole entire array; initialization; ampersand of the array. But then most of the other times this is used.

02:20 화자 2
Array name itself cannot be changed. Why? Because if the array name changes, how can we access the array? If you have another pointer pointing to this one—let's say you don't have it—so there is no way to access the array element anymore. That is not. But then you can have a pointer. Then you can increment the pointer. I want to access the individual [[unclear: missing object]]. The array name is—you can pass—the pointer is passed as a pointer.

03:08 화자 2
If this passes, so here, find_largest, that first argument, b, is an array, and when it passes, when it is being passed, it is a pointer. So a is an array, but then—so you can use it as it is an array.

03:42 화자 2
The cost of passing is low. We are just copying the address instead of the entire array elements. So the array argument isn't protected against the change in its element. So you're passing a pointer, and then the pointer can be dereferenced. You can read the content or you can also write the content. Okay, so if you indeed—that you do not want the contents in the array that is passed—if you do not want them to be changed inside a function, then you use const here, find—

04:34 화자 2
—largest, right, has the first parameter that is const int array. So inside this function you can read the element; you cannot assign the value to an element. Then if that happens, then the compiler will not produce [[unclear: compiler / compiled code]]; it will raise a compiler error. So I encourage you to use a const keyword whenever possible. Let's say if you look at the prototype of strlen, then the parameter is what? const character pointer. Because that means that I'm declaring that this function does not change anything of your content. That is a contract.

05:32 화자 2
But you can assume that string—so logically, strlen, it's just returning length. Does it have to change anything in this? Of course it should be const, right? But it can have it as a character pointer; it's still [[unclear: very simple / valid C]]. But if you look at the prototype, it's still const character pointer. Why const? It's a contract. It's saying, inside this function I'm not going to change any content that is pointed by this character pointer. So use const as much as possible. If you think this function is logically not changing anything about this parameter, you should have it. If you are familiar with C++, you also have a—

06:24 화자 2
—const function. If you have declared a function as const, that's also [[unclear: very quickly]]. So if you want [[unclear: to read a function / a read-only function]], then you may want to define the function itself as const. That means that I won't change any member field. Let's see. As far as the time required to pass an array, the function doesn't depend on the size of the array. As I said earlier, it's just copying the address, one address to the destination. There's no penalty for passing a larger array.

07:10 화자 2
There's no copy of the array. So if you copy an array, and if you pass an array, right, you just simply copy the value of the pointer. You know this, there is one [[unclear: range / way]] to copy the entire array [[unclear: to be done / to the function]]. So I want to—let's say I want to have an array having 100 array elements in its size. I want to actually copy this thing, 100 elements, too. So even if the function changes, [[unclear: that affect mine / that doesn't affect mine]].

08:08 화자 2
So you can just pass an array, yes. That's just passing the address of the first element. But if you really want to copy, you want to keep yours, you want to keep my copy. [[unclear: missing question or exchange about passing a copy]] Yes, we want to copy array elements to the destination parameter.

08:59 화자 1
Yeah.

09:04 화자 2
It's not important. But this is [[unclear: content if you decode itself / copying in the code itself]]. So you do the copy yourself. What I'm saying is automatically copy, like—so when you pass an integer, you copy [[unclear: missing object / the integer]]. When you pass a double, it's copying the double. I want to give an array of fixed size, the entire array.

09:40 화자 1
Structure, you can place—

09:45 화자 2
—the array inside a structure, and you pass the structure itself [[unclear: in paradigm / as a parameter]]. This is kind of—let's move. Consequently, an array parameter can be declared as a pointer if desired. So this thing, this int *a and int a[] are the same thing. Consequence four: a function with an array parameter can be passed an array slice, basically [[unclear: mature]].

10:28 화자 1
Passing.

10:32 화자 2
Address of an element. And then there's an assumption that the array is continuous, continuous. So here, find_largest, but then you pass the address of b[5]. Then from the perspective of find_largest, array number zero, that is b[5]. [[unclear: I'd say we are reviewed sales]]. What about this? The following declaration causes the compiler to set aside space for ten integers, the address of the first element. Okay, then *a = 0. What happens after this? Then another similar-looking code is int *a—

11:35 화자 2
—and then *a assigned by zero. Are these two different? Different, right? [[unclear: curve / It's the same.]] Okay, that's good. It's different. Then what happens in each case? The first case, what happens? So then the change is assigning zero to a[0]. [[unclear: missing exchange about a crash, initialization, and whether it always crashes]]

12:26 화자 2
So there is zero—nonzero possibility that it doesn't crash. Why? So here, of course, typically it crashes, most likely crashes. Then why does it crash again? I explained this earlier. Because does it point to any specific [[unclear: order / location]]? The [[unclear: OUTP A / value of a]], it will put a very specific number. So it's not like random, it's not like changing, or—it's fixed when you actually run it.

13:12 화자 3
A has a garbage [[unclear: variable / value]], and when a points to the [[unclear: knock of science]], a point in the virtual address space, the OS is—the a has the address that the function—a has the address that points to the function stack. It may be allocated stack.

13:34 화자 2
So if you go to the address, the problem is if that memory address belongs to you or not. So in order to use memory, it's given in the virtual address—this depends on virtual address, virtual memory's implementation. Okay, but then the way it is implemented is that you need to get the allocation first. Local variables are automatically allocated, in the sense no need to explicitly. But if you want to allocate, like [[unclear: identity / dynamic]] memory, anything beyond the stack, and then there are some other things that are typically allocated, but anything beyond that, you need to ask. Explicit allocation. Otherwise you can actually deal with these numbers.

14:32 화자 1
This is.

14:33 화자 2
But if you access the memory content there, even for reading or writing, the OS [[unclear: of all]]. Because that's not allocated, that is not [[unclear: only / owned]] yet. So it's most likely a has a random value. And then we are dealing with 64-bit addresses, which is huge. It's likely that it may have a number that is completely irrelevant to the area that you already have the permission to use. Still nonzero—it may have a valid address [[unclear: that's a suspect everywhere]], but most likely it's not. That's why it crashes. So you try to go there, or so—

15:24 화자 1
[[unclear: If this case is looking / If this case is lucky]].

15:26 화자 2
[[unclear: What is the work income]]—this access is wrong. That's why it will kill this program or process. Okay, double pointer. Okay, so p, q, [[unclear: for institute r / point to integers]], k is a pointer that points to a pointer. If you master double pointers, you can go to [[unclear: different / triple]] pointers easier. So, okay. First, so what is the type of k? k is a pointer to an integer pointer. So, okay, k is still a pointer. So if you [[unclear: filter / follow]] k, it's still an address. If you go to the address and then read eight bytes—

16:23 화자 2
—from the starting address, that content is an address to an integer. If you go, using again, using this content, and then go to the address, then you will see an integer sitting in the four bytes starting from that one.

16:54 화자 1
Okay.

16:57 화자 2
So *p assigned one, *q assigned two. So that means that [[unclear: you're hate to be valid by it / i and j have those values]]. What about this now? What does that mean? k is the address of—k has a memory address, sorry, k has the memory address of p. Then I want [[unclear: my own / to change the]] value. *k is p, so I want to change the content of p. Okay, with what? The content of q. So this is the same as—

18:02 화자 2
Do you understand? What does k have? k has p's address. And then *k means that you go to p's address and try to read or write these eight bytes, which is the content of p. I want to change the content of p with the value of q. Right. Okay, so now p—

18:48 화자 2
—p's value itself is the same as q's value. What does q's value—what does q point to? j. So now p has the address of j. [[unclear: 몇 대인지 모르겠다 / Do you understand? You still don't get it?]] Everyone, it's clear, right?

19:23 화자 1
What's that?

19:25 화자 2
What about this k? Now k = &q. It was pointing to p, but now k has the address of q. And then *k, so q is the content, right? q's value is now updated to &i, so now q points to [[unclear: missing object / i]]. Okay, yeah, this one, p—p points to j, so it updates j, j. What about **k? What is *k? k points to q; *k is q. So this one is *q. q points to what? q points to i, so it updates i's value to 4. Clear?

20:26 화자 2
Any questions? Okay, let's move on. Declaration again. So I explained this briefly earlier. Here is array, and then parentheses for—parentheses for—parentheses for grouping, [[unclear: literature]], has the highest priority. You go to the parentheses and then determine the type, and then—okay, let's look at the first example: int *p[10]. What is p's type? So array is applied to this. p is an array—

21:19 화자 2
—array of ten elements, and then each individual element type is what? Integer pointer. So what is the size of p? [[unclear: 머리 사이즈]] Okay, I'm going to say p is an array. We'll talk about this first one. Ten times what? Size of an integer pointer. 80 bytes, right? So what about the second one? Second again, it's not times, right? p is a pointer. If it's a pointer to what? Integer array of size ten. Okay, so what is sizeof(p) again, this sizeof(p)?

22:27 화자 2
What's the size of p? One number. p is a pointer. No matter what it points to, it's still—you can point to a structure of huge size, still the pointer size is about eight bytes. What about this one? pf is a pointer that points to a function whose prototype—it doesn't take any parameter, but it returns int.

23:10 화자 2
pf is a function. pf itself is kind of similar to an array. pf is a function name, and then this function takes no parameter, but it returns an integer pointer. Okay, what about this? You go to the parentheses, but inside the parentheses, what do you apply first? Array. Okay, so pf is an array of ten elements, and then its individual—

24:01 화자 2
—each individual element is a pointer. So pf is an array of ten pointers to functions returning int. [[unclear: wrote / What about]] this? pf is an array of functions, okay, that returns an integer. It's illegal. So just a practice here. Let's move on. Don't look at the next slide and try to fill it out yourself first. So we have int A1[3], int *A2[3], int—

24:53 화자 2
—parenthesis, star A3. So this is what? This A2 is an array of three elements, and its individual element type is a pointer. What is this A3? This is a pointer to an array of size three. Okay, different. So does it compile when you use this as An, right? Does it compile? All these compile, but does it potentially produce a runtime error? Does it crash? So like, as I said earlier, it's *a—

25:38 화자 2
—a can crash when a has a random address. Or think about yourself, [[unclear: as the pointer criminal / if the pointer is NULL]], so you try to dereference it and you crash, right? So what about these things? And then what is the size? Let's try to do sizeof. So first An, right? So the problem is An, there's *An, there's **An. Does it compile? All of these are [[unclear: domit / valid]] C grammar. So all these, whatever potential—when you access A1 itself here, that's it, for example, runtime—

26:42 화자 2
—it doesn't crash. It doesn't crash for accessing. What about A2? So A1 is an array, so accessing A1 is—so it's okay. Accessing A2, so A2 is also an array, so accessing A2 should be okay. What about A3? A3 is a pointer to an array, but using the A3 value itself is fine. We do not do—we do not do star. This side may be a problem, *A3. What if A3 is NULL?

27:44 화자 2
So using A3 itself, whereas A3 itself is okay, doesn't crash, but then *A3 may crash because A3 either points to a regular value or maybe it's not. Okay, so this part, yes, and [[unclear: the word]]. But what about sizeof? sizeof(A1), it's an easy one: four bytes times three. What about A2? sizeof(A2), eight bytes times three. What about A3, sizeof(A3)?

28:35 화자 1
응

28:35 화자 2
Eight. *A1, what is *A1? We learned it. *A1 is exactly like A1[0], right? So, and then, does it produce a runtime error? No. Because integer—you just read an integer or write an integer, write the integer, the integer, no problem. What is the size of *A1? Four bytes. Are you with me? Any question? Four bytes. About—what about *A2? What is that? That is again A2[0].

29:28 화자 1
Right.

29:31 화자 2
Does it compile, this? A2[0] is just a pointer, then it's valid C grammar. But when you use A2[0], so A2[0] is what? If you go there, there is a pointer. The pointer value itself is—you haven't done any dereference yet. You're just reading the pointer value itself, obtaining the pointer value itself. It's like p, p itself. You [[unclear: SNP / use p]] or assign something else to p. Maybe it doesn't crash, but you have [[unclear: He / the]]—

30:16 화자 1
응

30:17 화자 2
—the memory for the pointer itself or not? So A[0], so you want to read A[0] or you want to write something to A[0]. And so A2[0], you want to read something from A2[0], A2 element zero, right? Or write something to A2[0]. Does it crash? Let's say you have integer pointer p. I want to use p, [[unclear: sum vp]], or assign p with some other [[unclear: missing object]]. Doesn't crash. No, doesn't crash. But exactly the same, A2[0]'s type is integer pointer.

31:17 화자 2
You haven't dereferenced A2[0]. You want to read A2[0] or you want to write to A2[0]. That's all, no problem, right? It doesn't crash. But if you apply one more star, **A2, what does that mean?

31:41 화자 1
예

31:42 화자 2
You have *A2[0], right? A2[0] itself is a pointer, then you apply star. That means you want to dereference.

31:52 화자 1
Okay.

31:53 화자 2
Then it may crash. Why? Because depending on the value of A2[0] itself, itself may be zero or a value whose access is not allowed yet. [[unclear: It can be written]]. Remember, let's say the *p where p is [[unclear: equal / zero]]. Okay, so here, yes, it compiles, right? It doesn't violate the C grammar, but it may crash. Let's get back to here. What is *A2, sizeof(*A2)? What is sizeof(*A2)? *A2, again, is the same as A2[0], which is a pointer.

32:44 화자 2
When you [[unclear: size retirecting / dereference]], how many bytes? Eight bytes. What about A3? Basically it compiles when you have *A3, it's an array, the array name. A3 points to an array. *A3 is an array, okay, so it compiles, but then it may crash. Why? Because A3 may point to NULL, or it can be a random place that is not really a valid [[unclear: brand / address]]. So it can crash. What is the size of *A3?

33:38 화자 2
It's an array, so you need to think about the array size. How many bytes are needed to represent this array? Twelve bytes, right, because it's an integer array of size three. What about this guy, **A1? Does it make sense? It doesn't make sense. It doesn't make sense because A1—*A1's type is already integer, and then you apply star to integer here, to integer type. No, you cannot do that, right? So this doesn't make sense. No, no. What about this guy, [[unclear: Jesukin / Yes, you can]]?

34:27 화자 2
Apply star because double star. Why? Single star is a pointer. *A2[0]—*A2 is A2[0], and then you apply star again. So why? A2[0] is an integer pointer, and then compilation assumes that everything is okay. You don't know what value is [[unclear: plugging / plugged in]]. If that's okay—so this compiles well, but then in case that guy is wrong, in case A2[0] is not [[unclear: missing predicate]]. What about sizeof(**A2)? What is the type of star—sorry, integer pointer or integer?

35:24 화자 2
It's—no, integer, integer, right, right. It's integer; sizeof is four. What about this? Okay, **A3, so this is—*A3 is an array. Star, if you apply star to an array, what does that mean? You go to the—you get the first element. Okay, **A3, of course, compiles, but it may crash again. But what's the size of double star? Yeah, that's integer. Type is integer, so it's [[unclear: normal / four]].

36:20 화자 2
So it's some explanation. Let's move on. This one is most confusing. A3 is a pointer, B3 is a pointer, A and B are arrays, pA is a pointer to—so you can, A3 can point to A. This one, *A3 is A. *B3 is B. So now A assigned B, that's not. So you cannot assign one array to another array, or—that's not.

37:18 화자 2
But this one is—*A3 is an array. **A3 is A's first element. A's first element is assigned by B's first element. This is an element assignment. Then pA points to [[unclear: an odd]]. Points to the—*A3 is an array, but then when it's used as—it can be used, most times it is used as a pointer pointing to the first element. So pA now points to the first element.

38:08 화자 2
Okay, it's a little bit confusing, but a little bit of practice [[unclear: investment / makes perfect]]. So we learned pointers, their operations. A pointer has a memory address as its value. Ampersand is the address operator. Star is the indirection or dereference operator. Pointers as function arguments that we learned, relationship to arrays. Now let's move on to Unix files. By the way, in the second half of this lecture, [[unclear: it will explain the second / the TA will explain the assignment]].

39:00 화자 2
The Unix file concept, the filesystem, and filesystems and security. Simply, so a Unix file is a sequence of bytes. That's it. Now we are familiar with this file, right? We are familiar with an MS Word file, like a PowerPoint file, that's typically stored in [[unclear: this UDS capitalist / a disk]], and we can open it and then copy. It's called a regular file. But the concept of a Unix file goes to the other. Anything that can be named, you can call the name, you can treat it as a file, you can deal with this file with the name. Anything that has a name, anything that can do input or output, everything is a file.

39:59 화자 2
But then again, it does not assume any structure inside, so it just treats the file as a string of bytes. The understanding of this structure [[unclear: Here he spoke together]]. So then the first byte is at the lower address. And then again, so the file containing lowercase English alphabet a to z is like this. File offset zero has a, and then file offset 25 has z. In Unix or Linux, all the descendant—

40:48 화자 2
—systems, right, everything is modeled as a file. Regular files, we are most familiar with regular files. Also a directory itself is a file. A directory has the list of filenames. And then the network sockets. If you communicate over a network, or even inside one machine, you can create a socket. [[unclear: Instead of / Through the]] socket, you can communicate with another process. So that is also—you have an identifier, socket descriptor, name, and then you can read and write through.

41:38 화자 2
The filesystem supports virtual filesystems that start with /proc, and then anything below this looks like a file, file, looks like a file. So you can use the file file path, but then the content is dynamically generated by the kernel. And then devices, they have names, and then they also, you can read and write. Disk partitions—you can have multiple partitions, partitions of a single disk, right? Each partition is a file. The memory is a file. You can access memory through the file handle. The keyboard, mouse, display, all these devices are also files. So examples here, I/O devices are represented as files, /dev.

42:35 화자 2
Under /dev we have all of the devices connected to [[unclear: essential / the system]]. sda, SATA, is [[unclear: number eight / number a]], first SATA. And then /dev/input/mice, this is an aggregate of all [[unclear: the whole / the mice]]. The kernel is exposed with a number of files. So [[unclear: the earlier is if exposed to the entire network that it may / the kernel memory is exposed]]. So it looks to be a file because you can look it up, you use the path that has the entire kernel memory. Of course, that's a security concern, can read everything.

43:30 화자 2
So it's disabled by Ubuntu by default. Then all the kernel data structures are exposed through this filesystem, /proc. And then configuring system operation typically sits under /sys. So regular file, the files that we are always going to deal with, the file containing user application data, can be binary, can be text. That's really—the filesystem doesn't want to interpret whether this file is actually a text file. Text file means that you refer to a special code page, and then, like, oh, this is ASCII code, right?

44:12 화자 2
A, B, C, that's text. Binary, that has [[unclear: not linked to the inequality]], it can be executable or it can be just random numbers. It can be used [[unclear: as a biotech application / by the application]]. So the filesystem itself does not try to interpret the content. It's up to the application. OS does not know anything about the format, [[unclear: algorithms looks like]]. Directory file, a directory file contains names and locations of the files that are located—

44:55 화자 2
Okay, and then the location is this i-number [[unclear: in the class inside]]. The i-number points to the location of the inode. The i-number is just one—you can think of it as an array index. So it allocates one data area in [[unclear: this / the disk]]. That's called—the metadata of the file is stored in an inode. An inode has—what is metadata? File size, last modified time, creation time, and then permissions. These are all metadata. It's not the content, [[unclear: subway content subway]], but the inode has these [[unclear: distributors / attributes]], metadata about each file.

45:52 화자 2
And then you need to know which location you need to go to to know the metadata of this [[unclear: discussion / file]]. That's the i-number. The i-number is kind of an [[unclear: original / ordinal]] number. Seven means that you go to the seventh inode location; you can find the metadata. File data—the i-number points to the location of an inode. An inode is the index node that contains the metadata of a file: permissions, ownership, times, the size. But then the inode does not include the filename itself. The filename is stored as the content of what? Okay? [[unclear: possible missing answer]]

46:44 화자 2
Device file. Device file can be either a character device, character—character special file, or block [[unclear: chain / special file]]. Character special files provide an interface to character devices. What are the character devices? Devices, or virtual devices, hardware that handle data in a stream of individual characters. Properties—using the character device, you can do only sequential access; you cannot do random access. Okay? Exactly like a keyboard, when input is available one at a time, you cannot even write to it—

47:33 화자 2
—mice, again, same. Position data is coming only. You cannot do random access: I want to access in the future. No, you cannot do. No buffering, and real-time interaction, variable data rate depending on the actual devices. So character devices are devices including terminals, serial ports, keyboard, mice, sound cards. Then if you—inside /dev, right, if you see [[unclear: a lot of people are]], and then if you ls -l, then you will see c as the first character [[unclear: union]]. So c is the first character. That [[unclear: Look implies not / implies that]]—

48:27 화자 2
—the character special file. Block special files provide an interface to block devices. Block devices, typically storage devices that handle data in a fixed-size block. So you can read and write in the unit of a block. You cannot read just one byte. You can read a four-kilobyte page, four kilobytes at one—at a time. At one time you read four kilobytes, or you can write in the same manner. You can write one block.

49:07 화자 2
Properties: block-based access. Then you can do random access. You can go to the seventh block right away, skipping the previous blocks. And then buffered I/O, you need to have a buffer to read and write. Hard disks, USB drives, CD/DVD drives, right? These are [[unclear: reality / examples]], right? And then if you do ls -l, you will see b as the first character. So let's try this. Let's say you see c [[unclear: winner]], you see c as the first character.

49:54 화자 2
And then sda is the first block special file. And then FIFO, yeah, this is [[unclear: inhabited / another]] virtual file. So it's named pipe, conceptual pipe. So a special file that creates a unidirectional communication channel between processes. So two processes want to communicate with each other. So basically inside a process, one process, you cannot interact with another process. You cannot see the content of another process because all you see is—you have your own [[unclear: group for here]], the 64-bit memory space, huge space, but everything is yours.

50:55 화자 2
But [[unclear: main example / in an example]], you need to interact with other programs that are working in a process. So that's IPC, interprocess communication. And the one device, one mechanism that you have, you can do IPC, is the FIFO, named pipe. And then it creates a unidirectional channel. So I say something, and the other guy is only listening, only [[unclear: the]]. Because it's only in memory, unlike regular files, but it has a name, it can do I/O. That's why it's a file. Okay, so data written to a FIFO file by one process can be read by another in the same order. So one example here, mkfifo. I mean, I'm creating a FIFO file named abc. Then whatever—

51:50 화자 2
—you see abc in the current directory. So abc is accessible using a filesystem path. That's why it's a file.

52:00 화자 1
That's okay.

52:02 화자 2
And then what you want to do, I want to compress it from any input from abc. So gzip, [[unclear: that's the written]]. The standard input redirection with abc. The abc has nothing yet, no data yet, but then whenever there is data from abc, I want to compress it, and then the output is written to abc.gz. That's a regular file, okay?

52:33 화자 2
And then I want to run it as a background process, [[unclear: type in]]. Why? Because I want to feed the data to abc. So this cat document.txt, document.txt. Why? document.txt content is fed into abc FIFO. Then abc, some data is available to abc, and then that guy is sent into what? gzip. gzip produces output, sends it out. That goes to abc.gz, okay?

53:12 화자 1
Okay.
[[unclear: possible missing speech during the FIFO demonstration]]

53:50 화자 2
I couldn't. [[unclear: possible continuation missing]]

54:05 화자 1
Okay.

54:09 화자 2
Thanks. Then abc.gz. [[unclear: It's great / it's created]]. And there is [[unclear: an episode]]. Yeah, so here, because abc is FIFO, and then [[unclear: when you see the output will be abc that will replace remove]]. Because it's [[unclear: yes]], then what is—another thing is a socket. So here we talk about Unix domain sockets. This socket is for local—

55:05 화자 2
—communication inside one machine, but there are also network sockets using TCP/IP or other things. So you can communicate with one process in one machine, with another process in a remote machine. But here we talk about Unix domain, okay? The file type used for local communication between processes in the same machine, and similar to the FIFO files, but then it supports bidirectional communication. So if only unidirectional communication is required, then FIFO is okay. But if you need bidirectional communication, you can use a socket, or a Unix domain socket for local communication. Then here, that's used for X Window.

56:14 화자 2
So when you do this, you see s here because this file is what? Socket. And then, yes, through this file, it looks like a file. It's not a regular file again. It sits only in memory, but then this is used by X Window to generate a display for terminal zero, number zero. In detail, you [[unclear: can have to know the detail / don't have to know the detail]]. Like that, this is a different special file [[unclear: or check process]].

56:54 화자 2
So design concept: all input and output is handled in a consistent and uniform way. What do you mean, consistent? So all these files, although if you look at the details, they're different, but then you can open or create the file. Then it returns a file descriptor. File descriptor is an identifier, and the file descriptor is [[unclear: missing description]]. And then all the subsequent operations—so when you open it or create it, using the file descriptor you can read or write. You can use all the same functions regardless of the individual specific files. Read or write. Of course we cannot write to a CD. There are some operations that are not allowed for certain types. But basically you can do read or write.

57:50 화자 2
And then you close when you're done. So all these operations are applied regardless of the specific files. That's what it says: it's consistent and uniform. Elegant mapping of files to devices allows the kernel to export a simple, simple interface of Unix I/O. Simplicity is the key to scalability. You can support many other things if you're trying to support only the minimum common properties. Everything has a name, and you can read it, and then you can close it. Those things are common. And then of course, depending on the file type, you can do seek. For example, this block file is going to be able to go to a regular—

58:42 화자 2
—seek, but then seek is not supported by network. So depending on the file, this support is different, but then the fundamental operations are—okay, let's stop here. Do you have a TA here? Yes, come up here, and then please present. So assignment number one is out. If you look at the eTL, the [[unclear: class / assignment]] specification is already out, and then here you will explain assignment number one. Now I emphasize one more time, please do not use AI for programming. Okay, assignments one and two are relatively easy. You can do it, you can do it.

59:40 화자 2
Others, of course, other assignments also will be easy by me. I will teach you well, and you can do it well. [[unclear: possible missing handover speech]]

01:00:34 화자 4
And I'm [[unclear: 개인 이름·소속 비공개]], and [[unclear: 개인 이름·소속 비공개]], and we are the System Programming TA. And now we are going to go through—the first part is Lab 0, an introduction to the environment and tools. And this is the important thing, that we use the TA mailing list, [이메일], and we also use eTL for Q&A. And what is the goal for Lab 0? And see what it is like, we want to get familiar with the Linux programming environment, and we can feel comfortable to use the tools for System Programming assignments. And some of us might be using, like, shell commands, editors or compilers, others and others. Since this is on the slide, and we recommend some books here, like Computer Systems—

01:01:30 화자 4
—A Programmer's Perspective, from 2015. And also you can refer to The Linux Command Line, or Beginning Linux Programming, or Linux Command Line and Shell Scripting Bible. And these kinds of books are very useful for this course. And what we were using, what we are using in our [[unclear: agreement system, agreement language / development]] environment is Linux. And this is a free and open-source operating system, and we use an Ubuntu distribution, Ubuntu Linux, for our development. And how to use Linux? And we have Bacchus VMs, and we will maybe go through it after.

01:02:11 화자 4
We can use [[unclear: the Windows / remote]] access to Bacchus VMs, or you can install Ubuntu on your own PC in a virtual machine. For example, if you are using Windows 10 or 11, you can—you can see Windows Subsystem for Linux, which is [[unclear: all done, let's say, of]]. And here is the document for WSL. And if you are using the Mac, I recommend you to connect to the Bacchus VM because currently the Mac, the MacBook, is using the [[unclear: M1 or M5]] architecture, which is [[unclear: best online / based on ARM]], and which is not compatible with our [[unclear: competitive / grading]] system. And how to use WSL on Windows? Because I use Mac—do you have any questions, right? No, we—oh, yes, we have this file, maybe in Lab 0 on the eTL announcement or assignment. You can check it.

01:03:10 화자 4
And because I use a Mac, so I just go through with the slides. With WSL, you can use wsl --install on your command line, and it will show through, it will guide you to install Windows Subsystem for Linux. And also you can type Ubuntu from the Start menu, or type wsl in cmd, and you can get into your Linux environment. And for example, you can use uname -r to show your version of your Linux, or lsb_release -a for your Linux version.

01:04:00 화자 4
You are working on this VM, so Linux is installed on the remote VMs already. So please use port 22 when you're on campus, or 2222, 2222 when off campus. And your account is already created already, and so your ID is sp and your student ID without a dash. For example, if your ID is [[unclear: 2026, 3549 SP]], your ID would be without a dash here. And this is an initial password, and please log into your Bacchus VM and change your password first. And then I will maybe guide you how to get into the Bacchus VM. And you can use tools like ssh to access the machine remotely. And for—

01:04:54 화자 4
—you can also use ssh on Mac. You can use ssh as well, because this is maybe supported on Windows and—or if you want to have some GUI application like PuTTY or like MobaXterm, you can navigate to the documents here, and we can just navigate into the Bacchus VM. And MobaXterm is a more user-interface-friendly application here, so it has a better look and more features.

01:05:30 화자 4
And we have some useful commands for scp because sometimes you will need to copy your files or your lab assignment to your Bacchus VM. So you might use scp to copy your assignment into your Bacchus VM. And lastly, first is the most important, it's like we are using gcc800, which is installed in the Bacchus VM. And why—what is the difference between gcc800 and gcc? gcc800 is a special script made for this lecture, because as you can see here, we add some compiler flags here to detect your, maybe, warnings from unused variables, or use of variables without initialization, or [[unclear: national size]]. So you might use gcc800. If you are using your local environment, please make sure to move your gcc800 into your bin directory.

01:06:29 화자 4
And let me go through how to use, maybe, the Bacchus VM here. So let's get back to here, like we can use your terminal, and we can just [[unclear: add that edge / ssh]]. And because I have an ID here, [[unclear: 개인 계정 후보 비공개]]. Can you see? Let's wait. And I will make it bigger. And you can ssh into your Bacchus VM, like sp.snucse.org, and the port off campus is 2222, so you can connect to your Bacchus VM.

01:07:17 화자 4
Yeah, and now you were asked to maybe enter a password, and the different passwords, and you can change the password by typing passwd [[unclear: RBB]]. And you can change the current [[unclear: as well here]]. And please make sure to change the password here because maybe if you don't change it, some student might [[unclear: money / log]] back into your lab environment. So make sure to change your password here. And how can we—and how can we download Lab 1? And Lab 1 will open after this class, at about 1 p.m., and it will close, importantly, it will close on September 18th at about 9 p.m.

01:08:17 화자 4
So please make sure to submit your final source code into eTL. And let me download it first because maybe now you cannot—you are not allowed to access this point because now it's not 1 p.m. So let me try to demonstrate how to use this one. And we can just, maybe not at, maybe—can you see my files? So there is a Lab 1, and Lab 1 Decommenter, and for the directory here, so we can copy our files from Downloads to our [[unclear: missing destination / Bacchus VM]]. The -r is for recursive because this is a directory, so you have to recursively copy the files into the Bacchus VM. So [[unclear: as a choice]], and you will see the files were transmitted to the Bacchus VM like this.

01:09:18 화자 4
And we can go to the Bacchus VM again to check if the files exist or not. So we can see that there is a Lab 1 Decommenter in the Bacchus VM right now. So let's go to Lab 1 [[unclear: with the minutes, of course / Decommenter first]].

01:09:39 화자 4
Is there any question from Lab 0 right now? If no, we will continue with Lab 1. Lab 1 is Decommenter. And in this case, maybe, what is a decommenter? It's like, as you can see here, if you have written some C code, you might know what is a comment. It's like we can comment on your program without it affecting your code functionality, just let me describe your function to other people to understand what is important. And so by this, or maybe some IDEs' code has some hint or [[unclear: lit / highlighting]] for this kind of—it can show which one is a comment or which one is not a comment, which is not a comment. But we want to do this step for the decommenter, like remove the comments above and below to the [[unclear: price / right]] one. So also the definition of a decommenter is a tool to remove comments within source code files.

01:10:55 화자 4
And the usage for the decommenter is, after you compile your source code, you will get an executable file, and you will put some target input file, like this one. This one is the source file. And it will have maybe some output here, and we will have some error logs here as well. And what does this mean? This one is that we want stdin to the decommenter from this file, and we want to have some stdout for output too. And this 2 is for stderr, so it will [[unclear: net gasual / redirect the]] errors to the error—

01:11:43 화자 4
—logs here. So input and output for decomment should be handled via redirection, and this functionality is obviously the default functionality of the Linux shell, so you don't need to worry about anything here. And there is no special additional work required for this kind of command. The only thing you need to do is compile your decommenter [[unclear: doesn't mean input to comment]]. And there are some rules about the comments here. For example, if this is the standard input stream, for example, if you have abc, /*, def, and */, and this is for newline. This subscript s is for space. So if you see this one, you have to replace the def in the comment part with a space. So this is the first case.

01:12:39 화자 4
And the second case is for double slash. So here we have abc and double slash, def, and it should be the output to become abc, space, and newline, because here we want to replace this one, this comment, with a space. And multiply, sorry, and multi-line comments is like, as we can see before here, this one is the multi-line comment. Like we have def and newline and ghi and star and slash, [[unclear: a backslash]], and so we have to replace this one with a space and a newline. So maybe go through the third part. If you encounter this kind of comment, like we have an—

01:13:35 화자 4
—additional slash and star inside of block comments, you should skip it, like you need to replace all the strings with a space. And there are some exceptions for comments in string or character constants. For example, if we have some comments inside the string here, this is a string. So it is wrapped by the string, so this one should not be decommented. So the output should be the same as well. If you encounter the character constant here as well, it should not be decommented as well.

01:14:25 화자 4
And there is some, maybe, special case for character constants with the backslash because sometimes we want to print this string character, this double-quote character, out into the console. So it might be quotes, and there is a backslash that should be before it. So we need to handle this kind of stuff. So the output should be the def, and this should not be decommented as well. So same as the character constant as well, like the string, because both can do mostly the same.

01:15:12 화자 4
And sometimes you, like, encounter newline characters in the string or character constants, and it should not be decommented as well because it is wrapped inside the string or the character constant. And later there is some handling of unterminated string or char constants. For example, there is no double quote at the end of this kind of input string. But what we want to do is—the decommenter is not a compiler, so this kind of thing we need to keep it the same as before in the input stream. Also it is also the same for the character constant here because we are doing the decommenter, it's not a compiler.

01:16:09 화자 4
And there is the last case for this one. If we encounter a slash and star in the text, but we don't have a closing star and slash in the end, so in this case we should output the space and newline, but there is an error, and there is an error, like, because line two is an unterminated comment. So we should output which line the unterminated comment starts from because we don't have any closed comment here.

01:16:48 화자 4
So we need to return EXIT_FAILURE when an unterminated comment was detected, and so include stdlib.h in your C code. But we have some skeleton code here. So there are some requirements for miscellaneous features. Your program works for standard input lines of any length. It's like that, [[unclear: even so it is what size of what or size of media your user]], handle this kind of stuff. And you may assume the final line of the standard input stream ends with a newline character, and the backslash-newline character sequence does not occur in the standard input stream. And you may place all your source code in a single file. Please do not separate the files into two files or three files because we have a skeleton code actually already.

01:17:46 화자 4
So the only thing you need to do is try to write your code in a single file in the skeleton code, and we suggest using the standard C getchar() function or fprintf() function. And please read the README, and this file would be included inside the Decommenter. There is a README here, and so later at 1 p.m. you can start to read your README and start to write your programs afterwards. And there is some guidance in the README.

01:18:23 화자 4
And the last part, please try to do your assignment step by step. And because, as the professor said, this work, maybe this assignment is not quite difficult. It might be not easy, but you might design a state transition diagram, like we call a finite state machine in this kind of [[unclear: signal / situation]]. Because if you [[unclear: do resolve / do without a]] state transition diagram, you won't understand what will happen in the next step. So you have to design the state transition machine diagram and try to modify the source code, and we will put the source code in src/decomment.c.

01:19:07 화자 4
And compile the program via make and execute. And finally, if you say your work is done, in this case, a readme file without any file extension, just write the readme and submit all the code into eTL. And how to test your code? And this is, we have some reference executable files in the reference directory, so you can try to understand how it works in the reference directory. And there are some test files in the directory, but we will go through how to test the reference file and how to write the code a little bit, how to write the code for our decommenter, and try to do a sample test—

01:20:03 화자 4
—test file. And this diff command checks the difference between two files. And there are some examples here. We can use this command here for your reference. So there are some requirements for the submission format. Your assignment should include Makefile, and [[unclear: the comment here / the command here]], this one we will provide you, provide to you, and so you don't need to change or modify it.

01:20:29 화자 4
And you also need to submit decomment.c, the source file here. And we have a history, compilation history, like how many times you compile this program or assignment. So this is the way to maybe detect how you [[unclear: illustrate / did]] your homework, just to prevent someone from copying from maybe [[unclear: given I / Gemini]] or ChatGPT, [[unclear: the Recovery / the code]], and putting it into the homework. If you copy the homework from maybe AI models, we will see, maybe, the history of the compilation. So the line will be very low, and it will be a single shot, like you can compile this in one. But I think this is hard—

01:21:19 화자 4
—compile, and there's no errors occur. Because I just did the homework and I compiled for maybe ten times or twenty times, so maybe [[unclear: just competitively growth here]]. And this sets the file directory's name to match the example above. For example, your ID is [학번]. Make sure your directory and your file list are like this one. There is some example here. And also please place the history directory under YourID_assign1, and structure files and directories as shown above, and proceed with the compression. And the deadline is on September 18th at 9 p.m. So you have maybe nine days left, but I think if you work on this problem, maybe it costs you about five to six—

01:22:20 화자 4
—hours at least, and maybe ten to twenty hours at most. So if you have any question, you can try to [[unclear: make a glass of / send us an]] email or ask some questions on eTL or to our Google Groups, as shown in the introduction. And there are some helpful commands here to guide you to move your assignment and compress your assignment to eTL, and [[unclear: the bread in / put it in]] the submission format.

01:22:53 화자 4
We will give some penalty for noncompliance. For example, your ID, there is no underscore here, so you maybe get some minus [[unclear: performance / points]] here because we want to give you some compliance, compliance in the homework format. And some, from—from the user's point of view, like the students, you need to—you need to have the correct functioning of a program. But if you are working as a programmer, like maybe in the industry or in the companies, your program should have quality if it's well styled and thereby easy to maintain, like scalability or readability. There's a lot of, maybe we call, [[unclear: the site principle / design principles]] in the industry. And so, like now, you need to use clear and consistent style for variable and function names. Please do not use AAA or BBD or ABCE for your variables. You might use maybe, like [[unclear: stress / states]], or like maybe diagram, this kind of naming, because I think AA or bab is not good for maintaining.

01:24:11 화자 4
And each function should begin with a comment that describes what the function does from the point of view of the caller. And the function modularity: your program should not consist of one large main function. Instead, your program should consist of multiple small functions. And line length, 72 characters is recommended, because if you write code with a line, or line size, or a length of maybe [[unclear: what / 100]]—

01:24:42 화자 4
—or 200, it is hard to maybe maintain your source code in the IDE or any editors. And can you modify the provided skeleton code? Yes, you can modify parts or all of it. And can you write the readme in Korean? Yes, [[unclear: to congratulate]]. And also some corner-case questions: run sampledecomment first if you have any doubt, and if you suspect it [[unclear: is because this is the process / misbehaves, please report it]]. And now I will also—is there any questions?

01:25:19 화자 1
No.

01:25:20 화자 4
Other—or I will go through the demo, how to write some code, or let's get to the demo. Yes, actually I have a [[unclear: college / colleague]], so you can [[unclear: run / write]] it in Chinese if you are able to write it. And let me show you how to use this server. And now we have the Lab 1 Decommenter here. And also you can see the history, Makefile, README, reference, source and test files, and you can write the source file here.

01:26:10 화자 4
Yeah, now you have this kind of—you can use [[unclear: C in / Vim]], or you can use VS Code. Actually I will guide you, because you use VS Code via SSH into your machine. And this is kind of the source code, skeleton code for the decommenter. And obviously what you need to do is to implement the decommenting logic here. So for example, if I delete the TODO here, and we save the file and go back to Lab 1 Decommenter and try to type make, and there is some diff here because we deleted the TODO comment. So there is -1. This means that there is one line difference between your original source code and your after source code. And there are some, some errors here because we will catch the compiler with the, maybe—

01:27:10 화자 4
—[[unclear: Everyone on favorable like it says for studios]]. So please ensure your homework compiles with gcc800. And we can see which one is gcc800. We have set gcc800 in your own Bacchus VM Linux server. And what does this mean? Actually we can see here. We will try to catch all the warnings and all the errors and some pedantic [[unclear: or]], and we use -std=c99, and we will try to compile your file into this kind of stuff. And now we can show—we just tried to compile the file, so there is a history here.

01:28:02 화자 4
You can also see your history, and please do not modify or change the files inside here. And we can see here, here is this 001 and 002, and we can also see your make status by this one. The first time is success, and the second time is failed, so there is an error in the second time, second attempt, and there is line 42. So this is the way I would detect if you are trying to copy or copy the code from AI or not. So you have to write your code on your own. And there are some, maybe—and also we can try to show how to use the reference file here. And this [[unclear: mine / line]] is how to use this. This one is to execute this sampledecomment and try to pass your—

01:29:03 화자 4
—your maybe [[unclear: arrow / input]] from the test file. And this one is where you have test file zero, and this one is passing test0.c into the decommenter. And we don't actually have a log here, output for output0 [[unclear: and fight / file]], and we want to also have an error output, so we have to use 2>, an arrow, for error0. So you can see the output here. We can see that maybe the original, original test file, test0, this is the original source file here. So there are some comments here, and we—you can see the difference between the outputs—

01:30:03 화자 4
—output and test file, test0.c. Yeah, there are some comments decommented by—yeah, this is decommented by the source program. So you can also—we have some corner cases to try to catch. For example, we have maybe test1. Let me clear the screen first. And we have test1. Test1 is this one, test1 file is this one. There is some error for the unterminated comment. So we want to try to use test1 here, and we have the output and error. So we can see the error here. There is the error, line 15, unterminated comment here. And also we can see the output here. Because of what? Yeah, and as we can see, because there is—we can see the original file first.

01:31:06 화자 4
Because there is [[unclear: no block comment / a block comment]] for this one, so it should output like this, but there is no end for this code, so it should return [[unclear: for very step / failure]]. So we should return line 15, unterminated comment. And maybe I can—and let's show, show how to use VS Code for writing your code. And if you have VS Code, or—yeah, I recommend you to use VS Code because there are many extensions here. You can install SSH here from Microsoft, and you can install it and try to configure your SSH configuration file. For example, you have this one, you can configure, or more easily you can—

01:32:03 화자 4
—connect to host and add a new SSH host, and you can type your SSH command into this kind of [[unclear: missing object]]. And afterwards you are able to navigate into your VS Code, and yeah, you can write your code here, in src/decomment.c, so you are able to write your code here. With the VS Code extension, you don't need to use maybe [[unclear: field / Vim]] or some [[unclear: textbooks / text tools]], or [[unclear: don't have that]], that is very hard for it. So I recommend you to use VS Code here. This is easier for you to write and compile code. Is there any question for now?

01:32:53 화자 5
Is it okay to understand the sampledecomment program [[unclear: to generate]]? [[unclear: She's that ideal commander / Is that the ideal decommenter?]]

01:33:02 화자 4
sampledecomment is the reference file we actually compiled already for students for reference, to check the test file and your behavior of your—like the same, if you write the—just like checking the behavior of your decommenter, the sampledecomment, of the behavior.

01:33:24 화자 5
Our code must behave the same as the sampledecomment, right?

01:33:29 화자 4
Oh, yes. So there are maybe—clear the screen first—and there are some test files here. We have six test files here, so you can test your sample [[unclear: comments]], maybe like sampledecomment or your program, with these six test files. And these test .c are included, all the test [[unclear: tests / cases]] are included in these six test files, but we still have some additional hidden test [[unclear: comment / cases]] in our [[unclear: learning / grading]] system. So please ensure, maybe at least you need to pass these six test files and submit your code or your compressed file into eTL.

01:34:17 화자 4
Is there any question for Lab 0 and Lab 1? You maybe, you can see the, maybe [[unclear: I'll definitely ask into the student field]], because it will open at 1 p.m., so you can write your homework from that, after your lunch, over the lunch, and start to write your code. Because it costs about maybe, at least, I think it at least cost me about five hours to six hours. But at least it should maybe require you to write maybe ten hours or fifteen hours to debug, plus, [[unclear: except if you haven't]]. And if you have any questions for programming, please contact the TA or professor—

01:35:06 화자 4
—via email or on eTL, and we will try to provide you guidance on the code. But do not just provide your whole, your snippets into the email, please. Describe what you want to write and what thinking you have in the email [[unclear: of the code]]. So if you just send, maybe, for example, if you just copy-paste all the code into the email and ask the TA or professor, 'Why doesn't this work?', we won't reply to you because we want to see if you have something about your [[unclear: Option / approach]], what obstacle you have, or something else.

01:35:55 화자 4
Is there any question for Lab 0 and Lab 1? And I recommend you to log into your Bacchus VM [[unclear: and leisure / later]] and to change your password because otherwise maybe some students will be—previously some students will [[unclear: evaluate DNN]] and change your password and make sure [[unclear: CN / you]] cannot access. So please make sure to change your password first. And starting with [[unclear: guys, you thought]]. And if you have any questions about programming or the compiler or some thinking about your homework or some misbehavior of sampledecomment, [[unclear: deep comment first]], please [[unclear: that's not]]. Is there any question? Otherwise we can have lunch. And if you have any questions, you can stay here and ask as well. Otherwise you can go for the [[unclear: unfinished utterance]].
````
