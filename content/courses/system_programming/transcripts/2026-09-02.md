---
title: "2026-09-02 · System Programming · 보정 STT"
course: system_programming
date: 2026-09-02
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
---

강의 녹음을 바탕으로 보정한 STT의 공개용 사본입니다. 음성인식 오류와 불명확한 발화가 남아 있을 수 있습니다. 수강생 이름·연락처와 일부 개인 자기소개를 가렸고, 타임스탬프·화자 순서는 보존했습니다.

원문 보정본 SHA-256: `7d4c1409c10531c4da69dfb4f825b5551e54284398b5d0e4b0a0e562a75b3b1c`

`[[unclear]]` 등 표시는 원문에 남은 불확실성입니다. 코드·수식과 발화 표기를 그대로 읽을 수 있도록 본문을 텍스트로 제공합니다.

````text
02:34 화자 1
Okay, let's get started. Sorry for the delay. This is the System Programming course for the fall semester in 2026. This is the first lecture. First of all, I want to thank the people who allowed me to borrow the slides. Prof. Bernhard Egger is the primary source of the slides, and my colleague Prof. KyoungSoo Park used to be a professor at KAIST. I worked here three years ago—two and a half years ago—so I'm using some of the slides that I've made and also some slides from Princeton University, with permission. We're supposed to have, like, what, 120 students? But I don't think…

03:40 화자 1
It's flexible, I think. So most of you are juniors or seniors.

03:49 화자 2
Anyone? Sophomore?

03:52 화자 1
Freshman, really? Are you familiar with the C programming language? So, yeah. I think you've taken computer architecture—CPU architecture, TLB. Okay, because this course is a required course, I assume you have taken those courses. One course that's very closely related to this course is Computer Architecture: CPU, pipelining, instruction-level understanding. Of course, even if you…

04:41 화자 1
did not take it, you can understand this course. It's not really mathematical. It's up to you, but I recommend taking Computer Architecture before taking this course. So far, I assume you have learned programming in a few languages. You probably developed in Python, right? And then you probably learned the C language and practiced a bit.

05:21 화자 1
You've done some programming on your own, not AI. Again, I will emphasize this later: you should do the programming yourself in this course. No AI is allowed. You should know the basics of programming in computer languages, and have some idea of data types, algorithms, and computer architecture. In this course, you learn more about the API and the interaction between your application and the system, here called the operating system.

06:06 화자 1
You will know the inner workings of the underlying OS and acquire knowledge for more advanced courses such as Operating Systems, Compilers, Databases, Networks, and Embedded Systems. Eventually, I hope you will become a better system-level programmer. There is a course called Operating Systems that is focused on learning the internals of the operating system. Here, we learn the external side of the operating system—what the operating system provides—and then we use those APIs and system calls to write system-level software. You can build applications as well, actually.

07:00 화자 1
So today, first, we'll do an overview and logistics, and then a little overview of everything. Originally, the time is allocated for two hours on Wednesday and Monday. We are very passionate; we give up our lunch. So we have two hours allocated to each time, lecture and practice. Yeah, she said, right? But that was not my idea. Someone argued for having the practice time, but I'm not sure you can practice for the full two hours, so I think I will instead…

08:00 화자 1
We'll use one and a half hours for lecture, mostly. From time to time, we'll use one hour for lecture and one hour for practice. Practice here means the TAs come here and explain your lab, your programming assignment, and then do Q&A. We will also do some quizzes. Okay?

08:33 화자 1
We do not have any real experiments or lab practice; we'll practice in class here. And this applies to all required courses in Computer Science and Engineering: you are not allowed to drop this course beyond the first two weeks. You can change your course, drop the course, or switch to another course this week, no problem. Next week, maybe if you want to drop the course, you can tell me and we can do that. But from week three, you cannot drop. This is a common policy for all required courses in CSE, so exactly, no drop from September 15.

09:31 화자 1
Retaking this course may be difficult because enrollment—well, for the fall semester it's okay, I think, but for the spring semester it's very competitive. So my suggestion is: please do your best this time. If you think you're not ready, please drop it now and take it later. My name is KyoungSoo Park. Yes, this is my picture from some time ago. I got my PhD in Computer Science at Princeton University.

10:17 화자 1
I have some software development experience. Actually, my research area is systems, so we write a lot of code. Sometimes we use Python; mostly we write in C. Before starting the research, I was a developer of Hangul Wordprocessor. Do you use it? The drawing part is completely written by me. That's the worst part. Anyway, I did some development. I also had a startup company, CoBlitz, which was based on my PhD thesis.

11:07 화자 1
Video CDN. CDN is content distribution. You know YouTube, right? When you go to the YouTube site and download a video, it is not coming from Mountain View, California; it's coming from here. The video is distributed around the world. Transparently, whenever you try to download it, the browser tries to find the closest server that has the content. That's how it works; it's called a CDN. I did it, but didn't make a lot of money, so I'm still here. I do research in high-performance networked and distributed systems.

12:03 화자 1
So we have five TAs. They occasionally teach practice classes. They explain labs and do Q&A, and they grade your programming assignments, quizzes, and exams. They help with your questions and answers. You can reach all these TAs via the `snu-sysp` Google Group, but the preferred communication method is ETL. You can post any questions in ETL and we'll try to answer them in time. You can meet me after each…

12:58 화자 1
after each lecture, okay? Or you can make an appointment by email for office hours. For TAs, same thing: you can ask questions after each practice class or make an appointment. ETL is the platform for asking and answering questions at any time, so we make all important announcements on ETL. Please monitor ETL carefully. We do Q&A, discussion, and quizzes. [[unclear]]

13:59 화자 1
Class materials and programming assignments are also on ETL. Q&A is very important, so I encourage you to ask questions in class and online. Anyone can answer anyone's question on ETL. Your activity is reflected in your participation score, so we will count the number of your involvements in class, in person or online. We will count the number of postings per person at the end of the semester. And please search for existing questions before asking.

14:54 화자 1
And then, the textbook. This is *Computer Systems: A Programmer's Perspective* by Bryant and O'Hallaron. It's pretty old—2015—but the content is still up to date. It doesn't cover the latest C language, of course, but it has enough knowledge about computer systems.

15:40 화자 1
Then there are many supplementary books. I hope you're all familiar with C, but if you need more practice, *C Programming: A Modern Approach* is a good book for reference. Then *The C Programming Language* is a classic, very famous book by Kernighan and Ritchie. It covers the C programming language. *The Practice of Programming*—I'm not sure if you know this book—is very good. This is not about C itself; this is about how you write code and how you maintain code. I recommend this book. It's very good.

16:27 화자 1
And then *Programming with GNU Software*. It talks about specific Unix tools, like `gdb`, et cetera. You can use a previous edition, too. Manuals: for assembly language programming, you may need the Intel 64 and IA-32 Architecture manuals. For doing assignments for this course, you may consult `man`. `man` is short for manual, so `man strstr` shows the manual page for the `strstr()` function.

17:33 화자 1
Okay, the programming environment is not ready. The staff is very busy. We will have a few machines called lab machines for programming assignments and practice. They are supposed to have Linux and all the required software for programming assignments installed. Unfortunately, the machines are not ready, so don't consult this slide yet. When the machines are ready, we'll create your accounts on all machines and let you know by an announcement. You need to change your password before doing assignments, okay?

18:32 화자 1
You might want to set up your own programming environment, not just use a lab machine. You can go there by SSH, by remote connection, but that may be slow, so it's better to set up your programming environment locally. Use your own computer and run GNU tools to run your programs locally. You can install Linux, but my best option is to install Windows Subsystem for Linux if you're using Windows. Okay? Please install Windows Subsystem for Linux. If you're using a Mac, macOS has its own challenges, unfortunately. macOS is from the BSD tradition. That is different from Linux, and some libraries are slightly different.

19:33 화자 1
I haven't looked at it recently, but even `strstr` library parameters are different. It may not compile on Linux. It's up to you if you want to use the macOS shell, but if you're using a Mac, you may need to find your own solution. If you're using Windows, you can install Windows Subsystem for Linux. It's very easy to install, and then you can do pretty much everything. Another option, which is not recommended, is to run a non-GNU development environment locally—Visual C++, for example. You can do it…

20:32 화자 1
but then it may not compile again. If you're developing on macOS and using libraries there, it may not compile on Linux. We don't care where you do your assignments. Please make sure to test your program on our lab machines because we're going to use our lab machines for grading. We cannot give credit if your program works on your local machine but does not run on our lab machines. Please make sure to test it. Our recommendation is to use your local environment for coding and the lab environment for testing and debugging.

21:27 화자 1
Yeah, this is the grading breakdown. We'll have midterm and final exams, each 30%. Programming assignments are 25%. This semester, we'll introduce quizzes for each programming assignment. You may guess why; I will tell you the answer. That's 12%. Participation—class involvement or online postings—accounts for 3%. Three percent looks small, but it's as big as the first assignment. Participation is very important. We will do five programming assignments, carefully designed for educational purposes.

22:27 화자 1
Okay, the first one is Decommenter. The deadlines are tentative, so it's possible that class progress is not enough and we may give you an extension. But roughly, the first assignment is due September 18. The percentages are also the percentages of the final grade, so the first assignment is worth 3%.

23:09 화자 1
We'll provide some extra credit for some of the programming assignments. We plan to have a quiz for each programming assignment. Ideally, we'll have a quiz right after each assignment, and it will check whether you actually did it. We try to give you questions that you can solve easily if you did the homework yourself. We ask questions about the programming assignment. Quizzes can be bundled together with the midterm or final exam; it's not decided yet because it depends on the schedule.

24:01 화자 1
So that's 12%, and the relative weight will be similar to the weight here: 1, 2, 3, 4, 5. Grading assignments: your submission should be working code. That's the first criterion. We may also look at your code. It should be clean, readable, and maintainable. We do not accept any late submissions, so plan ahead and do it early.

24:55 화자 1
So no late submission is allowed except for extraordinary cases like hospitalization. Your own wedding—but, you know, you should tell us if you're going to get married. Tell us very early. Assignments from other courses being overwhelming—sorry, that is not extraordinary; that's the ordinary case. Okay? So please plan ahead. This is important, very important: no late submission. And then, again, cheating and plagiarism. Cheating is not allowed, and we will try our best to catch it.

25:52 화자 1
So please don't do it. That will result in an automatic failure. Typically it's an F, but I typically give a D-. Because it's a required course, in the worst case you cannot graduate if you get an F. With a D-, maybe you can graduate. Anyway, you don't want to graduate with a D-. I think a D- is almost as good as an F, so please don't do that. 여러분도 죽고 저도 죽습니다. 이 상황이 되면 여러분도 기분이 안 좋고 저도 기분이 안 좋아요. I will talk about it again later. Attendance is required but not counted, so it's not going to be reflected in the grade.

26:54 화자 1
But you will fail automatically by school policy if you're absent for more than one third of the classes. You know the rule, right? Participation is evaluated; that's 3%. Again, it's as good as the first assignment. Actively participate in class and discussion. You can ask a question, or if I ask a question, you can answer. Every involvement is counted. Ask questions, tell us your thoughts, or share your experience. In-class or online participation will be counted. It's offline or online.

27:44 화자 1
Each offline participation counts as 1%, and each online participation counts as 0.5%. So let's say you participate two times offline and two times online: you get 3%. Makes sense? Or if you make six postings online, that's also 3%. So it's easy; anything's fine. Please interact, tell us your thoughts, ask questions, or answer questions. We'll use ETL for that.

28:30 화자 1
How to get help: consult with the professor or TAs directly. What's good is that you figure out the symptoms first. “I have some code that has a problem. This one works well most of the time, but when I give this input, it crashes for some reason and I don't know why.” Explain that to the TAs. The bad thing is to just give your code to a TA and ask them to figure out what's wrong. We cannot help you this way—or probably we don't want to. Many people may be doing this.

29:21 화자 1
The worst approach is to give your code to AI and have it fix the code, or give the assignment itself to AI and have it generate the entire code. That is totally unacceptable. I emphasize this: we do not allow AI coding. This is System Programming, and the programming should be done by you. We will try hard to help you succeed. I'm willing to give a good grade to everyone if you actually do well on your own. So don't be shy. Last question: how to get a good grade? Internalize the basic material first. Know the C programming language well.

30:22 화자 1
You need to understand compiler errors, types, loops, structures, arrays, strings, pointers, static functions, and C runtime library functions. Read class materials before each class. Allocate some time for this course—at least seven to ten hours per week. It can be smaller or larger, but you need to allocate some time. A systematic approach will dramatically reduce your debugging time. And, again, consult with TAs and professors.

31:04 화자 2
Okay.

31:06 화자 1
Letter grade only. Try to ask questions in English. We have international students, so this first part is in English. But if you cannot express it, that's fine; you can ask a question in Korean, too. Before speaking Korean, please try speaking English, then we can switch to Korean. If you cheat, you get a D-—no exceptions. We'll thoroughly check plagiarism. Please don't test your luck. Unfortunately, last semester—I'm not sure if it's in a newspaper article or not—[[unclear: AI 부정행위 관련 검색 안내]].

32:06 화자 1
But we actually interviewed everyone last semester and caught more than half of the students. Everyone confessed in the end. I know you use AI every day, and using AI every day is good. Using AI to understand and learn things is good, I think. But this is a programming course. You should pick up programming skills on your own, and then you can prosper with AI. Anyway, my hope is that I see no one…

32:57 화자 2
No cheer.

33:00 화자 1
No cheaters. Practically impossible? Okay, that's my goal. So don't copy. Not just AI: don't copy code from your predecessors or across course sessions, and don't try out generative AI. Don't risk it. You may think, “Oh, how can you catch me?” So I'll talk about the AI policy first. You can use AI to…

33:30 화자 1
understand class materials. “How do I use library functions? How do I use system calls?” Good, you can use AI for simple examples. “I want to learn more about a concept taught in class, such as a zombie process.” Very good. You can use AI to learn more; I encourage you. But please do not use AI to write or debug code for any assignment. That should be done on your own. You should turn off automatic completion. If your editor has automatic completion, please turn it off. That's cheating. And you should write all documents and reports…

34:25 화자 1
yourself. The document is short; it won't take too much time. Okay? Please write the document on your own—everything on your own, virtually everything on your own except learning and understanding the literature. Is that clear? Any questions? We are committed to catching all plagiarism in this course. Like last semester, we may interview you at the end of the semester or not; it's really up to you. We will actually read your code, and we have some clues about whether the code was generated by AI or not.

35:20 화자 1
Why? Because you're doing the same assignment, and if you use AI, it tends to generate very similar code, sometimes identical code. Not all of the code is flawless; unfortunately, some lines are unnecessary, and we'll catch them. Then we will ask whether you know this function. If you cannot explain it, it was not written by you. So please don't test your luck. We may not be able to catch everyone, but that doesn't mean that if you get caught and get a penalty, you can complain.

36:03 화자 1
Okay, that's the rule. If you do not like this rule, maybe take another course. We will apply the same policy across the semester. If you don't like this policy, that's okay; you can take it later. What I can assure you is that the majority will be caught. We also do interviews, and we'll use a lot of information to determine whether you did the homework yourself or not. I'm not trying to give you a hard time. I'm trying to give you the opportunity to learn for yourself.

36:53 화자 1
Okay, don't give up your learning opportunity. Don't give it to AI. You're not thinking; you just offload your critical thinking. I know you're going to use AI after graduation, when you go to industry. Productivity is good, but if you want to be a better programmer at that time, industry also thinks that you need to do some manual coding in school. Okay, so I can…

37:34 화자 1
talk about this offline. If you want to have a discussion about this, that's good, but this is the policy. If you do not agree with this policy, please take the course later. Or, if you're not a Computer Science major and do not have to take this course, maybe you can take another course. If you think coding should be done by AI and there is nothing to learn, you don't have to take this course. So I emphasize this again: please don't complain later. Okay, sharing code—traditionally, looking at someone else's work during, before, or after the assignment time period…

38:32 화자 1
that's also cheating. Allowing anyone to view your work during, before, or after the assignment time period is cheating. Helping your friend write a lab line by line—that is coaching, and coaching is cheating. Don't Google code or algorithms directly relevant to the assignment. What's not cheating? Explaining how to use systems, system calls, or tools—that's fine. Explaining to your friends what is meant by the specification, or looking at output and telling whether it's correct or not. Okay? Googling basic C grammar is also fine. We will require a description of resources in your assignment…

39:28 화자 1
in the README file you're supposed to submit. In the README file, acknowledge all resources used. Whenever you're not sure, ask me or the TAs. Do not assume that it's okay to do X. If you're unsure, please don't guess; ask us. Okay, let's get into the real thing. Why should I take this course? Software is taking over. It's the hottest industry: Google, Meta, Amazon, Netflix, Zoom, Tesla, and many fintech companies. They all do software. Understanding systems and system programming is the basis for cloud infrastructure, so if you want to build infrastructure, you need this knowledge.

40:28 화자 1
We use C, not Python, for this course. Why? Because C is a lower-level language. It can describe details and is more expressive, so it provides more opportunities to create abstractions. It facilitates a language-level tour; it's close to assembly language. The immediate motivation for C was to replace assembly language. There are too many assembly languages, and they wanted a higher-level language to build an operating system.

41:17 화자 1
Okay, C facilitates this service-level tour. Linux is written in C, and using C, you can interact with operating systems. Of course, you can use Python, right? This is closer to the application level. We can write an operating system with C, but not in Python. With another high-level language it may be theoretically possible, but performance wouldn't be good and there are many issues. Typically, commercial operating systems are written in C or C++. C is a lot faster than Python. You can write games…

42:08 화자 1
with C better than in Python. I'm not arguing that C is better than Python—of course not. So don't get me wrong. C can be a very useful tool for system-level programming, with access to lower-level APIs and direct access to memory and I/O through low-level operations.

42:29 화자 2
So

42:29 화자 1
I don't mean that Python is not useful. Of course, Python is useful. So why do we use Linux instead of Windows? Linux is good for education and research: it's open source and well specified. It's good for programming; it's a variant of Unix, and many open-source tools are available. Topics in this course: a brief review of C programming. C is flexible and expressive. What do we mean by expressive? It's small…

43:28 화자 1
you can express many things without typing many instructions. In assembly language programming, you have a lot of instructions and each instruction does a tiny thing. But in C, we do `A + B` and it does many things, so it's expressive. It's a low-level programming language and allows you to write many kinds of programs. We'll cover file systems, the memory subsystem, processes and exceptions, shells, network programming, and concurrency control—all outside the operating system. That's the difference from the Operating Systems course. If you take Operating Systems, you get inside the operating system and try to build one. Here, we assume there is an operating system and interact with it.

44:26 화자 1
So, a total of 15 weeks. This is just a tentative schedule; take a look. Of course, the midterm and final exam dates are not fixed. They're tentative dates, but we will have the exams near those dates. Details are subject to change. The first assignment is due September 18, and then we'll have a quiz on the following Monday. Most lab due dates are Fridays, and on the following Monday we'll have a quiz.

45:32 화자 1
Okay, do you have any questions? So we are done with the administrative stuff. Ah, electronic attendance is on, I think. We don't do attendance manually, so you should check electronic attendance yourself. Anything else?

46:09 화자 2
[[unclear: My name is program and app.]]

46:12 화자 1
Okay. I don't use a Mac, but you can ask questions. The TAs may provide some solution.

46:30 화자 2
[[unclear: Wondering is.]]

46:31 화자 1
You can provide…

46:32 화자 2
[[unclear: Walker events for us too.]]

46:35 화자 1
We don't have a Docker image, so remember that. But the lab machines will be available soon, so you can check the Linux version there and create the setup you need. [[unclear]]

47:01 화자 2
안녕하세요.

47:03 화자 1
Okay, let's get into computers today. This is the mainboard of the latest desktop. What does a computer look like today? You have a CPU there, and these look like memory slots. And this is one, and this one looks to be a GPU. General-purpose hardware: the topic we will deal with is x86 architecture. CPUs these days have many cores. You cannot find a CPU with a single core. Four to sixty…

47:56 화자 2
Or even more.

47:57 화자 1
CPU cores are common. Probably your laptop has four cores or eight cores, but some server CPUs have sixteen or more.

48:19 화자 2
Of course.

48:20 화자 1
And then, the network card. Probably most of you have a one-gigabit-per-second network card. That's the norm for commodity PCs, but servers tend to have 10- to 100-gigabit-per-second networking, which is becoming the norm in datacenters. One trend in computer hardware is that it's getting smaller and smaller but becoming more powerful. The first computer we have here is ENIAC. ENIAC was completed in 1946. I updated this, but somehow… That's not important.

49:09 화자 2
Okay.

49:10 화자 1
The first computer was huge. Then, in the 1960s, IBM came up with mainframes. That's not for personal use. These days, the popular big tech companies are Google, Meta, and Amazon. In the 1960s and early 1970s, there was one big tech company: IBM. They produced mainframes, and that's one of them. Smartphones today are millions of times faster. Another trend is that computers are becoming ubiquitous—everywhere. Computers are dominating our lives, and more things are becoming computers. Cars, right?

50:10 화자 1
There are lots of electronic devices, and computers are at the center of the electronic devices in a car. Watches also became computers. They can use the network, get your schedule, and respond to phone calls. Trend three is that, on the server side, computers are growing to a larger scale. The cloud is prospering, with machines spread around the globe.

50:58 화자 1
Meta has hundreds of thousands of machines. Big companies do not reveal how many machines they have; it's a corporate secret, too. But these numbers are available in documents. Microsoft used to have four million servers around 2021. Google had 2.5 million—probably much more than that these days; that's from ten years ago, 2016. I updated this one: hyperscalers will spend over 600 billion dollars in 2026. One billion dollars is about 1.4 trillion won.

51:56 화자 1
And then, some snapshots of Google's datacenter. This part is called a rack. One big vertical thing holding servers is called a rack, and each individual machine is called a rack-mountable machine. Most services are based on the Internet. Okay, so if you use KakaoTalk, that message does not go to your friend right away; it goes to a server, and that server could be served by the cloud.

52:52 화자 1
Amazon is one of the representative cloud vendors. Relatively, the number of servers is smaller than the number of smart devices that we have, but the devices interact with services in the cloud. There are endless applications provided by the cloud. Computer systems and software are important in the cloud-computing industry, but not only in directly relevant industries. They can also have an impact on the semiconductor industry. Samsung makes memory.

53:45 화자 1
But that memory is now attached to GPUs. That's closely related to AI. Why do they produce high-bandwidth memory, HBM? Because the bandwidth is required by AI inference and AI training. The semiconductor industry is tightly coupled with software. In the end, demands from software are reflected in hardware. What's really interesting—I do networks, right?—is that HBM is so expensive that you cannot create a huge amount. You cannot have one petabyte of HBM in one machine, okay? So you can have a small amount of HBM here and a small amount of HBM there; they need to communicate.

54:41 화자 1
We need to serve this communication demand with networks. Typically, we focused on TCP/IP and then cloud networks. But many networking researchers are now working closely with AI training and inference—how to reduce latency due to communication between GPUs. Anyway, the automobile industry as well is also powered by software. Okay, let's look at a little bit of C history. I knew nothing about BCPL except that it influenced C, but actually we'll look at B today. B used to have `++` and `--` operators.

55:41 화자 1
It's pretty similar to C, but what was quite different between B and C was that B didn't have any types. Why? Because every datum was the same type, and they expected that to work. Anyway, B was invented by Ken Thompson. You know who invented Unix? Ken Thompson. He won the Turing Award in 1983. Do you know who invented C? Dennis Ritchie. This person, Dennis Ritchie, invented C because Ken Thompson and Dennis Ritchie were working on…

56:32 화자 1
an operating-system project called Multics. Multics was a big project. Think of an operating-system service like electric service to every home. They wanted to create a big operating system for the entire New England area. Somehow, it was too complex and had many, many components that didn't work well, so it failed. But Multics eventually inspired a simplified one called Unix, and in order to build Unix well…

57:11 화자 1
They needed a systems programming language. Assembly language was known because you can do anything with it, but it is not portable because it depends on the CPU architecture. They needed a higher-level language to displace assembly. That's why they came up with C, and using C they built Unix together. C was eventually built for that. Then, after that, the book *The C Programming Language* came out. That's a very famous book.

58:00 화자 1
Kernighan and Ritchie's book. The C grammar style in that book is called K&R C. But C was standardized internationally, so there were many standardizations: ANSI C89, or ISO C90, which are the same thing. C99 is another big standardization that we will use. The latest one after C99 is C23, but mostly C99 is okay for this course.

58:44 화자 1
And C also had an effect on C++. Design goals: it supports structured programming. What is structured programming? [[unclear]] Before that, everything was like—by the way, you don't have to… Okay. If you write assembly, there is no concept of functions. Everything is linked together, with no structure. So what is structure? What kind of language feature provides the notion of structure?

59:45 화자 1
그러니까 좀 정돈을 하고 싶거든요. So you try to understand large things. We're not writing multiplication tables all the time, right? That's not the goal of programming. We want to build real things. Real things mean that the number of lines of code is huge. It can be ten thousand lines of code, more than ten thousand lines of code. If you go to industry, it's millions of lines. Linux has more than ten million lines of code.

01:00:22 화자 1
Then how do you understand this code? You should deal with complexity. There are many things. At that time, Unix was written in three thousand lines of assembly-language code and seven thousand lines of C code. It's not big, but still it's not quite small.

01:00:50 화자 2
Okay.

01:00:51 화자 1
So, in order to manage all that code, you need to have some structure. Structure is, again, a way of abstracting things. You don't need to remember every detail: this code does X, this code does Y, and X and Y have names. How do you represent the names in a program? Functions. Structured programming focuses on functions. You write each function well and forget about the details, and then you understand the program as interactions among functions.

01:01:37 화자 1
Object-oriented programming, right? That is object-based. The interaction is between objects, and the abstraction level is different, but the goal is the same: to deal with complexity. Structured programming focuses on subroutines and functions that abstract details with helper functions. And, of course, C was invented to support development of the Unix OS and tools. As Unix became popular, so did C. What are the implications of these goals? C is good for system-level programming. So what is system-level programming?

01:02:38 화자 1
You build things that help applications. The operating system itself is doing nothing; when you run something, it helps you run it. It interacts with applications and provides services to applications. That's system-level code. C is good for system-level programming, but it can also be used for application-level programming. It's low-level: close to assembly language, close to machine language, close to hardware. In C, you have a pointer that can directly access each individual byte.

01:03:24 화자 1
Okay, that's a unique feature. Some other languages do not provide such a feature. And, of course, C focuses on efficiency over portability. It's more portable than assembly, but sometimes not portable to another machine. So, as described by Dennis Ritchie—this quote: “C has always been a language that never attempts to tie a programmer down.” What does that mean? It's flexible. It does not tie you down. 묶어주는 거죠.

01:04:12 화자 1
“C has always appealed to systems programmers who like the terse, concise manner in which powerful expressions can be coded.” They don't want to type many characters. With a small amount of code, you want to express many things. That's the definition of expressiveness: a small amount of code can mean many things. C allowed programmers, while sacrificing portability, to have direct access to many machine-level features that would otherwise require the use of assembly language. So one goal was to get rid of assembly as much as possible.

01:05:04 화자 1
Again, the original Unix consisted of three thousand lines of assembly-language code. Some portions still needed to be written in assembly language for speed or efficient access, but C largely displaced assembly. So: “C is quirky, flawed, and an enormous success. While accidents of history surely helped, it evidently satisfied a need for a system implementation language efficient enough to displace assembly language, yet sufficiently abstract and fluent to describe algorithms and interactions in a wide variety of environments.” To summarize, C is a general, flexible, versatile language that allows you to program almost everything.

01:06:05 화자 1
You can build applications, build system software such as an operating system, and directly control machines. So that's an introduction. Now, a Hello World program. In this course we're using GCC, but we're not usually just typing `gcc`; we'll use `gcc800`. This is just a macro with a bunch of GCC options.

01:06:38 화자 1
So you can just use `gcc800`. On the lab machine, we will provide and install `gcc800`. You can also install it on your local machine; we'll tell you how later. You can assume `gcc800` is just GCC, except that it has many options. When you compile the code, the output name is `a.out`. When you execute it, you will see “Hello, world.” So what is this? This is a standard header file: `#include <stdio.h>`. `stdio.h` is a file.

01:07:38 화자 1
And what does `#include` do? Conceptually, it copies and pastes the contents of `stdio.h` into this one, and then the C compiler compiles it. `main` is the entry point of execution, so execution starts from `main`. These are the statements of `main`; statements of `main` are enclosed in braces. And then `main` calls `printf`. So what is `printf` then: a system call, a library function, or your custom function?

01:08:31 화자 1
I've asked a question: is `printf` a library function, a system call, or your custom user-level function? Which one? It's a system call? Are you sure? It's confusing, sorry. `printf` is a library function, but eventually it will call the `write` system call. What is a system call? A system call is a service implemented by the operating system. What is a library function? Library functions are common functions written by someone else, right?

01:09:20 화자 1
They're for typical uses, to help you write applications better. Some programmers wrote these common functions for you, and you're using them so you don't repeat the same thing. But what does `printf` do? It takes arguments and tries to print them. This one is very simple—it's a string—but you can provide a format and arguments.

01:10:08 화자 1
The main job of `printf` is to parse the format. Eventually, it creates a big string and gives the string to the `write` system call, which is implemented by the operating system. The operating system has the right capability to actually change the screen. User level cannot directly talk to a device; the operating system can do that. It will display these characters on your screen through standard output. Standard output can be connected to a display—the monitor—by default, or it can be redirected to a file. We'll learn redirection later. Anyway, that is done by the operating system.

01:11:08 화자 1
The actual talking to devices is done by the operating system. So, yes, `printf` will eventually call a system call, but it is still a library function. Okay, what about a pure library function? Do you have any example of a pure library function that does not invoke a system call? Maybe you don't yet have a clear idea of what is a system call and what is a library function. It's okay not to know the answer; we will know for sure by the end of this semester. Do you have any guess? An example of a library function that does not invoke a system call? Math library. Math library functions are completely at user level.

01:12:09 화자 1
They run as code without any help from the operating system. Or string functions: `strlen`, `strstr`. These are all implemented purely at user level without going into the system.

01:12:29 화자 1
So we'll learn that in more detail. If you're interested in the meaning of the options, `-Wall` catches common warnings. `-Werror` treats every warning as an error; it stops and doesn't compile. Okay? So we try to give you the discipline to fix every warning in your file; otherwise, `gcc800` will not compile it. And `-pedantic` means strict compliance with the C/C++ standard. GNU has its own custom…

01:13:18 화자 1
extensions. Some code can be written with GNU options, using GNU-style extensions, but `-pedantic` doesn't allow them. You don't have to use them for this course. And `-std=c99` means conformance to the C99 standard. So what is programming? Programming can be defined very simply: it instructs the computer what to do. You can view a program as states plus commands that update the states.

01:13:59 화자 1
Commands that update the states. How do you organize a program? In structured programming, again, you view a program as a set of subroutines. You understand the program as interactions among subroutines. Subroutines improve clarity and reduce development time. Object-oriented programming deals with complexity differently from structured programming. Of course, it also supports structure, but it views a program as interactions between objects. Each object maintains states and has its own member functions as an interface.

01:14:56 화자 1
to update the state it maintains. So we're not going to study these things in depth, but from this slide we'll give a brief overview. I assume you already know the details, but let's review them. What is a variable? A name given to a memory area that a program manipulates. Each variable has a type. So what kinds of types do we have? Character type, integral type, floating-point type, and generic type. Character type is `char`, right? Then variable sizes: `char` is 8 bits; integral types are `short`, `int`, and `long`. `long` is 64 bits on a 64-bit OS. These days, general-purpose systems are predominantly 64-bit.

01:15:55 화자 1
But there was a period when 32-bit operating systems existed. At that time, `long` was four bytes, or 32 bits. Then floating-point types: `float`, `double`, and `long double` are 32 bits, 64 bits, and 128 bits. I hope you learned the format in a digital logic circuit course: you have a sign, exponent, and mantissa part, right? That's completely different from integer binary representation.

01:16:42 화자 1
`double` will be 64 bits. We know that. The person who devoted his life to developing this floating-point number format won the Turing Award, maybe. It's difficult to express floating-point numbers in binary form. A computer is all binary, zero and one, and there can be many errors. Anyway, I'm not going into that detail. IEEE 754 is the standard specification for floating-point numbers. It's a great work of engineering. `long double` is 128 bits. The generic type is `void *`, a pointer variable.

01:17:43 화자 1
This is 64 bits. So every pointer in this course has a size of eight bytes. What is a pointer? A pointer holds a number. If you print the pointer value itself, it's a number that looks like an integer. What is that number? It's the memory address of one byte, right? In modern computers, memory is addressed by each byte. In the BCPL and B period, memory was addressed by words—four-byte or two-byte words—but nowadays memory is addressed per byte.

01:18:40 화자 1
It looks like an integer, but we use the number for a different purpose through a pointer. Constants: there are three ways to define constants. You can use `#define MAX 10`. This is substituted by the preprocessor. When you compile C code, the first thing to run is the preprocessor. The preprocessor processes anything that starts with `#`, such as `#include`, `#define`, and `#pragma`, and removes it; the pure C compiler will not see it.

01:19:34 화자 1
Okay, so what the preprocessor does is, every time it sees `MAX`, it replaces `MAX` with the number 10. That's one way to define and use a constant. And `const` is a variable; it takes up memory, but you cannot change it. The variable itself is called a constant variable: `const int MAX = 10`. You can define it that way, or you can use an `enum` of integer type. An array is a collection of elements of the same type. You probably know how to declare one-dimensional and two-dimensional arrays. So how many dimensions are possible?

01:20:27 화자 1
A thousand dimensions? That's possible. Probably you won't need more than four; something is wrong if you have to do seven dimensions, right? Again, it depends on the implementation, but technically there is no small fixed limit. What about `sizeof(pi)`? What is the answer? How many bytes does each `double` variable take? This counts as one offline participation if you answer me. Okay, how many bytes does each `double` variable take in memory?

01:21:23 화자 1
Eight bytes, right? Then what is `sizeof(pi)`? How many bytes does this guy take up? It's okay to be wrong. Eight bytes? Raise your hand. Eighty bytes? Eight hundred bytes? The correct answer is 80 bytes. Why? Is `pi` a pointer? Is `pi` a pointer?

01:22:13 화자 1
Come on, you were using AI too much. You should learn C. This is a problem. Or maybe you're too shy. I'm sorry, I'm glowering. What is `pi`? Is it a pointer or not? What is the type of `pi`? What is the type of `c`? You should be able to say that. `c` may look like… No, `c` is an array of characters of size 10. That is its exact type.

01:23:03 화자 2
Okay.

01:23:05 화자 1
Actually, what `sizeof` does is extract the type information. It finds the type of `pi` and calculates how many bytes are needed to represent it. So what is the type of `pi`? `pi` is an array. It's not a pointer. It looks similar, and you can sometimes use arrays and pointers interchangeably, but its type is not a pointer; it's an array: a two-dimensional, 5-by-2 array of `double`.

01:23:51 화자 1
Then what is the size of `pi`? It has 5 times 2, ten `double` variables—ten elements—so that requires 80 bytes. Here we're seeing pointers. Of course, we will talk about the relationship between arrays and pointers. It's a little confusing. If you use `pi` in an expression, it may become the address of the first byte of this entire array, but that doesn't mean the type of `pi` is a pointer. The type of `pi` is an array. What about this? A pointer holds a memory address of a variable of some type. So what is the type of `p`?

01:24:53 화자 1
Integer pointer. What is the type of `*p`? `int *p`: `p` is an integer pointer and `*p` is an integer. What is the type of the number 2, the input constant number 2? Integer—by default, a constant integer. What about the type of `k`? `void` pointer. What about `*k`? What is the type of `*k`? Okay, that's difficult, I know. What about `sizeof(k)`?

01:25:40 화자 1
Again, we assume a 64-bit OS. What is the size of `k`? Same thing: 8. Every pointer has the same type—sorry, every pointer has the same size, eight bytes. What is `sizeof(*p)`? So you thought this course was easy, right? Starting now, you should know that. Four, because `*p` is an integer here: four bytes. What about `sizeof(*k)`?

01:26:40 화자 1
Someone said eight bytes. `sizeof(k)` is eight bytes. Is `sizeof(*k)` also eight bytes? Any other opinion? If you write code with `*k`, it doesn't compile because for `*k`, you don't know the type. That's why it's `void`. `void` is special: the actual type is not decided yet. Because C allows type casting, when you try to use `k` for your own purpose, you need to cast the type. Or it can simply hold the bare address number.

01:27:24 화자 1
The memory address is fine; it can hold any memory address. But to do real work, you need to dereference it, right? You cannot dereference `void *` because you don't know what type you're pointing to. So you need to cast it to, for example, an integer pointer. What does that mean? You go to that address and, starting from that byte, four bytes are interpreted as an integer. That's the notion of an integer pointer. A `void` pointer doesn't have a concrete type yet, so if you apply `*` here, it doesn't even compile. Okay, variables and pointers. You have integer `x`…

01:28:18 화자 1
If you print out `x`, you will print out 1 or 2. But for integer pointer `p`, if you print out `p`, it may print as 2. It looks like an integer, but it's not an integer; it's an address. Again, this is a confusing mode. A C string is a collection of characters. Precisely, C does not define a string type. C just uses characters, arrays, and pointers interchangeably to represent strings.

01:29:10 화자 1
But one contract or promise is that anything that ends with a zero character is interpreted as a string. That is just a contract among programmers; it's not an absolute rule. I'm not sure if you understand me, but let's look at these two things. The first `s` is what? `s` is a pointer. So what does `s` have? What does `s` itself hold?

01:30:00 화자 1
An address. Address of what? The specific, concrete address of the `h`. So `hello world` is located somewhere in memory, and somehow the compiler will find the address of `h` and assign it to `s`. This is confusing: you're not assigning the entire string to one pointer. A pointer only holds an address. This assignment is very special and confusing, I know. It assigns the address of `h` to `s`. What about `char s[20] = "SNU CSE00800"`? This is an array, right?

01:30:59 화자 1
When you print out `s` itself, what does it print out? So what is the difference between these two? They have the same name, but one is a pointer and the other is an array. In terms of capabilities, something is different. You can do something with the array `s`, but you cannot do the same thing with the pointer `s`.

01:32:05 화자 1
Okay, 이거 알아야 되는데. So I want to change the first character in the string to a character `K`. Let's say—don't worry about this line—I'd like to change `h` to `k`. Is it possible?

01:32:52 화자 1
So when you type this and compile it, does it compile? First of all, does it compile? Compile means it completes and produces output. Does it compile? Yes? Raise your hand. No, it doesn't compile? It compiles, although it may produce a warning. But when you run this, what happens? It crashes. Okay, so when you do the same thing—let's say instead of `char *s`, you have the `s[20]` array—what happens? That's the question.

01:33:53 화자 1
No? The array is writable. But then the `"hello world"` on the right-hand side is a constant. It's called a string constant, or string literal. With `char s[20] = "SNU CSE00800"`, this array is not a constant. You know the difference? This is just initialization. This is syntactic sugar: you don't want to assign `s[0] = 'S'`, `s[1] = 'N'`, and so on. You don't want to do all these things, right?

01:34:37 화자 2
So

01:34:38 화자 1
You want to assign the `SNU CSE00800` string one character at a time to `s[20]` automatically. So, on the array side, this entire thing is initialization. You set aside 20 bytes of memory, name it `s`, and write `SNU CSE00800` in that space. That's the array case, so you can change an array element. That's okay because the array is read-write. But if you look at the top case, `"hello world"`, what you're assigning is just the address of `h` to `s`. That's done by the compiler or runtime system, okay?

01:35:40 화자 1
And where is `"hello world"`? It is located at some memory location, but that's a read-only section. You cannot change anything there. If you try to change something, the OS will freak out and kill your process: this is wrong because a constant cannot be updated. It's a string constant. It takes up memory space, and you can read and print it, but you cannot write or modify it. If you modify it, then it will crash. Okay?

01:36:25 화자 1
A structure is a collection of elements whose types can be different. It's similar to an array, but in an array each element has the same type, while a structure's elements can have different types. Operators: arithmetic and logic operations. You probably know all of them. What is the percent operator? You're too shy—not because you don't know. This is not good. What is `%`? Remainder, modular operation, right?

01:37:21 화자 1
What about this? What is an operator? And what is the definition of an expression? You've heard of expression, statement, and operator. What is an operator? Plus or minus. When you apply an operator to operands, we call that an expression. What is an expression? An expression is a phrase that is evaluated, and in the end its output is a value.

01:38:08 화자 1
So, for example, `2 + 3`: you have the addition operator, and the output is 5. It produces a value, so `2 + 3` is an expression. The number `2` itself is also an expression because it produces the value 2. Anything that is evaluated and produces a value is called an expression. What about a function that returns zero or one? It does a lot of work, but eventually it produces a numeric value, so that function call is an expression. Anything that evaluates and produces a value is called an expression. But what about an assignment? Does an assignment produce a value? Yes. Why?

01:39:13 화자 2
네

01:39:14 화자 1
It will return a value. It performs an assignment and changes memory content, right? That is a side effect. But eventually the assignment itself also produces a value. Here you assign 10 to `i`. That's an assignment, and adding a semicolon makes it a statement—something that takes an action—but the assignment is also an expression. `i = 10` assigns 10 to the location of `i`, and it itself produces the value 10.

01:40:08 화자 1
That's why `i = j = 0` makes sense. The assignment operator is right-associative, so you evaluate the right-hand assignment first. You assign zero to `j`, and that itself produces the value zero, which is assigned to `i`. Okay? A statement is a fragment of a C program that is executed in sequence. Informally, it's a command that takes a specific action. It is typically terminated by a semicolon. We have assignment statements, `if` statements, and `switch/case` statements.

01:41:04 화자 1
`switch/case` is not strictly needed, right? You can write the same thing with `if` and `else`. So this is syntactic sugar, which is there…

01:41:15 화자 2
Or

01:41:16 화자 1
for convenience. Loop statements: I'm sure you know `for`, `while`, and `do while`. Here, I think a semicolon is missing. `break`: inside a loop, you get out of the current loop. `continue`: you skip the rest of the loop and go back to the start of the next round. With `goto`, you go anywhere—to a label. If you have nested loops and want to get out right away, it can be useful. Or if you have a common error case in a function…

01:42:07 화자 1
You test many things, and when something fails, you go to the error case. Then it's useful to have one location for handling all the errors, right? A function definition has a `return` statement. A function call itself is an expression. Of course, there is a `void` function that doesn't produce a value, but typically it is. A compound statement has braces and can contain multiple statements. A multiline comment is possible with `/* ... */`; `//` is a single-line comment. Okay, this is the final thing. Look at this program. Do you understand what it does?

01:43:03 화자 1
Do you understand what it does? You should write code like this. This is practice, okay? Use the right names. Proper naming will make code much better, especially if you're writing a large number of lines. So this translates Fahrenheit to Celsius in steps of 20. Proper comments and proper variable naming—probably we will not give a penalty for every issue, but this is very important. Please practice this in this course.

01:43:53 화자 1
So, yeah, this is the last slide. Overview of this course: learn C and programming environments, how to write large programs, and low-level programming. We had an overview of computers and an overview of C, a language designed for system programming, with variables and statements. Details are coming. We'll do a little bit of pointer practice, and then we'll go into file systems. Okay, thank you.
````
