---
title: "2026-09-14 · System Programming · 보정 STT"
course: system_programming
date: 2026-09-14
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
---

강의 녹음의 보정 STT를 공부할 수 있도록 제공하는 공개 사본입니다. 명확한 용어 오류를 보정했으며, 잘 들리지 않는 말은 불명확 표시와 함께 남겼습니다. 음성을 완전히 복원했거나 녹음 전체의 누락이 없음을 보증하는 자료는 아닙니다.

수강생 이름·연락처, 식별 가능한 자기소개·건강·사생활 구간은 표시를 남기고 가렸습니다. 해당 구간 안의 불명확 표시는 개인정보와 함께 비공개로 보존하며, 시간과 화자 순서는 유지했습니다.

보정 원본 SHA-256: `b00079ea9e21bfedc5cce5151b0e68d3828360f86f0003f2c961d34097f3ce43`

원본 음성 한계 기록: `{"recovery_status": "source_limited", "verbatim_complete": false, "source_limit_items": 340, "inline_unclear": 172}`

`[[unclear: …]]`는 확정할 수 없는 원문 후보입니다. 발화에는 수강생의 의견과 교수자의 예시·추측도 포함되므로, 검증된 사실이나 공식 규정과 구분해 읽으세요.

````text
02:34 화자 1
Okay, let's start wait. Okay, so last time we a little bit about the Unix filesystem concept, we will study a little bit more detail today in Unix. File system, there's only one root that rules them all, so you have. File system starting with a root. So in Windows you have opportunities C drive, D drive, F drive, typically back to a physical drive in Unix, no, it starts at the every file system path starts with root and you can actually [[unclear: balance Your own system on a separate right to the directory]].

03:32 화자 1
All I believe there is no concept of drive additional file systems are mapped into the file system tree as a directory so you can mount a

03:47 화자 2
Free.

03:48 화자 1
Like hundreds of different file system. Into a directory and then when you mount that file system to a directory, you can actually access that drive or any other device system using that name. So Mount point is a directory where a filesystem is so you see a stream of examples here, but it's the same thing, so we'll explain this. The mapped filesystems hide the contents of directory tree under the mount point so ls dash one so not L dash one means that what is LS shows up the files in the directory.

04:42 화자 1
Dash one means that one file per line, three line so there are three files shared and they might be factory, okay? So here we treat another directory mkdir mkdir directory extern. And then then you have four files right under the extra you create a file and what's the content of file so echo hello what does echo hello does it prints up hello and enter? It's actually six characters I pick it up to the standard out and then you redirect the content into a file, you create file which namely is hello and that position is extern slash.

05:42 화자 1
And then after the queue, look inside the directory extern, you see one file and then now I mount what do you mount the file system? This tmpfs system, but then this is the outset I want to mount that shared memory to history. That shared memory is a shared memory [[unclear: class]] what is shared memory [[unclear: classes]] so you can when you create a file?

06:24 화자 1
So you can pick up this is just a memory and then [[unclear: that memory files is ten files, you know, ten files]] so if you go to the Linux Okay, so.

06:55 화자 1
If you go to /tmp, you see sometimes these files are created by any process in your operating system so when you create a temporary file And then you don't want to have the file name clash because another process may want to open the temporary file measurement, then temporary file is created here. So for example, there's an API called [[unclear: hand file]] and then it creates file name and then it's open. It's a regular file system and then.

07:53 화자 1
So what is a shared memory, shared memory memory area where multiple processes can share data generally typically set up like half of the DRAM multiple system and then This area, you can think of this as a disk or drive, but then it's format, this is a raw stream of bits and then I want to do it as a file system so you can create a file.

08:32 화자 1
But then interestingly, that file exists in memory, it is not stored on the disk, so when you reboot it, it'll go away, but when you access it [[unclear: rate often if you write it with the test]] So you want to mount this shared memory device into a directory then what happens to hello so you have a hello [[unclear: in the yoke you originally have some files there took you can have option subjective]] Those things are hidden by this mount, so [[unclear: when you mouse a pails, you just tie what's a little bit there and then you will see the content of dividing them]].

09:21 화자 1
Okay, under the name of extern, so if you do list extern, you don't see anything because there's no file yet in the tmpfs system and then you create another file. With the content extern that [[unclear: this file not exists in children]] and then you unmount it. Then the tmpfs [[unclear: and step]] goes away at the original the content will show again when you ls extern you will see any questions.

10:08 화자 1
So originally here it looks like it's a [[unclear: device spa]] or SATA disk that this file system is on so you are at devel, so your ID is devel. Okay, so you are at home/devel directory and then inside the directory you have politically have shared what all of them. Or the temp and work are directory files, but share is already mounted by virtual files and then if you go down the path of share, you see grades labs lectures, but that is coming from another device. Because it's already mounted and then you simply created a mixture that the hello.

11:09 화자 1
We are here here and then you mount that [[unclear: shaft]] to extern originally didn't have anything, but then you create a file called extern.

11:28 화자 1
So when you unmount it umount so mount unmount this is required sudo privilege you need to have root privilege if you unmounted [[unclear: the original contemplation because you were high]] This is extremely powerful concept each mounted file system can have additional properties you mount something is readable so then you cannot really write it into the Okay, so do not well write ro read only do not update access time atime so In Unix file system you have typically you have three times one is atime access time so this access time is records the last access time.

12:32 화자 1
So access means that not just write, that's also a modification time, right, but also reads so whenever you opened up a file and read access time will change so if you kept access time changes that will be [[unclear: eventually spoken in this]] it's expensive. Operation it's good to check time of the last access time but when you don't deal with that pressure lots of IO to be so typically you save the [[unclear: load]] access time, don't update access time.

13:09 화자 1
Do not allow execution of programs sometimes you don't want the program under some directory to the execution so for example tmpfs that's for people read the right about Because the original tmpfs file system, you want to create a file priority and then store something and eventually you remove it, right, that's typically not for executing something, but then the Permission is not set of properties that can be exploited by the picker so to prevent that kind of attack at all rate, then you can mount the bottom established noexec.

14:38 화자 1
And then you do not allow set user/group ID so we will learn this SUID set user ID SGID so that means that when sometimes when you want to execute something and then when that program is running it behaves like what That's just what's the time that you do your work, but then if you want to change. If you want to update some directory, then you don't have access to I want to submit my assignment using limits and then submit to some of this typical just copy my directory, the file in my directory into file in the root directory. But you don't have access to the root directory, so normally.

15:27 화자 1
But when the executable is set up to use SUID setuid when it's running right, anybody can run, but when it's running, it has the [[unclear: pre delivery]] actually. We will actually learn this again and then so if you just type mount, you will see all the file systems that are mounted so sda4 is mounted as [[unclear: We write the excess line]] and then there are some other properties that we can take a look at it a little bit, memorize that.

16:15 화자 1
When you do ls -l, each file is owned by user and so you will see many fields the leftmost part shows permission's type, read or writable or directed or regular file. And then the next [[unclear: time]] hard links number [[unclear: context]], user ID and group ID and then you have the size and then modification time so I talked about three, three times, right, atime mtime ctime atime access time. We write mtime this last modification so modification of content. The change time, what change change time is only the update of the metadata.

17:13 화자 1
When [[unclear: metadata IGO becomes a little bit of the IGO file size]] so like if you write something not only update mtime but also update ctime, but it changes the contents of the file, but also it updates the size, something like that, but So what are these links hard links of user group of files point to each other by soft or hard links so user ideas your idea what is group ideas you have an idea group ideas you can create a group and then you can add user to group. And then you can set each file to set the permission.

18:10 화자 1
Of the file towards group so I can make this file wait on the write on my group then not only wait and write your group anybody in your group so you are working in a tool and then If you have group, if you have group by the present users and then you can set the file permission of group file permission for read writing, then others connections. And then the rest of them are others, are you? User group and other rest of the. You can set up the permission here we create [[unclear: a L test, the L test, the TXT]] and then using ln ln is a link with ln you can create a hard link.

19:10 화자 1
Or soft link. Hard link so both of them are aliases, another thing to the same thing.

19:18 화자 2
Okay.

19:19 화자 1
So hard link [[unclear: the true]] is actually shares the inode. Okay, so ln hello.txt so I want to have another name called [[unclear: hello or the text taxi]], but that points to the same file hello.txt. So the content is existing when you update hello txt and then when you read. And [[unclear: hello, mortality]] is still there, so if you delete all of them, it goes away, it needs to be reference counted, that reference counted here.

20:16 화자 1
Delete or even hello.txt that goes away but you will see this guy is decrementation one so when you delete this helloworld.txt then they share the inode. The only that they don't share is the name so they share the inode by the way, inode [[unclear: does not have to be finally finally who has the finally directory has lost the final Finance]] and then so you can have multiple directories and that points the same inode shares but then similarly you can create another alias.

21:10 화자 1
[[unclear: Test statistic]] I want to call it as testing it then this is called symbolic link or soft link, same thing. So hard links share the same inode soft links contains file paths so testing is file whose content is file paths so test.txt is the file content. Of the testing but then it's file type l l is link so whenever you give file system operation the file system follows the link when you type open.

22:03 화자 1
[[unclear: File up]] or open testing then it does not open just testing stuff, it opens [[unclear: test statistics]] you can disable following in API that's possible but without any option you want to see that it's the same thing. So the information is different, the purposes is similar because testing has the file testing and then it has its own inode. [[unclear: GX]] file size is different so testing not GXP has like thousand bytes but then testing itself if you see testing stuff, how many bytes does it okay, GH many bytes, it's much smaller.

22:57 화자 1
So it's different from hard link exactly [[unclear: safety]] with different filenames, so it has same it's shared and same it has its own inode. But then testing has just file path whenever you do file accessing operation, you follow the path that will be operation with the target. With different filenames that's kind of hard links limitation they say cannot cross file system boundaries so you can mount arbitrary file system to a directory and then you want to create a links over them that's not long.

23:55 화자 1
And it cannot link to directories but soft links can do [[unclear: author]] soft links is just a path here but then problem is that it's possible that the original the real target file is removed let's say you want to remove [[unclear: test tips]]. The one testing itself is failed there testing just points to test.txt so when you do file system operation with testing, it fails because there's no more argument there, okay?

24:31 화자 1
But then hard link doesn't it has reference any question about this I Sometimes I have to encounter [[unclear: something Sydney in capitalists]] but why do we use hard links why these cases are there to prefer hard links over symlinks so the alias why Why do you use hard links, you can say the stories these days, this case not your expenses, but in the past it used to be your expenses.

25:15 화자 1
But that's one thing so if you have many things, maybe you have the exact copy, let's say it's just one file it doesn't matter to me personally if it's just a one file doesn't matter but if you have like three thousand different files but then you also have a copy of the exact same thing for like data set but I want to use the same name in a different directory. And either one is fine, but then the symlink following also incurs a little bit of overhead. So hard link is exactly [[unclear: simply]] with different [[unclear: length]], so you don't have any overhead, but there is also inconveniences where you have [[unclear: multiple positives]]. About it symlinks can work symlinks can also do directory link, right, so it's a little bit different so it's up to you where you use it.

26:13 화자 1
Okay, let's move on so here we create so here we changed a helloworld.txt so we did create the hard link But then originally you have hello, I want to add a hello world, hello world and answer. And then when you cat exactly the [[unclear: hello doctors t]] is always changing, updated and then they look at this size, exactly say why because they initiate something. What about the symlinks so you change the content but the content test.txt is also so you have the same purpose like alias.

27:10 화자 1
So if you do cat testing, but then Cat will try and open this file and then you can print it out to standard out, but then Instead, opening the testing itself, right, it opens testing and of course there is no just one answer, not two answers.

27:31 화자 1
And then if you look at the size, size is different, [[unclear: a price instead, as the TXT]] is entire content, then you look at the Type of the file is l symbolic link so if you remove Yeah, if you remove [[unclear: hello tips B]], it's still fine you can use helloworld.txt, [[unclear: testing along the words]] testing itself is there, but you cannot really open the target file. Okay, type file type is left most capital CD.

28:22 화자 2
Thank you.

28:26 화자 1
[[unclear: P types of divided type and so can]] c character device of b block then hidden files start at dot. So if you want to see all times, then not just Dashl, you want to do a ls -la and then you have two special files [[unclear: current year, three, one is current year]]. Okay, so when you do ls -l ls -l directory that you see the content of directory, when you want to check the property of directory itself, ls -ld directory and sample this directory.

29:25 화자 1
So it's a directory and then the hard number of hard links for but if you look inside the sample, there are two more directories, dir1, dir2 Okay, so here you see the sample is the parent directory of [[unclear: two UIO one directory]] why do you see four it doesn't have any other content, you had other class, you have two. You only have two subdirectories, but the number of hard links is four, it does not have any other hard link, [[unclear: you conduct the hard link to directory]] but then Dot, then this is exception dot, then dot dot, then dot dot, these are the sisters.

30:24 화자 1
So y is four y two for the dir1 dir2 because we need to understand this special file that the dot is hard link to [[unclear: culture]]. So you have dir1, probably inside dir1, you can see dot. And dot points to dir1, it's hard link to dir1 that's why the dir1 number hard links two same thing as dir2. Then the y sample is four, [[unclear: sample is suffering and the dot is zero two]]. And two extra two is where does it come from, it comes from dot dot from dir1 in [[unclear: the IR]].

31:21 화자 1
[[unclear: The other one is data, the other two is data data points two parents step]]. So each directory has dot dot that points to the parent directory so number hard links of sample is itself, sample itself and dot and then dot dot inside dir1 directory. All right, let's move on. So what was the Filesystem Hierarchy Standard by Linux, it looks to be if you look at the root directory, there are many bombers inside it, but then they have some standard.

32:08 화자 1
Slash is root /bin has files the essential binaries required during boot-up /boot boot loader kernel [[unclear: their]] device /dev all the devices like [[unclear: business conditions]].

32:26 화자 1
Memory, audio, video, all these device files are there, /etc, configuration system wide configuration files like DNS server is Home directory and lib64 media mnt you don't have to memorize all this, but take a look at it so you don't get arrested if you don't need to change the name, but unless you have any So these are created by default [[unclear: optional]] proc is interesting process it doesn't have a regular disk files so /proc if you look go there and you can see all the information about the running processes.

33:26 화자 1
And then that is dynamically checked and then it provides the file system, name it so it looks like file but then when you open it and read it, the content is coming from [[unclear: Probably create a content on the.]] usr var variable files usr/bin usr/bin usr/sbin non-essential binaries so usr/bin is typically into your default path that's why that's how [[unclear: GCCA node]] that works right it's hack I know it's hack /usr/include this directory where standard C includes [[unclear: plasma]] stdio.h is down here.

34:33 화자 1
No, dot ssh so this is hidden but then you can look at it, you get [[unclear: decided]] dot ssh can have your identifier look and have a key SSH key, you can set it properly, you can actually log in, you can log in. ACL ACL [[unclear: is sent for]] access control lists standard Unix. Three levels of access you have owner and group with other permissions, three kinds of permissions RWX, readwrite x.

35:25 화자 2
Okay.

35:27 화자 1
So the leftmost character is type file type, but then you see nine characters actually RWX, RWX one here, for example, the leftmost character is a dash regular RWX, the first three characters, the permissions of the owner, the owner of this file can read, write, execute. Then the next three characters are dashes means that that's the permission of the. [[unclear: Group office group office is very table frame]] so it's readable but not writable readable not writable but it is actually still by [[unclear: The last three dollars, four dollars are interested in green]], but you cannot execute, you cannot write.

36:28 화자 1
So execute permission on file means that you get execute program but then directory what do you mean by executable for directory x allows entering the directory so when you execute the directory that means that you can enter So you need to have X and R in order to actually enter and X itself, you can enter the But if you have R, you can see the file name is that if you don't have R, you can go there, but you cannot actually list up what's interesting. Another take to notice that.

37:26 화자 1
So only when the directory is right, what do you mean by directory files are just right.

37:33 화자 2
You can.

37:34 화자 1
Change the content if you want to [[unclear: add a group file, delete the group file]]. In order to change the list of file matrix so directory simply has the list of context you want to change it what do you do by that that means that you add a new file or you delete the filer that that privilege is on [[unclear: only when the dirt which is right to the height]] If director is [[unclear: readable]], it can actually change the concept, okay? Okay, multiply with so how do you change the condition there is a command [[unclear: change group]].

38:22 화자 1
See it so you create a file test.txt and list it and then so it's read writable by the owner but readable, readable by group and others but I want to give write permission to The group chmod g means group U means what user owners and o means others and plus means why I want to add it, minus is deleted. So plus w, I want to add a write [[unclear: tool]] so the others, the group can change the content of the test.txt. And then echo, you create a [[unclear: script of data stage]].

39:17 화자 1
script.sh is a content is just ls -l but then you cannot execute it y because there's no x execute [[unclear: a little]] bit on so you want to change, you want to add x but then you want to use different command seven five zero. So each number is octal number tendency. So you can represent it from 0 to 7 why because there are three characters and each bit is each character is one bit, the three bits RWX, three bits so seven bits one, seven bits one. So we write so seven means this one is permission for the user, for owner, this one is permission for the group, this one is permission for others.

40:17 화자 1
To give the permission user permission to send seven minutes one one one, which means RWX are all on. This script.sh is readable writable executable by the user and then 505 is what 101 right in writing, what we run are readable and executable not writable. And zero means zero, nothing you cannot read write zero by gas. Okay, so chmod is useful, you can use just name GW or you can use just number to change the mode.

41:03 화자 1
So for all there's more details, right, so you can look at man, man one one in chapter one, chapter one list of all the commands because chmod Here again, we have [[unclear: 10 participants]] and if you look at [[unclear: 10 participants directory]] the RWT, what does t mean?

41:49 화자 1
The files in a directory with the sticky bit, the t when you see T here, sticky bit is on the directory with sticky bit set can be renamed and deleted only by the owner of the file or only by the owner of the directory. Or root privileged user processes so what does that mean why do we have sticky bit in temporary class and temporary classes can be used by every okay? If someone is monitoring temporary classes [[unclear: oh, comes through this trade u five]] that should not be allowed so only the owner of the class, the owner of the trade.

42:41 화자 1
Or the privileged user or process [[unclear: and]] renaming or deleting otherwise it could expose a security issue. Okay, so it's not just an article excluding there is a ticket with other things too, then each individual It is in hazard late it's a macro S_ISVTX represents citizens and then you can see Detail or explanation?

43:27 화자 2
I do that.

43:30 화자 1
So second is general. So everything is defined it's a macro S_ISVTX actually match to 0 1 0 0 so you don't just have three. Numbers so chmod seven five zero we learned that right actually can add there are two more numbers left and what's in the right one so one zero, one zero, one zero that means a sticky bit and then And then you have like regular case, right?

44:33 화자 1
So S_IRWXG group like this is group. [[unclear: Ir ir. Double the x r of the I double the r r of the x u]] so next. Because it is S_IRWXU I want to give RWX permission to user and then that number is correspond to zero zero seven zero zero you don't have to remember number you cannot remember all this that question. If you look it up kind of or you can.

45:46 화자 1
And two.

45:48 화자 2
네

45:48 화자 1
So two is the system. [[unclear: Step there are three years step F step L step step]] the first character is the We want to get the [[unclear: eye]] inode information but this one and then the result is stored in stat [[unclear: box]] then you will see this stat [[unclear: box]], this structure stat and you have

46:19 화자 2
What?

46:21 화자 1
[[unclear: Ui egib]]. [[unclear: It's this time]]. mtime change time inode number mode, st_mode that includes. Using [[unclear: statifi system]]. So that system call you can.

47:04 화자 1
So again, tmpfs system and its world-writable directory with Execute permission, the world-writable directories on a file system with execute permission if you have the execute permission, typically you create a file that not to execute something, you create a temporary file and then remove it when you're not. So if they. [[unclear: Hypermises is not public stuff]] that could be a security risk it's a world-writable directory in the file system with executable permission or a security risk anyone with executable has run in. So when you mount this intemporary class. You can have a property.

48:00 화자 1
You can set the property noexec under this file system. Nothing can run, you can set up the noexec on mounting and then you can check whether the file system has noexec or not by using [[unclear: f step and x is a step on classes]]. World-writable flag in structure. [[unclear: Send me on your chest picture]] set user group owner so if you want to change the owner I want to change the owner response you can [[unclear: change the CH]] you can change the owner.

48:58 화자 1
That's chmod setuid bit chmod so the last three numbers for starting to do the principle right now. Seven five five means we'd write executable for user, we'd executable for group bench others, but then you can have another numbers at the eleven. Or two seconds so what is set user ID so you can change set user ID, you can set it on [[unclear: aloft]] set user ID as such again again the purpose is that when the program runs, it frequently changes. From like the web as a like here your user ID devel and then.

49:55 화자 1
And then wai, whoami [[unclear: It's cute]]. It's there it just shows your user group and group user ID, group ID and then when it runs, if most times your user ID is to say to say but if you The executable as setuid [[unclear: this shared it on]], then it changes its effective user to that privilege when you're running.

50:35 화자 2
Okay.

50:37 화자 1
So the effective user still then both devel and group is good but then you change the owner, change owner tester and users.

50:49 화자 2
Okay.

50:51 화자 1
And then what is [[unclear: for forest]] you enable the [[unclear: central equipment]]. So when you look at the wai, you have [[unclear: SCS]], the setuid is on it so what does that mean that means that when you run wai is effective user which is E.

51:14 화자 2
오늘

51:21 화자 1
So this guy has what tester user ID and users group ID, then you run it. Um, your user. So the one that's running this wai is devel, users ID is devel and group ID devel, but then when the set user ID is on, so Effective user of this running wai is tester.

52:22 화자 1
So it has the privilege tester, so it just shows up, for instance, I think it just shows up there's a [[unclear: print southeast]], but it can, as I said, already copy the file, submit the file from the regulator in this course. Directly that the user has access. Effective user group under whose permission the process is executed the real user group [[unclear: funder]] original user group under which the process was started when you started the Executable, The original user group is real user group, an effective user group with the setuid [[unclear: that it is on]] the user group under information.

53:15 화자 2
Process actually.

53:17 화자 1
If you look at the. Go in here. So you have getuid, geteuid is effectively different, getgid and getegid effective group ID and then call getpwuid [[unclear: password UID]] so you get this structure. So using UID, I want to get the name of UID string.

53:49 화자 2
Okay.

53:51 화자 1
[[unclear: Pwiu]] you not only get they just maybe have some other information, but the interesting thing is that getting So getpwuid return value is static structure so it's not created if you call it again, that is updated. So in order to print out, in order to get the real name, you do strdup so pwd has pointed to the structure inside the structure you have pw_name that is string user ID string you want to duplicate. And then [[unclear: string tube does not operate]] so again, the day you need to free. So yeah, getpwuid, getgrgid.

55:01 화자 1
Okay, so because you use the same API, getpwuid with different UID, UID and effective UID and then [[unclear: Gr]] getgrgid with gid and effective gid so second call will basically update the previous structure that's why we do a strdup here do you understand any question about So what does [[unclear: this protein]] if you open the directory name is a directory path. And then it returns DIR pointer d and then I want to get the file descriptor of this as a director that's a dd.

55:56 화자 1
And then you get entry one entry at a time, one file name entry at a time, getNext using d and then fstatat you want to get metadata of directory entry itself. What do you do so I want to check mode that whether this mode is regular file so when you do a man [[unclear: set]] inode it shows up also the And all the macros they need, but also there is a function like macro two, whether this mode is regular file of that, say S_ISREG, is this regular file of that, it checks just a bit on the mode.

56:49 화자 1
If it's regular file and then uid is zero and then setuid is on, it's a regular file, but then UID. It's zero means that root zero is root privileged ID user is root and set user ID is set and set user ID is that's not very good at you so then if you can run this It runs as one root, it can be run by anyone else, but then when it runs property, it's permitted. Is good so if there is some vulnerability of this is executable.

57:37 화자 1
Then you can for example, like buffer overflow security [[unclear: above]], then you can feed the string so that you can [[unclear: captive with this software to show some]] So this code just iterates over the directory and define all the files, whether the file has the roots privileged as [[unclear: senuid or senuid or senuid]]. That's security risk and then if the file system property is [[unclear: not lowest yet]], that means that the file is executed, you can execute then yeah, you can execute. Yeah, it's dangerous.

58:36 화자 1
Okay, let's move on to extended file attributes. So original Unix file system, the permissions are pretty simple, you have a user group, [[unclear: the number]] that's it but what if this guy's not my group but I want to give read permission of this file probably only to user X. That is not possible that was not possible that was not possible that recently lady they added these words for them extended file attributes POSIX ACLs are limited to access [[unclear: to recent or]] user group everybody else extended file attributes provide an extensible And more flexible way to store metadata. Not just the permission, but also you can store the metadata like [[unclear: amplify something]].

59:33 화자 1
You can set and get ACL for particular user so there's a setfacl so these two commands. Are available. And xattrs are key value pairs where key has a form of namespace attribute so what are the namespaces there are four namespaces that are defined security system trusted and user all the previous things that security system trusted

01:00:08 화자 2
These.

01:00:11 화자 1
Security is used by the kernel modules like SELinux [[unclear: security installment like the advanced tank]]. System is used by kernel to [[unclear: support us]]. trusted is visible only to process with admin capability user if it's of course namespace is user then you can actually set up your user specification. So arbitrary, arbitrary, the additional information, about files that's mime type md5sum, whatever character encoding, and then you can set it up and then retrieve it.

01:00:51 화자 2
Okay.

01:00:54 화자 1
So key has the form of namespace dot attribute [[unclear: value distribute]] so example check whether file systems support xattrs so you type mount and then grep slash you will see this file system here. It's like then inside the proc file system proc.

01:01:19 화자 2
Fs.

01:01:20 화자 1
ext4 sda4 options so you look at the options and then whether it has user_xattr. Then it shows user_xattr.

01:01:32 화자 2
That's right.

01:01:42 화자 1
많이 오죠 So the [[unclear: user extend]] that should be supported by this file system so example here what do you create [[unclear: Telva file]]? Then you change the mode on 640.

01:02:42 화자 1
We write the whole and read the whole and others continue and check the information. Then you want to. Set extended another thing. And user is as here is your username, I want to give username read permission. Then when you do a ls -l, you see not just the x ray and then getfacl, then you will see not just this, but also Owner a group devel file file name the user permission and the user svn is reading the middle thing.

01:03:44 화자 1
Another thing is that you do md5sum of the file and then setfattr. As a user dot checksum dot md5 user is a namespace and then the rest of the checksum dot md5 is the If the name of this key, of the name of this key, you set up [[unclear: 105]] as valued as this guy. Then getfattr, then it shows user dot checksum dot md5 [[unclear: weeks]].

01:04:31 화자 1
And then how do you read the value actually that you can use user dot I want to read the key value of these user dots and then you will see the page so if you want to still [[unclear: Or an absolute press file]], we can use extended attributes, that's it. Use concept everything so Unix uses everything as the file file system support [[unclear: media domestic features]], the mount points of different file systems under common root mounting with different permissions. So you can set the mount of the characterization [[unclear: we don't need no exercise, et cetera part and supply similar seniority is generated use case in echo]].

01:05:32 화자 1
Not basically for user group others, but you can extend it to specifically give the permission to a specific user or group. Many dangerous configurations possible, especially sticky, typically don't use it. Okay, if it's a set of properties, okay, but if it's a set up improperly, it could be a security crisis. And the [[unclear: balance sheet is provided by a rate, which will]] it's more to this next lecture. So we learned in the structure of a Unix I/O standard.

01:06:26 화자 1
In the interruption between them it's okay so Unix what is Unix IO unix IO refers to the interface of [[unclear: Unix costs]] to perform input and output another name or Unix IO is Unix IO is implemented at system calls Okay, so when you read and write, you can use [[unclear: this temple]] or if you're familiar with file pointer, the uppercase FILE. That's a standard I/O that implemented on top of Unix I/O, they're using Unix I/O, but then they do some other features.

01:07:07 화자 1
So Unix I/O is a lowest level of I/O [[unclear: application product form]] Unix I/O is [[unclear: a disinformation created system]] so it can be that Unix I/O is a system same concept one so you want to have one standardized general interface to your access. File anytime you open the file regardless of the file type and then with the write you close the file so these are comments you can use the same system with write. And then opens it, returns a file descriptor that identify the file using the FD as the first parameter, you can read them right and eventually when you're done, you close it. Regular files doesn't matter whether the type is regular file directory, block device, character device.

01:08:01 화자 1
Type says okay, of course not every operation is supported by all device types of [[unclear: operator silicon you cannot write to silicon silicon is riddle you can apply riddle but write rule fail]]. But you can apply the same API if [[unclear: that is the pi 0]] The bottom file is identified by a file descriptor File descriptor is an integer, non-negative integer, it starts from zero. Uniquely identifies the file handle required methods to update the state of file returns when opening a file, creating a file used in all subsequent file operations.

01:09:00 화자 1
So when you open a file, [[unclear: your turns, if it doesn't value the return to value it files]], you create a file, same thing when you create a socket, it returns the file descriptor. Each open file has an internal pointer pointing to the current position in the file so when you open up file and read something and then when you call read again, you read from [[unclear: when you left the front]]. You don't want to provide offset for all the time so the offset, the position is updated automatically for this one socket the concept of this Of the file type it's a regular file opener file read 10 bytes and then read another 10 bytes you don't want to read the first 10 bytes multiple times right, no, the current position of this.

01:10:01 화자 1
Okay, for open file as an internal position in the file read write position, start from the position and advances so when you open up When you open up for rewriting, you want to position sometimes you want to position the write position at the end of the file [[unclear: what the attest is]] so there has to be a notion of position for regular file. [[unclear: Substance is the rise on it right period of processing]] file descriptors are non-negative integers uniquely identifying open files all the process it's a per process okay so process file descriptor number 10 in one process, file descriptor number 10 in another process may point to a completely different one.

01:10:57 화자 1
Okay, in each [[unclear: class state, like]] with three open file descriptors defined in [[unclear: UBSTD values]], so standard in standard out standard error, they are all connected to the terminal. What is a terminal terminal you have a keyboard and display this abstraction terminal so standard in is connected to your standard issue. Standard out is connected to standard out and errors are connected to your display and then file descriptors are already 0, 1, 2 these are already when you script the program. So these are connected to two terminals and [[unclear: used for infrared output and paradigm]].

01:11:53 화자 1
Any other file descriptor must be obtained using so these are already open so you can just use 0, 1, 2 and you read write system call so printf original how do you implement printf printf writes The string is to standard out of course there's a format it's typical part so eventually but eventually it translates this [[unclear: format and the opportunity to watch]] Then we eventually call write system and then the first parameter, write is what? [[unclear: Right after the buff length, right can be happy used right testing for no area]].

01:12:49 화자 1
write system call is prototype is FD file descriptor buffer and length so I want to treat out these many lengths from the buffer to wear standard out so FD should be what? 1 so write 1 buffer has the string that you format it and then length of string that's it eventually cause write system for example. And then standard error number two.

01:13:33 화자 1
It's then called each system call [[unclear: income and]] kernel, it's running in kernel, it's not so the difference between library functions like C, strlen, strcat, these are the functions that are running in the user level. It does not have to have a special frequency just to change the system, it's written by someone else for you, but then this system call is a service that's provided by the kernel to the application. I want to print out three characters in my display you cannot directly access this display from the user level program you ask the kernel to write something there. It's sometimes very convenient, right, you don't do all the work then to do that, you need to access the display.

01:14:31 화자 1
And then [[unclear: find the phone think that pasting]] you need to use a special registers of I don't know, that's dirty, that's error prone, that's [[unclear: already hung by the tongue]], you just need to call the function. It's useful, but then the real reason for that is that they want to provide me security you don't want anybody changes that the hardware into weird link changing memory. So that protection is done by convenient wrappers provided by C standard library, so open.

01:15:25 화자 1
So why do you say convenient wrappers provided by the C standard library so all. These are Unix, IO Apis, a system open creat read write seek you go to the offset, you [[unclear: change the information quite often]] you want to remove all of these are systems. What I mean by [[unclear: company provides]] the standard library, provides convenient wrappers, you don't need to [[unclear: use the ADI]] to call the system. Of course, of course you need to write the [[unclear: second context in language]] you can provide the system call number, system call number.

01:16:19 화자 1
So each system call has unique system call number from 0 to like 300 different system calls are defined already so you need to know the system call ID and then you need to pass the parameters using A simple one is no, all this can be written by you without this C-like function, but why we use C-like function because that's easy. Convenient inside the open it actually does this for you so if you go to the source code of open, [[unclear: it has a sample]]. So it places the system called ID and parameters into right registers and then call switches the context [[unclear: what is actually user juice]] so please run this.

01:17:18 화자 1
If it's not usually a function call so, you know, function call it, you usually a function, you call it that is done by how does it work, you push different things that you said and then eventually go to We go to the different instruction [[unclear: number]] than similar, but then the kernel does not have the go to. It means that you know the function location, but you don't know the function location was prompt from the, you know, the idea of the function. Even ideally read the right, then you call the [[unclear: same language kilogram]] to switch to the call system call.

01:18:14 화자 1
[[unclear: This enter]] is the [[unclear: similar]] instruction and then that interprets this system called ID and parameters and [[unclear: that sq the]] It looks similar, but that's why it's [[unclear: called retina]], it's not the same as usual. [[unclear: Often great you see exercise t you will see some different off on the mountain]]. It's a special type defined by the Linux and if you want to actually you want to know the [[unclear: node type T]], you can use this.

01:19:10 화자 1
If you have, what does this do [[unclear: includes 10 h]], so it echoes its prints up the screen. And that is fed into my pipe, into gcc -E, what the gcc -E do you love it? It does pre-processing so it assumes this string at C source code and then pre-processor what does preprocessor do pre-processor finds this stuff. [[unclear: Directory that tries]] to open it [[unclear: standard library H]] and [[unclear: inside standard library H]], it has a bunch of other inclusion so it tries to open it recursive eventually it'll get all the type defined C.

01:20:11 화자 1
All the type definitions for this [[unclear: vision type]] off_t is nothing. Especially it's a long depending on the platform it may be to be defined different that's why we want to use off_t for example in an embedded system maybe you might want to use off_t as 16 bits. Okay then but you don't want to change the source code itself or different so off_t is defined elsewhere and you just simply use it.

01:20:51 화자 1
So that entry point is standard if you just include standard library of all the type techniques of special types of So you can you preprocess it that means that I want to preprocess with the pipe standard and then that standard out is fed into grep I want to find the [[unclear: Words of underbar this type is he is shipping because of underbar. Tackle. The library of each.]] So when you do this, simply print out this guy.

01:21:56 화자 1
Then yeah, preprocessive. Eventually includes all the types that's all I want to do and then you want from this mess I want to require. On the bar, it starts from on the bar. So off_t eventually off_t is same as [[unclear: under bond of of t]] [[unclear: under bond of of T]] is same as off_t offset type is same as long in the system. So depending on platform you can change [[unclear: is low making something else]].

01:22:52 화자 2
Okay, size, size.

01:23:07 화자 1
So if you bump into a type that you don't know, you can look it up this button opening and create a file opening up file informs the kernel that you are getting ready to access the open open creat. So you see the pathway in this lecture and then interesting, you have two opens, you have two opens and then

01:23:34 화자 2
Okay.

01:23:38 화자 1
It says possible if you have in C, you have two different same function name with different parameters.

01:23:49 화자 2
I think.

01:23:59 화자 1
And so let's say if the copy and paste the two, right, [[unclear: the opposite offer]], right, then as a compiler.

01:24:07 화자 2
Yeah.

01:24:11 화자 1
Because you have two different names, two same names with different functions. Is it C++ functionality that's the nice feature but eventually so I'm not lying that the current support holding these different things but then how does the You already know the answer why variable parameters. A printf printf has the number of the parameters for printf is different, right, same thing here when you declare.

01:25:02 화자 1
Open or define so you have open pathname flag [[unclear: forma]] that's what you find as a product so like same thing as what I printed. Then you can use both things if you provide just two parameters that I understand, three parameters that I understand, there's no As my name made the file [[unclear: flat status Christian flat]] it's been a different read only I want to open up with the read only writable [[unclear: or pen]] or I want to also specify permissions I want to create a file that is read write executable for user.

01:25:54 화자 1
All these permissions can be rewarded in the mode, the file mode for newly created sorry, that's a mode file mode for newly created files. As S_IRWXU for user r only read for group so yeah, you have to memorize mandates and all the and then this guy will turn the file descriptor. creat is actually not necessary or [[unclear: open can be created or create all mandoma create]] when this fire does not exist, you will create a one, you can do it so creat is not absolutely necessary but it's there for backward compatibility.

01:26:49 화자 1
And the creat if you look at it by e is missing, that's the once that Ken Thompson [[unclear: had entered you]], what do you read about the Unix so he was asked the question, what do you regret? Was his answer the joke anyway, [[unclear: trade is not whether it's open can open include the oxidative rate]]. Minus one is typically error when you call the system call, when you look system call and then you tell the system call and then the reason why y error happens errno is global variable.

01:27:44 화자 1
And then that's defined somewhere else in the [[unclear: error vendor number]], just the number, but then you can print out [[unclear: this string at Christmas to the number error string]], then you can figure out why Okay. Just look at this example of the information opening a file informs the kernel that you are getting ready to access the file so you open an /etc/hosts file. And then you want to open it for read only.

01:28:33 화자 1
So you can write to the using fd and creat you want to create a file and then the mode pressure is this should be readable by user, this should be writable by no other conditions. They're closing a file so when you're done another, you can close the file if the [[unclear: Sure phase with x c n y f d]] so it returns zero on success typically when it's successful so unless you're writing for reading that returns the number of data bytes read or [[unclear: reading]] Open close open returns file descriptors so it can be [[unclear: eat the unit value]] on that process.

01:29:32 화자 2
Okay.

01:29:33 화자 1
So zero one, two already opened right, so first time you call open, it succeeds in your process that it returns what? Because it returns smallest available file descriptor on that process if you don't want to already open, so [[unclear: it returns a little more open]]. Close when it's [[unclear: successfully intensive]] closing already closed file is an error. So if it's an error, it's okay, so FD is already closed, but it [[unclear: feels return minus]] maybe it's fine, but then if you double close.

01:30:21 화자 1
And if you use a multiple thread so you do not have just one thread, you can multiple thread and then for example, [[unclear: a equals a final FD, the offset file that FD is reused]]. But that points a different one but then c things that A didn't do that that I want to [[unclear: close the premises then B is a file operational file as well because of this already closed]] so this should be You should be careful with it, okay, let's stop here.
````
