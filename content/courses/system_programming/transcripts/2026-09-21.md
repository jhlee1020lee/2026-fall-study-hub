---
title: "2026-09-21 · System Programming · 보정 STT"
course: system_programming
date: 2026-09-21
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
date_confidence: user_confirmed
---

사용자가 2026-09-21 수업으로 확인한 녹음입니다. 파일의 날짜 메타데이터를 독립 확인한 것은 아닙니다.

명확한 전문용어 오류를 보정한 공개 STT 사본입니다. 불명확한 말은 원문 표현 또는 `[[unclear: …]]`로 남겼으며, 전체 음성의 완전 복원이나 누락 없음이 확인된 자료는 아닙니다. 부정확하거나 뜻이 통하지 않는 발화는 검증된 개념 설명과 구분해 읽으세요.

개인정보 검토를 거친 공개용 사본이며 시간·화자 순서를 유지했습니다. 교수자·수강생의 예시, 의견과 추측은 검증된 사실이나 현재 공식 규정과 구분해 읽으세요.

보정 원본 SHA-256: `0d3356a6383f4c1d595f07b92d2203ec057433938ae5e4f350c0729e95ad0485`

````text
00:00 화자 1
So Thank you.

00:36 화자 2
Okay, let's start. DJ Bob, wonderful weekend.

00:41 화자 1
Thank you.

00:41 화자 2
So we'll have a quiz today about programming assignment number one. And it will happen at 11 30. It will spend-- I don't think it will take a lot of time. But I think we can finish it all here. Okay, last time we talked about this structure, stat. stat structure mainly provides information about an inode. And then information like UID, GID, number of hard links, protection and file type, size, and the number of 512B blocks allocated.

01:31 화자 1
What's your name?

01:32 화자 2
These are the information, the sort.

01:39 화자 3
- Ta -da! - What's in it?

01:41 화자 2
This is missing here. You're including the thing that's missing. What is that? I know it's metadata.

01:48 화자 1
Thank you.

01:48 화자 2
About the file. The directory also won't matter, right? It's a file name, right? What do we say about the file name? - Deng, Deng is in a directory file. But that's not in inode, okay. Then, that's one more information.

02:12 화자 3
Here, but then they don't want to expose it to you. You don't give a fuck.

02:16 화자 1
Thank you.

02:19 화자 2
What's the information? Information is actually the location of the data. Okay? And then, I know the subject has a bunch of pointers.

02:31 화자 1
Bye.

02:32 화자 2
If the file size is not small, if it is not big, then you can directly access the blocks from the system. But what if the file size is really large? Then you go to another directory, it points to one block, inside the block all of the entries point to this data.

02:59 화자 1
Cool.

03:00 화자 2
That information is missing, but that's also stored here. All right. The perspective we use here is, we use these system calls, stat family. Given the path name, you get this stat operator. That's the basic operation of it. lstat does not follow the symbolic links. And then fstat uses an open file descriptor instead of a file path. fstatat, you probably saw this in the source code line. So there is an at family. There's open, also there's an at. What is an at? It has this information for it. The first parameter is the directory that you open. And then if the path name is relative, to this stat.

03:59 화자 2
It's an absolute path. What is absolute path? The path that starts from the slash. Relatively path is one. It starts with dot slash blah blah. Or dot dot slash blah blah. So, and then anchor point is your journey. So, step from your Why do they have this family? Because it's possible that the directory itself can move. And then when it happens, when it's tapped, and then you get some information and they try to open it, then you might open something different. Finally, the server is saying, but then directory is different. That's possible. So in order to avoid that, you can have this directory.

04:54 화자 2
You need to open the directory file first, and then you can buy a relative path. This is an absolute path. This one is just beautiful. And then fstatat has all the information. So it covers the functionality of stat and lstat. We can just use fstatat It's a superset function. statx has extra information you can get using stat So, let's go over to the directories. So, C, standard library functions, to deal with directories, here is the list of functions.

05:42 화자 2
First one is opendir, so you provide the file name, the directory file name and then it opens and it returns directly into DIR, the install structure. Then after that, using that pointer, you can read one file entry at a time. readdir, readdir, so you will be able to call readdir until you turn it off. Okay, each entry represents one file. Then you can see, you can check the file name. Okay, and then when you're done with DIR, you can get file descriptor of the open directory using dirfd.

06:28 화자 2
So, are these Unix I /O system calls or are these uh standardized DIR is structured, that is managed inside the C-level library. That's not structured, that's a custom code. So this is a standard I /O function. And at the end of the day, right? opendir, at the end of the day it opens directory files, it opens the system. And then it will store somewhere inside the DIR. If you want the FD cell, you can get the FD using dirfd.

07:25 화자 2
Then you can make a directory, mkdir. Let's look at the source code here. We opendir. If it's null, it's an error. If it's not null, it's successful. So argv[1] is what? The first parameter. Command line. Command line argument from the user. So if no parameter, no argument that you open the current then the dirfd, you get the file descriptor. Then what do you do? You have to speak while you're good. We read one file entry at a time.

08:15 화자 2
And then what we do is that we call fstatat using the directory fd, then file name, and then information, structure, stat structure. And if it fails, it will send out error number. So you'll print out something, but then you reset the error number here. That's it. When you're done, closedir. Inside this closedir, you eventually close this FD. FB sub -track. It's similar to fopen and fclose. fread, fwrite, F2.

09:15 화자 2
Two descriptors referencing two distinct open disk files. Descriptor 1 points to terminal, descriptor 4 points to open disk file. So typically, two different file descriptors point to two different files. That's a typical situation. Then 012, file descriptor 012, allay off to the video program. But then, after that, inside your program, you can also create or socket. I'm not sure if you did any data program. Socket or socket pair, pipe, all these things you create, you buy stuff.

10:13 화자 2
And then it's highly current and it manages this information. And then the user process deals with this number, right? The number one, four, etc. Inside the kernel, the kernel needs to maintain which one transfers to which one. So the per process, each process has this descriptor table, one table per process. And then, it's just pointer to open file descriptor, open file table, or open file descriptor. Typically, there is one open file table per process. If you're using this file descriptor for yourself, and you do not do any copy of your file descriptor itself, then you have just a single file descriptor per process.

11:13 화자 2
You do not care about the process. So what? Each entry, open file table entry has the file position. You need to remember which is the file position. And then reference count. So reference count is difficult in 0 but 1 because you're the only one using this. But then, so you have small file, find it here. You can pass this file descriptor to another person. Send a message, receive message. It's not just sending just one number. What I want is that I want to share this file to another process. So I need to call a special system called sendMessage.

12:11 화자 2
And then decide that I want to test this file to find this number. 5, I'm going to give it to another process. ReceiveProcessor will receive, will not receive 5, will receive another FD. These two FDs are sharing the same open file table entry. Then reference count increments. So that's file descriptor passing. Another thing is that fork. Fork is you're making a copy of yourself. Making a copy of your entire process. Then we'll learn later, everything is exactly copied. Your memory, conceptually, all the memory is copied. And then all the file descriptors are copied. The state of being open, being closed, all these file descriptor information is copied to you.

13:09 화자 2
to the newly created process we call child process. From then on, the parent and child process on their own. They can actually keep the same code, but they can divert depending on, you can have if statement, right? The result, the function fork result is this and I want to do this. So you can really fork, fork your Then, their information is that all the descriptors are shared between the parent and the child. Unless you could. Then this reference count will go up. If you open another file, yes, you have, let's say, 0, 1, 2, and the number 3 is the first file that's being used.

14:07 화자 2
Now when you call open, you call, maybe this one is closed. No, this one opened the first link. Then it'll point to your regular business. Let's say you open A .txt. Let's say you get FD number 3. Let's say you open the same file again. Then you get number 4. Number 3 and number 4 are sharing the same open file table entry or not? yes no okay i don't know No is the correct answer So typical, you don't have structure these two files as a script. It has its own file position. But then, even inside one process, you can make them share.

15:08 화자 2
How? You can call a special file system called dup. D-u-p. Duple dup. Copy. D-u-p. or dup, dup2, dup3, there are vandalism of this system. basically you are creating, it's exactly the same thing, so you're sharing the files, the script, and the people together so that you can share the file position together. okay, you can do that, so I will talk about that. Okay. Two distinct descriptors sharing the same file through. Distinct open file. Calling open twice with the same file, the argument will have a separate open file entry.

16:06 화자 2
Even if the open file is the same file. But let's think about how we improve them. Are you without a reach? You know this standard out, without a reach is standard in, without a reach is standard error, without a reach any, it can be without a reach. all three standard IOs can be related to memory. The first one is what? ls? So without the greater than symbol, it prints out the result to the standard out. But I want to say that information can be applied for output.txt. How would you implement it? How do you do that? So you will implement this as part of the 7 .0 .4 Another thing is piping.

17:04 화자 2
I want to give the output of 1 process tool to the input. It's the standard input of the sort. Or you can redirect standard in. You can direct the file content to standard in. how they shell implement I/O redirection. So the key is that we standard out file descriptor number one, standard out file descriptor number zero. Somehow, you close real at number one. You make a copy. So, sorry, let's say, let's look at the first example. output.txt here.

17:56 화자 2
So what I want is that anything that calls write system call with parameter number one, this is the first parameter, that goes to what? The disk. It needs to go to the disk, not to the disk. So what you need to do is that you open the file or output.txt for writing. And then somehow this number one and the second, that's the number three. I want to share this, these guys together. So anytime, code is a political component, right? So anytime you call the write system call, it goes to number three. How do you do that? You can do that by dup. So you can call dup2. And then I want to use number 1. I want to, so all the 1 is number 3.

18:53 화자 2
I don't really need to use number 3 itself. I can close number 3, but then I want to make number 1, right? Number one should be a measurement. And then that's dup2 is exactly accessible. Or, earlier implementation was that Only implementation was that one. You have a continuous file descriptor space, 0, 0, 1, 2, and then you open output.txt that will be 3. Why? Because of the position. Then you close number 1. And the end is good. And then, call this dup. dup has no parameter.

20:11 화자 2
Then, I'm sorry, dup takes one parameter. dup takes, so you dup with number three. Then, the output should be the smallest file descriptor number. So number one. Number one and number three will point to the same. same So someone asked earlier that why projects have this weird restriction. So every time you create a file descriptor, the kernel is supposed to return the lowest FD that's not used as a result. And then I actually looked it up, and this is the reason they implemented this using dup. The dup2 was not available at the time, dup2 and dup3 were added later. So I'll talk about the details in a minute.

21:11 화자 2
You see it's kind of like where you have options, allow manipulation of file descriptors here. So we talked about dup and dup2. dup takes the one parameter, and it created an exact shared file entry, open file entry, and then returns the new file descriptor. We got that same entry in open file table as all the other ones. So dup, number three, will return one because you recently closed the role. And then if you don't need number 3, you can close it. dup2 is more versatile. you can have oldfd, newfd. entry in newfd is written with value in oldfd. so number 3 and then number 1.

22:11 화자 2
and then number 1 becomes number 3. the biofuel numbers of the Nyan cancer are a little bit of a learning. So here you open file.txt or read only. So the FD3 is made to point to an entry of And then you call dup. Then FD2 must be 4 because all these are the users. So the smallest, lowest number is not used, it's number 4. Then they have to say, they should have to say, open file table. Okay, output.txt. So this is for standard out redirection. You open it for read only.

23:11 화자 2
And then this fd is a file. output.txt. I want this guy to behave as So, the original standard in is closed and at the same time you copy FD and that number one becomes FD. Same as the LDS. These two are done automatically inside the Example, what is the output of the program if the input file contains System Programming? Input file is what? Okay, so fname. fname is a file, let's say, that file already has System Programming. Open three, file descriptor in the data. So do and then read.

24:11 화자 2
The three file descriptors, file descriptors are open and they will have this team of open file table initially. But then what do you do? dup2. Then I want fd3 to behave as F3. And then the original FD3 will go, let me go on, close. Okay? Then FD3 will point to FD2. FD3 and FD2 have the same open file entry that share the same location. Okay? So I want to read one character from... And then you print out. So what is c1? So assume everything is accessible. What does he want? 3 levels. s, S, yes.

25:10 화자 2
c1 is S. what about c2? ub. s. why? because these two are independent. so, regardless of the location of the fd1, fd2 has its own separate entry and it starts from zero.

25:29 화자 1
Thank you. Okay.

25:37 화자 2
What are these? So you open the file for creation, then you provide some And then we write CSAP. We open another file with the same name. And I want to open it as append. So I want to open it for writing, but I want to go to the end of this one and write it at that one. And then you write M1522. so let's say you are here. what's the file company? What's the file? CSAPM1522. right. then you do fdd.

26:36 화자 2
so fdd. fd1 is duplicating. The fd2 will share the same open file entry in the table. And then it writes SNU. It writes 800.

27:00 화자 1
Thank you.

27:00 화자 2
So what's the content? So what is fd2? So fd1 is the position 1. 0, 1, 2, 3, 4. So it's the opposite. It's the block here. Which is the distance to n, right? So it overrides n153, essentially.

27:26 화자 1
All right. With a test engine.

27:28 화자 2
Because fd2 is same as fd1. And then fd3, fd3 is what? This guy. So, SNU, then 2, 2, then 800. Okay.

27:44 화자 1
Thank you.

27:47 화자 2
CSAP. And then you write M1522. That's good. But then, FD2 is exactly same as FD1, and FD1's position is over here. So you write SNU. So these three characters were replaced with SNU, but 22 is still there. But FD3 is right, one position right to E2. So you write 800. You understand? Using these techniques, we can implement standard in, standard out, redirection. And then we need a little bit more techniques beyond that. But we'll talk about that later. Just to give a sense.

28:40 화자 2
The most frequently used Unix I /O system calls open creat read /write, lseek, stat, close, read, learn, or have standard I /O library function, fopen, fread/fwrite, fflush, end of file, ferror, fseek, and app, get and then directory it gets opendir readdir the IR that have been closed I'm going to find an example. So let's say we have been dealing with text files, but then content may not be ASCII. There may not be any character in the canvas. It can be just numbers, JPEG, images. You shouldn't use online files.

29:37 화자 2
You should use line-oriented I/O. fgets, that makes sense. You know, it was just fine. Try to read enough characters up until about fine. Enter, ask for this. A, A, 10, right? That's it. It still works. It doesn't fail. It doesn't make sense because it's not like a review. The line in Linux, Vortex uses this single capital T, but then Windows uses CR, LF, carriage return, and line together.

30:28 화자 2
If you look at the web protocol, HTTP protocol, we assume you use CR+LF string functions, it's a person lights, light value is zero. So you can use string function in binary file, binary content, it still works. It doesn't like, it will follow you over. But then what it does is it tries to find zero. So the string length is the number of characters until you find the first set, that's the content. But yes, number of shares may not be sure in the hydrate environment. So then, it's a little trouble. Okay, another, so this is Boo. Advanced Programming in the Unix Environment.

31:28 화자 2
It's a very good book and it has all information about the system calls, standard I /Os and all the more information. Alright, do you have the K? K on the K? So let's start the pace.
````
