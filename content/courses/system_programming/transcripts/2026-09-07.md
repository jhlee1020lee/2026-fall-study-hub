---
title: "2026-09-07 · System Programming · 보정 STT"
course: system_programming
date: 2026-09-07
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
---

강의 녹음을 바탕으로 보정한 STT의 공개용 사본입니다. 음성인식 오류와 불명확한 발화가 남아 있을 수 있습니다. 수강생 이름·연락처와 일부 개인 자기소개를 가렸고, 타임스탬프·화자 순서는 보존했습니다.

원문 보정본 SHA-256: `0f1022ba6efa106ba0480ba3b54edef1a9c312f97b1cbdc9b054ed85b13b8bcd`

`[[unclear]]` 등 표시는 원문에 남은 불확실성입니다. 코드·수식과 발화 표기를 그대로 읽을 수 있도록 본문을 텍스트로 제공합니다.

````text
00:00 화자 1
Compiling the high-level C source code into assembly, and then using the assembly language code, what you do, you use an assembler to assemble this assembly language code to machine code. And then you link with the library, external library code, then the final binary code. Okay, the example number one is the program that reads the characters of the standard input. By default, the standard input is [[unclear]]. You can redirect it to something else, to a file, but by default your keyboard is standard input. And then print the number of English alphabets and the number of digits from the input string.

00:59 화자 1
So the source code file is count.c. Then we use gcc800 to compile and name the binary count, right? And then we run the binary, and then we run the binary, and then the input [[unclear]]. So we just use the source code itself. Okay, as input first, so we redirect count.c, and then this input into count, output [[unclear]] digits or other characters. Maybe not very simple. So if you.

01:48 화자 1
Participated in the class last time, please come out. Also, if you will participate in this class, participation means that you can do any Q&A. If you ask a question or I ask a question, you answer or you make a comment, everything like that, please mark [[unclear]] by checking this [[unclear]]. So this is good. Okay, take a look, and then we have a while loop, right? Then we use getchar. getchar reads one char at a time from standard input. The return value is [[unclear]], right?

02:43 화자 1
And then we compare whether, the first if, right, compare, determine whether that character is alphabet. If the newly input character is alphabet, you count the number of alphabets. If it's a digit, you count the number of digits. Others, you count the number of others. There are some [[unclear]]. So here one question: why do you use int instead of one-byte character here? So getchar, return value is int instead of char.

03:46 화자 1
If an ASCII code is one byte or seven bits, the original ASCII code is seven bits; extended ASCII code takes [[unclear]]. Why do you need integer with it? And then, yeah, but what I'm asking is why is the return value, the type of getchar, int? That's what I'm asking, because it is supposed to [[unclear]]. What can it be?

04:49 화자 1
If it succeeded, it gives one, it returns ASCII code itself, so you are almost there. Char was unsigned? [[unclear]] So character is, what is this, okay, the unsigned character, let's say. Can you change the type of getchar to unsigned, return type of getchar into unsigned char? No? Character is signed, but you cannot represent minus one. Why do we represent minus one here? You're almost there, I think you're almost there, maybe because of English.

05:49 화자 2
But.

05:52 화자 1
It's not very accurate. Any other questions? EOF and other signals can be represented in [[unclear]]. Yes, again, minus one is not the key.

06:35 화자 1
The entire eight bits are already defined for extended ASCII code. There is no more space. But what you want to do, so yes, getchar typically returns a value that's a character, one byte, but then has to finish at some point. There has to be a way to tell the user that, oh, there is no more character. That no-more-character itself is not a character, right? There has to be one more value that tells you, that tells the user that, oh, this is not a character, this is for end of file. Yeah, of course, end of file happens to be minus one here. We define it that way, but it doesn't have to be, right?

07:28 화자 1
So minus one, of course, that's a different thing. So the key requirement is that you need to represent the entire space of one byte, right? Zero to 255, entire number is used for ASCII extended code, but then there's no space, no value that represents [[unclear]]. So we need to, they could have two bytes, short, but integer is the [[unclear]], like C for the CPU architecture to read and write them, so they used int instead of short. But yeah, the answer is that you need to have one more value that represents something other than the character itself. What about this, can you read this?

08:29 화자 1
So c is between 65 and 90, what does that mean? Or c is between 97 to 122. Then either c belongs there, then we assume that that's a character, alphabet. So what is this? This is actually ASCII. It's a number. Everything, right, the calculation number [[unclear]] is a number, color is a number, sound is a number, everything is a number. And then here ASCII code 65 is ASCII code A, and ASCII code 90 is Z, in uppercase characters. This one is lowercase characters.

09:25 화자 1
It's not wrong, but it's not readable. It's difficult to understand. Of course this one is ASCII code zero and ASCII code [[unclear]]. This is not wrong, but it's difficult. So yeah, popular character set, ASCII, initially seven bits were defined.

09:50 화자 2
Okay.

09:51 화자 1
And then digit, this character zero is ASCII code 48; nine corresponds to 57. Alphabet, lowercase alphabet, and then later it is extended to one byte, eight bits. So this code itself is ugly and unreadable. We should not write the code like this, but it's [[unclear]] understanding. What does this do? So 97 is, 97 is, 97 is lowercase a.

10:41 화자 1
So we print out, so 97, and then %c, that is lowercase a. Then 'a' + 3, what does that mean? So [[unclear]] a is actually value 97, so 97 + 3 is 100. So I want to print out the ASCII code that matches 100. And then I don't know how to actually draw this font. Actually you get the font from this memory, and then the OS will actually draw it. That is a, b, c, [[unclear]]. What about this?

11:33 화자 1
C, uppercase C, plus small a minus large A. What is that? What is the intention, you see? Yeah, uppercase to lowercase. You want to go to lowercase, and then in order to know that, you need to know that a, lowercase, starts later. So you need to have some understanding of ASCII code in order to understand. And then the other thing is that you're converting this lowercase c to uppercase C. Like this, a, b, c, [[unclear]], this is not difficult to [[unclear]].

12:25 화자 1
Yeah, but it's better than just using the literal number that you're using the character constant. That's easier to know what's going on, right? But still this is not what I want, is code-page-neutral code. And so whenever you run an app, the code, the binary, you are tied to an underlying code page. What code page you're using, it can be a typical one, can be something that there may be 100 to [[unclear]], 200 different code pages. Depending on the code page you use, maybe it's different, the number. Typically ASCII code itself is really [[unclear]], no matter what code page you use.

13:16 화자 1
ASCII code is already there, so I wouldn't worry too much about it, but in theory it's possible. You can define a completely [[unclear]] code. So you don't want to depend on just ASCII code numbers here. So instead you can use these standard functions as a library. So regardless of code page, it identifies whether c is alpha, isalpha; whether c is digit, isdigit; whether c is space, isspace, right? You can include ctype.h. So this is a little [[unclear]]. Let's move on to second example, word counting.

14:04 화자 1
So we count the number of words with input from stdin. A word, we define the word is sequence of non-space characters. So that may not be accurate, but we assume any non-space character sequence. Space character here, space or tab or enter or whitespace, anything that's recognized by isspace.

14:35 화자 2
Please.

14:43 화자 1
So how we approach? So we need to track the state. So state one is a state where you are inside a word. So you're reading a character, you need to know where you are, whether you're inside a word or outside, that's it.

15:05 화자 2
Very simple.

15:09 화자 1
Number of words is the number of [[unclear]], number of times when the program switches from state two to state one. Initially you are outside the word, and then whenever there is a non-space character, you get inside the word, and then you increment the counter, okay?

15:29 화자 2
Is it?

15:31 화자 1
So in order to do this, you can use a tool called DFA, deterministic finite state automata, DFA. Finite states, the number of states, and then transitions between two states. Transitions labeled by input characters and optional actions. Initial state, initial state is recognized by just an arrow from the outer space. So outside-word state is initial state, and then when there is [[unclear]], arrow.

16:15 화자 2
Right.

16:17 화자 1
So non-space character, when you meet a non-space character, you move, you switch, transition from this state to inside-word, and then you do this action. And you count the number of words by one. And then when you are inside the word, when you get outside of the word, then why? When you encounter a space character, then you get outside of the word. Then when you're outside the word and then you keep seeing space characters, you stay there. When you're inside the word, when you keep seeing characters, you stay.

16:57 화자 2
Simple, right? Oh, right.

17:05 화자 1
So using the input string, I want to recognize whether there is an SNUCSE in the string sequence. And this is one example. So you are starting with S state. When you see S, you go to number one. You see, from number one, when you see N, next character, you move to number two. You see U, and then you move to number three. If it's just another one, you get back to S?

17:46 화자 1
It gets a little bit complex, very easily complex, so I didn't write everything here. What about detecting the integer? So integer looks like zero or plus-minus, [[unclear]], optional plus-minus sign. And then the second one, the first digit cannot be zero. If you start from here, if it's plus-minus, you go to one. If it's one to nine, you already recognize the number, okay? Double circle is the final state. When you get there, at least recognize one such number happening there. Also this is also double circle, that's a final state. This is initial state.

18:43 화자 1
And if it's a zero, of course this does not represent all the transitions, right? Here x star means zero or more, inside bracket one to nine means any character one to nine. That's the same thing. You can actually use this representation, Linux, the shell. The star means zero or more repetition of the character x, and x question mark means zero or one repetition of character x. So there is optional. When you want to represent optional one, you can use, 그렇죠.

19:40 화자 1
Okay, so this is not the formal treatment of DFA, but you can use the concept DFA to do assignment number one. Assignment number one is to write a simple preprocessor for C language code. So you basically get rid of all the comments. It's called decomment program, and you can use the concept of DFA to easily write. Okay, let's look at the code, okay?

20:15 화자 2
We need to.

20:17 화자 1
So again, it has one big while loop that gets the input from the outside, the standard input, using getchar. As long as that is not EOF, you check which state you are. If it's state zero, what is it? State one, so what is initial state? State one. State one is what? Outside of word. If it's state zero, that means you're inside the word. Then as long as the next character is not space, you stay there.

21:01 화자 1
Say if the next character is space, you change the state to outside, okay? Then you are, so that's the only thing you can do. What about number one? If you are outside of word, if c is not space, then you can get into, change state zero, and then nWords++. Very simple. Okay, what's wrong? The program itself is not.

21:38 화자 2
Wrong.

21:39 화자 1
But how can we improve it? Again, don't write the code like this. What's the problem? Don't look at the next slide, that's cheating. Yeah, but that is, programmers, I know zero, one is outside, zero is this. But of course this code will not be used by [[unclear]]. Okay, this is just, just a toy code, but you write the code that you collaborate with other people in the team. Your code should be read, read by the people.

22:32 화자 1
Is it readable? You just assume that number one is outside state, number zero is inside state, but that's not clear until you actually read the code and understand the code. So this is difficult. So this is called magic numbers. Of course the writer, developer himself knows it when he's writing, or maybe she's writing, but after two months later you have [[unclear]] idea of what this number is. So don't do that. Instead use a named, named constant. Okay, so there are three ways to represent named constants in C. One is enum. Enum variable name, and then constant number one, constant [[unclear]]. Can bring up the DFAState, IN and OUT.

23:28 화자 1
You can use, instead of zero and one, you can use IN and OUT in your program code. That is easier to understand what this means. But enum number is actually a constant integer, so you can do integer operations. Another thing is #define. #define is handled by the preprocessor. Define constant name and value. Constant name is replaced by value at preprocessing. So the C compiler cannot see #define. Preprocessor actually translates the C code into, removes all sharps, actually. And then compiler will see actually zero, so it's okay. Compiler is [[unclear]], the guy can deal with.

24:27 화자 1
Numbers, but then we are looking at, right? So we want to see meaningful names in the code. So instead of using zero, you can use IN and OUT. Another, the third one is const integer. So you can actually use a type const int, constant name, then IN and OUT. You can read it but you cannot change it, okay? So we chose to use enum here, IN and OUT, and then we change it. Okay, looks better, but is it perfect? What's missing?

25:24 화자 1
Yes, yes, whenever you write switch-case statements, you must think of a default case. Of course, yes, there's no default case, you're either IN or OUT, but what if you make a mistake? Okay, then without default, it will take a lot of time to debug that one. So what another? What kind of function [[unclear]]? You can speak in Korean. We need to, and this is very easy.

26:20 화자 1
But let's make a habit to write comments for each function. Doesn't have to be long, long. It can be just one line. But that is much easier for others to understand. So comment on this function and default case is easy. So when you get here, sometimes, then assert. What does assert, can you use assert, anybody? Okay, what does assert do? It raises error, it raises an error, [[unclear]] exception when the expression is [[unclear]]. Yeah, it raises error and then it stops execution.

27:13 화자 1
So you need to fix this bug before continuing, okay? So when you are here, you can use debugger, or you can actually take a look at code. Then you figure out why, when there is [[unclear]]. You don't want to continue because it's a logical error. This is called defensive programming. In a correct code it doesn't happen. In your code it may be [[unclear]]. Of course this program is [[unclear]], it can go.

27:54 화자 1
But in realistic code, yes, there's more complex, and then make sure they have default case always. And then the main function is, first, count and print the number of words in input. Okay, so the comment should focus on what instead of how. How can be figured out by reading the code, but how, it tends to be lengthy, long. Maybe [[unclear]], so you focus what does this?

28:36 화자 1
Then it's easier to understand what the function does, and if the function is correct, then you can easily understand the entire [[unclear]]. Write comment on each function is required for a good grade for this course. So bad function-level comment describes how: read the character from stdin and check if it's a non-space character. If so, the program state moves to state IN and increment counter. If it's a space character, the state moves OUT. It keeps reading input until EOF and prints out the word counter. Nothing wrong.

29:24 화자 1
Too long, and then you don't want to learn everything like this. It's [[unclear]], don't write that. Good function-level comment describes what: count the number of words in input from stdin and print it out. Simply, easy to maintain and understand. Number two, so we need to design an algorithm before. I know with this review you already know that you need to come up with algorithm, come up with data structure first before writing, starting the writing. It should reduce complexity. And then program, again, is you have current state plus input character, and then you move to next state and output. So focus on this one and then think about the code. Also we need to improve code readability.

30:23 화자 1
Avoid magic numbers, and then use named constants. The comments and defensive programming. Function-level comments focus on what. Defensive programming is desirable. Let's move on to, do you have any questions so far? Kind of wondering about this, one of the [[unclear]] was instead of having one of the [[unclear]].

31:14 화자 1
You know, yeah, I was wondering, is it in C programming you can use, what, const integer, const integer? You can use it. From my understanding, you can only use it in [[unclear]]. No, you can use C for sure. I use it. Const int is available. So if you use IN as a const int, then you will see an error in state, like switch-case statement? Is that what you mean?

32:15 화자 1
Yeah, I haven't tried for a long time. Should work, why not? I think it should work. I'll check it. Do you know why? Why doesn't it, doesn't work? But so this is an integer, so you need to have an integer here, so it can be constant integer. What's wrong? I don't know.

33:09 화자 1
Let's take it offline. I think you understand it's complex because you're using C++ code in addition to C code. Then yeah, they have different [[unclear]] strategy. Their function name is different, actually the [[unclear]] name is different, differences, and then also the [[unclear]]. It's not, of course, different, but [[unclear]]. Const [[unclear]] type of variable. Anyway, we can talk about it. Sorry, let's move on, and then let's move on to building the C [[unclear]]. So hello world, you see this program.

33:55 화자 1
In every introductory book, because [[unclear]], the first program they show is hello world. That was the very great [[unclear]] program, and then all other C books. Anyway, let's compile and run it, and compile. So you use gcc800, and then use this [[unclear]]. I want the binary name hello. Otherwise binary name's default name is a.out. Then you run it, hello world. Again, gcc800 is [[unclear]].

34:43 화자 1
-Wall, -Werror, -pedantic, -std=c99. We talked about it last time. But behind the scene what happens is that it runs C preprocessing, and then it runs the pure compiler that translates C code into, C source code into assembly language. And then hello.s is assembly language code. That is going to be assembled into object code that is understandable, that is understandable by the machine, CPU, but then it lacks the external library. So finally you need to link the library, and then come up with final binary. And then the shortcut is of course gcc800.

35:43 화자 1
So, so we have hello.c. This is gcc800 [[unclear]], hello.c, and this is gcc800 -E. That's preprocessing. Yeah, you don't have to memorize them. And then the output is not written to file, so the output goes to standard output. So you need to redirect it to another file. And then hello.i is the source code that the compiler actually sees. It does have [[unclear]], that compiler understands that.

36:43 화자 1
That encodes source code line of the header file, so that when there is an error, line error message by the C compiler points out exact correct [[unclear]], what line we have. But yeah, [[unclear]]. So you wrote this part, and then include stdio.h is, that's logically, we copy and paste the content of stdio.h. But it's not that simple. Actually inside stdio.h, it calls another file, and another file calls another file. Okay, that is after the.

37:43 화자 2
[[unclear]]

37:45 화자 1
Then let's compile this. So [[unclear]]. It's producing, gcc produces, option -S does only compilation. So it translates it into assembly language code, and the output is hello.s, hello.s. This is 64-bit assembly language, and then we compile this to [[unclear]], compile.

38:41 화자 1
And then now you have hello.o, and then you cannot look at it. It's a binary. So you have [[unclear]], and then that's not runnable. You cannot run it. Why? Because you call what function? printf function is part of libc. It's written by someone else, and then it's called library.

39:16 화자 2
Function.

39:17 화자 1
And then it's compiled already by someone else, and then the compiled name is libc.a or libc.so, depending on how we link it. So then eventually you need to, so you have hello.o, and then I think you can just do this, then you can run it, or you can specify the library name, -lc. -l means I want to use a library, c means libc.

40:18 화자 1
Okay, so the final step is linking with library. That was easy, like you already [[unclear]]. Try it. So the next slide, preprocessing C code. So that source code [[unclear]]. So it removes, when you apply preprocessor, it removes comments. It does process the #define, #pragma, to make sure you know that these directives and preprocessor directives are handled by the preprocessor. Preprocessor is a separate program. Okay, that's not.

41:20 화자 1
This technically is not a pure C compiler. The output is source code, contains declaration of printf function, missing definition of the printf function, and removes comments. So hello.i, and then hello.i is compiled into assembly language code here. The assembly language, the output, 32-bit assembly language file, and then [[unclear]] step, it produces an object file. That is a machine language, that's written in machine language, unreadable by human.

42:12 화자 1
But then it does not have the definition of printf. So what is definition of a function? The function body. So you could use printf because you have the function prototype, right? But you did not have function body. Function body is already compiled by someone else, and then that sits in another file, libc.a. libc.a is a library file containing machine language definition of printf. So using hello.o and libc.a, combine them together, and then it produces hello [[unclear]]. And then the final one is executable, still like this, but everything is in there. You can actually execute the code.

43:12 화자 1
So try it. I think this is part of your assignment number one, but you can just try this command and let's see what's going on. You don't need to memorize the command. So the difference between gcc800, gcc800 is a special script made for this course. What is script? It's a text file that contains commands to execute a series of tasks. So anything, text file, anything that starts with sharp-bang, that is script. So when you say that, of course you create this text file, you need to make it executable. In order to do that.

44:11 화자 1
If you do chmod +x, executable. So you type in gcc800, that's a text file, and then make it executable. And then move it to /usr/bin. Then /usr/bin is typically in your PATH string, so whenever you type gcc800, [[unclear]]. gcc800 adds a number of options to catch improper programming. Educationally, this is good.

44:50 화자 1
So yeah, you can use Emacs, vi, or VS Code, whatever editor. You can type in this guy, and then in your local side, I know you're using your local environment, right? And then create that, and then chmod, and then move it to, move this file to a folder that can be accessed [[unclear]], like this, mv. And then [[unclear]], to /usr/bin, you need to have sudo. But you own your machine, you have sudo. Let's [[unclear]].

45:48 화자 1
You can set up your machine like this. The lab machine is already set up. Let's move on to the next one, and after this we'll actually be moving to [[unclear]]. Okay, pointers. We again review the pointers here. Help you learn about pointers, memory and addresses, pointers and applications, pointer variables, operators, relationship, relation to arrays. Yeah, mastering pointers is difficult, but it is required for system programming. Very efficient, often dangerous if not careful. Requires lots of practice to internalize it, but efficient and convenient.

46:44 화자 1
So let's look at [[unclear]] C code. You have integer variable i. You assign one. So if you look at the elements of a variable, you have a name, i, and you have a value of that i [[unclear]]. That one is what the value is, sits on four bytes that i is allocated. That address is memory location where its value is stored. Typically we use the start address of consecutive bytes. Start address of the memory location of i is the address of i. Pointer variable, the special variable whose value is an address, called X, type, pointer.

47:40 화자 2
We

47:41 화자 1
We can have int pointer, char pointer, long pointer, et cetera. Its value is interpreted as the start address of type X. We all know this. Memory is divided into bytes. Each byte stores eight bits of information. Each has a unique address, so it's byte-addressable. [[unclear]] In B language, B [[unclear]]. From C, the hardware finally supports byte addressing. Layout of n bytes, you can view the memory as a linear, zero to n minus one.

48:32 화자 1
And then variable, C variable, each variable occupies one or more bytes of memory. The address of the first byte is said to be the address of the variable. The address of the variable i, say, let's say [[unclear]]. Pointer variable, a special variable that holds an address. When we store the address of a variable i in the pointer variable p, we say that p points to i. If p has address of i, we say p points to i. Graphical representation like that. Pointer variable declaration, you have star in declaration. This star is not an operator.

49:25 화자 1
You can use the star to dereference the pointer variable. That is the operator. Here, when you write type here in declaration, that's not the operator, that just tells the type of.

49:43 화자 2
네

49:47 화자 1
Name preceded by asterisk indicates that it is a pointer. p is capable of pointing to memory storing data of type int. q is also capable of pointing to memory storing data of type float. It can be declared along with other variables. Every pointer variable should point only to memory storing a particular type, called referenced type. Okay, so p itself has a value. That value is the address of [[unclear]], but address [[unclear]]. If you go there and read four bytes, that is integer. That is my interpretation. Same thing, double *q.

50:48 화자 1
Can have a value, and if you dereference it, then you go to that address, and then from that point you read eight bytes, and I want to interpret these eight bytes as double format. According to IEEE 754, I want to interpret this bit pattern as a floating-point number of type double.

51:15 화자 2
Okay.

51:17 화자 1
Okay, no restrictions on referenced types. Can reference structure, can reference, what is star-star p? You go there, let's say you dereference it. What value is, you go to the address that p holds, and then read, what, how many bytes? And these eight bytes are now interpreted as integer pointer, address of another integer.

51:53 화자 2
Okay, okay.

51:59 화자 1
The challenge, what is this? What's the difference between these two? Yeah, we have the answer to that. So [[unclear]] in this declaration, parentheses have highest precedence, so you need to, you need to interpret first. So what is p? p is star, p is a pointer, okay? Point to what? To integer array of [[unclear]]. p points to an array, and that array has ten elements.

52:46 화자 2
Okay.

52:48 화자 1
Do you understand? What about this? Oh, you don't have parentheses. Then if there's no parentheses, array takes precedence over star. Okay, so what is p? p is an array of what? Array of ten elements, and each individual element type is what? Integer pointer. Each individual element of [[unclear]], that is an integer pointer. You have ten pointers, array. Okay, ampersand.

53:40 화자 1
Operator is there to find the address of a variable, so this is an address operator, called address operator. And star operator, asterisk or star operator, to read the data that pointer points to. So we want to dereference, called indirection or dereference operator. Don't be confused with pointer declaration, okay? So declaring a pointer variable allocates eight bytes, eight-byte memory to store a pointer variable, pointer value. So declaration doesn't make it point to anywhere in the memory yet. So p is usable, but then p has some value in there, but that is garbage value. So p is allocated memory.

54:41 화자 1
Eight bytes might be allocated for p. So when you print out p, how do you print out p using printf? Which format specifier do you use? Have you printed out? 눈을 맞추세요. Have you printed out pointer? So then integer, %d, right? [[unclear]] Then what do you need to do for the p itself, the pointer? %p. [[unclear]] %p, %p, and then it prints out the address.

55:31 화자 1
But then, so int *p, then you can print out the value here. That doesn't crash the [[unclear]], but that value is garbage. Rather than that, you should not dereference it. You go there and try to read it, then OS will not like it, because it's possible, it's plausible that you go to some memory area that is not allocated to your program yet. OS will [[unclear]] and kill it. Can assign the address of a variable with ampersand. So p is, now holds the.

56:17 화자 1
Address of i, or p points to i, again that is assigned to p. Now you can actually draw like p points to i. And then interestingly, you declared i, and then now you can use the address of i to initialize, that's possible. Okay, if i is declared later there, you cannot do that because C will read one line at a time. And then if this guy is here, then C will be confused with this i. What i does this mean? So it needs to be declared earlier, and you can use the address.

57:10 화자 1
So i, to assign the value of p, to assign p, this is fine, this is not fine. [[unclear]] So p points to i and *p in printf. i itself is random if the code is like this. You didn't initialize it, but print out *p.

57:49 화자 2
응

57:50 화자 1
If this is the value of i, it is okay, it doesn't crash.

57:57 화자 2
Okay.

58:04 화자 1
What if you don't do, and then, sorry. You declare p but you did not initialize it. You print out [[unclear]] p value, not p, *p, dereference it, dereference it. Then what's the typical behavior that you want? Undefined behavior, okay? Yeah, I know, but when it runs, what's it different, what do you see? This program proceeds?

58:59 화자 1
Crash, this is the crash, probably crash. Why? Because again, you're trying to access the address, memory that doesn't belong to you yet. [[unclear]] So it tries to access the memory address that doesn't belong to this process, this program yet. And then if that happens, OS will intervene, and OS will not like you. OS will kill you, kill the process. That's an access violation. Have you learned the concept of virtual memory? Have you learned the concept of process? Okay?

59:49 화자 1
So let's say you're using 32-bit [[unclear]]. So vaguely, maybe, I'm not sure if you already know this. Whenever you run a process, you have 32-bit address space. Entire 32 bits should be accessible. You can use the entire four gigabytes of memory. But then here we are using 64. Again the same thing applies here. Of course not entire 2^64 memory space, well, no, smaller than that, but theoretically, right? This is my virtual memory space, and then I should make up the [[unclear]], no matter what. If that is, that's my territory, why does OS kill it?

01:00:50 화자 1
So going to someone else's process, that's even more serious. If you can go into someone else's process and look at it and then change the value, that's horrible. Used to be like that in the system, there's no concept of process, every memory is shared. But these days we are living in a virtual memory space, virtual memory space and [[unclear]]. So inside one process I see only my virtual [[unclear]]. I cannot see someone else's. But then this world should be visible to access. That's true, but then yes, that is what OS wants to provide. But then in order to do that, you need to allocate the memory first. That is practical catch. You cannot just use any memory address that is not allocated yet.

01:01:46 화자 1
Actually then use itself, in order to use the memory you need to allocate. You need to get the permission from the OS, and now I'm going to use this memory space. Then you get access to read and write, whatever. If you didn't do that and try to access it, that's not [[unclear]]. That's how the memory management by operating system runs. We actually learn this concept later. So we not simply learn the C grammar, we understand, try to understand what it is like. Star reads what's stored in the memory address, so if you apply.

01:02:44 화자 1
Address and star, same, it's i. If p points to i, *p has the same value as i. Change the value of *p, *p and i, so if you change the value of *p, then it changes the value of i.

01:03:06 화자 2
Okay.

01:03:18 화자 1
Print, print i, that's one. Print *p, that's one, because *p is exactly same as that. *p, you assign two to *p, then i's value changes to two. You all know this, right? Then star, if I start uninitialized this thing, that's the [[unclear]] here. int *p, you declare p but you don't properly initialize it, and you try to dereference. This is logically wrong, undefined behavior, [[unclear]], OS kills the process.

01:04:09 화자 1
And then yeah, so here what I'm emphasizing is that reading random address, reading the value of random address, that's [[unclear]]. Writing to it is also, because that memory address, memory area may not be allocated. It's possible, it's possible that that memory may belong to you. Then it doesn't [[unclear]], it's very rare. But then it is a logically wrong statement. Reading or writing a value at a random address is prohibited by the OS. OS stops the program. And learn the reasons. Pointer can be assigned to another pointer of the same type.

01:05:03 화자 1
So you have p and q that [[unclear]]. p points to i, and q is assigned by p. p, so p, what does p have? What does p have? p has address of i. Address of i is now assigned to q, so now q points to i. p and q both point to i. So *p, you assign one to *p, that means i's value is updated. And then, okay, q, *q, you assign two, i's value is updated. Any number of pointer variables may point to the same memory. Um, so not just two pointers, you can have any number of pointers, you can get to the same memory.

01:05:59 화자 1
So how different are these two statements? q assigned by p, *q assigned by *p. q assigned by p is that q now points to what? Now q points to what p points to. *q, *p. It's possible p and q point to something else, and then *p is the value dereferenced at the time. That is assigned to what q points to, so it's different. So here example, p points to i, q [[unclear]], i is one. So what this guy means, that it's the same as j assigned by i.

01:06:59 화자 1
Right, because *p is same as i, *q is same as j, j is assigned by i. But what if you're going to have [[unclear]] q assignment, then q now points to i. scanf, scanf reads the standard input characters and converts them into a specified type. The %d means that I want to read characters and then interpret that as integer, and you store them, insert into another [[unclear]].

01:07:55 화자 2
Okay.

01:07:56 화자 1
So that's standard usage. Given i, integer i, you provide the address of i so that scanf will update the value of i. Without ampersand, the value of i is provided to scanf. So if you do not have ampersand in the upper case, right, that's [[unclear]], so scanf will go to address of what, address of [[unclear]] i. That may be random. What does scanf do with %d? Converts input string, we write to.

01:08:44 화자 1
Write it to the provided address. The arguments need to be pointers. Doesn't mean that ampersand, doesn't mean that ampersand is always [[unclear]]. p is already a pointer. p points to i, so you can just provide p instead of ampersand i. These two are different ways to do scanf. So this one is the same as that, you provide the address of i to scanf. What is this one? Does it crash? So when you run this, if you use gcc800, it doesn't compile. Let's say you use gcc only, it will compile.

01:09:39 화자 1
Does it crash when you do this, does it crash or not? Crash, raise your hand. Crash, crash, raise your hand. Crash, crash, crash. I don't know. It doesn't crash. Why? What does scanf read? [[unclear]] One, two, three characters, three ASCII codes, one, two, three, and I want to make it into one decimal, [[unclear]], and I store it into the integer variable i in the upper case.

01:10:34 화자 1
Here, but mechanically what does it do? It goes to the address. It tries to update four bytes, four bytes. That's it. Doesn't matter, this is a pointer variable, this is an int variable, even float variable, even double variable, doesn't matter. You have address to write, and you can write to it. That address is already allocated by OS to you, I can use it, right? Doesn't matter if that is, yes, my intention is that this area is the integer pointer variable space. But from the perspective, actually perspective of CPU, CPU does not know anything about type. CPU just reads and writes to a set of memories.

01:11:26 화자 1
And then when you do it, if you do not violate memory constraint, it already belongs to this process, so you update four bytes of this eight-byte space that is reserved to p. And then you're updating four bytes by scanf, and then the entire, if you want to actually look at p itself, that's a [[unclear]] here. Four bytes updated, maybe four bytes are still there, but still this doesn't crash. You understand? Are you saying, question?

01:12:20 화자 1
Okay, functions allow you to return pointers. So given an a pointer, b pointer, you compare the value of a and b, the dereferenced value, *a, *b. Depending on the result, you return [[unclear]]. So here you have i and j. i and j are somehow assigned, and then you want to get the [[unclear]], you want to point to the bigger integer. Then after the call, p points to either i or j. [[unclear]] Similar, but different. What's wrong with this following function? So you have int a, that's integer, and you compare the values similar to before, and you want to return a pointer to either a or b.

01:13:20 화자 1
So p will point to what after this? p will point to what? Is it, is it correct, is it wrong, does it crash?

01:13:32 화자 2
First of all.

01:13:34 화자 1
When you run this, [[unclear]] or not? So you haven't gone into lots of memory access violations yet. This is good, actually we will learn a lot of them in this [[unclear]]. So is it correct? Yeah, if it's correct, I wouldn't have said, what's wrong? What's wrong? It's not the [[unclear]] memory address.

01:14:40 화자 1
No, [[unclear]], you're saying that a's address is different from i's address, is that what you're saying? Yeah, but then the one doesn't [[unclear]]. Again, you're right, i's address and a's address, and one is a's address. Then yeah, that's called stack, function, function stack. When you are running a function, we will learn that.

01:15:22 화자 1
So, and then if a, if b are parameters, then even if you declare a local variable at this point, all these local variables, parameters are allocated in the [[unclear]], special [[unclear]]. That's typically one megabyte for the entire system. You share that using it across [[unclear]]. So i is allocated in the stack, it's not malloc. malloc is dynamically allocated. That's heap. Physically nothing different if you look at the DRAM cell, that's exactly same, but logically.

01:16:12 화자 1
Sits in the function stack. Stack is allocated by [[unclear]], and then you run it, then you share it across [[unclear]]. So then local variables, so here i, j and p are local variables to the caller function. If they are in the [[unclear]], in the stack, this function, but then if you call max, you go to max, and max, it will have its own memory space for stack. But then what's interesting is when it returns, the allocated memory space that this function uses will be deallocated, because you no longer use it. So, but what max does is it returns the address of that memory space, so p will have address of formerly allocated stack memory address.

01:17:12 화자 1
That may or may not be accessible. It may crash sometimes, sometimes it works, sometimes it doesn't. But assigning the memory itself, memory address itself [[unclear]], you can hold your memory address itself. Always what's problematic is dereference. You go there and try to interpret, try to access, really try to write, that could trigger the OS to kill the process when the access is coming. i and j are passed to local variables of the function, and then function will return memory address of the local variable.

01:17:59 화자 1
And when it returns, the memory area of the function local variable will be deallocated. Returned [[unclear]] may hold garbage. So this is what, this one is correct, this one [[unclear]]. Array and pointer relationship: &a[i]. &a[i] is a pointer to element, element i of the array, array a, the array a. You want element i of the array a, and [[unclear]], each element has an address. I want to pick the address.

01:18:48 화자 1
Often useful to return a pointer to the array element. Function that returns a pointer to the middle element of a, assuming that a has n elements. So here find_middle, the first [[unclear]] is an array a, and int n, so middle is n over two. So you get the middle element, and then you get the address, and it's [[unclear]] for you. And then here, when you pass an array, you know this array, you do not pass all elements, copy all elements to the parameter. You pass the pointer to the parameter.

01:19:48 화자 1
So a is an array, it looks like array, but it's used as [[unclear]]. That's confusing. Pointer-to-pointer and array relationship is confusing because sometimes [[unclear]]. Accessing array elements with pointers, p goes to the first element. And then you can access array element using [[unclear]], assign five. So that means a[0] is updated by five. Like this. Pointer arithmetic simplifies access to array elements with pointers. C supports three forms of pointer arithmetic.

01:20:46 화자 1
Plus, minus with an integer, or subtraction between two pointers. That's it. No multiplication, no division. p + j points to j places after the one, after the one p points to. p points to a[i], p + j points to a[i+j], okay? So here, example here, p, q are integer pointers, i is integer. p points to a[2], a[2] element, and q is what? Assigned with p plus three.

01:21:43 화자 1
Then q points to what? q points to a[5] because p points to a[2]. Does it make sense? Did you know this, did you know this? Okay, when you do this, q is character pointer. Typecasting, typecasting to character [[unclear]]. Then what does q point to?

01:22:40 화자 1
So to make it easier, let's say a, a, so p points to a[2], let's say a[2]. [[unclear]] Do you see the first? So if you do not have this typecasting, if you do not have this typecasting, this addition is not simply integer addition. Takes care of, it considers this type, so [[unclear]] integer.

01:23:27 화자 1
Difference that you add, right? You're not just adding four to the address of p, you add four times sizeof(int). This is 16. p has, holds the address, and then +4 without typecasting means that, okay, this four means integer, four integers. Okay, 16, actual value is 16 is added to p's value. But if you typecast it to character pointer, then p's pointer now is supposed to point to character. So four times sizeof(char), which is four bytes.

01:24:13 화자 2
Okay.

01:24:14 화자 1
Okay, don't be confused. So yeah, it was [[unclear]]. Again, p advances by six elements, six integers, [[unclear]], four bytes, p advances by [[unclear]]. So p used to point to a[2], so p + 6, p points to a[8]. p points to a[i], and p minus, the same thing. Again, minus integer, that is interpreted [[unclear]], so the unit here is one type.

01:25:03 화자 1
p points to a[8], p minus three is now, q points to a[5]. And p minus six, which means p now points to a[2], okay? This should be easy for you. The result is, this is subtracting one pointer from another. So we get this distance between these pointers. Again, you apply the type. So p points to a[5], q to a[1], i is p minus q. Then five minus one, four. i should be four, four integers.

01:25:53 화자 1
Of course p and q should have the same type, integer pointer type. If you have one guy's character pointer, the other guy's integer pointer, compiler will, I don't know what you mean by this. You can typecast it, but if you make it same type, then it makes it how many elements are apart between these two. q minus p, same thing, minus [[unclear]]. Operations that cause undefined behavior: performing pointer arithmetic that doesn't point to an array element. Okay, this is completely possible because you declare integer pointer p, and then you can assign it.

01:26:45 화자 1
And that assigning itself, assignment itself is okay, but if you go there, there's no array. You can actually think differently, you can point to anywhere, and then you think, oh, this is integer, that's possible. You see what I'm saying? p, typically p points to integer, so integer, but it doesn't have to, because it can have some valid memory address, and I think [[unclear]] these four bytes should be integer. I define that, that's possible. It doesn't have to be named with [[unclear]].

01:27:35 화자 1
But then if you know, so, okay, C assumes that the developer, programmer is excellent, you know?

01:27:48 화자 1
So but then we are not, we are not, we are not. [[unclear]] That's a good difference in [[unclear]]. So C tries to be very [[unclear]]. If you know what you do, and then you want to treat this way, I will support that. That is the concept. But then oftentimes we make mistakes. We are not experts, and then we try to do something because we don't know what's happening with [[unclear]]. Yeah, performing pointer arithmetic that doesn't point to an array element is allowed, there's no way to do this [[unclear]], but then you need to know the consequences. Subtracting pointers, [[unclear]], subtracting pointers unless both point to elements of the same, same array. So you subtract two pointers, then yeah, it should be on the same array.

01:28:49 화자 1
But it's completely possible that you have another integer pointer that points to another completely different array, and you have another pointer that points to another completely different [[unclear]]. When you do the subtraction, and then C is okay with that. Oh, this is the integer pointer, this is the pointer. Subtraction is difference in terms of number of bytes divided by four. Four is the size of integer. How many elements can you squeeze in between this p and q? That's it, but then that's allowed. That, C will compile and run it, but the programmer is responsible.

01:29:38 화자 1
Operations using relational and equality, let's say [[unclear]], greater than, greater than or equal to, equality. Meaningful only for pointers, elements, pointers to elements of the same array.

01:30:04 화자 1
Outcome depends on the relative positions of two. What now is actually, it checks, compares the address, kind of integer, which is p is larger than q, in which [[unclear]]. Okay, so here example, your p points to a[5], q points to a[1], so p's address will be larger than q, because the array is allocated from lower address to higher address. What's the outcome? Is p less than or equal to q? No, p is larger than q. And then this is expression. What is expression? Expression, it evaluates to a number.

01:30:57 화자 1
Value, so this one produces a value zero. Zero is false in C. And what about this? There is one. Almost there. The name of the array can be used as a pointer to the first element. So here a is an array of ten integer elements, and *a. What is *a? *a is exactly same as a[0]. So you assign seven to a[0]. a + 1, that's a[1], [[unclear]].

01:31:49 화자 1
Exactly same as array, a bracket one bracket. It's the same as *(a + 1). It's the same, *(a + i), you can say a[i]. a + i is the same as address [[unclear]]. Okay, a's type, what, a is, a is array of ten integers.

01:32:14 화자 1
But it decays to integer pointer most time, and this is confusing. And then sometimes you apply [[unclear]], when you do sizeof, then it is [[unclear]]. So sizeof(a) is what? How many bytes? Do you understand my question? sizeof(a), how many bytes? Do you understand? [[unclear]] Eight bytes? Right here? [[unclear]] Again, it decays to integer pointer most time, but exception it is.

01:33:06 화자 1
Like sizeof array means that I compute the number of bytes required to allocate this, so 40 bytes will be the output of this guy. And ampersand is array's, ampersand array. So you have 40 bytes of memory that will be used as ten elements, reserved, and the first address is [[unclear]]. &a is the address of this entire array. Initialization, so a, so a bracket without any [[unclear]]. This is [[unclear]].

01:33:54 화자 1
You assign two, this is an initialization with declaration, initialization, and you assign one to a[0], [[unclear]], two. And then array size of a is two elements. We have two elements here. The array name cannot be assigned. The array name is, so it can be readable, but it cannot be written. Then if it's written, then this array cannot be accessed at all. You leave your house, you have your house of array, and then you are the name. Through your name people access your house, but if this guy is updated to point to another thing, you can't access.

01:34:51 화자 1
So this is not [[unclear]], so a itself cannot, but then you can use pointer to point [[unclear]]. p has address of a, and then iterates one by one, by what, element, access the whole element using [[unclear]]. Okay, let's stop here. So mark your name today, and if you participated today or you participated the last time, please come out.
````
