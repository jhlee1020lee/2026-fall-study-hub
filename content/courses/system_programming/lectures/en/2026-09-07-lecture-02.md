---
title: "2026-09-07 · System Programming Lecture 2 · English"
course: system_programming
date: 2026-09-07
lecture_no: 2
tags:
  - system_programming
  - lecture
  - detailed-en
lang: en
translation_of: "content/courses/system_programming/lectures/2026-09-07-lecture-02.md"
concepts:
  - "function"
  - "loop"
  - "type"
review_status: approved
draft: false
source_assets:
  - "01.CProgrammingExamples.pptx"
  - "02.CPointers_24a7628c.pptx"
---

> Korean version: [[courses/system_programming/lectures/2026-09-07-lecture-02|한국어로 읽기]]

## 10-Minute Review

This lecture connects readable C programs to the machinery that builds and executes them. Its central habit is to identify what each operation actually acts on: a character value, a state transition, a function declaration, a pointer object, or the object reached through that pointer.

1. **Preserve the complete result of `getchar`.** Store it in `int` so that character values remain distinguishable from `EOF`. The essential issue is the extra outcome beyond the character range; `EOF` is not universally `-1`. [STT 06:35] [STT 07:28] [M01 p.4]
2. **Make character-processing intent visible.** ASCII numbers explain representation, character constants improve readability, and `isalpha`, `isdigit`, and `isspace` express classification directly. [STT 12:25] [STT 13:16] [M01 p.6] [M01 p.9] [M01 p.10]
3. **Count word beginnings.** With states `OUT` and `IN`, increment the counter only when a non-space character causes `OUT → IN`. Repeated spaces and additional characters inside one word must not increase the count. [STT 15:09] [M01 p.12]
4. **Expose the design in the code.** Use meaningful state names, explain what each function accomplishes, and consider unexpected states explicitly. These were lecture-specific expectations, including the importance of function comments for coursework. [STT 25:24] [STT 28:36] [M01 p.17] [M01 p.18]
5. **Separate the four build stages.** Preprocessing produces C source for compilation; compilation produces assembly; assembling produces an object file; linking connects the required implementations. A declaration of `printf` does not supply its definition. [M01 p.21] [M01 p.22] [M01 p.24] [M01 p.25]
6. **Distinguish the pointer from its pointee.** `q = p` changes the address stored in `q`; `*q = *p` copies a value into the object already designated by `q`. This distinction also determines where `scanf` writes. [STT 01:05:59] [M02 slide 14] [M02 slide 15]
7. **Check lifetime and bounds before access.** A returned pointer is useful only while its target remains alive. Pointer arithmetic operates in elements and must remain within the permitted array bounds. [M02 slide 17] [M02 slide 18] [M02 slide 23]
8. **An array is not a pointer variable.** Ordinary array conversion, `sizeof(a)`, `&a`, and the invalid expression `a++` reveal different aspects of the array’s type. [STT 01:32:14] [STT 01:33:54] [M02 slide 25]

Three exam connections sharpen these checks: identify the destination object in a malformed `scanf` call; identify the operand whose size is requested; and explain why linking remains necessary after an object file has been produced. These connections do not make the entire historical questions part of this lecture’s scope. [EX:sp_2024_2_midterm_q01 p.3] [EX:sp_2025_1_midterm_q01 p.2](exam_questions/sp_2025_1_midterm_q01) [EX:sp_2025_2_midterm_q05 p.12]

## Connection to the Previous Lecture

The September 2 note establishes the earlier discussion of library functions, pointers, arrays, and `sizeof`. This session develops those distinctions through processing stages and memory diagrams.

- **From calling a library function to obtaining its implementation:** a declaration tells the compiler how a function may be called. The function body must still be supplied through its definition. The lecture makes this distinction concrete through `hello.i`, `hello.o`, and the executable. [STT 41:20] [STT 42:12]
- **From type names to memory interpretation:** [[concepts/타입|type]] determines what a pointer references and the unit used when an integer is added to it. The lecture asks students to connect declarations with actual reads, writes, and movement through memory. [STT 49:47] [STT 50:48] [STT 01:23:27]
- **From the array–pointer distinction to practical tests:** `sizeof(a)` measures the array, `&a` points to the whole array, and `a++` is invalid because the array name is not a modifiable pointer variable. [STT 01:32:14] [STT 01:33:54]

The recurring question is now more precise: **Which object does this expression designate, and is the proposed operation valid for that object?**

## Lecture Flow

Times follow the transcript’s sequence. Material ranges identify supporting slides, not exact slide-change timestamps.

| Time | Material | Guiding question | Learning outcome |
|---|---|---|---|
| 00:00–01:48 | [M01 p.2] [M01 p.3] [STT 00:00] | What will the character-counting program read and report? | Explain standard input, input redirection, and separate counts for letters, digits, and other characters. |
| 01:48–02:43 | [STT 01:48] [M01 p.4] | How does participation work, and how does the input loop begin? | Retain the participation reminder without identities; recognize the `getchar` loop. |
| 02:43–08:29 | [M01 p.4] [M01 p.5] [STT 06:35] | Why does a character-reading function return `int`? | Preserve character values and the separate `EOF` outcome. |
| 08:29–12:25 | [M01 p.6] [M01 p.7] [M01 p.8] | How can characters participate in arithmetic? | Read ASCII ranges and explain the demonstrated character conversions. |
| 12:25–14:04 | [M01 p.9] [M01 p.10] | How can classification become clearer and less dependent on numeric encodings? | Compare numeric ranges, character constants, and `ctype.h` functions. |
| 14:04–17:05 | [M01 p.11] [M01 p.12] | What information is sufficient to count words? | Track `IN` and `OUT`, incrementing only on entry into a word. |
| 17:05–20:17 | [M01 p.13] [STT 19:40] | How does state tracking extend to recognition and comment removal? | Interpret the partial recognition diagrams and understand the DFA connection to the decomment assignment. |
| 20:17–24:27 | [M01 p.14] [M01 p.15] | Why replace state numbers with names? | Explain magic numbers, `enum`, macro replacement, and `const` objects. |
| 24:27–30:23 | [M01 p.16] [M01 p.17] [M01 p.18] [M01 p.19] | What makes a working program easier to review and debug? | Add purposeful comments and consider invalid states through defensive programming. |
| 31:14–33:09 | [M01 p.15] [STT 31:14] [STT 32:15] | Can an ordinary `const int` serve as a C99 `case` label? | Preserve the unresolved classroom discussion; distinguish it from the editorial language-rule clarification. |
| 33:09–35:43 | [M01 p.20] [M01 p.21] | What does a single `gcc800` command perform? | Identify the four build stages and their intermediate products. |
| 35:43–40:18 | [M01 p.21] [STT 36:43] [STT 39:17] | What appears after preprocessing, compilation, and assembly? | Distinguish included declarations, assembly, object code, and library implementations. |
| 40:18–43:12 | [M01 p.22] [M01 p.23] [M01 p.24] [M01 p.25] [M01 p.26] | Why is an object file not yet the final executable? | Explain the declaration–definition distinction and the purpose of linking. |
| 43:12–45:48 | [M01 p.27] | Why use the course wrapper? | Understand its diagnostic options and the setup reminder for personal environments. |
| 45:48–49:47 | [M02 slide 2] [M02 slide 3] [M02 slide 4] [M02 slide 5] [M02 slide 6] | What are a variable’s name, value, and address? | Interpret byte-addressed memory and a pointer’s arrow to another object. |
| 49:47–53:40 | [M02 slide 7] [M02 slide 8] | How do declarations determine the referenced type? | Distinguish `int *`, `int **`, pointer-to-array, and array-of-pointers declarations at an introductory level. |
| 53:40–58:59 | [M02 slide 9] [M02 slide 10] [M02 slide 11] [M02 slide 12] | What does pointer initialization establish? | Separate pointer initialization, pointee initialization, and dereference. |
| 58:59–01:02:44 | [STT 59:49] [STT 01:00:50] [STT 01:01:46] | Why can a process have an address space yet still fault on access? | Understand the motivation for virtual memory, allocation, and access protection. |
| 01:02:44–01:06:59 | [M02 slide 11] [M02 slide 13] [M02 slide 14] | Does an assignment change an address or the target value? | Trace shared targets and distinguish `q = p` from `*q = *p`. |
| 01:06:59–01:12:20 | [M02 slide 15] | Which address must `scanf` receive? | Identify a valid `int` destination and explain why `&p` is wrong for `%d` when `p` is an `int *`. |
| 01:12:20–01:17:59 | [M02 slide 16] [M02 slide 17] | Does the returned pointer designate an object that survives the call? | Contrast returning a caller-owned object’s address with returning an expired local object’s address. |
| 01:17:59–01:20:46 | [M02 slide 18] [M02 slide 19] [M02 slide 20] | How do pointers identify and access array elements? | Explain `&a[i]`, returning a middle element, and the three basic forms of pointer arithmetic. |
| 01:20:46–01:25:53 | [M02 slide 21] [M02 slide 22] [M02 slide 23] | What is the unit of pointer movement and subtraction? | Trace element positions and separate cast-before from cast-after arithmetic; retain uncertainty about the exact board expression. |
| 01:25:53–01:30:57 | [M02 slide 23] [M02 slide 24] | Which pointer operations have defined meaning? | Apply array-bound restrictions and distinguish relational ordering from equality. |
| 01:30:57–01:34:51 | [M02 slide 25] | When does an array expression convert to a pointer? | Distinguish indexing, addresses, whole-array size, and a separately incrementable pointer. |
| 01:34:51–end | [STT 01:34:51] | Where did the lecture stop? | Record the stopping point at M02 slide 25 and retain the closing participation reminder without identities. |

## Core Concepts

### 1. `getchar` and `EOF`

**Definition:** `getchar` produces either a character value or the special result `EOF`.

**Lecture framing and evidence:** The instructor repeatedly asked why a one-byte input character requires an `int` result, then focused on the need for an additional distinguishable value. [STT 02:43] [STT 06:35] [M01 p.4]

**Worked example:** In `while ((c = getchar()) != EOF)`, assignment preserves the complete result before comparison.

**Misconception:** The purpose is not simply to accommodate the number `-1`; it is to avoid confusing a character with the end condition.

### 2. ASCII and readable classification

**Definition:** ASCII associates characters with numeric codes. Classification functions express properties such as alphabetic, digit, and whitespace membership.

**Lecture framing and evidence:** Numeric arithmetic was used to reveal representation, while the instructor explicitly criticized it as a model for readable application code. [STT 09:51] [STT 12:25] [M01 p.8] [M01 p.10]

**Worked example:** Under ASCII, `'a' + 3` produces the code for `d`. For testing alphabetic membership, `isalpha(c)` communicates the intended question more directly.

**Misconception:** Being able to derive a result from ASCII arithmetic does not make that arithmetic the clearest implementation.

### 3. Word counting with a DFA

**Definition:** A DFA records a finite state and selects its next state from the current state and input.

**Lecture framing and evidence:** For word counting, the necessary history is whether the scanner is inside or outside a word. The increment belongs to the transition into a word. [STT 14:43] [STT 15:09] [M01 p.12]

**Worked example:** `a   b` produces two `OUT → IN` transitions and therefore two words.

**Misconception:** Counting spaces, non-space characters, or visits to `IN` does not implement the stated word-counting rule.

### 4. Named constants, comments, and defensive programming

**Definition:** Named constants expose meaning; function comments describe purpose; defensive checks expose violations of intended program state.

**Lecture framing and evidence:** The progression from numeric states to `enum`, comments, and a guarded `default` was a deliberate code-improvement exercise. [M01 p.14] [M01 p.16] [M01 p.17]

**Worked example:** `state = OUT` expresses an initial condition more clearly than `state = 1`.

**Misconception:** Producing the correct count on one input does not establish that the program is easy to maintain or diagnose.

### 5. Building a C program

**Definition:** Preprocessing, compilation, assembling, and linking transform source into an executable through different intermediate representations.

**Lecture framing and evidence:** The instructor emphasized understanding what each stage contributes rather than memorizing every command. [STT 35:43] [STT 43:12] [M01 p.21]

**Worked example:** `hello.o` can contain a call requiring an external implementation even though machine code has already been generated.

**Misconception:** Including a header does not insert the compiled implementation of every declared library function.

### 6. Pointer objects and dereference

**Definition:** A pointer object stores a pointer value. Dereference designates the object reached through that value.

**Lecture framing and evidence:** The lecture separated the name, stored value, and address of a variable, then used diagrams to show how `*p` accesses the target. [STT 46:44] [M02 slide 6] [M02 slide 11]

**Worked example:** With `int i = 1; int *p = &i;`, assigning `*p = 2` changes `i`.

**Misconception:** Declaring `p` does not create an initialized target integer.

### 7. Pointer assignment and input destinations

**Definition:** Pointer assignment copies an address value; assignment through a pointer changes its target.

**Lecture framing and evidence:** The contrast between `q = p` and `*q = *p` leads directly to the question of where `scanf` stores input. [STT 01:05:59] [M02 slide 14] [M02 slide 15]

**Worked example:** If `p = &i`, both `scanf("%d", &i)` and `scanf("%d", p)` designate `i`.

**Misconception:** Adding `&` mechanically to every `scanf` argument is incorrect.

### 8. Returned pointers and object lifetime

**Definition:** A returned pointer must designate an object whose lifetime permits its subsequent use.

**Lecture framing and evidence:** Two similar maximum-selection functions differ critically in whether they return an incoming pointer or the address of a local integer parameter. [STT 01:12:20] [M02 slide 16] [M02 slide 17]

**Worked example:** Returning `&a[n/2]` can designate a live element of a caller-owned array when the array and index satisfy the function’s preconditions.

**Misconception:** Surviving address bits do not extend a local object’s lifetime.

### 9. Pointer arithmetic and array expressions

**Definition:** Pointer arithmetic moves in units of the referenced type. Array expressions often convert to pointers, but the array object retains its own type.

**Lecture framing and evidence:** The final discussion combines element movement, subtraction, comparison, `sizeof`, and the inability to increment an array name. [M02 slide 21] [M02 slide 23] [M02 slide 25]

**Worked example:** Within one suitable array, `&a[5] - &a[1]` gives an element distance of four.

**Misconception:** An address expression and the value stored at that address are not interchangeable: `a+i` corresponds to `&a[i]`, whereas `*(a+i)` corresponds to `a[i]`.

## Detailed Explanation

### 1. Character processing: representation before classification

The first program reads standard input and classifies each character as a letter, a digit, or something else. Its input may come from the keyboard or from a redirected file. Feeding the program its own source illustrates that source text can also be ordinary input data. [STT 00:59] [M01 p.3]

The highlighted code on M01 page 4 connects two locations: the declaration `int c` and the expression that assigns `getchar()` before checking `EOF`. These are one design decision. Narrowing the result prematurely can lose the distinction that controls loop termination.

For the lecture’s eight-bit-byte model, there are 256 possible byte values:

$$
2^8 = 256.
$$

An interface that must distinguish all those values from an additional end condition needs an appropriate result representation. The instructor’s explanation at 06:35 supports this argument directly; the unclear earlier response is unnecessary to establish it. [STT 06:35] [STT 07:28]

**Reading the ASCII table.** M01 page 6 places a row base at the left and a column offset at the top. Add them to obtain the code. Thus `A` appears at row 64, column 1, giving 65. The highlighted groups are:

| Character group | ASCII range | What the grouping explains |
|---|---:|---|
| Digits `0`–`9` | 48–57 | A numeric interval can recognize these characters. |
| Uppercase letters `A`–`Z` | 65–90 | The example’s uppercase test uses this interval. |
| Lowercase letters `a`–`z` | 97–122 | The lowercase test uses a separate interval. |

The arithmetic examples on M01 page 8 then produce `a d z` and `c C` from the displayed expressions. The correspondence between uppercase and lowercase letters explains the demonstrated offset calculation. These results are ASCII-based examples, not general recommendations for text processing. [M01 p.6] [M01 p.8]

The code evolves in two steps. Character constants replace unexplained numbers on page 9; classification functions replace explicit encoding arithmetic on page 10. The instructor’s emphasis is readability and reduced dependence on a particular character-code layout. [STT 12:25] [STT 13:16]

An editorial qualification is useful: `ctype.h` does not turn byte processing into arbitrary Unicode text processing. Its classification rules operate within the relevant C character and locale model. Its arguments must be `EOF` or values representable as `unsigned char`; the retained `getchar` result fits that requirement.

### 2. Word counting: remember only the history that matters

The lecture defines a word operationally as a maximal sequence of non-space characters. Consequently, punctuation can belong to a word: `blue!` is one word under this definition. This is a scanner specification, not a claim about linguistic word boundaries. [STT 14:04] [M01 p.11]

**Reading the DFA figure.** M01 page 12 has two circles. The incoming arrow selects `Outside Word` as the initial state. Each circle has a self-loop, and two arrows connect the circles. Only the arrow from `Outside Word` to `Inside Word` carries a counting action.

The complete transition rule for this example is:

| Current state | Input classification | Next state | Counter change |
|---|---|---|---:|
| `OUT` | Space | `OUT` | 0 |
| `OUT` | Non-space | `IN` | +1 |
| `IN` | Space | `OUT` | 0 |
| `IN` | Non-space | `IN` | 0 |

This placement solves several boundary cases simultaneously. Leading spaces do nothing. Multiple spaces between words do nothing beyond the first transition to `OUT`. A final word is counted even if the input ends without trailing whitespace. [M01 p.12] [M01 p.17]

The useful invariant is that, after processing a prefix, the count equals the number of word beginnings encountered in that prefix:

$$
W=\sum_{k=1}^{n}
\mathbf{1}\!\left[
s_{k-1}=\mathrm{OUT}
\;\land\;
c_k\text{ is non-space}
\right].
$$

This equation is an editorial formalization of the slide’s action rule, not reconstructed speech.

M01 page 13 extends the idea to recognizing `SNUCSE` and the integer-shaped pattern `0` or `[+-]?[1-9][0-9]*`. The first diagram includes fallback transitions and an explicit note about omitted transitions. The second distinguishes an optional sign, a nonzero initial digit, subsequent digits, and the separate zero case. Neither diagram is a complete specification of all C integer syntax. [STT 17:05] [STT 17:46] [M01 p.13]

The instructor linked DFA thinking to the decomment assignment: design the relevant states before implementing comment removal. That connection supports a design method; it does not supply a complete assignment implementation. [STT 19:40]

### 3. Make the program’s intended structure visible

The initial word counter uses `0` and `1` as states. The program can work, but a reader must infer what those values mean. The instructor emphasized that collaborators, and even the original author later, should not have to reconstruct this mapping. [STT 21:39] [STT 22:32] [M01 p.14]

M01 page 15 introduces three naming mechanisms:

| Mechanism | Meaning in the lecture | Important distinction |
|---|---|---|
| `enum DFAState { IN, OUT };` | Names the two state values | Enumeration constants suit this `switch` example. |
| `#define IN 0` | Replaces a macro name during preprocessing | The replacement must itself be suitable for its use. |
| `const int IN = 0;` | Declares a const-qualified integer object | It is not interchangeable with an enumeration constant in every C context. |

The question about `const int` in a `case` label remained unresolved during 31:14–33:09: **검증 필요** for the classroom conclusion. The independent C99 clarification is that an ordinary `const int` object is not an integer constant expression for a `case` label. Enumeration constants and suitable constant macros can satisfy that requirement. This clarification must not be attributed to a recovered answer in the recording. [C99 consolidated draft N1256, §6.6 ¶6 and §6.8.4.2 ¶3](https://www.open-std.org/jtc1/sc22/wg14/www/docs/n1256.pdf)

The completed example also adds a function comment and an assertion in `default`. The comment states the function’s purpose rather than narrating each branch. The assertion exposes a state outside the intended `IN`/`OUT` set. [M01 p.16] [M01 p.17] [M01 p.18]

The course-specific emphasis is explicit: write comments for functions, consider `default` when writing `switch`, and design the algorithm before coding. Standard background supplies the broader interpretation: an assertion expresses an invariant expected of correct execution. Assertions can be disabled, so they should not be mistaken for unconditional input validation.

### 4. The build pipeline: what each intermediate file still lacks

**Reading the build figure.** M01 page 21 shows a long route through four tools and a shortcut across the top. The shortcut invokes the same overall build through the driver; it does not eliminate the intermediate responsibilities.

| Stage | Input → output | Main contribution | What remains unresolved in the example |
|---|---|---|---|
| Preprocessing | `hello.c` → `hello.i` | Processes includes and macros; removes comments from the resulting source representation | The library function’s implementation is still separate. |
| Compilation | `hello.i` → `hello.s` | Translates C into architecture-specific assembly | An external call can still refer to a separately supplied function. |
| Assembling | `hello.s` → `hello.o` | Encodes assembly into object code | The object is not yet the complete executable. |
| Linking | Object files and libraries → executable | Connects the required components | The resulting executable may still use shared libraries at runtime. |

[M01 p.21] [M01 p.22] [M01 p.23] [M01 p.24] [M01 p.25]

The preprocessed output can be much larger than the original file because a header may include other headers. The lecture also mentions information retained to associate diagnostics with source locations. The important conceptual distinction is that declarations become available without inserting the entire compiled library implementation into the source. [STT 36:43] [STT 41:20]

The slide diagram uses `libc.a`. The instructor separately mentions `libc.a` and `libc.so`, depending on how linking is performed. Therefore, the diagram should not be read as proof that ordinary GCC invocation always produces a statically linked executable. [STT 39:17]

M01 page 23 contains historical 32-bit illustrative assembly and typographical problems. Its role here is to show the intermediate representation and the remaining external call. It is not reproduced as executable assembly or equated with the 64-bit output discussed during the demonstration.

**Exam connection.** The 2025 fall midterm Q5(a) asks about the linker’s principal responsibilities. Today’s declaration–definition distinction provides the motivation for **symbol resolution**: an external reference must be associated with the intended definition. **Relocation** concerns adjusting address-dependent references as components are placed together, but its detailed calculations require later material. [EX:sp_2025_2_midterm_q05 p.12]

The solution approach is to ask what remains after an object file exists. A machine-code encoding alone does not ensure that every needed external implementation has been connected. The trap is answering only that the linker “combines files,” without identifying what connections must be resolved.

### 5. Pointers: distinguish names, stored values, and addresses

The lecture describes a variable through its name, its value, and the address of its storage. A pointer introduces another object whose stored value identifies a target. [STT 46:44] [M02 slide 3]

The memory figure on M02 slide 4 separates the address column from the contents column. Slide 5 then uses the first byte’s address to identify a multi-byte variable. Slide 6 replaces the numerical address with an arrow from `p` to `i`. The arrow means that `p` stores a value designating `i`; it does not mean that `p` contains a copy of `i`. [M02 slide 4] [M02 slide 5] [M02 slide 6]

Referenced type matters. An `int *` designates an integer object; a `double *` designates a double object. The lecture explains this through the size and interpretation of the accessed data. Its four-byte `int`, eight-byte `double`, and eight-byte pointer are environment assumptions, not universal consequences of the spelling of those types. [STT 49:47] [STT 50:48]

The declaration examples introduce several distinct structures:

| Declaration | Reading |
|---|---|
| `int *p;` | `p` is a pointer to `int`. |
| `int **p;` | `p` is a pointer to a pointer to `int`. |
| `int (*p)[10];` | `p` is a pointer to an array of ten integers. |
| `int *p[10];` | `p` is an array of ten pointers to integers. |

[M02 slide 7]

Start with the identifier and respect parentheses and the binding of `[]`. The brief introduction of `int **` does not establish that the extended double-pointer exercises later in the deck were taught.

Pointer initialization and target initialization are separate:

```c
int i;
int *p = &i;
```

This establishes where `p` points. It does not initialize the value of `i`. Assigning `*p = 2` initializes or changes the target; reading `*p` first would read the still-uninitialized integer. The lecture itself notices the missing initialization in the displayed example. [STT 57:10] [M02 slide 9] [M02 slide 10]

The virtual-memory discussion explains why an address-space model does not authorize arbitrary memory access. A process needs suitable mappings and permissions. A theoretical address range is not a promise that every address is backed by usable memory. Allocation and protection were motivations introduced here; page-table mechanics were deferred. [STT 59:49] [STT 01:00:50] [STT 01:01:46]

The instructor repeatedly asked what an invalid operation might look like when executed. Those questions are useful for understanding faults, but the absence of a fault cannot establish valid C behavior.

### 6. Pointer assignment and `scanf`: locate the destination object

The paired diagrams on M02 slides 13–14 make two different effects visible.

With `q = p`, the arrow from `q` changes. Both pointers may then designate the same object. Assigning through either pointer changes that shared target.

With `*q = *p`, the arrows remain where they were. The value read through `p` is stored into the object designated by `q`. [STT 01:05:03] [STT 01:05:59] [M02 slide 13] [M02 slide 14]

This distinction gives the correct interpretation of `%d` input. `scanf` needs a pointer to a suitable `int` object into which it can store the converted value.

| Expression, assuming `int i; int *p = &i;` | Meaning as a `%d` destination |
|---|---|
| `&i` | Address of the integer object |
| `p` | The same destination address |
| `&p` | Address of the pointer object; type `int **`, unsuitable here |

[M02 slide 15]

The lecture’s wrong-`&p` discussion describes how a four-byte write might affect storage reserved for an eight-byte pointer without immediately crossing an OS protection boundary. That is an explanation of a possible machine observation under the stated environment. The call still violates the required destination type and has undefined behavior. [STT 01:09:39] [STT 01:10:34] [STT 01:11:26]

**Exam connection.** The 2024 fall midterm Q1(c) asks students to reason about input directed into pointer storage and subsequent interpretations of that storage. Its provided output predictions must not be treated as portable C guarantees. [EX:sp_2024_2_midterm_q01 p.3]

A disciplined approach is:

1. Identify the actual destination object.
2. Compare its type with the format conversion’s requirement.
3. Establish the language-level validity boundary.
4. Discuss any requested machine observation only under explicit assumptions.

The trap is concluding that writable memory makes every typed operation on it valid. The original variant below instead uses a correctly typed destination while preserving the useful pointer-versus-pointee reasoning.

### 7. Returned pointers: follow the target’s lifetime

M02 slide 16 compares the values reached through two incoming pointers and returns one of those pointer values. The caller’s integers remain the targets. The function’s local pointer parameters can cease to exist without destroying the caller-owned integers. [M02 slide 16]

Slide 17 changes the parameters to integers passed by value and returns the address of one of those local parameter objects. Those objects do not survive the function’s return. The result cannot provide valid subsequent access to them. [M02 slide 17]

The classroom explanation uses stack allocation and reclamation to motivate the distinction. The decisive language-level question is object lifetime, not whether the same bytes remain mapped or happen to contain an old value. The fixed stack-size remark in the transcript must not be generalized into a universal limit. [STT 01:15:22] [STT 01:16:12] [STT 01:17:12]

The middle-element function supplies the constructive alternative:

```c
int *find_middle(int a[], int n)
{
    return &a[n / 2];
}
```

Its intended preconditions are a live array with at least `n` elements and `n > 0`. For positive `n`, `n/2` identifies a valid element; for an even length, it selects the higher-indexed of the two central elements. The lecture explains that this call passes access to the array rather than copying every element. [STT 01:18:48] [M02 slide 18]

The fuller consequences of array parameters on M02 slides 26–29 remain later material.

### 8. Pointer arithmetic: movement has a type and a boundary

For a valid operation on a pointer of type `T *`, moving by $k$ elements corresponds to:

$$
\text{byte displacement}=k\times\operatorname{sizeof}(T).
$$

This describes the unit of a permitted operation. It does not authorize arbitrary address arithmetic. [M02 slide 20] [M02 slide 21]

The diagram on M02 slide 21 should be read as a sequence:

| Statement | Target of `p` | Target of `q` |
|---|---|---|
| `p = &a[2];` | `a[2]` | Not yet established |
| `q = p + 3;` | `a[2]` | `a[5]` |
| `p += 6;` | `a[8]` | `a[5]` |

Changing `p` later does not move `q`. The assignment to `q` copied a pointer value; it did not create a continuously updated relationship between the variables.

Slide 22 reverses the direction: starting with `p = &a[8]`, `q = p - 3` selects `a[5]`, and `p -= 6` selects `a[2]`. The extracted code and the diagram support the same movement despite the rendered slide’s imperfect code layout. [M02 slide 22]

For pointers to elements of the same array, with a representable result:

$$
p=\&a[i],\quad q=\&a[j]
\quad\Longrightarrow\quad
p-q=i-j.
$$

Thus the result measures elements, not bytes. Subtracting pointers into unrelated arrays is not legitimized by compilation or by the possibility of subtracting their displayed address numbers. [M02 slide 23]

For `p = &a[5]` and `q = &a[1]`, the slide’s `p <= q` is false and `p >= q` is true. However, the blanket wording about “same array” should not be applied identically to `==` and `!=`: equality and relational ordering have different rules. [M02 slide 24]

One-past-the-end formation is also distinct from access. A suitable pointer just beyond the last element can be formed and used as a boundary; it cannot be dereferenced as an additional element.

**Original comparison, not recovered board work:** assume `p` points to an element with a following element and `sizeof(int) == 4`.

- `(char *)p + 1` converts first and advances one byte.
- `(char *)(p + 1)` advances one integer first and then converts.

The exact cast expression discussed around 01:22 remains **검증 필요**. The slide’s `+3` trace and the later spoken `+4` illustration must remain separate. [STT 01:21:43] [STT 01:23:27]

### 9. Arrays, conversion, and `sizeof`

An array declaration reserves storage for its elements. An ordinary use of the array expression can supply a pointer to the first element, but this conversion does not replace the array object with a pointer object. [M02 slide 25]

For `int a[10]; int *p = a;`, under the lecture’s four-byte-`int`, eight-byte-pointer assumptions:

| Expression | What it denotes or measures | Result or type |
|---|---|---|
| `a` in an ordinary converted expression | Pointer to the first element | `int *` |
| `a+i` | Address of element `i` | Corresponds to `&a[i]` |
| `*(a+i)` | Element `i` | Corresponds to `a[i]` |
| `sizeof(a)` | Entire array | 40 bytes |
| `sizeof(p)` | Pointer object | 8 bytes |
| `sizeof(*p)` | Referenced integer type | 4 bytes |
| `&a` | Pointer to the entire array | `int (*)[10]` |

[STT 01:31:49] [STT 01:32:14] [STT 01:33:06] [M02 slide 25]

`&a` and the first element’s address concern the beginning of the same storage, but they have different pointer types and different referenced units.

Likewise, `int a[] = {1, 2};` is initialization at declaration. It does not imply that a later array assignment is allowed. The invalid `a++` example on slide 25 demonstrates that the array name is not an incrementable pointer variable.

A separate `p` can advance through elements, but the displayed sentinel loop still requires a terminating value within valid bounds. Being able to increment a pointer does not prove that an unbounded traversal is safe.

**Exam connection.** The 2025 spring midterm Q1(a) distinguishes `sizeof` applied to basic objects, a dereference expression, and a string literal. The useful method is **declared type → operand of `sizeof` → applicable size assumptions**. [EX:sp_2025_1_midterm_q01 p.2](exam_questions/sp_2025_1_midterm_q01)

For the fixed types in that question, `sizeof(*d)` determines the size of the referenced character type without evaluating an ordinary read through `d`. This does not make an actual read through an uninitialized pointer valid, nor should it be generalized to every variable-length-type case. The trap is answering “pointer size” whenever a pointer variable appears somewhere in the expression.

## Examples, Equations, and Code

### 1. Original example: classify characters and count words in the same input

The following notation represents seven input characters. `\t` means one tab and `\n` means one newline:

```text
 A9\tb!\n
```

| Character read | Classification | State afterward | Word count |
|---|---|---|---:|
| Leading space | Other | `OUT` | 0 |
| `A` | Letter | `IN` | 1 |
| `9` | Digit | `IN` | 1 |
| Tab | Other | `OUT` | 1 |
| `b` | Letter | `IN` | 2 |
| `!` | Other | `IN` | 2 |
| Newline | Other | `OUT` | 2 |

The totals are **two letters, one digit, four other characters, and two words**. The words are `A9` and `b!`. Character classification and word recognition answer different questions. [M01 p.10] [M01 p.11] [M01 p.12]

### 2. Reconstructed lecture code: implement the transition table

This version follows M01 page 17. Assume the educational input’s word count fits in `int`. The `return 1` in `default` is an editorial addition that preserves termination if assertions are disabled.

```c
#include <stdio.h>
#include <ctype.h>
#include <assert.h>

enum DFAState { IN, OUT };

/* Count whitespace-separated words from stdin and print the count. */
int main(void)
{
    int c;
    int nWords = 0;
    enum DFAState state = OUT;

    while ((c = getchar()) != EOF) {
        switch (state) {
        case IN:
            if (isspace(c))
                state = OUT;
            break;

        case OUT:
            if (!isspace(c)) {
                state = IN;
                nWords++;
            }
            break;

        default:
            assert(0);
            return 1;
        }
    }

    printf("%d\n", nWords);
    return 0;
}
```

There is exactly one increment location. It corresponds to `OUT → IN`; the `IN` branch merely detects departure from a word. [M01 p.17]

### 3. Lecture demonstration: expose the build stages

These commands summarize the demonstrated workflow. They are not an execution log from preparing this note.

```sh
gcc800 -E hello.c > hello.i
gcc800 -S hello.i
gcc800 -c hello.s
gcc800 hello.o -lc -o hello
```

The shortcut is:

```sh
gcc800 hello.c -o hello
```

[M01 p.21] [M01 p.26]

The wrapper runs GCC with the course’s options:

```sh
gcc -Wall -Werror -pedantic -std=c99 "$@"
```

`-Wall` enables a useful group of warnings, `-Werror` treats warnings as errors, `-pedantic` requests standards-related diagnostics, and `-std=c99` selects the intended language mode. These options improve feedback; they do not prove the absence of undefined behavior. [M01 p.20] [M01 p.27]

The operational reminder was to make the script executable and available through command lookup on personal machines; the lab environment was described as already configured. [STT 44:11] [STT 45:48]

### 4. Original variant: pointer assignment followed by input

Assume this fragment appears inside a function with the appropriate header available, and input conversion succeeds with the value `9`.

```c
int x = 3, y = 7;
int *p = &x, *q = &y;

*q = *p;
q = p;

if (scanf("%d", q) != 1)
    return 1;
```

| Step | `x` | `y` | Target of `q` |
|---|---:|---:|---|
| Initial state | 3 | 7 | `y` |
| After `*q = *p` | 3 | 3 | `y` |
| After `q = p` | 3 | 3 | `x` |
| After successful input | 9 | 3 | `x` |

The first assignment copies an integer value; the second copies an address. The input therefore changes `x`. This original variant practices the destination reasoning relevant to the historical `scanf` question without reproducing its invalid storage manipulation. [M02 slide 14] [M02 slide 15] [EX:sp_2024_2_midterm_q01 p.3]

### 5. Original variant: modify a caller-owned object through a returned pointer

```c
/* Return a pointer to the larger of two live int objects. */
int *larger(int *a, int *b)
{
    return (*a > *b) ? a : b;
}

int main(void)
{
    int x = 4, y = 9;
    int *chosen = larger(&x, &y);

    *chosen = 12;
    return 0;
}
```

The comparison selects the pointer to `y`, so the final assignment changes `y` to `12`. The local pointer parameters disappear when `larger` returns, but the caller’s integers remain alive. With equal values, this particular comparison selects the second pointer. [M02 slide 16]

### 6. Original variant: combine element movement, assignment, and size

Assume `sizeof(int) == 4` and the relevant pointer size is eight bytes.

```c
#include <stdio.h>
#include <stddef.h>

int main(void)
{
    int a[6] = {4, 8, 12, 16, 20, 24};
    int *p = a + 1;
    int *q = p + 3;

    *q = *p + 1;

    printf("%d %td\n", a[4], q - p);
    printf("%zu %zu %zu\n",
           sizeof(a), sizeof(p), sizeof(*p));
    printf("%p\n", (void *)q);

    return 0;
}
```

`p` designates `a[1]`, and `q` designates `a[4]`. The assignment stores $8+1=9$ into `a[4]`. Consequently, the first two output lines are determined by the trace and stated size assumptions:

```text
9 3
24 8 4
```

The final address representation is not fixed. The code uses `%zu` for `size_t`, `%td` for `ptrdiff_t`, and `%p` with a `void *` argument for the object pointer. These results were checked through arithmetic and state tracing, not native C execution. [M02 slide 21] [M02 slide 23] [M02 slide 25]

## Misconceptions and STT Notes

### Technical distinctions to preserve

| Tempting conclusion | Correct interpretation |
|---|---|
| `EOF` always means the integer `-1`. | The loop must compare against `EOF`; its exact negative value is not universal. |
| ASCII arithmetic is the preferred classification interface. | The lecture uses it to explain representation, then improves readability with character constants and classification functions. |
| Every space finishes another word. | Only entry from `OUT` to `IN` increments this counter. |
| `enum`, `#define`, and `const int` are interchangeable everywhere. | Their language roles differ; the C99 `case`-label issue requires that distinction. |
| A header contains the linked implementation of each function it declares. | A declaration and the separately supplied definition serve different purposes. |
| The pipeline diagram proves GCC always uses static libc. | The lecture also discusses shared libc; the diagram is an instructional model. |
| Declaring `int *p` initializes a valid target. | It declares the pointer object; its value and any target’s value need separate attention. |
| Printing an uninitialized pointer is necessarily harmless. | Evaluating an indeterminate pointer is not a valid inspection technique. |
| If `p` is initialized, `*p` must contain an initialized value. | Initializing an address does not initialize the designated object. |
| `scanf("%d", &p)` is valid if pointer storage is writable. | For `int *p`, the argument has the wrong destination type; a missing crash does not repair it. |
| Returning a local address is safe until the bytes change. | Object lifetime, not leftover bytes, determines whether access is valid. |
| Casting away `const` permits modification of a const object. | A cast does not remove the object’s original const qualification. |
| Cross-array subtraction becomes valid if it compiles. | The same-array requirements still apply. |
| `a+i` is the value of `a[i]`. | It is an address expression; `*(a+i)` designates the element. |
| A valid one-past pointer is another accessible element. | It can serve as a boundary but cannot be dereferenced. |
| `a` and `&a` have the same type. | Ordinary conversion points to an element; `&a` points to the whole array. |
| A fixed stack limit applies to every program. | Limits depend on the execution environment and configuration. |

The historical exam’s crash-oriented treatment of const objects and string literals is a further reason to separate OS protection observations from C validity. Its provided crash list is not a complete list of language-level errors. Detailed memory-section analysis remains future work. [EX:sp_2025_1_midterm_q01 p.3](exam_questions/sp_2025_1_midterm_q01)

### Uncertain speech and review boundaries

| Interval | What remains **검증 필요** | Treatment in this note |
|---|---|---|
| 05:44–06:43 | Parts of the short response about the `getchar` result | Use the clear explanation from 06:35 and the slide’s representation argument; do not reconstruct the response. |
| 31:14–33:09 | The classroom resolution of `const int` versus `case` labels | Report that the discussion was unresolved; label the C99 correction as independent clarification. |
| 01:13:26–01:14:48 | The intermediate response about the faulty pointer-return function | Explain object lifetime from M02 slides 16–17 without assigning invented wording to a speaker. |
| 01:21:35–01:22:48 | Exact cast placement and board expression | Keep the slide’s `+3` example separate from the later `+4` explanation; label new comparisons as original examples. |

The three omission candidates were reviewed with uncertainty. Two low-density intervals remain in the review record, both belonging to previously reviewed windows. Workflow closure therefore does not establish complete recovery of every spoken word. The audit notice supplies reliability information, not independent evidence of lecture content.

## Concept Map

| Concept connection | Question the connection helps answer | Evidence |
|---|---|---|
| [[concepts/타입\|type]] → `getchar` and `EOF` | Can the chosen representation preserve every required outcome? | [M01 p.4] |
| [[concepts/반복문\|loop]] → DFA | What changes on each input step, and what history must be retained? | [M01 p.12] [M01 p.17] |
| [[concepts/함수\|function]] → comments | Can a reader identify the function’s purpose without tracing its implementation? | [M01 p.18] |
| [[concepts/함수\|function]] → declaration, definition, and linking | Is the calling interface known, and has its implementation been supplied? | [M01 p.22] [M01 p.25] |
| [[concepts/타입\|type]] → pointers and dereference | Which object does the pointer designate, and how may it be accessed? | [M02 slide 7] [M02 slide 8] |
| [[concepts/함수\|function]] → `scanf` | Which caller-owned object receives the converted input? | [M02 slide 15] |
| [[concepts/함수\|function]] → pointer returns and lifetime | Does the target survive the return? | [M02 slide 16] [M02 slide 17] |
| [[concepts/타입\|type]] → pointer arithmetic and `sizeof` | What is the movement unit, and what object or type is being measured? | [M02 slide 21] [M02 slide 25] |
| [[concepts/반복문\|loop]] → array traversal | What bounds or sentinel establish a valid stopping point? | [M02 slide 25] |

## Active Recall

For the directly connected exam topics, the questions proceed through **concept check → historical exam demand → original variant**. Historical requirements are summarized briefly; the answers below are independently reasoned explanations rather than reproduced answer blocks.

### 1. Why must the character-counting loop retain the `int` result?

Explain the issue without assuming that `EOF` equals `-1`. [M01 p.4]

<details><summary>Answer</summary>

The result must distinguish character values from the additional `EOF` outcome. Converting to a character type before checking can lose the required distinction. The actual numeric choice for `EOF` is secondary to preserving it separately from valid character results.

</details>

### 2. What does the ASCII arithmetic demonstrate?

Under ASCII, determine the character produced by `'a' + 3`, then explain why `isalpha(c)` is preferable when the intended question is alphabetic membership. [M01 p.8] [M01 p.10]

<details><summary>Answer</summary>

`'a' + 3` produces the code for `d`. This demonstrates that character representations participate in arithmetic. `isalpha(c)` expresses classification directly, avoiding a reader’s need to reconstruct a numeric range or offset.

</details>

### 3. Where does the word-counter increment belong?

For the original input `  red\tblue!`, determine the word count and explain why missing trailing whitespace causes no problem. [M01 p.11] [M01 p.12]

<details><summary>Answer</summary>

The result is two words. The first non-space character after the leading spaces causes one `OUT → IN` transition; the first character after the tab causes another. The exclamation mark remains inside the second word. Since the algorithm counts beginnings, it does not require a final whitespace character.

</details>

### 4. What different problems do named states, comments, and `default` solve?

Use the word counter to explain each contribution. [M01 p.16] [M01 p.17] [M01 p.18]

<details><summary>Answer</summary>

Named states expose the meaning of the state values. A function comment states the intended result without repeating the implementation. The guarded `default` exposes a state outside the algorithm’s intended state set. These improve understanding and diagnosis beyond merely producing a correct result on one input.

</details>

### 5. Linking: concept → exam demand → original variant

1. Why is a declaration insufficient to execute an external function?
2. What additional distinction does the historical linker question require? [EX:sp_2025_2_midterm_q05 p.12]
3. **Original variant:** an object file contains a required call to `adjust`, but no definition is supplied during linking. Does successful object-file creation establish that the executable can be built?

<details><summary>Answer</summary>

A declaration supplies interface information; execution requires an implementation. The historical question distinguishes symbol resolution from relocation. Today’s material explains why references must connect to definitions; detailed relocation mechanics remain later material.

The original variant can produce an object file yet fail during linking because the required definition of `adjust` remains unresolved. Assembly into machine code and completion of external connections are separate responsibilities.

</details>

### 6. How do these declarations differ?

Read `int *p`, `int **p`, `int (*p)[10]`, and `int *p[10]`. [M02 slide 7]

<details><summary>Answer</summary>

They declare, respectively, a pointer to integer, a pointer to an integer pointer, a pointer to an array of ten integers, and an array of ten integer pointers.

Parentheses change how the identifier combines with `*` and `[]`. Recognizing these declarations is within the lecture; fluency with extended multilevel assignment traces requires additional practice.

</details>

### 7. What has been initialized here?

Consider `int i; int *p = &i;`. Can `*p = 5` and a preceding read of `*p` be treated identically? [M02 slide 9] [M02 slide 10]

<details><summary>Answer</summary>

The pointer has been initialized to designate `i`, but `i` has not received an initial value. Writing `5` through `p` establishes a value in the integer object. Reading its indeterminate value first is a different operation and is not justified by the validity of the address.

</details>

### 8. `scanf`: concept → exam demand → original variant

1. What does `%d` require as its destination argument?
2. Why does the historical pointer-storage question need a distinction between C validity and observed machine behavior? [EX:sp_2024_2_midterm_q01 p.3]
3. **Original variant:** starting from `x=3`, `y=7`, perform `*q=*p`, then `q=p`, then successfully read `9` through `q`. What are the final integer values?

<details><summary>Answer</summary>

`%d` requires an appropriate pointer to an `int` destination. Passing the address of an `int *` object instead violates that requirement. A machine-level account of writes into allocated storage does not establish defined C behavior.

In the original variant, the first assignment makes `y=3`; the second redirects `q` to `x`; input then makes `x=9`. The final values are `x=9`, `y=3`.

</details>

### 9. What must survive a pointer-returning function?

Contrast returning an incoming pointer to a caller’s integer with returning the address of a by-value local integer parameter. [M02 slide 16] [M02 slide 17]

<details><summary>Answer</summary>

The referenced object must remain alive for subsequent access. Returning an incoming pointer can preserve access to a caller-owned integer that outlives the call. Returning the address of a local integer parameter does not preserve that parameter’s lifetime. Leftover bytes do not make the later access valid.

</details>

### 10. Trace the pointer movement without using byte addresses

Starting with `p=&a[2]`, execute `q=p+3`, then `p+=6`. Where do both pointers end? [M02 slide 21]

<details><summary>Answer</summary>

`q` points to `a[5]`, and `p` points to `a[8]`. Updating `p` does not update `q`; `q` already holds the previously computed pointer value. The trace assumes those positions lie within the same suitable array.

</details>

### 11. `sizeof`: concept → exam demand → original variant

1. Why can `sizeof(a)` differ from `sizeof(p)` when `p=a`?
2. What must be identified before answering the historical `sizeof` question? [EX:sp_2025_1_midterm_q01 p.2](exam_questions/sp_2025_1_midterm_q01)
3. **Original variant:** for `int a[6]; int *p=a;`, determine `sizeof(a)`, `sizeof(p)`, `sizeof(*p)`, and `sizeof("cat")`. Assume four-byte `int` and an eight-byte pointer.

<details><summary>Answer</summary>

The array expression does not undergo ordinary pointer conversion as the operand of `sizeof`, so its complete array type determines the size. The pointer expression measures a different object type.

Identify the operand’s type before applying size assumptions. For fixed types, `sizeof(*p)` measures the referenced type without reading its value.

The original variant gives 24, 8, 4, and 4 bytes. The string literal’s array includes three letters and the terminating null character.

</details>

### 12. Separate pointer formation, dereference, and incrementability

If `q` points to `a[4]` in a six-element array, compare forming `q+2`, reading `*(q+2)`, incrementing a separate pointer, and writing `a++`. [M02 slide 23] [M02 slide 25]

<details><summary>Answer</summary>

`q+2` forms the one-past pointer. It does not designate an accessible seventh element, so dereferencing it is invalid.

A separate pointer variable can be incremented subject to the array-bound rules. The array name is not a modifiable pointer variable, so `a++` is invalid regardless of the desired traversal.

</details>

## Next Review

The categories below describe correspondence between lecture coverage and question prerequisites. They do not assess personal mastery or predict the next examination.

| Stage | Review target | Scope and exam connection |
|---|---|---|
| Accessible now | `getchar`, `EOF`, ASCII, classification, and word-count transitions | Reconstruct the current lecture examples first. No direct exam is forced onto the elementary DFA exercise. |
| Accessible now | Identify what `sizeof` measures | The basic operand distinctions in 2025 spring midterm Q1(a). [EX:sp_2025_1_midterm_q01 p.2](exam_questions/sp_2025_1_midterm_q01) |
| Partially connected | Diagnose a wrong `scanf` destination | The type and destination reasoning in 2024 fall midterm Q1(c) is accessible; its machine-specific output treatment remains separate. [EX:sp_2024_2_midterm_q01 p.3] |
| Partially connected | Explain why external implementations must be connected | The motivation for symbol resolution in 2025 fall midterm Q5(a); detailed relocation remains later work. [EX:sp_2025_2_midterm_q05 p.12] |
| Partially connected | Distinguish pointer movement from array-element updates | Basic operations connect to 2025 fall midterm Q1(b), but the complete multilevel-pointer trace needs further practice. [EX:sp_2025_2_midterm_q01 p.3](exam_questions/sp_2025_2_midterm_q01) |
| After additional prerequisites | Structure layout, padding, and multilevel assignments | Revisit 2024 fall Q1(a–b) and 2025 spring Q1(b) after studying their additional layout and pointer requirements. [EX:sp_2024_2_midterm_q01 p.2] [EX:sp_2025_1_midterm_q01 p.2](exam_questions/sp_2025_1_midterm_q01) |
| After additional prerequisites | Detailed memory-section and protection analysis | Revisit 2025 spring Q1(c), retaining the distinction between predicted faults and undefined behavior. [EX:sp_2025_1_midterm_q01 p.3](exam_questions/sp_2025_1_midterm_q01) |
| After additional prerequisites | Stack-direction experiments and allocation trade-offs | The recursive address comparison and `alloca` discussion in 2025 fall Q1(c–d) require more than today’s lifetime introduction. [EX:sp_2025_2_midterm_q01 p.3](exam_questions/sp_2025_2_midterm_q01) [EX:sp_2025_2_midterm_q01 p.4](exam_questions/sp_2025_2_midterm_q01) |

For the next review, redraw three structures without looking: the word-counter transition table, the build pipeline, and a pointer diagram before and after assignment. Then change a boundary condition rather than merely changing numbers: remove trailing whitespace, make two pointers share a target, let a local object expire, or place a pointer at the array’s end.

M02 slides 26–40 remain outside the taught endpoint. Array-parameter extensions, extended double-pointer traces, and later declaration exercises are future study. Unix I/O, allocator internals, detailed memory sections, PLT/GOT, and machine relocation also require additional prerequisites. Seeing a string-recognition DFA does not establish coverage of recursive wildcard-matching implementation.

## Sources and Verification

**Public source asset names**

- **[M01] `01.CProgrammingExamples.pptx`** — 27 slides covering character processing, ASCII, DFA-based word counting, readable code, defensive programming, and the build pipeline.
- **[M02] `02.CPointers_24a7628c.pptx`** — 40 slides in the complete deck; this lecture covers **slides 1–25**.

Page references identify the corresponding slide pages in the PDF renders. The discussed ASCII table, DFA diagrams, build stages, pointer diagrams, and code slides were visually checked against their extracts. Existing rendering overlaps were treated as source-layout limitations; the original decks were not changed.

**Transcript and audit status**

The complete normalized transcript was reviewed: **137 segments with 161 remaining unclear markers**. Coverage includes repeated conceptual questions, the compiler demonstration, participation reminders, the virtual-memory motivation, and the stopping point.

`audit_notice.md` records suspected omissions and review status. It is not an independent source for technical content. Closed review tasks, remaining uncertain speech, and explanations supported by slides are distinct forms of evidence. No missing response or board expression was reconstructed from an exam.

**Exam relevance review:** All 27 course candidates were considered through the guide and index before selecting relevant subquestions. The main connections are `scanf` destination objects, `sizeof` operands, and the need to connect external function implementations. Multilevel-pointer questions are partial connections; structure layout, allocation details, memory sections, recursive matching, Unix I/O, PLT/GOT, and relocation calculations need further prerequisites. Character processing, elementary DFA tracing, comments, and defensive programming remain fully represented despite the absence of a sufficiently direct selected exam exercise.

| Historical source | Use in this note | Verification and caution |
|---|---|---|
| **2024 fall midterm, Q1** | Q1(c): destination/type analysis; Q1(a–b): prerequisite-aware future review | PDF pages 2–3 checked. The document assumes a specific 64-bit Intel environment; provided observations are not portable C guarantees. [EX:sp_2024_2_midterm_q01 p.2] [EX:sp_2024_2_midterm_q01 p.3] |
| **2025 spring midterm, Q1** | Q1(a): `sizeof`; Q1(b–c): later layout and memory-protection work | Relevant PDF pages 2–3 checked. Provided sizes and crash predictions require qualification. [EX:sp_2025_1_midterm_q01 p.2](exam_questions/sp_2025_1_midterm_q01) [EX:sp_2025_1_midterm_q01 p.3](exam_questions/sp_2025_1_midterm_q01) |
| **2025 fall midterm, Q1** | Q1(b): partial pointer-trace connection; Q1(c–d): future prerequisites | PDF pages 2–4 inspected. No complete multilevel trace or supplied answer block is reproduced. [EX:sp_2025_2_midterm_q01 p.3](exam_questions/sp_2025_2_midterm_q01) [EX:sp_2025_2_midterm_q01 p.4](exam_questions/sp_2025_2_midterm_q01) |
| **2025 fall midterm, Q5** | Q5(a): conceptual connection to linker responsibilities | PDF page 12 checked. Detailed relocation and later PLT/GOT tasks remain outside current coverage. [EX:sp_2025_2_midterm_q05 p.12] |

Exam page numbers are **PDF 1-based, including the cover**. The selected PDFs match their indexed hashes; relevant text extracts and rendered page layouts were checked. Their semester labels agree with the covers. The index carries no separate recalled-exam or uncertain-semester flag for these selected sources. Instructor attribution is not independently verified beyond filename metadata, and the official status and overall correctness of provided answers remain unverified.

The four connected question IDs belong to **three distinct exam groups**, counted through `exam_group_id`. Masked variants are not additional examinations. These historical documents establish practice demands, not the current instructor’s emphasis or future examination probability.

**Example verification:** Character totals, word transitions, pointer destinations, array indices, and assumed sizes were independently checked with arithmetic and state models. **No native C compilation or execution was performed.** Undefined behavior was not executed to validate a predicted result. Original variants are identified separately from lecture examples and historical exam requirements. Exam PDFs, supplied answer blocks, private source links, and personal identifiers are excluded from this note.
