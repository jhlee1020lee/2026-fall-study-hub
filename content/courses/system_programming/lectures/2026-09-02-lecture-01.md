---
title: "2026-09-02 · System Programming 1강"
course: system_programming
date: 2026-09-02
lecture_no: 1
tags:
  - system_programming
  - lecture
  - detailed
concepts:
  - "library function"
  - "system call"
  - "pointer"
  - "array"
  - "string literal"
review_status: approved
draft: false
source_assets:
  - "00.Introduction.pptx"
---

> 교수 자료: [[courses/system_programming/materials|System Programming 강의자료]]

## 수업 직후 10분 복습

이 과목은 operating system(운영체제) 내부를 직접 구현하는 수업이라기보다, OS가 application(응용 프로그램)에 제공하는 API(응용 프로그래밍 인터페이스)와 system call(시스템 호출)을 이용해 system-level software를 작성하는 방법을 배우는 수업이다. Operating Systems 과목이 kernel(커널) 내부에 초점을 둔다면 System Programming은 이미 존재하는 OS와 program이 만나는 외부 경계를 다룬다. [STT 05:21-06:06] [M04 slide 3]

수업 언어로 C를 선택한 이유는 C가 assembly language(어셈블리어)에 가깝고 pointer(포인터)를 통해 memory(메모리)를 byte(바이트) 단위로 다룰 수 있으며, OS가 제공하는 low-level API와 직접 연결되기 때문이다. Python이 쓸모없다는 뜻이 아니라, abstraction(추상화) 아래쪽을 관찰하기에는 C가 더 적합하다는 의미다. [STT 40:28-43:28] [M04 slide 28-31]

첫 시간의 가장 중요한 기술적 구분은 library function(라이브러리 함수)과 system call이다. `printf()`는 library function이고, format string(서식 문자열)을 해석해 출력 문자열을 만든 뒤 최종적으로 `write()` system call을 호출한다. 실제 device에 접근할 권한은 OS에 있으므로 user-level code는 system call을 통해 OS의 서비스를 요청한다. 반면 `strlen()`이나 수학 함수처럼 OS 서비스 없이 user level에서 계산만으로 끝나는 library function도 있다. [STT 01:08:31-01:12:09] [M04 slide 49]

C의 array(배열)와 pointer는 비슷하게 사용할 수 있는 순간이 있지만 같은 type(자료형)은 아니다. `double pi[5][2]`의 `sizeof(pi)`는 전체 array의 크기인 `5 × 2 × 8 = 80` bytes다. `void *`는 객체의 주소를 구체적인 pointee type(가리키는 대상의 자료형) 없이 보관한다. 값을 읽으려면 실제 객체에 맞는 pointer type으로 변환해야 한다. C에서는 적절한 pointer 변수에 대입하는 암시적 변환도 가능하므로 cast(명시적 형 변환)가 항상 필요한 것은 아니다. [STT 01:20:27-01:27:24] [M04 slide 52-53]

String(문자열)에서도 이 차이가 중요하다. `char *s = "hello world";`에서 `s`가 가리키는 string literal(문자열 리터럴)을 수정하면 C에서 undefined behavior(정의되지 않은 동작)가 발생한다. 일반적으로 read-only 영역에 놓여 program이 crash할 수 있지만, crash가 보장되는 것은 아니다. `char s[20] = "SNU CSE00800";`은 writable array(수정 가능한 배열)를 만들고 문자들을 복사해 초기화하므로 array element를 변경할 수 있다. [STT 01:29:10-01:35:40] [M04 slide 54]

## 이전 강의와의 연결

첫 강의이므로 직접 연결할 이전 회차는 없다. 대신 programming에서 사용해 온 function call(함수 호출)·variable(변수)·array가 system level에서는 어떻게 해석되는지 연결해 두면 된다. 특히 `printf()`를 하나의 편리한 명령으로만 보지 않고 `library → system call → kernel → device`로 분해하는 것이 이번 과목의 출발점이다. 이후 pointer, file descriptor(파일 디스크립터), process(프로세스)를 배울 때도 같은 방식으로 abstraction 아래의 OS interface를 추적한다.

## 강의 흐름

| 시간 | 내용 | 이번 회차에서 남겨야 할 것 |
|---|---|---|
| 02:34-14:54 | 과목 목표, 담당자, ETL, 교재 | OS API와 system-level programming이 과목의 중심임을 이해한다. |
| 15:40-24:01 | 참고문헌, manual, 개발환경, 평가 | local 환경에서 coding하고 lab machine에서 최종 test하는 원칙을 기억한다. |
| 24:55-39:28 | 출석·참여·부정행위·AI 정책 | assignment code 작성·debugging에 생성형 AI를 사용하지 않는다는 과목 규칙을 구분한다. |
| 39:28-46:09 | C와 Linux를 쓰는 이유, 전체 일정 | C는 low-level access를, Linux는 open-source Unix 환경을 제공한다. |
| 47:03-54:41 | 현대 computer, cloud, datacenter | device는 작아지고 강력해지는 동시에 server 측에서는 cloud 규모로 커지고 있다. |
| 54:41-01:06:05 | C의 역사와 설계 목표 | C는 Unix 개발을 위해 assembly를 대체할 수 있는 portable systems language로 발전했다. |
| 01:06:05-01:13:18 | `hello.c`, `gcc800`, library/system call | compile·execute 흐름과 `printf()`가 OS까지 이어지는 경계를 설명할 수 있어야 한다. |
| 01:13:18-01:30:00 | variable, type, constant(상수), array, pointer | type이 memory의 크기와 해석 방법을 결정함을 이해한다. |
| 01:30:59-01:43:53 | string, structure(구조체), expression(식), statement(문), control flow(제어 흐름) | pointer/string literal과 array의 차이, expression과 statement의 차이를 확인한다. |

## 상세 해설

### 1. Application에서 hardware까지

```text
application code
   ↓ library call: printf()
C runtime library
   ↓ system call: write()
operating system kernel
   ↓ privileged device access
display / file / terminal
```

`printf()` 자체가 화면 hardware를 직접 바꾸는 것은 아니다. library가 format을 처리하고 OS가 device 접근을 수행한다. `stdout`은 기본적으로 terminal에 연결되지만 redirection(재지정)을 사용하면 file로 연결될 수도 있다. 따라서 같은 `printf()` 호출도 standard output(표준 출력)의 연결 상태에 따라 다른 대상으로 출력된다. [STT 01:09:20-01:11:08]

### 2. `gcc800`이 강제하는 compiler(컴파일러) discipline

강의의 `gcc800`은 별도의 compiler가 아니라 다음과 같은 option을 묶은 macro다. [M04 slide 49]

```bash
gcc -Wall -Werror -pedantic -std=c99 "$@"
```

- `-Wall`: 흔한 warning을 활성화한다.
- `-Werror`: warning을 error로 취급해 build를 멈춘다.
- `-pedantic`: GNU extension에 기대지 않고 표준 C 규칙을 엄격하게 검사한다.
- `-std=c99`: C99 표준을 기준으로 compile한다.

핵심은 “warning이 있어도 실행되면 된다”가 아니라, compiler가 제공하는 정보를 이용해 문제를 일찍 제거하는 습관을 만드는 것이다. local computer에서 동작하더라도 lab machine에서 compile·실행되지 않으면 채점할 수 없으므로 최종 test 환경을 동일하게 맞춰야 한다. [STT 20:32] [STT 01:12:29-01:13:18]

### 3. Type은 memory를 읽는 계약이다

이 강의는 x86-64, 64-bit OS를 가정한다. [M04 slide 51]

| 선언 | 의미 | 강의 가정에서의 크기 |
|---|---|---:|
| `char c` | character 또는 1-byte integer | 1 byte |
| `int x` | integer | 4 bytes |
| `long x` | long integer | 8 bytes |
| `float x` | single-precision floating point | 4 bytes |
| `double x` | double-precision floating point | 8 bytes |
| `void *p` | type이 아직 정해지지 않은 address(주소) | 8 bytes |

pointer가 담는 것은 대상 전체가 아니라 address다. `int *p`를 dereference(역참조)하면 그 address부터 `int` 크기만큼을 integer로 해석한다. `void *k`는 address만 보존할 뿐 해석할 type이 없으므로 `*k`는 유효한 값 표현이 되지 않는다. [STT 01:16:42-01:27:24]

보충(C 언어 규칙): `k`가 실제 `int` 객체를 가리킨다면 `int *p = k;`처럼 대입할 때 암시적으로 변환할 수 있고, `(int *)k`처럼 명시적으로 cast할 수도 있다. 중요한 것은 변환 후 type이 실제 객체에 맞는지다. [GNU C Reference: Void Pointers](https://www.gnu.org/software/c-intro-and-ref/manual/html_node/Void-Pointers.html)

### 4. Array와 pointer를 같다고 외우면 생기는 오류

```c
double pi[5][2];
int *p;
void *k;
```

`pi`가 expression에서 첫 element의 address처럼 변환되는 경우가 있어도, 선언된 type은 pointer가 아니라 `double[5][2]`다. 이 때문에 `sizeof(pi)`는 pointer 크기 8이 아니라 array 전체 크기 80을 반환한다. 반면 `sizeof(p)`와 `sizeof(k)`는 pointer 자체의 크기인 8이다. [STT 01:21:23-01:25:40]

### 5. String literal과 writable array

```c
char *a = "hello world";
char b[20] = "SNU CSE00800";

// a[0] = 'H';  // compile될 수 있지만 실행 중 실패할 수 있음
b[0] = 's';     // writable array element 변경
```

첫 선언은 `a`에 string literal의 시작 address를 넣는다. 둘째 선언은 20 bytes의 array를 확보하고 initializer의 문자를 array에 복사한다. “둘 다 문자열처럼 출력된다”는 사용상의 유사성이 “둘의 storage와 type이 같다”는 뜻은 아니다. [STT 01:32:05-01:35:40]

### 6. Expression과 statement

대부분의 expression은 평가되어 value를 만들지만, `void` 반환 함수 호출처럼 값을 만들지 않는 expression도 있다. `2`, `2 + 3`, 값을 반환하는 function call뿐 아니라 assignment(대입)도 expression이다. `i = 10`은 memory를 바꾸는 side effect(부수 효과)를 수행하면서 동시에 value 10을 만든다. 그래서 assignment operator의 right associativity(우결합성)에 따라 다음 문장이 성립한다. [STT 01:37:21-01:40:08] [STT 01:42:07]

```c
i = j = 0;
```

먼저 `j = 0`이 평가되어 value 0을 만들고, 그 value가 `i`에 대입된다. statement는 실행 순서에 놓이는 command이며 보통 semicolon으로 끝난다.

## 수업 운영에서 놓치면 안 되는 사항

- 중간고사 30%, 기말고사 30%, programming assignment 25%, assignment quiz 12%, participation 3%다. [M04 slide 16]
- 첫 assignment `Decommenter`의 예정 마감은 9월 18일이며 일정은 변경될 수 있다. [M04 slide 17]
- assignment code 작성과 debugging에 생성형 AI를 사용하는 것은 금지된다. 개념 이해나 library/system call 학습에는 사용할 수 있다. [M04 slide 25-26]
- late submission은 갑작스러운 입원 같은 예외적 상황 외에는 허용하지 않는다고 안내했다. [M04 slide 18]
- local environment는 coding에, lab environment는 testing·debugging에 사용하는 것이 권장된다. [M04 slide 15]

## 오개념과 STT 주의점

- `printf()`를 system call이라고 바로 부르면 library가 수행하는 format 처리 단계를 놓친다.
- array가 expression에서 address처럼 사용된다고 해서 array의 type 자체가 pointer로 바뀌는 것은 아니다.
- `void *`는 “아무 값이나 저장하는 변수”가 아니라 아직 pointee type이 정해지지 않은 address를 담는다.
- string literal 수정은 undefined behavior이며 compiler가 항상 막아 주는 오류가 아니다. runtime protection에 따른 crash는 가능한 결과 중 하나다.
- `-Werror`는 warning을 없애 주는 option이 아니라 warning이 남은 build를 실패하게 한다.
- 02:34 이전 오디오의 반복 “Thank you”는 독립 재전사 환각으로 판정되어 강의 내용으로 사용하지 않았다.
- 학생 질문 등 7개 불명확 구간은 핵심 개념의 근거로 사용하지 않았다.

## 능동회상 문제

1. System Programming과 Operating Systems 과목의 초점 차이는 무엇인가?
<details><summary>정답</summary>전자는 OS가 application에 제공하는 API와 system call을 이용하는 외부 경계에, 후자는 kernel 내부 구조와 구현에 더 초점을 둔다.</details>

2. `printf()`와 `write()`는 각각 어느 층에 있는가?
<details><summary>정답</summary>`printf()`는 C library function이고 `write()`는 OS가 제공하는 system call이다.</details>

3. `double pi[5][2]`에서 `sizeof(pi)`가 80인 이유는 무엇인가?
<details><summary>정답</summary>`pi`의 선언 type이 pointer가 아니라 double 10개로 이루어진 array이며 double 하나가 8 bytes이기 때문이다.</details>

4. `void *k`를 곧바로 dereference할 수 없는 이유는 무엇인가?
<details><summary>정답</summary>address에서 몇 bytes를 어떤 type으로 해석할지 정해지지 않았기 때문이다.</details>

5. `char *s = "hello"`와 `char s[20] = "hello"`의 수정 가능성 차이를 설명하라.
<details><summary>정답</summary>전자는 string literal을 가리키므로 문자를 수정하면 undefined behavior다. 후자는 writable character array를 초기화하므로 array element를 수정할 수 있다.</details>

6. `i = j = 0`은 어떤 순서로 평가되는가?
<details><summary>정답</summary>assignment가 right-associative이므로 `j = 0`이 먼저 평가되고 그 결과 0이 `i`에 대입된다.</details>

7. `gcc800`에서 `-Wall`과 `-Werror`는 각각 어떤 역할을 하는가?
<details><summary>정답</summary>`-Wall`은 흔한 문제에 대한 warning을 활성화하고, `-Werror`는 남아 있는 warning을 error로 취급해 build를 실패시킨다. 따라서 실행 파일이 만들어졌다는 사실만으로 코드를 판단하지 않고 compiler 진단을 먼저 해결하게 한다. [M04 slide 49]</details>

8. `printf()`의 출력을 file로 redirect했을 때에도 library와 OS의 역할 구분은 유지되는가?
<details><summary>정답</summary>유지된다. C library는 format을 처리하고, OS는 system call을 통해 실제 출력을 수행한다. 바뀌는 것은 standard output이 연결된 대상으로, terminal 대신 file에 연결되면 같은 `printf()` 호출의 결과가 file로 전달된다. [STT 01:09:20-01:11:08]</details>

## 다음 복습

- 오늘: `application → printf → write → kernel → device` 흐름을 보지 않고 그린다.
- 1일 뒤: array/pointer/string literal의 차이를 작은 C program 세 개로 확인한다.
- 3일 뒤: `gcc800`의 네 option을 설명하고 warning 하나를 일부러 만들어 본다.
- 다음 강의 전: pointer 연산과 `sizeof` 결과를 먼저 예측한 뒤 compiler 출력과 비교한다.

## 출처와 검증 상태

- [STT] 2026-09-02 최종 보정본: 138개 timestamp·화자 block 보존, inline `[[unclear]]` 7개.
- [M04] `00.Introduction.pptx`: 과목 운영, C/Linux 선택 이유, C 개관과 code 표기를 slide 1-64에서 대조했다.
- 누락 밀도 후보 2개는 `large-v3`로 확인했으며 복원할 별도 발화는 없었다.
- 검증 상태: 기술 개념과 운영 정보는 공개 학습용으로 사용할 수 있다.
