---
title: "2026-09-17 · Computer Programming · 보정 STT"
course: computer_programming
date: 2026-09-17
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
date_confidence: user_confirmed
---

사용자가 확인한 9월 17일 수업의 녹음입니다. 녹음 파일 자체의 날짜 메타데이터를 독립 확인한 것은 아닙니다.

명확한 전문용어 오류를 보정한 공개 STT 사본입니다. 불명확한 말은 원문 표현 또는 `[[unclear: …]]`로 남겼으며, 전체 음성의 완전 복원이나 누락 없음이 확인된 자료는 아닙니다.

개인 이름·연락처 등은 비식별 표시로 가렸습니다. 시간·화자 순서는 유지했습니다. 교수자·수강생의 예시, 의견과 추측은 검증된 사실이나 현재 공식 규정과 구분해 읽으세요.

보정 원본 SHA-256: `b9d19f4f6e7521e78a385e95862d0c54e0986df0347321889d57180d08b855ad`

````text
00:00 화자 1
Session. We have two components as mentioned before. The first part will be about the The first part will be about learning about object -oriented programming, which we actually learned about in the lectures before. And we will review the key OOP ideas listed here, which are objects and classes, constructors, and memory states. In the second part, we will use the concepts in object oriented programming to create a fighting game simulation and I will explain this in the second part of the lab session. So basically object oriented programming is looking at a software system as a collection of different objects.

00:55 화자 1
There are some key features of object oriented programming which are encapsulation and inheritance and polymorphism. You will learn this more as you go on through the lectures. But to give you a brief understanding about the features of OOP, they are that for encapsulation, it usually means that An OOP program can limit the visibility of individual components of an OOP program so it can be used to protect data of an object from the outside and this also makes it easier to abstract the programs. inheritance in object -oriented programming. Classes can be programmed to inherit properties of other classes and this actually makes it very easy to code these codes from other parent codes.

01:51 화자 1
And finally polymorphism is about how different classes can have similar behaviors, but they similar behaviors that actually behave very differently. And moving on, so those were the key features of object -oriented programming. But the basics of object -oriented programming actually focus more on objects and classes. So basically, an object is, as we know, a representation of an entity. And classes are the blueprints for which we can use to create multiple objects. So classes use attributes or variables in methods or functions to depict objects.

02:44 화자 1
And using these classes you can create objects similar properties and these newly created objects will all be different instances but they will all have common attributes and methods. So basically Basically classes consist of members which are attributes and methods. Attributes are more or less the properties of an object that works like me or properties of the class like an appearance or state. Methods are how the task or the object changes its attributes or performs actions. And some important keywords for object -oriented programming in objects and classes are instances and instantiate.

03:40 화자 1
So an instance is an object that we create using classes, and instantiate is the app that's creating an instance for the class. And the given code gives us an example of an instantiation of the Car class. And then we have a different type of method or a function that can be used for object -oriented programming, which is called constructors. So these are special class methods for initializing objects. They are usually called once when an instance is instantiated, and they have the same name as the class that's being constructed. Unlike other methods, constructors don't have a return type and cannot be called explicitly from the outside.

04:37 화자 1
Some other features of constructors are that they can be given multiple parameters to initialize different attributes of the class. There can actually be multiple different constructors within the class. And this was also from the lectures and I thought it was important so I kept it in. So it's about this keyword and it's a special keyword to indicate the self -confidence or the So it can be used to indicate attributes of the class, or indicate the methods of the class, or to indicate the constructor of a class inside of the class itself. So this keyword can be used to manipulate various aspects, attributes and methods inside the class itself.

05:41 화자 1
Moving on, we will talk about static members. Basically, a static variable is shared by all of the objects of the same class. In the given example code on the left, we have a class definition. Inside the class, we define a static variable. variable, the static string by the name owner and the constructor inside the code actually calls, uses like the input to the constructor to modify the static owner variable and then we use the code basically uses the owner variable of the Car class to entrance the owner variable of the Car class.

06:33 화자 1
So if we program the main method as on the right side of the code, on the code on the right side, we basically if we basically make two different instances with different names and then change the name of one instance to in this case Carol, the name Carol, and then we print the owner variable of the second instance, we will actually get the name of the first instance. And then if we change the owner variable of the class to the main David, and we print any of the owner variables in the in any of the instances with classes, we will actually get the new name, the new name, David.

07:35 화자 1
So that was about static variables in a class. Next we will talk about static methods in a class. A very important feature of static methods is that static methods cannot directly access non -static variables. In this code we have a non -static variable fuel and we use a static method called totalFuel and we use a non -static method called the non -static variable fuel and if we instantiate the code, the class, the program will actually print out that there's no. Here are some other things we need to think about when we're setting memory space.

08:32 화자 1
So basically two equal signs actually compares the addresses of two objects and one equal sign just assigns the address of one object to another. In the example code, if we make two different class, car classes and then print out the print out whether the two cars are the same. We're not using the double equality. We don't get false because the two objects will have different addresses. But if we assign the first car's address into the second car object, It will change the second car object's address to be the same as the first car object's. So if you put the same double equality, you will get the value true.

09:27 화자 1
And here is another important concept in Java. So it's called callByValue and it's a way to call a method by passing values. So... If we have a method that takes in a variable, say an integer as in the example, and we change that variable's value, since the function is actually only taking in the value of the variable, the local variable itself will not be changed, but the variable outside the function will not be changed, but the variable inside the function will be changed.

10:14 화자 1
So if we use, if we, as in the main code, if we make, variable by the name of number and then if you pass it on to a function inside the main method and then click the value number, you will see that the value of the integer, the variable number is actually the same as it is before we actually pass it through the function. And the other side of this concept is call by reference. And it's a way to call a function by passing a variable reference pointing to the variable called memory space instead of actually passing on the variable value itself.

11:07 화자 1
And so here we have a class with a variable or an attribute by the name of speed. And if we define like a if we define a function that takes the Car class as an input and then the Car class's speed attribute is changed the value of the the value of the speed variable inside the Car class can actually change.

11:52 화자 1
This actually happens because when we pass the Car class to the changeSpeed method, we're actually passing the memory reference points of the class. So the method actually changes the speed variable inside that memory space. And finally, another important feature in Java is that it's very dynamic and automatic at the same time. So some coding languages require you to allocate memory remove memory inside the code itself. But for Java, you do not have to allocate any memory for objects explicitly. This is done automatically.

12:50 화자 1
And Java actually has a garbage collector for reclaiming memory of unused objects. So this was the 93 for the option for each program. And now we will begin the third program class. So here you will be required to enter the three related classes. The player class, the Fight class and the main class. They will simulate a very simple fighting game. So if you go on to ETL, there is a section for the Lab 3 assignment, the Lab 3 session.

13:47 화자 1
So download the skeleton zip file and unzip the file and open the folder as an IntelliJ project. You will have to write the code inside the appropriate source file and then submit them as the lab assignment. So now I will briefly talk about the first problem in the... I will talk about the fighting game simulation problem now. For the fighting game simulation, the students will be required to create a program in which two players fight against each other. So during each round, each player will take turns attacking and /or healing, and after the final round, the program should determine the winner of the fight. And as mentioned before, there are three you will be required to implement the player class, the Fight class and the main class.

14:47 화자 1
So first we will play the player class for the fight simulation. So to give you a brief explanation of the player class, the player class manages the user ID and the health of the player inside the game. And this class will implement methods such as attack, heal, isAlive, and getTactic. So, as mentioned before, classes consist of attributes and methods, and the attributes for the third class should be as of the following. So, it should have the user ID attribute, the health attribute, and a random attribute. The user ID attribute should be a string, and it should contain the ID of the player.

15:41 화자 1
The health attribute should be and each player should have 50 health at the beginning and the health attribute should always be non -negative because if a player's health reaches zero, the player loses and the game terminates. And also implement a random attribute. to be used in various methods inside the Player Class. The second component, the second portion of the Player Class show consists of these five methods. the attack method, the getDamaged method, the heal method, the isAlive method, and the getTactic method. And if you need, you can create new methods for accessing different types of information within the each of the classes. So please keep that in mind.

16:37 화자 1
And the specifications for the methods are as in this slide. First, the attack method should decrease the opponent's health points by an integer, selected from 1 to 5. There's a hint, you can use it if you want to, but if you don't need to, you don't have to. Or the getDamaged method. I wasn't going to include this, but I just wanted to help you guys, my friends told me. So this is like the top -of -mention method that decreases the player's health points by a specified amount. And then we have the heal method, which increases player's health points by an integer selected from 1 through 3 at random.

17:39 화자 1
One thing to note is that the health points of each player should not exceed the initial value of the system. Then we have the isAlive method, which should return a Boolean, and it should return true if the health attribute is higher than zero and false otherwise. And finally, the getTactic method is a method in which a player decides whether or not to attack or heal. The player should decide to attack with a chance of 70 and to heal with a chance of 30%. If a player decides to attack or turn the character,
````
