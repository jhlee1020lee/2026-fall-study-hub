---
title: "2026-09-08 · Computer Programming · 보정 STT"
course: computer_programming
date: 2026-09-08
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
---

강의 녹음을 바탕으로 보정한 STT의 공개용 사본입니다. 음성인식 오류와 불명확한 발화가 남아 있을 수 있습니다. 수강생 이름·연락처와 일부 개인 자기소개를 가렸고, 타임스탬프·화자 순서는 보존했습니다.

원문 보정본 SHA-256: `35312b5901111a0924e182238989f49c7ed6344f9dce7978fb0b7384850fffe9`

`[[unclear]]` 등 표시는 원문에 남은 불확실성입니다. 코드·수식과 발화 표기를 그대로 읽을 수 있도록 본문을 텍스트로 제공합니다.

````text
00:00 화자 1
We'll go to the world of OOP, which is more interesting and the core concept that I really want to talk about during this course. And there's one thing to announce. So I guess you remember our introduction. So this course is about talking about OOP. And this course consists of two, like Java and C++. But, well, I'm just—recently thinking about how we can prepare this course better. I mean, the remaining part, like the second half of this course is about C++, but that is mostly about re-teaching the OOP concept that we learned for Java in a different language. So I personally think that it's not that interesting to learn because that is almost the same thing, but just in a different language. So I'm just thinking about what would be more, [[unclear: as I]] fun to learn in the second half of this course instead of the C++.

00:58 화자 1
So for that, there might be a little bit of change of the topics, especially in the second half. So I'm just saying this to you before it's too late. But our first half, which is about OOP, Java, and how to write a great program with Java will remain the same. And again, we will have four homeworks, and each of these homeworks can be a little bit more difficult than before. And that is because recently we know how to use AI, and even though we don't use this AI directly to do this homework, which is not recommended, obviously, even though we do not do that, we have an AI which answers every question that we have. So AI is a great partner, so when you do your work, homework, instead of just copying and pasting the assignment instruction, you can ask anything.

01:50 화자 1
Like for instance, if I want to write this function, what would be the better way, what would be a good way, or what would be the optimal way of designing this whole framework like that? So that alone increases your performance a lot. I think at least by two times or three times or even five times. So that is the reason why we need to make the assignments more complicated and more real compared to the toy simple examples that we had for previous understanding. So that was a brief introduction and things that I wanted to talk about. So any questions on the things that we learned last time or about this whole course, about anything? Questions? No? Great, good, okay. So then I'm just gonna—[[unclear: I finished]] this first part of Java Basics.

02:41 화자 1
So we, I mean, if I just go through these things that we learned, first we learned what is Java and Hello World, how to compile, what is a Java compiler, the class and testing a class, compile and run, main method like printing, consoles, variables, data types, all these very basic things, right? Strings, immutable, [[unclear: random]]. So the last part of this slide is operator, which is also a very basic concept that everyone needs to know to write in Java. So what is an operator? Operator is a operator, like a plus, minus, divide, like multiply like that. So operators perform operations on variables and values. So this operand which are the inputs of this operator can be a variable that will contain some values or the values themselves.

03:35 화자 1
And the operation is defined, like what kind of operation that we want to perform is defined by the type of the operator. So here, obviously what we do is x plus 10 because there is a plus as an operator. And there are obviously various operator types. For instance, the arithmetic operators, assignment, comparison, logical, bitwise operators. So I think the definition of these operators would be the same in almost all programming languages they might see. Let's look at the example. x++, so what is the x++? That is an operator that assigns x + 1 to x itself. So that is an operator that works with only a single operand. So that is the reason why it is called a unary operator. So that alone changes the value of x by adding 1 to it. And the second line is about arithmetic operation, which is plus 1 plus 1, 1 plus 2 is obviously 3.

04:33 화자 1
The third line is a shift operator. So I'm not sure how many of you understand how these numbers are represented in computers. So could you raise your hand if you don't know what a shift operator means? Anyone? Anyone knows what it means? I mean, I don't have time to, like, introduce how this computer is represented, right? Obviously we all know that computers represent these two or three numbers as bits, like the—you remember the, like, big endian, like little endian, how to convert our integer or floating-point number into a bit, right? So that shift operator means we have two. And in its bit representation of this number 2, we want to shift these bits to the left by 3. So that is more like a low-level operation that is not seen by just a bit. You need to understand how these numbers are represented to understand the shift operators.

05:32 화자 1
Or there is a relational operator which means we check whether 2 is greater than 1 or not, which is true, right? And there is a bitwise operator, which is, we have two variables or values, and in terms of their bit representations, we want to do this AND operator bit by bit. So that is the reason why it is called a bitwise operation. And there is a logical operation which checks whether true, what is the return value of this AND, or true AND false. Which is false, because AND, it checks whether both sides of this condition are both true. The right-hand side is false, the result is true. Or the result is false, sorry. And like that. So we have a lot of operators, and there is a ternary operator as well. So we check if x is less than three or not. There is a question mark, right?

06:30 화자 1
If x is less than three, then the first statement is executed, which means we have a String which says x is less than three. Otherwise, we go to the other part of this colon and we get a String which says x is greater than 3. So this ternary operator is a simpler version of the if-else statement when the return type is the same between the if part and the else part. And then we have also an assignment operator, which is we assign the value, which is x plus two, to the x itself. So this assignment operator is similar to the [[unclear: inner operator]], but here we have two of them, x and 2. So these are just examples of diverse types of operators defined in Java. And if we look carefully at each category, then we can see a lot more diverse operators.

07:28 화자 1
For instance, if we look at arithmetic operators, there is addition, subtraction, multiplication, division, modulus, increment, decrement. So these increment and decrement operators are unary operators, again, which take only a single operand, but they are still arithmetic. So these are popular arithmetic operators that probably all of you already know. So let's look at the division operator, which is funny to look at. What happens though? You know that every variable in Java is typed. So when we, for instance, create an integer of 15, then it is an integer. So if we create a double y of 3.0, then this value is created as a double. So if we divide an integer by an integer, then the result that we get is also an integer. Because Java operator basically does not change the type of the input.

08:27 화자 1
So if we divide by 2, that makes sense, and the result would be 2, because the result should be an integer and we have no possible way to store the value of 0.5. But what happens if you try to divide 5 by 0? That will obviously create an error, because the division operator is not defined for the zero, obviously, right? How about this one? What if we divide integer of 15 by three? So, in this case, Java has an internal rule when—and the operands have different types. For instance, here, x is an integer and y is a double. So if the Java takes integer and double with the division operator, then it converts the value of x—sorry, the type of x into double and returns the double as a result.

09:18 화자 1
So we can [[unclear: say this toward]] result as a double instead of—So this is one, I think this is one, another example of, what was it, type casting. So this type casting is automatically done from the int to the double when we get this operator. How about this modulus operator? We have x of 5, we have y of 2, and we pick up the x modulus y, which means we want to get the remainder of the division of x by y, which is 1. So we get the result integer of 1. And we also—we also have the increment and decrement operators. For instance, we have two integers x1 and x2. And we do x1 plus y1, but we have a weird operator in front of y, which is an increment operator.

10:13 화자 1
So that means we want to add these values of x1 and y1, but before that, we want to increment the value of y1. So the result of the second line here is that the value of y1 changes from 1 to 2, because we do this increment operator and then we do the addition of x1 and y1 which gives us the result of 3. Which makes sense. But here the order of this increment is different. So this plus-plus sign is after y2 while the plus-plus sign is in front of y1. That means here we first add the values of x2 and y2, which gives us 2. And then we increment the value of y2 after that. So the result of these two blocks of code is almost the same. So y1 becomes 2, y2 becomes 2. But the printed value is different.

11:10 화자 1
Here the printed value is 2, because we do this addition before we increment the value of y2. So that's the difference that comes from whether we put this increment sign in front of the variable or at the end of the variable. Okay, thankfully the result is correct. So the first block gives us three while the second block gives us two. And there is another type of operator which is assignment operator. So this assignment, like let's say we want to give this value of 10 to a variable x. And this is also an operator. I mean, although it doesn't look like the arithmetic operations that we did before, but still this operator is also called an operator because it is an operator. So in this example below, it just says what I just said. We assign a value of 10 to the integer of x, right?

12:10 화자 1
And then we can do this thing as well. So we initialize this variable x as a value of 10, and then we can increase this value by 5, right? So this line is called the declaration and initialization at the same time. So we declare a variable x, which is an integer, but at the same time we initialize this value as 10. So when we declare something, it is a very good practice to initialize it as well. Because if we just declare it and we don't initialize this value, then we are not very sure what value that it will contain. It is basically determined by the JVM compiler. So it is, again, a good practice to initialize everything that we declare. And there are many assignment operators. So the same thing across all these operators is that everyone contains this equality sign.

13:08 화자 1
Because in many, many programming languages, we use this equality sign as an assignment. But the first one is just an assignment, right? It's just an equality sign. But after that, all these assignments contain another operator. For instance, the addition, minus, like multiply, division. So all these things mean a unary operator, which means we do this operation, and then we assign the result to x. So these assignment operators are basically a simpler and shorter versions of the existing operator. So we don't really need this to write a code. But if we have these things, then we can write probably a simpler and easier to read code. So how they work is basically the same. Plus, minus, greater.

14:06 화자 1
And, okay, so probably one thing to state is you need to keep in mind the order is fixed. For instance, if you do this x, for instance, shift equals three, that means we do this shift operator, for—which means if you write as a binary version, we do this x equals x shift three. It's not three shift x. So the order is fixed across all these assignment operators. The x comes first, and then the value comes [[unclear: next week]]. Then let's go a little deeper. So now we understand this arithmetic operators and assignment operators and how these operations are done. Let's think about the String. So I guess you remember the difference between the primitive types and non-primitive types. So if you go back, I guess we have a slide or not. So, okay, let's go back here.

15:04 화자 1
So you remember that when we create a String, it is different than a primitive type because a primitive type contains values. So if you, for instance, create a String, like for instance, if you create an integer x equals 3. That means x contains 3. But if you think of the String, which is a non-primitive type, then what each variable has is not the value itself. What each variable has is the memory address that contains the value. Why is that? Because the non-primitive type can be larger than a single byte or multiple bytes. A String can be really, really long. So we assign this String, we create this new String and store them in memory, and then we store their addresses in this variable. So this is a fundamental difference between primitive types and non-primitive types, right? You really keep in mind this difference.

15:58 화자 1
So let's think about this comparison based on the fact that non-primitive types work differently from primitive types. We create two Strings, apple, object, appleObjOne and appleObjTwo. And we create these two Strings with the keyword new, which means we create another memory space that contains these two Strings, Apple and Apple. So although these two look like the same Apple, but they are residing in a different place in memory because we create them with the new keyword, right? So if we compare these two Strings, appleObjOne equals appleObjTwo, the result is false. Why is that? Because these two variables are in fact different. Because these two variables contain different memory addresses, although their contents are the same.

16:54 화자 1
So if we just do this equality comparison operator between these two non-primitive type variables, we compare their addresses, not values. Is obviously false because, as I said, they refer to different memory addresses. But let's say we want to compare your content. Let's say you want to see whether these two Strings are actually the same or not. Then we need to call a function which actually compares the String contents inside the memory addresses. So what we call is, later we will learn about this in classes, like members and attributes and methods later, but what we actually call is, we have this apple which accesses a function defined for Strings, equals, which means the called function compares the contents of Strings.

17:48 화자 1
And then the result of this should be true because the content Strings are the Apple in both cases. So this is like a very slight difference. And if you don't know this, details or how these languages work, you might not understand what it means. But now you understand what that means, what this comparison represents for these non-primitive types. What if we do it a little bit differently? What if we create these two String variables with the constant Apple? Then the computer, like the JVM compiler, already knows that this String constant is the same here and there. And this compiler knows that this is constant, which means that String has no possibility to be changed once it is declared.

18:44 화자 1
So as the JVM compiler looks at these two lines, it uses the same memory address for creating these two variables. Because it's, again, because these are constant. And one thing to remember is that where these Apple Strings are created is different in these two slides. Here, we use the heap memory where the memory is dynamically adjusted and addressed by the compiler and that these Apple Strings can be created and removed during the execution program. But here, we create a constant. So the computer uses a different memory area to create this constant Apple. So there is a slight difference, but anyway, here because we do this thing with the constant, they all, these two refer to the same memory address. That is the reason why when you compare these two String variables as before, the result is true.

19:41 화자 1
Because, not because they are the same, just because they have the same memory addresses. Okay. Any questions? This is not difficult, right? So in that sense, we also have the comparison operator, which is to compare different things, different values or different variables, whether these are equal, these are not equal, these are greater than, less than, greater than or equal to, less than or equal to. So what is the reason why I showed the previous slide? I mean, this one is because although we simply say that that is an equality operator, what it really means can be really non-obvious, especially when we deal with these values and memory addresses at the same time. So that is the reason why we have that slide before to let you know how sensitive it is.

20:44 화자 1
And these are some common mistakes. Well, let's just keep it simple. And there is a logical operator. Logical operators are used to determine the logic between variables or values. So we check whether the logically AND, logically OR, or logically NOT when we create a conditional thing. So what if, let's go back here. So what if we want to check whether these two Strings are not equal. But the function that we have is an equals, right? So either we want to find the function called not equal, or we can put a sign here to make the result to be reversed, inverse version, considering the logical meaning.

21:36 화자 1
So this punctuation mark, this one, is used to make the reverse, make the result reverse, like from the false to the true, from the true to the false. So because of that, we don't really need this not equal operator for Strings as long as we have an equals operator. And honestly this is not that important, but we have the short-circuit evaluation which is a thing in Java. So what this means is we usually need these logical expressions to create a conditional. Like if something is true, we do something. If something is not true, we do something. So for creating this complicated conditional to run a block of code, that we need such a complicated, mixed expressions.

22:27 화자 1
So here, AND-AND means a logical AND, which is, let's say we have this block of code which means if this is true, we run something. If this is true, we run something. So that AND means we run this code if expression one is true, the expression two is true. That is the meaning of AND. And this is called OR. I guess you all know what it means. This means we will run a block of code either if expression 1 is true or expression 2 is true. So these two represent different conditions for running something. Then what is the meaning of this short-circuit evaluation? Which means result is always false when expression 1 is evaluated to be false. So what does that mean? What it means is if we create this conditional, let's say we look at the first one.

23:27 화자 1
If the expression 1 is false, then we will never execute a block of code whether the expression 2 is true or not. Why is that? Because the result of these two mixed expressions becomes true only when these two are both true. So if you look at the next slide, this is a table that represents how this AND works. So this AND expression returns true only when—I think it's, uh, you know, the thing is, yeah, so the result is there, sorry for that. So the result is true only when these two expressions are true. So once the first expression is false, the result can never be true, right?

24:17 화자 1
So because of that, what this short-circuit evaluation means is if the expression one is false, then we never execute the expression two because it's not relevant. The result cannot be run anymore. Based on the same thing, if the expression is OR, if the expression 1 is true, then the result is always true. Because this OR expression checks whether either one of these two expressions is true or not. So if the expression 1 is true, we never execute expression 2. But this is like an optimization technique. So let's look at this example code block to illustrate this thing better. So we have an integer denominator which is 0 and we have an integer num which is 100. So in that if condition we check two things.

25:11 화자 1
First whether this denominator is not equal to 0 or not and then we also check whether the num divided by denominator equals 1 or not. So what we know is if we divide this num by the denominator, then it will cause an error. Why is that? Because no number can be divided by 0. The truth is, this if conditional has another expression in front, and that checks whether the denominator is 0 or not. And obviously, denominator is 0, right? So the first condition, the first expression returns false. So because of that, and because of the conditions are connected by the AND, the compiler knows that this thing, whatever, that can never be executed if the first expression is false. So it never executes the second block, which will cause an error.

26:11 화자 1
So the result of this is it will not produce an error because the second expression does not execute. So that is a pretty unintuitive way of running from Java, which is a result of optimization because we already know that we don't have to run the second one. And here is another example. So let's say we have a complicated condition and let's say we have a complicated line of work. And let's say we don't want to run this whole block of code for debugging or for instance for testing something. Then instead of making this code change in an appropriate way, we can create a boolean variable which is true, skipLongCode. And if you put this boolean variable in front of this complicated condition and if you combine these two by OR, then this—

27:10 화자 1
Okay, so my previous expression was a little wrong, but if you do this thing, then this line of code, this block of code is always executed. What if we want to not execute this line, whatever the condition is, then we create this boolean as false, and we change that OR operator into the AND operator. Then this block of lines should never be executed. So this is how to control this [[unclear: ML conditions]] and to understand the short-circuit evaluation. Great. Great. It's not hard, okay? So we have a few remaining slides for talking about this operator. So let me just keep walking. Okay, so the order of this operator—I think I already talked about this, right?

28:06 화자 1
So we, if we do run this first block, then we have this addition operator between a String and on the integer. So if the Java has a rule that if it adds a String and a number, then it converts the number into a String. Probably you can think of that as another example of widening casting. Then 2 becomes a String, 3 also becomes a String, and the punctuation form is added to the String. So every 2 and 3 is added to the String, in fact the concatenate is into the String without doing actual arithmetic operations. While if we change the order, then we do this arithmetic addition between 2 and 3 first, and then we convert the 5 into the String when we add that value with the String. So here, it's an example of how Java changes the types of operands and how this order matters. Okay.

29:09 화자 1
So, order matters. This is another example on how this order matters. And this is, let me show you that, a really bad coding. Let's say we have these three variables, x and y and z, 1, 2, and 3. What if we write code like that? It is quite intuitive to understand what this means, right? But what it actually does is, we assign the value of z to y first. And then we get the result of this assignment, which is three. So we get this result of this assignment operator and we assign the value to x, which makes x also three. So here we have two [[unclear: red]] lessons. First thing is, if we have more than one assignment operators, and it's evaluated from the right to the left. So this rule is already implemented in Java compiler. So we have an order for every operator on how to run it, or this assignment—assignment operator, we go from the right to the left.

30:09 화자 1
The second one that we learned from this example is the result, so every assignment operator also has a result. And the result of this y equals z is the value itself, which is three. So that result is becoming the operand of this assignment operator again, which makes x equal to 3. So if you are really good at Java, and if you have taken this class very well, then you will understand how this works. Is very intuitive. So when we write a code, it is a bad practice when it is not obvious on how it works. So this is, although I show you this as an example, but this is a bad habit of coding. Let's look at this one. So we have this x plus y multiplied with three, and there is an increment operator.

31:04 화자 1
So one thing that we might wonder is what would be the order of these operators to be executed? Because there is an order. So if you look at the result, the result is 36. So we understand why the result is 36 because if you look at this operator, I think, so my memory might not be super correct, but as far as I understand, as far as I remember, this increment operator will be the most prioritized expression among the arithmetic operators. You need to double check. Okay? But as far as I understand, the x—the value of x is incremented first because that is the most prioritized operator. Then, after the value of x changes from 5 to 6, then the next operator to be run is y times 3 because we know that multiplication is more important than addition.

32:03 화자 1
We change the value of x from 5 to 6, and then we change the value of y. In fact, we do not change the value of y, but anyway, we get the value of 30, which is the result of 10 times 3, and then we add these two, which makes us the result of 36. So this is another example of not a good habit of coding. Because, as again, if you are a very expert on this operator, you might know about how it works. But for many not perfect developers, how it works is unclear. So that's not good. So here is, okay, here is actual operator precedence. Let me see where is the increment operator. There is no increment operator in this table, but anyway, this is precedence and the first one, like the top row is more important than the bottom row.

33:02 화자 1
From the most important to the least important. So it's not that you need to memorize this table. It's really not that. But at least for some important, like things, some important operators, you might naturally understand what is more prioritized than the other. You will just know if you code a lot of assignments. You don't have to memorize this table. So that's a summary. In this part, like the first part of Java Basics, I literally tried to explain the basics of Java, like the introduction to Java and the variables and data types, type casting, Strings and operators and precedence, all these things, which are mostly shared with other languages as well. These are not very specific to Java because almost all languages have their own versions of these operators and type casting, even Python and C.

34:00 화자 1
Some languages do not support automatic type casting, some support automatic type casting, so it differs, but the actual core idea is the same. So that's just the remainder of the first part. Any questions? Okay, just good? Good. Alright. Great. Okay, then let's move on to the next part. So, I mean this is a two-hour class and I'm honestly not sure, like, what speed would be optimal for me to have one. Like for instance, if I just keep talking about two hours, then this would be too exhausting, like to me as well, to you as well. But I cannot just, well, take five minutes off or anything. So, okay, I'm just gonna proceed.

34:56 화자 1
So this is the second part of Java Basics, and what I talk about is some fundamental data structures. So what I talked about in the previous slide is more about the fundamentals, like the data types, variables, and here I will talk about more data types and conditions and loops and functions, which are more related to code compared to the knowledge that we have. So, array. So in Java, you will see a lot of different data structures. And for instance, like String is also data structure, array is a data structure, and there will be a lot, and you will create more if you do these assignments later. So array is one of the most fundamental and primitive data types, data collections that we have. And we will see.

35:46 화자 1
So an array is a collection of objects or values with the same type, and an array is stored in contiguous memory space, which means if you want to create an array of size 100 or a size of 1,000, that we allocate this memory of size 100 or 1,000 and we store the memory there. So because of that, array is quite efficient and fast data structure compared to the others. These arrays allow to handle multiple values of a single variable and this array index starts at 0, then 1, and ends at 0—size minus one. So just let's look at the example. So here we create an array. We define a variable, we declare a variable, variableName, which is an arbitrary name. And here the type of this variable is the type array. So we, like for instance, integer array, float array, double array. So we put the type and this bracket.

36:46 화자 1
So we present that this is an array. And we create new. Why is new? Because array is not primitive type variable. We need to allocate a new memory and we need to make this variable refer to the memory address of this new memory. So that is the reason why we put new here because we allocate a new memory of all type, which can be an integer, character, like floating-point, [[unclear: both size]]. So when you create an array, we need to specify the size of this array in advance. Why is that? Because we need to allocate that much memory when we create an array. So we need to know the size of this array in advance. For example, we create an integer array, array, or arr, and new integer of size four.

37:38 화자 1
So what it does is it creates a memory space for four integers and then allocates the memory and makes a variable refer to that memory address, which is obvious. So the previous slide is about declaring an array without initializing its value. So if you create this array this way, we cannot guarantee what values would be inside this array. So that is not, again, very good practice because we want to be sure which variable contains which value at all points of the code. So a better way of doing this is to initialize this array as well. So one way to do it is we create this array variable, variableName, new type bracket, and we put the values inside, these actual values.

38:30 화자 1
Then, what it initializes is, it puts the object 1 into the first position, object 2 into the second position, etc. So it initializes the array from the start to the end by using these values. Or, if you look at this first one, it is obvious that we created an array with this value, right? So it is okay for us to omit this new type, this keyword at all. Because it is obvious from the values that what array we want to create. But bottom part is, what is not good about the second one is it does not specify the size or anything. So it is more like the implicit way of putting, while it is more explicit. But the second one is also okay. So let's look at an actual example. We create an integer array ages, of size not known.

39:30 화자 1
We didn't specify the size, but it's okay because we provide five values. So because of the five initialization values, this system will create an array of size five. But what if we create another variable, seatNums, where the length is 3 and we put 3 values as well? Then this compiler causes an error. So honestly, I don't think there's an error. But it is a rule, which is if we want to provide these initial values, then we—we shouldn't provide the length as well at the same time. We should be one. Either we specify the length or we specify the content. And I think the reason would be probably because if you put this length and value at the same time, and if the length is different from the number of values that we provide, that might be more buggy than this strict rule.

40:24 화자 1
So that might be a reason why this compiler does not allow us to put this length and the values at the same time. We should be the one. And some things can be omitted. For instance, if we create an integer array of these five values, then we don't have to specify this new integer because it's obvious that our array is an integer array of size [[unclear: nine]]. It's the same as a double, which is the same as a String. Okay, so here is a funny thing. So this integer array is an array of primitive types. What it means is, in this line, we create an array of size 5, and its element is the number itself, because integer is a primitive type. If you look at the second line, we create an array of size five, and every element is a number, a primitive number. So every element in this array will contain the number itself. But if you look at the last one, this is a String array.

41:21 화자 1
But we know that String is a non-primitive type. So every element in this array is not the String itself. That is the memory address that contains every String. So in fact, this is not an array of String. This is an array of memory addresses because the String is a non-primitive type. So the reason why I tell you is there are some details that are hidden from the code itself, but you really should understand how internally this works. That difference between primitive types and non-primitive types is really important to [[unclear: Alain]] and we'll talk about it later, probably for the next topic. So how can we access a value in an array? Because we want to access a value, otherwise why do we create an array? So we create this array of Strings, cars, and then if you put this index 0 in this variable, that means we get to the first element of the String array.

42:20 화자 1
So the cars of index 0 is a String because cars is a String array. So because of that, we can directly print the value of cars[0], which is Volvo, in this printing function, which is obvious, right? This is obvious. With the similar manner, we can change an element in this array. So the first block is the same as before. So we get the Volvo, which is the first element of this array of Strings. Then what if we want to change some elements? What if we get this Opel and assign it to the first element of cars? That works. So we can anytime change the value in array with the new one. So again, because the String is a non-primitive type, what we replace is not the String of Volvo itself. What we replace is the memory address that is referred to by the first element.

43:19 화자 1
So we probably create another memory space as a constant String Pool. We'll create a String Opel, and we get the memory address, we will make this cars[0] to refer to that memory address. That is how exactly this works. What if we want to get the length? So if you know C, C code, I'm not sure how many of you understand the C programming, but you know that in C, we don't have a way to get the length of an array. But Java has one. So in Java, in fact, array is kind of the class itself. So array is also a type, not just a wrapper. Every array has a member attribute called length. Here the words that I use like members, classes, attributes are not taught yet. They will be discussed in detail in the following lecture 4.

44:17 화자 1
But let me just quickly tell you the idea. This String array has a member variable which is inherently defined inside of this memory address. We know that there is a variable length for every array, no matter what type it is. So if we call this ages.length, which means—that means we can instantly get the length of the array and we can use it. So if you put this ages.length, that means we get 3. Because even though it does not contain any contents, its length is 3, obviously. If we get the length of cars, which is four, then it's returned. So that is at least better than in C programming. In C programming, we always need to create another local variable that tracks the length of every array. If we lose track of that integer, then we have no way to understand the length of array.

45:17 화자 1
But here, we have a better way of doing this. Well, that is not necessarily a better, but at least it's easier for us to program. And there are many cases where we need to loop through a array. So let's say we have the same array of Strings, Volvo, BMW, Ford. Then we here in the for loop, we create a local integer variable i, which is initialized with 0. And as long as this variable i is less than the length of cars, we do this. We run this code. And then after running this block, we increment the value of i. So by the condition of the for loop, print out from the first element of this array, which is Volvo, to the last one, because i starts from zero to the cars.length minus one.

46:16 화자 1
So it prints all the elements in the array at this time. So that is a very typical way of enumerating all the elements in an array. Let's go deeper. We can also create a multi-dimensional array. So here, we create a variable ticTacToe, but it's not an array. It is an array of an array of characters. It is a two-dimensional array where every element of this ticTacToe is a character array. Okay? So inside every element which is an array, there is a character. So we can increase the dimensionality of an array from two to three, four, five, as much as we want. So this might look confusing, but it's not confusing at all, because it is the same as a String array.

47:10 화자 1
So if you think about the String array, we have an array where every element is a String, which is a memory address, which refers to another address. So here, what it means is, this is basically an array of non-primitive types, where every element is just a character array. So in that element, inside that element, it just contains multiple characters. So this is how we initialize a multi-dimensional array, and we can also access a value in a 2D array, as we do. For instance, we create a two-dimensional array of myNumbers, integer, bracket, bracket, 1, 2, 3, 4, 5. So here what we see is this is not a formal matrix because the first row has two elements and the second row has three elements. So it is not a matrix that you might think. This is more like a, like, array of arrays. So the thing is Java allows us to do that.

48:09 화자 1
In Java, every integer array inside this multi-dimensional array can have a different length. So if you create this variable in this way, how can we access every variable? So if we do myNumbers[1][2], that means myNumbers[1] means we get to this multi-dimensional array and we get the second element first. Which is, so we have this integer array and the second element is this one, 3, 4, 5. So this myNumbers[1] returns an integer array which contains three values, 3, 4, and 5. So if you go to the index 2, which means the third element, then we have 5. So to access an element in a multidimensional array is basically the same as a one-dimensional array. We just go to one block at a time from the row to the column like that.

49:08 화자 1
Not hard, right? How can you change the value to the array? Same. We create this 2D [[unclear: maker]] array, and we change the value, myNumbers[0][1]. What if the 0, 1, the first element, the second element? If you look at this initialization line, the first element and the second element, two. So there is a zero. So we change that two into zero. So if you print it again, that it will represent zero. How can we loop through a 2D array? We create this 2D array myNumbers, 1, 2, 3, 4, 5. Then we need two for loops. Why is that? Because we have 2D array. In the first loop, we create this local variable i, which iterates from 0 to myNumbers.length and there is an increment.

50:06 화자 1
So for every row, in that array, we need to enumerate the elements again. So we create another local variable j, which iterates from zero to myNumbers[i].length. Why is that? Because of the first loop, we go into an integer array, which is indexed by the value variable i. So here the condition is not just myNumbers, it is myNumbers[i].length. And we increment the value j. And then we print myNumbers[i][j], which will eventually print all these values, values one, two, three, and four. So, okay, I'll give it like 10 seconds or 20 seconds. So what would be the order of these numbers to be printed by this line of code, by this block of code?

51:07 화자 1
Yeah? 1 to 5. 1, 2, 3, 4, 5. Yeah, correct. Okay, so I mean that's not hard, right? Because we enumerate the first one and the second one. So for the first row, we enumerate the three elements one by one. So we go to the first array and we look at these three values, 1, 2, and 3. And then we get out of this for loop and then we go to the second row and we print 4 and 5. So the answer is 1, 2, 3, 4 and 5. Okay, that was a very brief introduction of arrays. Any questions? So, this is very easy, right? Yeah, do you have any questions?

51:57 화자 2
Okay.

52:15 화자 1
Great question. So I think that is a little beyond the scope at this point because that is really complicated. So probably we can keep that question for later. So my answer is basically not that different from how I do it. For instance, let's say we create a two-dimensional array of Strings. Then basically how we access every element is the same. For instance, let's say this is a two-dimensional array of Strings, and let's say we do myNumbers[i][j]. Then we just get the String, right?

52:47 화자 2
Thank you.

52:47 화자 1
From there, we have an option to change that address to refer to a String or a String itself to the memory address. So that is not exactly related to how we deal with this array, but it is more like a better understanding on how these classes and objects and memory addresses are interacting with each other. So probably our next topic, lecture four, is more about that. So yeah, we can—a little bit more about [[unclear: legacy]]. Right. [[unclear: work essence]]? No? No? Great. Great.

53:23 화자 2
Thank you.

53:25 화자 1
So what I just instantly want to say is, computer programming is not just about reading and writing code. It's about having a deep understanding on how this actually works underneath the code, which means in the computer architecture, computer memory, like the CPU compiler. So we really need to understand all these components to be a proper programmer. Making AI and write this code doesn't mean you are a very good programmer. So that's the reason why we do all these things. Anyway, let's go to the second part, conditional. So obviously conditional is an if-else [[unclear: distinct]]. Okay, skip it. So I mean the conditional is basically this algorithm, whether we check one condition at a time and we take a branch or not based on whether the condition is true or not. And the most obvious statement among all these condition concepts is if.

54:22 화자 1
So we use if statement to conditionally execute a block of Java. So if condition is true, then we execute this block. If the condition is not true, then we will skip it. So here the condition is basically a value. This condition is a value in a boolean type. So you need to understand that this condition is not that different from other variables. This is just a boolean value which is either true or false. So this condition can be complicated as much as we want because we know how to compose a boolean variable from other boolean variables. So here, all these expressions are examples of conditions, although they are not inside of if statement. For instance, the first one, x divided by two.

55:16 화자 1
So the remainder of the x divided by two, we check whether that x is equal to 1 or not. Which is we check whether x is an odd number or not. So if x is an odd number, the result of that expression is true. If x is an even number, the result of that expression is false. And here we see a little more complicated condition. So we check if whether x is greater than 1 and x is greater than—sorry, x is greater than 0 and x is greater than 1 at the same time. But that is a little obvious because if x is greater than 1, then x is obviously greater than 0, right? And, OR, so whether x is greater than 1, or—x is less than 0 and x is less than -1 at the same time. So what we check is, we check whether this absolute value of x is larger than or greater than 1 in either side, in the positive side and the negative side.

56:16 화자 1
So the result of that expression relies on the value of x. So, and here we can look at these two lines of code, they are equivalent. Why is that? Because we have expression one, we have expression two, and we do the logical NOT for every expression. So expression one is reversed, expression two is reversed, and we have this AND operator between these two. And I just don't, I just forgot the rule of this thing. I think it's [[unclear: to lower something]] that we heard, right? It is equivalent to expression one OR expression two, logical NOT in front of them. So that law that we learned from this logical courses also applies to this program. And this also, these two are equivalent. So what we can say is the same expression has diverse ways of to be represented. So we can always choose what would be a better way of representing something.

57:16 화자 1
And because there is an if statement, there is an else statement. So this block of code is executed if the condition is true. While this block is executed if the condition is false. So in many cases, we have this combination of the if statement and the else statement. And we also have this else if statement. So we check whether the condition one is true. If that is true, we execute this one. Else, which means if condition one is not true, but condition two is true, then we execute this line. Else, if the condition one is not true, condition two is not true, then we execute this line. Right? Not this one. Here is an example. So the current time is 22, probably 10 p.m. If the time is less than 10, which is probably morning, so we say good morning.

58:11 화자 1
If the time is not less than 10, but less than 20, we say a good day. Else, if the time is greater than or equal to 20, then we say good evening. And in fact, this ternary operator is kind of the middle ground between this conditional expression and the operator. So I think I just talked about this one in a previous slide, right? So we check a condition, whether this condition is true or not. So we have the question mark here. If the condition is true, we get that expression. If the condition is false, we get that expression. So this is equivalent to writing this if-else statement, and in fact, this can make you a little confused. But in some way, in some sense, this can be better than the if statement if what we do is obvious.

59:06 화자 1
So for instance here, it is fixed that we get the value of the expressionTrue or expression to this variable. So what we do is very obvious and short and it is just a simple check of the condition that sometimes this ternary operator is better than the if-else statement, not in how it works but in how well we can read the code. For instance, let's look at this example. We have a and we have b, 10 and 20. So we will create a String result which checks whether a is greater than b or not. If a is greater, we say a is greater. If not, b is greater. Or b is greater or equal to a. So this line of code is quite intuitive to read, right? Because we want to create a String based on the comparison between a and b.

01:00:00 화자 1
So we can say this line of code [[unclear: can't]] be better than the full statement because we can, like, decrease the number of lines and it is also very readable. So it can print the result based on the ternary operator. What if we have, so this switch statement is kind of related to this ternary operator. So almost all conditional expressions can be represented as if and else and else if in Java. But what if the conditions that we want to check is a very specific form, which is, we want to check a single expression, like for instance whether the value of x would be—or something like that. So based on whether the result of expression is x, or y, we can create this switch statement which checks the expression a single time and which maps the result of that expression to multiple cases.

01:01:00 화자 1
So in some cases, if the expression is a single thing, but if you want to create a huge branch based on the value of that expression, then this switch statement can be better than if-else statement if the if-else statement is going to be very long and complicated. So we use this switch statement to select one of many code blocks and code in the switch statement execute from the matching block and with specific types of variables can be used. So what does the third mean? The third statement means is because this switch operator, switch statement, checks a single expression with many possible variables, it does not support all the variables. For instance, some non-primitive variables are very hard to express in this switch statement, right? So this switch statement mostly covers mainly primitive types and only some non-primitive types.

01:02:00 화자 1
So you can check what types are actually supported. And another thing is this switch is terminated at break and there's a default. So if you look at this switch operator, there is a break at every case. What does that mean? It means, for instance, if the expression is matched with x, then the first code block is executed and only when it enters a break, it gets out of the switch statement. If there is no break here, then it will just run the code from the start to the end until it gets the break statement. So what that means is if this expression is x, then the second code block is also executed if we don't have the break at the first case. So we really need to keep this break for every case so that we don't create a [[unclear: buggy console]]. I will explain it later.

01:02:56 화자 1
The default is executed when none of these cases is true. So here is an example. Our grade is B. Probably you'll get [[unclear: better about here]]. This guy's grade is B. So if the grade is A, then we say your score is 4. If the grade is B, we say your score is 3. If not, default, we say there's no grade, and we print the grade. So there is a break between cases properly. So based on the value of the grade, then we print these things. And if you look at this block of code, this can be better than just writing this long if and else statements, and we check every time whether the grade is A, grade is B, grade is C, something like that. But what if we do not put the break between these cases?

01:03:51 화자 1
Then this becomes problematic because let's say if the cases is B, let's say our grade is B. Then this line is executed, so the statement is printed. And then because there is no break here, it goes to the next thing as well. So it will print this thing as well. So it prints two things at the same time. So it is working. So what we really keep in mind in the switch statement is this order matters and sometimes we can explicitly skip this break statement if this is really like the thing that we want to run. So it's hard for me to create an example something like that, but in some cases we might want to run this multiple cases if the switch is mixed perfectly. So in some cases we can explicitly keep the break in some cases while we remove it from some cases. Okay?

01:04:53 화자 1
So if we compare this switch and if, all of them are basically the same. So some people will prefer this switch statement. Some people will prefer the second thing, although they are the same. So I mean, it's obvious that they have different styles of coding, right? So here, everything is good. In fact, we need to compare the Strings multiple times. string.equals, string.equals, it is not required here. But honestly, in many cases, I do not use the switch statement at all, because that is slightly better if it is done properly, while the risk of using it is, I think, is a lot. Because I may skip the break statements, and I may do things wrong. So personally, I don't like writing a switch statement. [[unclear: method some of you might be pretty]]. So here it is.

01:05:53 화자 1
Questions? Great, okay. This is easy, so let me just keep going. Right. So we look at these loops. So, I mean, these are just super basic things that we learn. I think this array should be later than these conditions and loops, but anyway, these conditions, loops, functions are the first thing that we learn for running a program, right? So loop is required for every code because many codes need to run a block of code multiple times based on the condition. So this while loop runs this code block whenever the condition is true. So it does not finish as long as the condition is true. Just keep working, keep working, keep running that block and check the condition every time the code is executed.

01:06:50 화자 1
So if the condition becomes false, then it just gets out of the while loop and goes to the next line. There's another variant of the while loop which is called the do-while. I think I almost never seen it from existing code. So how it is different is, in the while loop, we check the condition first. And if the condition is false, we never execute this code block ever. We just go out of this while loop if the condition is false. But sometimes, sometimes we want to run this block at least once. So in that case, we probably can use this do-while. So here, do. We run this code block, whatever the condition is, and after we run this do block, we check the condition. So it is basically the same as the while loop, only with a slight change in this order. So we first do the thing, and we do the while loop.

01:07:46 화자 1
And if we compare this do-while and while, it's obvious that we can do the same with the while loop. For instance, we can create a temporal variable like boolean temp, which is equal to true. And here we put [[unclear: temp at condition]], and here we can put temp assign equals false. So I mean, basically we do not need do-while. But, so here what it means, what is important is programming is like an art. Everyone has their own style of programming, and some people obviously will prefer using this do-while a lot compared to what I just did from the while loop. So you can consider this whole thing as an option for a very artistic programmers so that they can use. Okay? And we also have the for loop. So the while loop is good, but the bad thing about the while loop is we have to control the condition statement on our own.

01:08:45 화자 1
So in many cases, for instance, for enumerating a String, enumerating an array, we have a very specific way of working. So we specify this way of working as a for loop. So in this for loop, we have three different statements. The first statement is executed whenever we get to the for loop. And then we check whether the condition is true or not. If the condition is true, we run this code block, and then after this code block is executed, we run the statement 3. And then we check the statement 2, we run this code block, we do the statement 3, we check the statement 2. So basically this is a for loop, it's another variant of while loop with a specific composition of three statements. So in many cases, for loop is quite useful. So, this is, again. Statement 1 is executed one time. One time. Running the code block one time.

01:09:45 화자 1
So statement one is mostly for initialization. And then we check the statement two. And then we run the code block, and then we run statement three. So it's better to look at an example. If you look at this code, we initialize a variable, integer variable as a value of zero. And then we check whether i is less than five, which is true, because i is 0. So we print out the value of i which is 0 and then we increment the value of i which becomes 1 and we check another—check again with the 5, which is true. So we print 1, we increment this value to 2, we check it, print it, 3, do it, 4, do it.

01:10:31 화자 1
So after printing 4, if we increment this value to 5, then the condition will be false because 5 is not less than 5. Then this print is never executed and the pointer will get out of the for loop and go to the next line. So this is how most for loops would look like. We initialize a variable, we do something, we check the condition, and we increment that value. That is not the only way of how we use the for loop, but a very common way of using it. And similarly, there is a for-each loop. So it is another variant of the while loop and the for loop, but in some cases, this for-each loop is better than the for loop in readability. So what we do is, let's say we have an array, array of Strings, array of integers, can be anything. And let's say we want to enumerate every element in that array.

01:11:28 화자 1
Then what we need to do is, let's go back, let me go back. Okay, so this is basically how we enumerate all the elements in an array, which is okay. Why? Because we can just initialize a variable, we can enumerate from 0 to its length, and we can do something for every element, which is good. But some programmers are really—they don't like this way of coding because that variable i is kind of unnecessary. We are very sure what we want to do. So for something like that, we, not we, people have created a new way of enumerating array, for-each loop. So here comes a colon. We create a variable.

01:12:23 화자 1
And then we, colon here, and array, we put array here, which means we brought this for loop for every element inside that array, only once. So, let me show you an example. We have an array of Strings, cars, which is like these four. So in this for-each loop, we create a variable String i. That means with the String variable i, enumerate from Volvo, BMW, Ford, to Mazda, and we print every car name, only one. So obviously this is better than the for loop in terms of readability, right? Because here readability is not a complicated thing. It's just a fact that how quickly we can understand the meaning of this code.

01:13:14 화자 1
If we read the code of other, or let's say you write a code that AI writes, so you want to double check the code created by an AI, for instance, [[unclear: cloud and clix]] or any AI, then readability matters because you need to understand how it works by just looking at this code. Then, in terms of the good coding, a better readable code is [[unclear: Mozilla]], mostly better than less readable code. And yet, this for-each loop creates more readable code compared to the for loop, and this makes a better code. So at least when we enumerate, [[unclear: I'll do it in a second already]]. I think this for-each loop is very useful. What if we want to break the loop? So we know that this for loop has its own condition to check whether we have to continue this for loop or not. But what if we want to break a for loop?

01:14:10 화자 1
We want to get out of this loop during the execution based on some condition. So here, this is done like this. We put an if condition. It checks whether i is equal or not to 3. Then if this value of i is 3, then we break. Which means if you have a break, it breaks the loop statement that it shows first, which is, if is a conditional statement, so it is not a loop. For is a loop statement. So if we enter this break here, then it breaks the for loop that it encounters. So then what this means is, we basically want to enumerate some variable based on the value of i, but we want to break this entire for loop when the value of i equals 3. So here comes also the readability issue. When we look at the for loop, we expect the termination condition to be this one. Why is that?

01:15:10 화자 1
Because that is basically why this for loop is designed and why it is different from the while loop. But if it has an internal break conditions a lot inside of the condition, inside the for loop, then it decreases the readability a little bit. Because if we need such statements to finish this loop, then probably I'm going to just use the while loop for. But still, in terms of the function, we sometimes need this break condition in the for loop. So that's how it does. And there is the continue. Okay, continue. So this is break, which breaks the for loop. But we can replace it, replaces break statement into the continue statement. So although it's not shown in this block, but continue is also a statement that controls the loop. How it works is continue is not breaking the loop itself. It just goes to the next iteration of the for loop. So what if this is a continue? Then, then.

01:16:09 화자 1
So if the for loop starts with i equals 0, print, i equals to 1, print, print 3, then it says that i equals 3, and if it enters the continue, then it skips the printing function and goes to the next iteration, which is i equals 4. So then it prints 4, and then it stops. So if break is continue, then it just skips that iteration and goes to the next one. So break and continue work entirely differently. But they are related. [[unclear: They that are on this window]]. And here is, well, a thing that I don't recommend. So when the expressions are very obvious, we can skip using the curly brackets. For instance, here we have if, else if, and else statement, but here you see that no brackets are there. Probably because what we do is very obvious.

01:17:09 화자 1
We just want to create some statement for every condition. So you might think it is not a code that requires a bracket. Or here we create a while loop, which is while true, we print something, infinite. Why is it infinite? The while loop checks the condition. If the condition is true, that means it never stops. So that is the reason why it is infinite, or we can create a single-line for loop. Is it okay to use something like that? Obviously okay, but as I gave you, I would not recommend it because it reduces the readability unless it's super clear how it works. I mean, if the code is very, very straightforward, you are okay to do something like that if you really want to reduce the number of lines, but in general, that is not a good habit. So let's look at some examples of the for loop to give you a better understanding on how this for loop works. Question.

01:18:09 화자 1
How does the code, what does the code below print? What would the result? Okay, you are so fast. So I'll give you like at least like 20 seconds so that everyone can figure out the answer. I mean, it's not that he's wrong. Thank you for raising your hand, but I'll give you just 20 seconds. Also, it's super pretty easy. Okay. What was the answer? I'm not going to ask you to do it. I'm just checking how well, how good you are in understanding this code. Could you raise your hand if you know how it works? I guess most of you know how it works, right? So, yeah.

01:19:11 화자 1
So this will print only the star lines, but the number of stars will differ. So for instance, the first line, it will print 5 stars, I guess. The reason is because we start this for loop with the i of 5. The value of i is 5 in the first iteration. And then the second for loop, we run this second for loop for the number of times equals to i. Because why is that? Because we start our variable j from 0 and we run this iteration every time while j is less than i, so that means we start j as 0, 0, 1, 2, 3, 4. So we run the second for loop 5 times, which print the star, but here a slight thing is this is a print function but without ln, where this is ln is a new line. So here it prints the star but without new line.

01:20:10 화자 1
So it will print the five star marks without new lines, and then here it will print a new line. So that means we'll print the five stars and we go to the second line. Then the value of i will be decreased to four. Why is that? Because the statement three there is a decrement operator. So the second for loop will run only four, four times. Because the value of i is now 4. So it will print the 4 star marks and then the new line. And then it goes to the first for loop where i is 3. It will print the stars 3 times and then the new line. So the result is a triangle where we have 5 stars, 4 stars, 3 stars, 2 stars, 1 star. Okay? Okay? So—being a good programmer means you are able to read code quickly and more precisely.

01:21:07 화자 1
So for instance, such a code is, in fact for programmers, it's too obvious to read. But if the code gets longer or complicated, then it requires a deep understanding of the computer programming to read something. Let's go to the next example. Again, I'll give you like the 30 seconds to think about how it works and what it actually does. So one thing, that Integer.MIN_VALUE is a constant, which is a very small value. So you can just ignore that expression and consider that as a very small value. Great. Yeah, I saw a student raising his hand, but I'll just give you like 30 seconds.

01:22:13 화자 1
Anyone else want to answer? Probably you are too shy to talk about, so I don't think it's very good to only have one person to answer, but it's better than no. Could you? Three six, why is that?

01:22:33 화자 2
So.

01:22:46 화자 1
Great. So in fact the answer is there, the title of the slide, finding maximum number. So it works to find the maximum number. Why is that? Because we have the for-each loop here, we enumerate for all the elements inside the array, and its int is every element starting from 23 to 36. If num is greater than max, what is max? Max is a very small number. So at the first element, 23 is obviously greater than this small number, so max becomes 23 if you look at the first element. If you go to the second one, we obviously go to the second one because we have a for-each loop. The value is minus 43, which is less than our max, so it passes, passes, passes, passes, until it gets 36, which is obviously greater than our current max, which is 23, so it replaces the value into 36.

01:23:41 화자 1
Although this is a few lines of code, what it does is simply to find the maximum in the integer array. Or we can say it can be a double array, because how it works does not change whether the array is an integer or a float. So what it prints is a max number is 36. It's not hard, right? So there is one more example that this is, I will just do it because this also is not complicated. So we have a String of characters. So you need to keep in mind that an array of characters is different from a String. I mean, in C programming, they are mostly the same. Not really the same, but mostly the same. But here they are different. We have an array of characters. So what we do is we initialize this local variable i from 0, and we enumerate this value from 0 to the chars.length minus 1.

01:24:38 화자 1
And what we print is chars[i] plus comma. And we add this character and the String, the result is String. If you put a single quote, this is a character. If you put the double quote, this is a String. So here, chars[i] is a character, and the comma is a String. So if we add the String and the character, the result is a String. So what we do there is we print every character in the array, but with commas inside. And there's no new line here, right? The new line here. So what we print is characters first, the c, comma, o, and comma, and we do the same to the end. And what we do is—okay, here's a slight thing. So the condition here, if you look at this condition, is not chars.length. It's chars.length minus one.

01:25:36 화자 1
So if you compare this slide with the previous one, for instance this one, that is different because if we are going to put the less than sign in that statement two, then the right-hand side of the expression to be the desired length or the number. But here the length is not the length, it is like length minus one. So why do we have such a thing? Because the comma shouldn't follow the last character. So this for loop does not print the last character and it stops when it reaches e. So it prints e and comma and then it breaks out from the for loop and it prints with the new line the last character r. Okay? So that is the great slight detail about understanding this code. Questions? Questions? Okay.

01:26:33 화자 1
So if you want to do good in the exam, you should be a human compiler. So you should be able to quickly understand what the code means and whether if the code has a bug, or not, and how to fix them up. So because in the exam, we cannot evaluate, it's very hard for us to evaluate your ability to write a code because we cannot just let everyone to write their own code during the exam hours, right? But other than that, we can make you test your reading skill and understanding skill. So what we had is during the classes, like understanding this code would be important for you. In fact, not just for the exam, but in general for your ability to be a programmer. So we have 10 more slides and I'm just thinking whether it's a great timing to stop the class or not.

01:27:34 화자 1
What do you think? Which one will you be happy? Like if I stop the class at this moment or you want to learn more? I don't know. Okay, so since we already have ten more slides, I will quickly do it because it's only a single part remainder. But I will just end the class at least before like [[unclear: 4:00 a.m.]]. So let me just quickly do it. So function. In fact, function is super important. Not just in programming, but function is like a—I cannot say that how important this word is. So in programming, we can consider a function as a kind of a member, kind of a black box, which is a single thing, which takes some input whose shape and whose meaning is pre-specified, and it does the thing over and over. So once we create a function like that, we can reuse that function function over and over.

01:28:34 화자 1
So programming cannot be done without using a function. For instance, if we make a function of name, like finding the maximum number, and we implement this thing, then we don't have to rewrite that code block whenever we need to find the maximum value from the array. We just can reuse that functional block every time. That is a core component of programming. So let's say we have a code without functions. So we have three arrays, three double arrays, arr1, arr2, arr3. So again, this is not a very good style of coding, but we can initialize and declare multiple variables at the same time if they are the same type. So here in the first line, we create these three double arrays, arr1, arr2, arr3, with the values inside, four values each, and we create—then, let's say we don't have a function.

01:29:33 화자 1
And let's say we want to compute the sum of these arrays. Then we should just copy and paste the same code block for every array, arr1, arr2, arr3. And for example, let's look at the example for arr2. For each double f inside the arr2, we add the value of f to the sum, where the f is every element in the arr2, which means we simply compute the sum of all elements in the array, and then we just print the value, print the sum, divide it by its length. So what we actually do here is to compute the average of the array, which is the same for all three arrays. So it is really redundant to copy and paste these three blocks of code for doing basically the same thing, right? So what we actually do is, what we really want to do is we create a function.

01:30:30 화자 1
So here you might see the keyword static, though there are many keywords that are not clear, but let me pass it for now. Just keep in mind that we can create a function called average, which is basically this block of code. So this average function takes as input a double array, arr, and it returns a value of double. So that is the meaning of the double in front of the average and in the parenthesis, the double array of arr is the input. So this is a function, it takes an array and it computes the sum based on the for loop and returns divided by the length. So it computes the average, but it does not print the average. It just returns the average to whatever we call this function.

01:31:23 화자 1
So if you look at the main, if you go back to the main code block, we create the three arrays as before, but we print the sum, the averages, by calling these functions, these average functions for arr1, 2, and 3. So by doing this, now we don't have to copy and paste the same, I mean, it is a little bit of copy and paste, but what we copy and what we paste is not the entire code block. We just copy and paste the fact that we will print the average of every array. So although this is also result of copy and paste, this is readable. We can obviously see that we can reprint the three different numbers which are averages of these different three arrays, which is much better than the previous version, this one. So, functions, with which we call methods in Java sometimes. A function is a block of code that runs when it is called.

01:32:23 화자 1
You can pass data, known as parameters, into a function. So why is the function is important? Modularity. By using a function, we can break the code down into small pieces with meanings. These meanings are important. We will not create a function with an arbitrary name with an arbitrary functionality. We create a function with an interpretable name so that we can instantly see what it does. I mean, the name of average is obvious for us because it does mean average, right? Or sum, or divide, or concatenate. The meaning that we already know, it can be transferred well to a function and can be modularized. So, that is the meaning of modularity. I just think this function is reusable. If you define the code once, [[unclear: no lack of code once]], then you can reuse it many times for many places.

01:33:21 화자 1
So this is a syntax of declaring a function or designing a function. So we create a public function. Why is it public? Because we want this function to be accessible from outside. Static? I'll skip it. I will talk about it later in the following topic. Int. What is an int? It's a return type. So this function computes something and it will return it as an integer. So the type of this function is int, and the name of this function is intPlusFloat. Do you think that is a good name? Well, I think it is because what it does is int plus float. And this function takes two inputs, integer i and a floating-point number f. So what it does is, just by looking at this declaration, we just know what it does. It takes an integer, it takes a float, it adds these two values, it returns the result, and it's an integer. So here comes ambiguity.

01:34:19 화자 1
How can the result become integer if we add the integer and the float? Because the information inside the float will be lost if the return type is the integer. That creates some curiosity. So if you look at this actual code block, what it does is it adds the integer i with the integer-converted f. So that is what it does. So I'm not going to say that whether this is the right way of doing the addition or not. But what I want to say is the return value, the type of the return value should match the functional declaration. So if the functional name is declared with the integer keyword, then we can expect that the return type is always the integer. So just by looking at this declaration, we can instantly know that this function will somehow lose some information inside the float because we just know that there's no way to preserve all the information inside the float when the return type is an integer.

01:35:20 화자 1
Why do I spend much time in this slide? Because it's important. Function is not, again, a random block of code. It should have a semantic meaning so that people can just instantly understand how it works and what it returns. So there are many types of functions, like many examples of functions. For instance, we create the integer function f, which takes two integers, i and j, and it returns i plus j. So it corresponds well with the name add. What if we go to the second line? printInt, which takes a parameter as an integer, which means it prints an int. But printing is not returning. If a code prints something, it doesn't have to return something, right? So the printInt function has a return type of void, which means it doesn't return anything, because it doesn't have to.

01:36:18 화자 1
What this print function does is it prints the value, given value i, and then it returns nothing. So there is no return statement inside the function, which is okay if the return type is void. If you look at this third one, what it does is getSpecialValue, and it takes nothing. So this is a function, it takes nothing. It just returns a constant that we already already defined, which is 777. So that is a function which returns the same value again and again. And there is another function, printMyName. You know that we have a professor named Youngki in our department. So in this function, printMyName, it does not take any parameters and it does not return any return values. So its type is void. It takes nothing. It just prints the [[unclear: precipice item]].

01:37:16 화자 1
So these functions are just there. So, return keyword is the core component of a function. So the return is used to exit from a function with or without a value. So inside a function block, when we encounter a return, then the function stops. That is like super strong stop. So whenever we find a return, then that is the end of the function. But if the return type is not void, the return should always exist in the function body, and the return must be followed by a specified type of value. So let's say our return value, return type is an integer. Then the function cannot stop without having an integer to return, because that is like a violation of the rule. But for a method not returning a value, the return statement can even be skipped. It is OK for us not to put this return statement at all in a function.

01:38:14 화자 1
And just putting return is also okay. So it will exit from a function, but it will not return anything, which is okay if the function type is void. Okay? So when we call a function, as we have seen from the previous slide, we can just simply type the name of the function with proper parameters. So I will skip explaining this [[unclear: good lab. moment]] in this code block. But we anyway, which take [[unclear: four point numbers]]. Now once we define our function here, we can just instantly call this and we can put these four numbers as parameters.

01:38:59 화자 1
If these parameters are well responding to the declaration definition of the function, then this function will run well and it will return the floating-point number as a result, which is stored in the intrp variable. It is very easy for us to call a function. Last slide. Function orders does not matter. So for some programming languages, the order matters. Here what order means, let's say we have multiple functions inside the same class. And let's say these functions are nested. Some functions call another function, which makes sense. For instance, the average function can call addition functions. Like length function can call [[unclear: in no]]—So these functions are always nested. But here in Java, the order between these function declarations is OK. What it means is, let's say we declare a function first, which calls the function second.

01:39:57 화자 1
But the function second appears later than the function first. Which means, if you just write this code line by line, when we read this first line, we don't know what the second means, because we didn't get to the second line. Not yet. But that is okay. So some people put the small functions first, some people put the small functions later. I think that is just a style of programming. But what is good for us is the order doesn't matter much for making this code actually work.

01:40:33 화자 1
[[unclear: testing]], that's the end of today's class. So I really wanted to end this, like, slides today because next time we have much more fun things to study. So I wanted to quickly finish with our basics by today. So what we covered today is arrays, conditionals, like loops, which would be obvious for computer science, CS department students or those who have coded very much before. But for those who are kind of new to programming, these fundamentals are really important for you for doing the lab assignments and the assignments later. So I guess you will have no public questions at this point, so you can just [[unclear: come from]] if you have any questions. So see you next week. Thanks for listening.
````
