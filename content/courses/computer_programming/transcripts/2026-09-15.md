---
title: "2026-09-15 · Computer Programming · 보정 STT"
course: computer_programming
date: 2026-09-15
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
date_confidence: user_tentative
---

강의 날짜는 사용자 잠정 확인을 기준으로 9월 15일에 연결했습니다. 녹음 파일 자체에서 날짜를 독립 확인한 것은 아닙니다.

명확한 전문용어 오류를 보정한 공개 STT 사본입니다. 불명확한 말은 원문 표현 또는 `[[unclear: …]]`로 남겼으며, 전체 음성의 완전 복원이나 누락 없음이 확인된 자료는 아닙니다.

개인 이름·연락처 등은 비식별 표시로 가렸습니다. 시간·화자 순서는 유지했습니다. 교수자·수강생의 예시, 의견과 추측은 검증된 사실이나 현재 공식 규정과 구분해 읽으세요.

보정 원본 SHA-256: `1de6a7a7a0d22295b15b0391676ec8e68fc3a99def7602ab1300a73ff7277438`

````text
00:00 화자 1
So that's the goal of our course, not learning about Java basics. That's what I really want to talk about during the entire course. So the simple outline is here. So a simple announcement is we were gonna release homework one this week, so please visit ETL and check the instructions for the homework one. So it will be a little different, I think, from the last year, so please keep an eye on how the homework will look like. And for deadline or for the exact instructions are not yet fixed, but we will make it public at least in this week. Anyway, so today we'll talk about this class's constructors like memory states, this concept related to OOP. And I will begin what that word really means, OOP.

00:57 화자 1
So, Object-oriented programming is a way of looking at a software system as a collection of interactive objects. So the word literally means that it is a way of programming, but it is oriented to objects. Which means, when we look at a program, when we look at code, or let's say when we look at the world, we often see the reality as a relationship between things. For instance, we have a person named Tom, and we have a car, we have a house. So, I mean, the world is very complicated, and it's hard to summarize the world into a few words. But at least in this slide, you can say that we have three objects, like Tom, a house and a car, and there are relationships that connect these two entities.

01:48 화자 1
Which means if you write a code, instead of understanding that code as a sequence of instructions like the for, like the if, like the else, instead of that we want to look at the program as a more abstract way of representing something higher or something bigger. So here, when we say the word object, that means an entity. which can be a symbolic thing or a conceptual thing or a physical thing. For instance, a car is obviously an object, which is a physical entity. Or let's say Bill Gates' bank account is also an entity, although there is no physical thing that represents that bank account. That is more like a conceptual entity. And let's say we have a data structure called linked list, although we didn't study it yet. Probably you've heard it from data structures course.

02:39 화자 1
weight so that is called the software entity so each object is something that we name it's some entity either physical or or software or conceptual thing so let's think about a code so let's think about the procedure of making coffee. So let's say I want to describe this whole process of making coffee to someone else, like let's say my friend. And probably I can say we get coffee beans first and we ground these coffee beans and we get water, we boil water, we put coffee beans in boiled water, we mix it and So this is a perfectly normal program or we can say an algorithm that describes every step of this process. So there is nothing wrong in this code, but if you look at it, the problem is if the code gets longer and longer and longer, let's say this code is a hundred lines or a thousand lines, then it's becoming more hard to understand.

03:38 화자 1
harder and more difficult to describe something or manage something or improve something. So let's think about how can we make this into a more abstract way. And probably one way is more object -oriented style, which is instead of saying we follow all these processes, let's think about the entity called coffee machine. Then we can say that we first build a coffee machine that can grind stuff, boil liquid, and mix stuff in boiled water. So these three bullet points are the things that can be done by a coffee machine. They are not a process, they are not a part of this whole process, but they are more like descriptions about the coffee machine.

04:25 화자 1
So we first get this coffee machine, or we buy it from the market, and then So the actual process starts from number two, we get coffee beans and water, and we put all the coffee beans and water in the coffee machine, and then we have a coffee. So that changes are possible because we have a coffee machine that can do a lot of good stuff. Without having this coffee machine, we need to do all these things ourselves. But with it, our whole process of making coffee can be more simplified. So this is super easy and silly example, but we can think of this OOP, object -oriented style, as making a machine or making an entity that can do things itself. So by having these entities, we can simplify the whole process based on this thing. That is like a very basic concept for OOP. And here comes the word class.

05:22 화자 1
So if you think about the coffee machine, then there should be some blueprint that describes what a coffee machine is and what can it do. So this word class is the blueprint or template of object. And these classes use attributes and methods to depict objects. For instance, we have a class called, for instance, like fish bread. Then this class or a blueprint describes how this fish bread behave. For instance, it tastes good, it smells good, for instance, it's stinky, and it's hard, something like that. So we have a class which describes objects. Then we can create objects from a class with different names. For instance, if you look at the example of a car, then car is a class. And then we have different cars, like Audi, Nissan, Volvo, which are cars.

06:21 화자 1
So these objects follow the blueprint defined the class car. So we can create multiple objects of one class even though they will follow the same procedures, like they will follow the same behavior defined by the class. And then this object will have the common attributes and So let's think about these attributes and methods which are both important properties of a class. So let's say we have a car class. Then these attributes determine the appearance, state, or other properties of an object. So every car will have a fuel, right? And every car will have a speed. So this speed is not a physical component of a car. But we know that every car has a speed, which is either zero or 100 or anything. Every car will have a color, will have a windows, will probably have people inside.

07:20 화자 1
So these attributes describe the status of a car. And these attributes are shared between all different car objects, but the individual values of these attributes will be certainly different from between cars, obviously, because this car can be blue while that car is red. So we have a set of attributes. And we also have a set of methods. These methods define the way an object can change its attributes and perform actions. For instance, if we have a method called refuel, then we know that this refuel will change the status of fuel, which is an attribute. And if we call a function named accel, then this will change the status speed. probably it will change other attributes as well. So we have these attributes and methods to describe what a car is.

08:16 화자 1
So a good thing is, once we define these attributes and methods well, then we don't have to redefine them for every different car. Once we have this blueprint, we can create as many cars as we want, so that our code is more readable to people, because people can understand what a car is. Let's take an example. We can create a class with that class keyword followed by a class name. And then we can create an object with a new keyword followed by a class name. In this example, let's say we have a class car and we didn't put anything inside it. In fact, in this here, there are some attributes and methods that are defined for for this Car, but we didn't put anything at this moment. So we have this Car class, and then we can declare a new variable, which is a type Car, so that we can call this myCar as an object.

09:15 화자 1
So this myCar is a variable of type Car. So that's the reason why we put this new in front of the Car. Why new? Because we need to allocate new memory. Because even though we have nothing here inside this Car class, it can actually have many different attributes and methods, and they require a new memory address with new memory allocated for that class. So because of that, we can think of this myCar as a non-primitive type, which requires a new memory allocation for that. So that's the reason why we put new in front of that car keyword. So what are attributes? I think it's much easier to look at for example. So we can define class attributes with variables and here we define the class car but here we define two variables, attributes. The first attribute is width and the second attribute is color.

10:12 화자 1
So here the width and color will be defined differently for every object. So when we say a width to be 10 and a color to be red, these variables can change in the actual generated object, because this is a class definition, not an object definition. But here, if we put this 10 as a value of this width variable, that means we initialize this variable's 10 to red, if no initial variables are given in each object. So they're just initial values, they're not the actual values that are true for every object. Because as again, these are class definitions. Then, if we have this class definition, we can create a new car. So you note that we create a variable that starts with a lowercase while the class name starts with an uppercase.

11:11 화자 1
So we create a car, and if you print the car's width, then we know that width should be initialized with 10, right? So if you print the car's width, then it will print 10, right? Because that is initial value. Then if we change it with five, so here we can see the notation. So if you put car.width, that means we access the attribute inside this car, okay? car.width to be 5, that means we change the value of that attribute to 5. So if you print the car's width again here, then it will print out 5. So here's a ten, here's a 5. Very simple, right? So these attributes can be initialized with constant values when declared.

12:04 화자 1
And as I said in the previous lecture, it is always a good practice to initialize all the variables that we create, even though these are attributes, because that makes the code more easily maintainable because we can expect the values of the initialized variables with this initialization. So if not initialized explicitly, they have default values. For instance, if we create a Boolean variable, the default is false. If we create an integer variable, the default is zero. So it is okay not to initialize these variables, but again, this is not a very good practice because these implicit default values are not seen from the code, right? So it's always good to explicitly say what would be the initial values. So, at least in your individual environment, we can check what would be the default values.

13:00 화자 1
For instance, if we create a class Foo, which is a random name, and if we create all these attributes without initializing them, and if you create an object called foo, like a lowercase foo, and if we print all these attributes one by one, then we can check what are the default values in this object. For instance, for the bool, the default value is false, integer, zero, floating point, 0.0. This is to just make sure what are the initial values inside this object. But there is no meaning in doing these whole things if you create just one object for a class. Class is meaningful because it is reusable.

13:45 화자 1
So in this example, we create two Car variables car and new car which are both car right you know what i'm saying right so if you put the car's color and if you create the new calls new cars color then probably these colors would be what was it red right because the red was the initial value for that class But if we change the color of this new car to blue, and if we print these colors differently, then only the new car's color will be blue, while the original car's color is red. It's obvious, right? Although it's obvious, it is clear from the code because we put this new keyword two times, not once. That means these two variables do not share their memory addresses. They have their own memory addresses.

14:42 화자 1
So if we change the color of one thing, that does not affect the other one. And then since we now understand what these attributes are, let's go to the methods. So another name to call these methods is member functions because these methods are basically functions, but these are defined for a specific class. So we have a car and we define find a function inside this class, void, which means this return type of this function is nothing. And we print something, hello world, inside this function. And this function is inside this class. That means this function is belonging to this car class. So if you create an object car, and then if you could car.printHello, which means we call this function inside this object. So you need to understand that this function is not belonging to the class. This function is belonging to the object.

15:41 화자 1
So when we call this thing, we call the print hello from this car, not that car. So that is a slight thing that I want to clarify in the following slides. So here we have a car which has a speed as its attribute, and we have a function get speed which simply returns the speed of the current car. So in this class definition, we define these two different cars. We create these two different objects called myCar and yourCar, and we set different speed, myCar and yourCar. Then if you print the speed from these two cars, they will be obviously different. Because what this getSpeed does is it returns the speed of the current object or the current car. It does not affect the others. Now I think you understand why we do this whole thing, the reason behind these classes.

16:40 화자 1
Because once we define this class once, then we can reuse this class a lot of times with different attributes. If you think about it, that makes the whole code more efficient to maintain. of terminologies. For instance, when you say if I say a word instance that is almost the same as an object. And if I say instantiate, that means we create an instance of a class. And so we can say my car is an instance of the car class, which is okay. And we can say we instantiate the variable my car. So because the word instance is the same as the word object. And of course we know what attributes are, what methods are, or what members are. So here, members incorporate both attributes and methods.

17:35 화자 1
So if you call these are the members of this class, that means it includes both attributes and methods, because they all are the members of the class. So that was very simple definition of classes and one test Questions? So let's go to the second thing, constructor. So every class has a constructor, which means the constructor is a special class method to initialize an object. And a constructor is called once when the instance is created. That means when we define this function, this special function constructor, that it is not called by when we want or where we want. It is just called only when the instance is created.

18:33 화자 1
So because of that, because it is a special function, the name of the constructor is always the same as the name of the class. And also there is no return type of constructor because what this constructor does is construction. So it means nothing. and to return something. And as I said, you cannot explicitly call a constructor. So let's think about this example. Okay, so we have a Car, a class Car, and we have a function named Car. So that doesn't look Well, because we have learned that every function needs to start with the lowercase letter. But that starts with the uppercase letter and the name of the function is the same as the name of the class. So that means this function is not a random function, but a constructor, a special function.

19:33 화자 1
It does not have a return type, so there's no void in front of that. And in the constructor, it says... a car is created. And if we create this car here, then the let function is automatically called and it calls a car is created. It prints the car as created. Okay, so why we need such a constructor? Because we sometimes need a construction process. So let me explain that in the following slide. This constructor Parameters are functions that do not return anything, but they can take parameters to initialize attributes or to do useful things. And we can also have multiple parameters and we can also define multiple constructors. Okay, for instance, we have a car class. And let's say we have two different attributes. It's weight and it's color.

20:27 화자 1
So we can probably create the initial values of these attributes, weight and color. So although here we do not have an initial value for the weight, but we have an initial value for the color which is unknown. So if we define our constructor in this two way, the name of this function is the same between these two, but the set of parameters is different. If we look at the first constructor, it only takes an integer w and it initializes the value its attribute to weight. And the second constructor, it takes another parameter, a string c, and it initializes two variables at the same time. So if you look at this too, we can understand two things. First, it does not have a constructor which does not take anything. which means we don't need the initial value for the weight because in these two constructor cases, it always takes the value w.

21:24 화자 1
So either we create this instance by the first constructor or the second constructor, we always have the value for the weight. So that is the reason why we don't need to initialize that value in the class definition because that will be initialized in the constructor. The second thing is, because it has two different constructors, a user can decide which one to use. For instance, if you look at that one on the right, then you can create a car with its weight and color both, or if the color is unknown, then you can put only the weight. And then we can call this function printInfo for that two objects which will return differently because their weight and colors are different. So if you look at this whole code, you might think this is probably not necessary because we can have a single constructor and then we can just put something like cars color equals C, something like that in the code.

22:24 화자 1
to avoid having these different constructors. So what I want to say is this whole class thing or OOP thing is not about doing something not possible without OOP. So there always is another way to do exactly the same thing even by the procedural programming. But if you think about For instance, if you don't have these constructors at all, then we need to construct every instance and then we need these lines of initialization for every different object. That increases the number of lines per code and it reduces the readability. So by having these different constructors, we can create a very well written and easy to understand code as shown on the right. And related to this one, we need more and more and more to provide many useful things about these classes. And another thing that we want to learn is this class. This. That is the name of the class.

23:22 화자 1
So this is a special keyword used to indicate the self-object. And you can usually omit this keyword in many cases, but sometimes you do. And it is used to explicitly indicate attributes. So let's just go to the example to make you understand. understand what that is. So our car class has attribute color, it also has a function change color. So Sometimes I want to refer to a local variable named color and sometimes I want to refer to the color attribute that is inside this current object. So when we say this .color, that means we look at the color variable which is an attribute of the current object. not the current class.

24:11 화자 1
So if you put this .color red, that means we change the value of my current color to be red, while if you do this one, like color blue, that means we don't affect the color which is inside the class. We just change the value of this color local variable. So this keyword is quite useful when we have to create many variables in a complicated function. So this is an example. So if you go back to the previous slide, this keyword is used to explicitly call a constructor, like call this current function, and if we call this as a function, as shown in this line, then we can also call the constructor of the class. So this is not for today.

25:10 화자 1
We will talk about it later. This is more like part of the bigger concept. But what I want to say is that this is useful when we refer to the current object, as shown in this example. And we can also call a method with this keyword. We have a car class, and its attribute is 100. And then And we have a member function or method, speed and unit, which returns a string. which is created by concatenating the speed and kilometers per hour. So here, we didn't put this into that, but that is okay because we don't have any other variables of name speed. That speed just refers to the speed defined inside the class. So that just returns my current speed with the kilometer per hour string. And then we have another function, printSpeedAndUnit.

26:08 화자 1
which prints something which is created by this dot speed and unit. So now we understand why these member functions are useful. Because let's say we have many many different ways, different necessary situations where we need to they return we had some 100 kilometers like 150 kilometers something like that but we don't want to put that speed plus kilometer per hour for every use cases of that So once we create this function, speed and unit, then we can just reuse that member function for every use case where we need such a thing. So here, if you put this into the speed and unit, this is more like an explicit calling of the member function. If you don't have it, this is okay.

27:00 화자 1
So this can be omitted because we don't have any other variables called Speed and unit so we don't need that speed keyword specifically in that but probably we can make it explicit by making it appear in that. So this is good, right? And let's say we see many different use cases like that. So sometimes we want to create a function which means which changes the speed for instance sometimes i really want to change the speed of my instance right but let's say we have a local variable speed and we get this variable as an input of this function change speed then we see a lot like this function so we take the speed and we change the variable of this speed with that speed.

27:55 화자 1
So I mean if you think about it, it is quite a good practice of coding because by using it, by having this, it is very clear that this new variable speed is equivalent with my local speed. So having the same name is sometimes good because it makes very clear what we want to do. Okay? But because of that, because we use the same thing, speed as an input variable, if you look at it, we have no way to you access the attribute speed if you don't click the this keyword. So in fact, this line doesn't do anything because this speed is also the local variable and this speed is also the local variable. So this is just the assignment of the same thing. So the first one doesn't work while the second one actually works. So that is a case where we need that this keyword. Good? Okay.

28:51 화자 1
So like, what I really want to say is you, when you learn this OOP, you need to think about what is a class and what is an object. So that comes from the next slide, static members. So now you understand that almost all attributes and methods are belonging to an object, not a class. That means if you want to call something, if you want to call a member function, you should first create an object. of a class because when we call this method that is belonging to this object. But there is a way to make this function belong to the class, not an object. That is done by the keyword static. OK, let me first show you an example, OK? Because before, just talking about it too much. We have a Car class, but we have two variables, like one attribute and one method.

29:50 화자 1
But I put the keyword static. What does that mean? That means that attribute and that method belong to the class, not an object. So that means we can call something like Car.num. where this car is a class of the object, which means we just access the variable num inside this class which is 3. So that means if we change that num of one object, that will change the num for all the objects that are in the same class, because that variable num is attest with the class definition. So if we put something like what it says is the number of cars is something. So now you understand what that means.

30:39 화자 1
If you want to keep a variable that says the number of total cars is three, or the number of total cars is four, or five, then that number, like the number of total cars is not managed by every car. That is managed by the whole class of the car. So that is the reason why we maintain that variable as a static integer. inside here, okay? So because of that, because of that, when we create a new instance, that instance does not require a memory space for storing the static integer, because this static integer, as a game, is not inside an object. Let's look at another example. We have a car class, and we have two attributes, static attributes num and totalMile and we also have a non -static, but pivot, just integer mile. So these three are different. And we have a constructor.

31:39 화자 1
And we, inside the constructor, we increase num, because here plus plus is just increment operation for an integer variable. So what does that mean? That means that num counts the number of total cars, and whenever we create a car, we increase the value of the num. so that that static variable works in our own way. And we have another function setMile which takes mile, int mile, and it changes the mile attribute of the current object because it's a non -static member. And then it adds the mile to the static member called which is a segment. So now we understand that num and total mile are a static summary of all the cars that we have, while the mile attribute is only about that specific car.

32:37 화자 1
So here, on the example on the right, we create three Car instances, 1, 2, and 3, and we set their values differently, 20, 30, and 40. So here's a quiz. What are the printed values of the first one and the second one? It should be super clear for you what are actually printed there. It's clear, right? First, it is printed. So here the obstacle is we didn't provide the initial values there. So that is the reason why this is a bad coding. We always need an initial value especially for the static variables. So let's just assume that the number is initialized as zero and the totalMile is also initialized as zero. Then the first thing to be printed is obviously three, right? Because num starts from zero and it adds one by one when a car object is created.

33:33 화자 1
And one is printed in the second line, the value should be 90 because every time we call this setMile function, that given integer mile is added to this total mile which is initialized as 0. Okay? So now you understand why we need the static members and why sometimes we don't need the static members. Is it clear? Good. Because this thing is, if you are really new to programming, then this can be a little confusing. But this is super important to understand what is OOP. So let's go back to the definition that I just skipped. So the static members can be accessed without creating an object because we have a different separate memory address for the static members.

34:26 화자 1
And we also have static members are associated with a class of an object and non -static members cannot be used inside a static method, which is important. So we can create static members, static methods, not just static attributes. Okay, where was the example? If you look at it, this function is a static function which can be called for a class, not an object. But what if this static method has lines that refer to non -static members? Then that doesn't make sense. Because this static method is associated with a class and it doesn't know which values are there for non -static members. So every static function should call only non -static members.

35:25 화자 1
Or if you put it in a different way, non -static members cannot be used inside a static method, which is obvious. Okay, again, common mistakes. A static method cannot call an instance method, or we can say a non -static method. A static method cannot use instance variables, or we can say a static method cannot use non -static variables, because the static method does not know which object you referred to. However, static methods can be called in an instance method. So it should be clear to you. It should be so clear that I don't even have to say it to you if you understand this OOP. Are you very clear? Any questions? No? Am I too easy?

36:24 화자 1
Okay, so let's go to another example. We have a car, we have a float, which is a non -static attribute for that class, and we have a static floating point function, total fuel returns fuel. Obviously that creates an error, right? Because if this is a static float function, then this function is about, should be about summary, should be about different objects, should be about classes. But if it returns fuel, then, I mean, in terms of the static method, we don't know which fuel it refers to. So once we create an instance like that, it just creates an error. The definition does not make sense. So it does not allow us to compile the code. That's better. So we talked about classes and we also talked about constructors which are also related to classes.

37:18 화자 1
So at this point I feel that you are beginning to understand what would be the advantages of doing this thing to separating the blueprint and the object that we have. So let's go one step further. How does equality work for objects? So every object is made basically a non -primitive type, which means the variable itself only contains the address of the object because we have a separate memory block for having that object. That is the same as in a string. So you remember that if you had created these two strings, string one and string two, although their contents are the same, hey hey, because we put the new keyword in front of each one, If you compare these two, they are in fact different. Why is that? Because their memory addresses are different while their contents are the same.

38:18 화자 1
If you create another string, string 3, and if we declare its value, initialize its value to string 1, that doesn't create another memory block. It just copies the memory address from string 1 to string 3. If you compare string 1 and 2, they are obviously different, but if you compare string 1 and 3, they are the same. Because this does not compare the contents, it only compares the addresses. So when I talk about these strings are the same as when I talk about classes and objects, because they are fundamentally the same. So let's think about the general thing. Let's think about the general class, which is a car. So if you put that assignment operator that assigns only the address of an object to another not the value.

39:13 화자 1
If you create a car, my car, with the new keyword, it will create a memory block only once. So if you create these variables, car1, car2, and car3, all these three variables refer to the same memory address. We didn't create a new car object. If we have only one object, but we have four different variables that refer to that single object. So if we change car1.something, then that will change myCar.something, my car2.something, my car3.something, because they share the same set of attributes. So in this sense, now we kind of have a grasp on what is the difference between a reference and the value. So let's go further and let's think about the concept called call -by -value, which is a very common concept that appears from C and C++ programming when we talk about pointers.

40:13 화자 1
So let's think about this class test. We have a class test, and we have two static functions. So one is just a main function, public static void. So now we understand the meaning of the static, right? We have a function, static, public, which is accessible by outside, static, which is defined not for an object but for a class, void, the return type is void. The function name is main, it takes an argument, list of strings. So at least now we understand every keyword that appears here. So we create two integers, a and b, those values are 2 and 3. And we call the swap function, and probably our intention of calling this function is to swap the values between a and b. So what we want to do is to make a to 3 and b to 2. So let's think about whether this is actually done, either function or not.

41:05 화자 1
So if you look at that function, static void swap, It takes two values, a and b, it creates a local variable temp, and it assigns a value of a to temp, and we assign the value of b to a, we assign the value of temp to b. So as a result of doing these three lines of code, the resulting a and b will have swap values inside this swap function. But if we return to the mother function, the main function, the results of this swap does not affect these two values. Some will know why it is and some will not know why it is. So let me explain why the a and b are not swapped.

41:53 화자 1
Because when we call a function in Java with primitive type attributes, we call this function by the method called callByValue, which is a way of calling a method by passing values or by copying values. And this applies when primitive type values are passed to a method and by doing this callby value, there's no way to change the value of the original variable outside. So let me show you this clear figure that describes our memory blocks, okay? So we first have our main function. In main function, we have two symbol tables, a and b, and their values are 2 and 3. We don't store their addresses, we store their values because integer is a primitive type.

42:47 화자 1
And then if you call this swap function, what we do is we copy the values of A and B, and we create new memory addresses of A, B, and temp, three of which are local variables, and they have their own memory addresses which are separated from the memory addresses of the main function. So everything that we do in that swap function, does not affect the original variables because everything that we do is inside that swap function. So as long as we pass the values of the variables by the call -by -value, not by call -by -reference, then there is no way to change, no way that these changes affect the original variables. And this is very basic way of calling a function in Java or in other different languages. But let's do some interesting thing.

43:42 화자 1
We really want to swap this value, so we are gonna try with a new idea. And this is called the call -by reference. The previous thing was a call -by value. So now what we do is we will create a class IntHolder and it has an attribute which is an integer. So this class is just nothing but a class that contains an integer inside. And the constructor, what it does is it gets a value and it just assigns its self -value to the given one. So as a gain, it is nothing but a holder of an integer. It does not do a useful thing. But let's think about this new swap function. We create a variable A and the variable B as instances of the class IntHolder and we provide the values 2 and 3 respectively to A and B.

44:40 화자 1
And we also call the swap function A and B. And if you look at the definition of the swap function, it takes IntHolder A and B, integer A and B, and it swaps A and B's values, not A and B themselves. Will it work? Yes, it works. Why is that? Because when we call a function and when we pass a very very pass on non primitive type variable or when we pass on instance variable not the primitive type then the call way is a call by reference not call by value which is we don't past the primitive values, we passed the addresses of variables. And if you think about it, it is obvious.

45:28 화자 1
Because when we call a function with a class variable, if we have to copy the entire object, then that is a huge like a reduction of available memory because we don't know how large every object to be because by how we define this class, this class can be huge. It can contain hundreds of different attributes and even hundreds of different arrays and all other things. So it is almost very, very inefficient to copy this whole object and pass it to another function. So instead of doing that, when we call a function with an object, we do this call by reference. And in Java, this is just fixed. In Java, when we call it, feed a primitive type, it is done by callByValue. When you feed a non -primitive type, it is done by callByReference. It's just written inside the language description.

46:25 화자 1
So because of that, because we do not create another memory block when we call a function, we can actually change the object's attributes inside the method. So that is like the magic that we expect from the new function. So what actually happens here is we have two variables a and b inside the main function's memory, but they are like different memory blocks. So these a and b are not inside the stack frame, although you might not understand what the stack frame means, but anyway, they do not inside the memory space which belongs to the function. In fact, this memory space, the stack frame, only contains the addresses of a and b, while the actual classes are outside. So when we pass these variables a and b to the swap function, the swap function takes only the addresses of a and b.

47:25 화자 1
So when I refer to a .value, that refers to the original a. and b dot value the same. So here we swap the A dot value and b dot value. So that changes the values inside this original memory addresses. So you need to understand when this call -by value happens, when call -by reference happens, and what will be different if the vice versa. So if you think about it, now you understand this memory management is quite complicated. Sometimes we need to create objects, we need to delete objects, and sometimes we go to the memory block of constant variables, sometimes we need to dynamic memory space, and sometimes we need to call a function, so memory management is quite complicated.

48:25 화자 1
So if you think about the C program, if any one of you is an expert of C programming, then the programmers need to take care of the memory. So for instance, when you create an object, C programmers need to explicitly create a memory space. And when we stop using that object, C programmers need to explicitly create a block of memory so that everyone can use it. But that is done automatically done by Java. So Java has a garbage collector which looks for unused objects and reclaims the memory that they are using, which is totally different. So let's think about an example where we create another object IntHolder C inside the swap function. C equals new IntHolder, for instance, four.

49:18 화자 1
Then that memory block for the new object IntHolder C inside the swap function is not in the stack frame on the swap function is outside. So when we return this function and go back here in the C program, that memory space for the new variable C is not free. That is inside the memory somewhere. So that tree is the memory to overload. But in Java, when we return this function to here, and if no other things can refer to this variable C, then the C variable is automatically freed by the garbage collector so that we don't have this unused memory anymore.

50:04 화자 1
So this is one advantage of java compared to c or c plus plus and it makes us easier to program while it also makes Java more slow than C++. So if you write a program where the speed is really, really important, then C++ is always better than Java because it's at least 10 times or 20 times better than Java. But at the expense of that, it requires programmers to take more and more efforts in writing code and managing this memory. Questions? Good. So it should be clear to you. And so in your brain somewhere, it should be very clear to you when you look at a code or when you look at an equation or a question, how it happens.

51:03 화자 1
So let's think more about swaps because it's really important. Let's go back to these data types in Java and now we understand we have primitive data types and non -primitive data types and this distinction is very important So when we think about this primitive type is primitive types store the primitive values themselves. So in every program we have a symbol table. What is a symbol table? Symbol table is a simple table where it contains a name and it contains a memory address. So this symbol table is not set to be inside the memory. This is more like a map before we go into the map. So this symbol table shows us the address of every variable. So if you actually go to the memory for the primitive types, it contains a value, not reference. So you don't...

52:02 화자 1
I have to think that this is a memory and that is also a memory because that is not the truth. This is a symbol table and this is a memory. So when we do something like integer x equals 2, that means we create a new symbol variable we store the primitive value 2 inside that memory address. When we go to the non-primitive type, as you already know, this does not work in that way. So if we do this string s equals Hi, although this Hi is a constant, not a dynamic memory anyway, but we create a symbol table, and if we go to its actual So value that does not contain a value anymore. It has a reference to Hi, which is inside another present memory. So that is how these non-primitive types are different from primitive types. So this example is the same as before. This swap doesn't work, right?

53:03 화자 1
And why this swap doesn't work is also very obvious, as I just explained. So inside this swap function, what you can see is just three local variables, a, b, and temp. And their addresses are different from this X and Y, which means their symbols are different. So because these new variables are totally separated from these old variables, the value change does not affect the original variables. And this is the same as before, this is a different illustration. But now, let's think about a little intermediate case. So this is an example that I didn't have in the previous slide. So here, we have two strings, Hi and all, and we also call of swap function. But here, what it takes is not a random instance. What it takes is a string, and it takes the string A and B and it swaps them inside.

54:00 화자 1
So this is like, as again, an intermediate case, and it is not clear whether the swap is done or not. So given this code, you need to be able to draw the memory yourself. Like how does memory looks like and what actually happens there. So let me show you this one. So here, just imagine yourself. We have these two string variables s1 and s2, and because they are non -primitive types, there will be another memory space having Hi and all. So these are non -primitive types, so what we pass to this new function are their addresses of Hi and all. It's not call by value. So if you create these two variables, you need to make sure that in the symbol table, their addresses are different.

54:55 화자 1
Because, again, I think it can be a little confusing, but this symbol table is not in fact a memory address. This is just a mapper between symbols and initial addresses. So if you go to the memory addresses, the variables as, where is it? The variables a and b, which are given from the static name, so the s1 and s2 are the original names that a and b are passed in front, right? So a and b are passed by the references. So if you look at the memory, we see that their references are the same between S1 and A and S2 and B and the actual memory addresses are just Hi and all. So we didn't create a new memory address. So what we do here is we swap the references, right? So we swap the references, not the values. So eventually the swap works, right?

55:51 화자 1
Because the values stay the same and we just change the references so that the values stay the same. The values stay the same while the references are changed. That works. So we go a little deeper. We think about the wrapper class where we have a class Wrapper which has an attribute string s. And we swap the attributes of the wrapper. So this is a more complicated example because here When I talk about the code like reference here, this IntHolder holds integers, which are primitive values. But here, things are more complicated because these holders contain strings, which are non-primitive types. So this is more complicated than before. So this memory looks like computing, okay? So let's go back.

56:50 화자 1
We have four variables S1, S2, A, and B. which are not just strings, which are wrappers of strings. So, what we do is, we have a symbol table here, and this symbol table addresses our copy from S2 to B, from S1 to A. And what they store is not the strings themselves, they store another address of a string. So we have two two levels of going from the wrapper to a string of reference to a string value. Two stacks of references, which is super confusing.

57:33 화자 1
So if we swap the values of a .s and b .s and temp, that means we swap those things okay, so What would be the best way to explain this? So what we... Okay, I'm just thinking about what would be the most non -confusing way to say this. So when we call this swap function with these wrappers, we copy the wrappers' addresses from S1 and S2 to A and B. So we copy these addresses, that means our new variables A and B refer to the same objects of the wrapper class. So when we change a .s or a .b .s inside the new variables, what it changes is the reference that this object refers to.

58:31 화자 1
So this swap function does not change the string values, they just change the string addresses. So what they change is here. If you look at these two, right? Look at these two. the S2 object now points to a Hi and the blue one, the S1 object now points to all. So previously it should be the parallel. So previously that one refers to the Hi and this one refers to the all. But that is now the opposite because of our swap calling thing. So this can be super super complicated because an object can contain another object which can contain another object. So this chain of memory addresses can be long and long as much as we want.

59:27 화자 1
So it's not super complicated if we take one by one. For instance, if you look at this code and we just follow one by one, one will happen and one does not happen. So that is okay. So this should be, in fact, we have four different lines Every line changes these arrows and references inside this memory block. So what I show is just two snapshots of it. So I think it's a little hard to explain how things are really happening just by two snapshots. But I guess you now understand what is the point of me explaining this whole thing to you. All right. an occasion? no? okay. okay. so now, wrap up. why are you over here? By using OOP as a game, now I think you better understand what this OOP means.

01:00:23 화자 1
So instead of understanding code as a sequence of operations or sequence of things, we now create this blueprint, which are classes, and we create objects from this blueprint so that we understand the entire code as an interaction between entities. And it has, importantly, three big advantages compared to the previous way of programming, which are encapsulation, inheritance, and polymorphism. So when we talk about OOP, we should always talk about these three different properties, and we will learn these different properties very soon, starting from the lecture 5. So now we just understand what OOP means and how these classes work. So we have better. So let's think about one by one. What is encapsulation? Encapsulation is we want to hide the details that people don't have to know.

01:01:19 화자 1
For instance, when you think about the car, I think some of you will drive a car, me also driving a car, but I don't have to know all the details inside this car, like this engine, like the fuel. We just have to know this handle, like this wheel and this accelerator, how to drive a Knowing how to drive a car is totally different from knowing what car is. So when we do this OOP, we want to keep these details from the programmers or users so that they don't have to know the details. This encapsulation is very important for writing a super long and complicated program. What is inheritance? It's not just about relationships between objects. Classes also have their relationships. For instance, if we have a class called organism, We know that every animal and every plant is an organism.

01:02:17 화자 1
And we also know that every dog is an animal and every cat is an animal. Every tree is a plant and every grass is a plant. So if we define a function for an organism, like, well, let's say, think about an animal. every animal will have a function breed. Then this breed function is the same for every class that inherits this animal. Or for instance, for every organism in Earth, we can say, for instance, well, water PD, water PD. We can say that every domain component of every organism is a carbon. Then that fact does not change for all the classes that carry this organism. So by creating this relationship between classes, we can write a code that is more better and easier to A class can inherit the characteristics of another class, which we call the parent class.

01:03:16 화자 1
And the child class can reuse the codes of the parent class without rewriting everything. And the child class also adds a new characteristic. For instance, not every animal has a tail. But we know that cat has a tail. So we inherit all the characteristics of animals when we create a cat, and then we can add a tail only to this cat. And then we will also have many objects of this cat, for instance, like my cat, your cat, all these different cats, right? So we have classes, we have objects, and they are related to each other. And all this relationship, or all this hierarchy makes programming much easier when it's used appropriately. Another concept is polymorphism, which means we have a function, member function, that is shared by all the animals. Say we call the animal sound function.

01:04:10 화자 1
So this animal sound function will probably be defined by the class animal. But the thing is, every given different animal class that inherits that animal class will have a different member function of how they sound, like the baowa or myaowa like these things, right? So inheritance is about inheriting a characteristic inside the parent class. Polymorphism is about changing the characteristic based on this specific class. And here, because we know that, I mean, almost all the animals sound we just define the function there, declare the function there, and we call, we say to the other programmers that, hey, we have many classes that inherit our parent class, and we have declared the function animalSound. I don't know how each animal exactly sounds, but I just know that every animal has an animalSound function.

01:05:10 화자 1
So you can call this function whatever that means because I at least know that every animal sounds. So these three concepts like polymorphism, inheritance, encapsulation makes the program more interesting to do. It changes the way of programming. I think I say it pretty much several times but it changes the way of programming as just designing this whole algorithm one by one, and if and else these things into more structured and more interesting relationships between things. And this will be super important in modern programs. It's like the principle of all the long and complicated programs. So summary, we have learned classes and related concepts or objects like instances, like attributes and methods. And we also learned the constructors the this and static keywords.

01:06:10 화자 1
And related to the this and static keywords, we also talked about the memory states, like call by value, call by reference, and the primitive types, non -primitive types, all these swap things. And then we also learned why this OOP matters based on our previous understanding of the classes, constructors, and memories. This is not the end of today's class. Any question? Clear? Great. Okay, then let's quickly go to the second part. So, you know, our class is actually a two -hour class. I don't really want to do this full class, but I also cannot stop the class So I'm just gonna do this second part a little more, for about 20 minutes or 30 minutes, and that will be the end of this class.

01:07:10 화자 1
I want to give you some moments before going on to the next one because I don't want to push too hard. So I believe this whole process is about OOP. And to understand OOP, we need to understand how Java works. And later we will learn C++. So I mean, what I'm saying is just give it some moments to So C++ is also OOP language, and which is more advanced from C. And C++ also has this concept of classes and OOP and inheritance and polymorphism and all these things. But C++ is much more lower level compared to Java. So in C++, we need to do a lot of things that are done automatically by Java. So that's the reason why we first learned Java, which is easier language, and then we go to C++, which is a harder language. So the first half of this course will be about Java and OOP, and the second half will be C++. And we are in the middle of that.

01:08:07 화자 1
We are just in the first take, and we are talking about this OOP for Java. So this encapsulation topic is, I think, is the easiest one among the three properties that I just shared. Because this idea is really simple. So let's do what I want. Encapsulation is about hiding complexity. I mean, we have a dishwasher, or what is it? Anyway, let's say we have a dishwasher. The right one is obviously easier to use than the left one. If we are an engineer who needs to fix a dishwasher, then probably we need to open the machine and look inside. Probably we will look at all these lines and different gears and all super complicated.

01:08:56 화자 1
But for 90 of people, for almost all programmers, they will assume that this machine is good We don't assume this machine is broken and we lose something So as long as we believe that this machine is okay then the right one is obviously better than the left one We just want to use it We don't want to understand it. So this is a total idea of encapsulation. Let's say, for instance, I'll pick three of you, and let's say I say you implement the arm of a robot. You implement the head of a robot, and you implement, for instance, the chest of a robot. Then every one of you will not want to understand the other parts. Like the arm guy will not want to know the details of the head or the chest. And you will probably just believe that the other two will make their jobs done. So that's important when we programmers work together.

01:09:54 화자 1
We don't have an energy and the efforts to understand the others' work. We just want to believe that they are good. So this is also the basis of the encapsulation. So I'll give you a motivation. Okay, so we have a fruit store and we have a balance. We have a stock. Balance is money and stock is the number of fruits, right? So we create an object, fruit store, store, we buy a new fruit store and we, let's say, create the current stock and current balance. So let's say I buy something. I go to this fruit store and buy three apples. And let's say the price of every apple is 2 ,000 won. Then when I...

01:10:38 화자 1
buy three apples or if this stock store if this store sells three apples then probably we need to change these two attributes in this way we need to decrement like decrease the stock by three and we need to increase the balance by six thousand because that is how this sell behaves okay so i think this looks pretty good this has no problem at all but what We did we implement this sell function So in this sell function, we just take the number of apples and it automatically changes the balance and stock this is better than before because for outside people outside users We don't have we don't want to know the exact attributes inside the store. We just want to do the behaviors of buy and sell.

01:11:36 화자 1
So what if someone just decreases the value of stock and just go away without increasing the value? This is like a thief, right? So we can avoid this appearance of thieves if we provide only the sell functions, not the attributes. So we have the details that we want to keep ourselves. We don't disclose these details to other people. We want people to interact with this object only in a limited and approved way, like so. So that is the distinction between the methods that I want to show to the world and the attributes that I want to keep ourselves. That's the thing about the encapsulation. So again, encapsulation is one of the most fundamental concepts in OOP, and it bundles non -visible attributes. Or some methods can be non -visible.

01:12:31 화자 1
So it just separates the non -visible details from visible public interfaces. So it has two different advantages. of encapsulation. So the abstraction is we define public interfaces to interact with an object and we hide the details under the hood. So the abstraction is the main purpose of encapsulation. But the other thing is about defensive programming. So it prevents people from handling our attributes in a non -expected way. So we want to protect our data from an unauthorized access like the thief who stole three apples from our store.

01:13:13 화자 1
We want to defend our store so the other purpose is about defensive programming okay two different things so encapsulation in java is implemented in different so in using basically classes access modifiers and setters and getters so i will explain each one in a So in Java, we have a keyword that I didn't explain before. So we have a keyword like private, default, protected, public, which defines whether every attribute or a method or a class is visible to outside or not. And we also have getters and setters which control data access. So I will explain them one by one. So we have access modifiers which are put in front of the method or in front of the attribute or in front of the class.

01:14:06 화자 1
So when the keyword is private, that means this code block, whether it is method or whether it is attribute or class, that code block is only accessible within the declared class. So if the method is private, that means the outside people do not know whether there is such a method or not. What is the default? Default is default. So we don't have a keyword default, but that is like when we didn't put any modifier. So that is the reason, meaning of the default. So if we didn't put any modifier, then that code block is accessible only in the same package. So I will talk about the package later. The package is a collection of classes. So we have a map, let's get class, and we have a bigger thing, package. So the default is a bigger space compared to the private.

01:14:59 화자 1
And then we have a protected, which makes the code block accessible in the same package which is the same as the default, and inherited child classes. So we also go to the broader world, and then we go to the public, which is the code is accessible by all classes. So as we go from the top to the bottom, the range of people who can see the code block increases. Let's think about the private. We have a person class, and our weight is private.

01:15:33 화자 1
We don't want to I don't want you to know my ego process and this weight is private attribute that means we so that means this private integer probably attribute is not accessible outside so let's say we have an instance jack and we let's say we want to know the weight of this jack but that Causes an error because this weight is private I mean it is obviously true that some attributes need to be hidden from us. So let's go back to the Apple Store example. So now we talk about this Apple Store, which is more serious about protecting its information compared to the fruit store. Then its two attributes, balance and stock, are now private integers. And since it provides the other method. So there, we don't have any access modifier for these functions, right?

01:16:32 화자 1
Then in this case, their modifiers are default. So that means that these functions are accessible in the same package outside the class. So in this example, we assume that every code is in the same package. So we would think that these packages are almost public. So what I want to say is we keep this information hidden balance and stock while we make these three functions public so we allow people to interact with this apple store only by the sell function get balance store get balance function and get stock function so you will think If we provide the balance and stock freely by these functions, then what is the meaning of hiding this information? But that still matters because these getter functions do not allow them to change these values.

01:17:28 화자 1
The only way the people can change the balance and stock is by the sell function. So if we implement our class in this way, then we can make sure that there is no mistake or there is a donor who changes the balance of stock in an unexpected way. So what I want to say is making this private is not about hiding information because we can still provide this information by the getter function. This is more like making people interact with our class only in a designated way. So this is an example, we create an instance, Apple Store, and we let them know about the current balance and current stock.

01:18:17 화자 1
And if this store sells three icons or apples, because this is Apple Store, probably different from the apple that I bought, but in that case, the balance increases, and then the stock decreases, which makes sense. Also, we can also have a private method. These private methods are methods, not attributes, which are not accessible outside. Okay? So, You can imagine the function inStock here. So here we have two private attributes, balance and stock. And we have a public, so this is not public, but I will just call it public for convenience. Public, balance and stock, which people can use. But this function is about checking whether the number, the given number, for instance, the three apples or five apples are in stock because it is possible that this apple store has only two apples.

01:19:16 화자 1
Then, if our store has two apples, then this store should decline the order to sell five apples. Otherwise, the stock will even go to the negative number. In that sense, we can say that this definition is bad because this does not prevent people from selling 50 apples or 100 apples. In that case, the stock will be minus 70, which does not make sense if you think about this apples store. So a better class definition is we want to prevent such a thing. So we implement the private Boolean function, which returns either true or false, given the number. So these are definitions. So let me first check the inStock function. That private function takes a number and it computes the shortage, which is num minus stock.

01:20:14 화자 1
If the shortage is greater than zero, which means the order is greater than the stock, it returns false, which is the given number is not in stock. Otherwise, it returns true, which means you can order that number of apples from our store. So if you look at this new definition of our sell function is better than before because it first checks whether the given order is in stock or not. If it is in stock, it does the same thing and it returns true. Else, it returns false, which is better, right? One more thing. In our previous definition, the sell function was a void function because selling something does not have to return something, so their return type was nothing. But in this new sell function, it needs to return whether the sell was successful or not.

01:21:14 화자 1
Because when we call this sell function, if a user calls this function, if the sell was unsuccessful, people should know that this sell was not okay. So then, because of that reason, we make this sell function return type to a boolean. and returns either true or false. So again, this whole thing is about design. This whole thing is not about making this code work better. This is more about making this code more bulletproof and more human friendly. So now let's look at an example code. We create an apple store and we get its balance and its stock and we, let's say, do this function. So we call this function store.sell and we want to sell 50 apples, which is not successful.

01:22:07 화자 1
So we get the result of that sell condition and store it as a local variable success. If success, which means it has succeeded in selling this apple, so we get this balance and stock. Else, if it is not successful, then we can show you that not it's not. This is again a better code. So what it prints is, it shows that initial balance is 10 ,000 and the initial stock is 30, but it also says I have apples and stuff. I mean, it's not hard at all, right? This encapsulation is possible because we do OOP. So let's go to these getters. Probably I think I can finish the lecture by just getter, setter before going to the third part.

01:23:05 화자 1
So yeah, we just have five minutes remaining. So getter, setter These are also very easy. So sometimes we want to hide this attribute with the keyword private. But we often also need a way to access these variables outside, even though these attributes are private. So we have a name for this function. So let's say we have an attribute age. Then we implement a function called getAge, or we implement a function called setAge. These are not Java syntax. These are not some kind of special functions inside of Java. These are more like a convention. We just call these two functions getter and setter and this is like a common way to call these functions in a broader sense of OOP. So we just call them getter and setter.

01:23:59 화자 1
So for almost all private value, Even if we want to really hide these values, we can create this getter and setter. But if we want people to know these values, then we create this getter and setter so that they can change the value and they can get the value. So this getter and setter are basically nothing. I mean, what they do is silly because it's not that different from people accessing this public variable age. I mean, it's in fact not different from making this age public, right? But it is different because it's more like creating a window. Like for instance, when we have a public field, then this external world can interact with this public field in any way they want, like the bugs can... crawl this field, right? But if we create a window, so this is an example of encapsulation.

01:24:58 화자 1
If we encapsulate this private field with this public getter and setter, although it is not super different from that one, it makes only indirect access possible to the user. And that is quite safe way of putting. Why is that? Because we can do a lot of things. For instance, some attributes need to be read only. Some attributes need to be write only. So for these cases, we can choose to provide only the getter or only the setter function for these cases. Or we can validate accesses for private attributes. For instance, I think it also is shown in the following slide, but what if people want to change the value of my age to -20? I want to prevent something like that because age cannot be a negative value.

01:25:52 화자 1
But if we allow people to interact with this age by the setter, then we can have a condition here that says if the given value is negative, then it's set, you can say no. Or if the given value is 100, you can say no. Okay, not 100, 1 ,000, you can say no. Or we can also trace accesses to private attributes for debugging. For instance, we can count the number of accesses for our private attributes, or we can even say that, okay, someone, for instance me, trying to access my private attribute, that we can even store the log in our internal system. So let's look at some examples. We have a class person, we have a private string name. So, okay, my name is private. And there is a setter setName, which is public, because it's public, and it's void, because it returns nothing.

01:26:50 화자 1
So in many cases, setter does not return anything, because setter does not need to return anything. So it takes a local variable name, like parameter name, and it has a condition where if name is null, So every non primitive type can be null which means it is a non primitive type which is a reference type but it has nothing at the actual reference point. So null is kind of the default value for every non primitive type or every instance, but it doesn't mean anything. So if the name is null, Or, so here the EqualitySign is okay. You need to keep in mind that why the EqualitySign is okay there. Because comparing an instance with a null is comparing whether that memory address is zero or not. So it is okay. So that is not about comparing the contents. It is about comparing the reference.

01:27:49 화자 1
So if the null is, name is null, Or if the name equals the empty string, then it says name cannot be null or empty, which is about protecting our private variable attribute. Else, we assign the value and we change the name. Okay, so here is the one funny thing. That is not funny, important thing. If we call something like name .equals to a null instance, it raises an error. Because if the reference is null, which means if instance does not refer to anything, then our trials to access the attributes of that instance causes an error. Because if the variable is null, that means it doesn't refer to anything. So there is nothing, no attribute or no method inside the attribute instance. So that name dot equals causes an error if the name is null.

01:28:49 화자 1
But you remember, I just forgot the name of that. What I want to say is, if the name is null, we don't go to that condition because this is OR. If the name is null, then we just break out from the condition and go to the line. You remember that thing I just talked about, the Java basics, right? Because of that, it is okay to check if the instance is null, and then we check the contents. We need this too. If you don't have the first condition, name equal to the null, then we directly check whether the name equals the string, the empty string or not, which can create an error if the given name is null. And we can also trace accesses to private variables. Let's say we have a class ChangableVar, and we have a setter and we have a getter.

01:29:46 화자 1
So in some cases, we want to count the number of changes, how many times people have changed these attributes. So in this case, when people call a getter, then we can increase the readHistory variable by one. Or if people set the value, then we can increase the set count of changes. So if you ask why are they important, well it's hard to say. It will depend on cases. Sometimes we want to keep these values traced for some reason, for debugging or for, I don't know, this is for a logging behavior. So I think I can just skip this example because this is just an example that shows how this logging works. So this code is a little bit long I think but there's not much to say about this code.

01:30:43 화자 1
Because this is only about when we call getter, when we call setter the designated code run and the counter happens, the count value increases. So this getter and setter are an example of encapsulation. So this whole thing is about, as a game, protecting things and making things invisible from outside and making things work in an expected way. So the remaining part is packages, but since we have more than 20 slides here, I think we can stop here and see you next week. So that's it, and just remember that homework will be out this week, so please keep updated about the ETL where we will put the announcement. And that's it.
````
