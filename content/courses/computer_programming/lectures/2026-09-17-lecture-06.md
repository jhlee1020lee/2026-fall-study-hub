---
title: "2026-09-17 · Computer Programming 6강"
course: computer_programming
date: 2026-09-17
lecture_no: 6
tags:
  - computer_programming
  - lecture
  - detailed
lang: ko
concepts:
  - "Attributes와 Methods"
  - "Call-by-Value"
  - "Class와 Object"
  - "Constructor"
  - "Encapsulation"
  - "Garbage Collector"
  - "Inheritance"
  - "Object reference"
  - "Object-Oriented Programming"
  - "Polymorphism"
  - "static Members"
  - "this"
review_status: approved
draft: false
note_layout: content_first_v1
source_assets:
  - "4 oop.pdf"
  - "Lab03 v2.pdf"
  - "Lab03 skeleton.zip"
private_source_assets:
  - "Lab03 skeleton.zip"
---

9월 17일 실습은 Object-Oriented Programming(객체지향 프로그래밍, OOP)을 복습한 뒤 Lab3의 상태와 동작을 읽는 수업이다.  
9월 19일 확보한 공식 자료로 보충한 명세는 불완전한 녹음에서 확인한 내용과 구분한다.

## 강의 내용과 설명

**Source provenance(자료 출처와 확인 시점):** 강의 날짜는 사용자가 확인한 2026년 9월 17일이다. STT는 그날의 불완전한 녹음이며 마지막 17:39 구간이 문장 중간에서 끝난다. M01은 이전 OOP 자료이다. M02 `Lab03 v2.pdf`와 M03 비공개 skeleton ZIP은 9월 19일 후취득한 공식 자료이다. 아래의 **후취득 자료 보충**은 이 자료로 확인한 설명과 과제 명세이며, 빠진 강의 발화를 복원한 내용이 아니다. [STT 00:00] [STT 17:39]

**Citation legend(인용 범례):** `[M01 p.n]`과 `[M02 p.n]`은 PDF 쪽이다. M03는 PDF가 아니며, `[M03 p.1]`은 Section 1 `src/Fight.java`, `[M03 p.2]`는 Section 2 `src/Main.java`, `[M03 p.3]`은 Section 3 `src/Player.java`를 가리키는 section 번호이다.

**기출 관련성:** 기존 OOP 기출 연결은 각 문항의 요구와 선수 개념 범위 안에서 읽는다. 후취득 Lab3 명세 자체를 기출 스타일의 근거로 사용하지 않는다. 기출 연결 여부와 관계없이 전체 강의 학습목표를 유지한다.

### Object-Oriented Programming(객체지향 프로그래밍, OOP): 대상과 상호작용으로 프로그램 바라보기

OOP는 소프트웨어를 서로 상호작용하는 **Object(객체)**들의 모음으로 바라보는 방식이다. Object는 프로그램에서 표현하려는 대상이며, 현실의 물건에만 한정되지 않는다. 자동차처럼 물리적인 대상뿐 아니라 계좌처럼 개념적인 대상도 Object로 표현할 수 있다. 이번 실습에서는 이러한 관점을 복습한 뒤 두 플레이어가 대결하는 게임에 적용한다고 안내한다. [STT 00:00] [M01 p.3] [M01 p.4]

**자료 그림 읽기 — 현실의 대상과 모델의 관계.** M01 p.3은 위쪽에 집·사람·자동차를, 아래쪽에 이들을 표현한 모델을 배치한다. 아래 그림에서 중요한 것은 상자뿐 아니라 상자를 잇는 관계이다. 사람이 집에 거주하고 자동차를 운전한다는 관계를 함께 표현해야 대상들이 하나의 시스템을 이룬다. OOP를 이해할 때는 “어떤 Object가 있는가?”와 “그 Object들이 어떻게 관계를 맺고 동작하는가?”를 함께 살펴야 한다. 이 그림은 강의 개념을 보충하는 자료이며, 이번 녹음에서 그림의 모든 요소를 설명했다는 뜻은 아니다. [M01 p.3]

<details>
<summary>M01 p.3 — 원본 자료 페이지</summary>

![M01 p.3 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/4.oop/page-003.png)

</details>

M01 p.4는 자동차, 계좌, Linked List(연결 리스트)를 나란히 놓는다. 여기서 Linked List의 구현을 배우는 것은 아니다. 소프트웨어 내부에서 사용하는 구조도 Object가 나타내는 대상이 될 수 있다는 점을 읽으면 된다. 따라서 “Object는 화면에 보이는 물건을 그대로 코드로 옮긴 것”이라는 이해는 너무 좁다. [M01 p.4]

**후취득 자료 보충:** M02 p.4도 OOP를 상호작용하는 Objects의 모음으로 정의한다. M02 p.5의 Car·bank account·Linked List 그림은 각각 physical entity, conceptual entity, software entity를 나타내며 위 설명을 뒷받침한다. [M02 p.4] [M02 p.5]

**Procedural Style(절차 중심 방식)**과 비교하면 차이가 더 분명해진다. 자료의 커피 만들기 예시에서 Procedural Style은 원두 준비, 분쇄, 물 준비, 끓이기, 섞기와 같은 작업 순서를 나열한다. Object-Oriented Style은 분쇄·끓이기·섞기 기능을 수행하는 Coffee Machine을 구성하고, 그 대상에 원두와 물을 넣어 작업하게 한다. [M01 p.5] [M01 p.6]

두 방식 모두 실제 작업 순서는 필요하다. 차이는 프로그램을 설명하고 구성할 때 작업을 어디에 묶느냐에 있다. Coffee Machine 예시에서는 관련 기능을 하나의 대상에 묶어 생각한다. OOP를 사용한다고 조건문이나 순차 실행이 사라지는 것은 아니다. 이 커피 예시는 이번 회차의 발화를 복원한 것이 아니라 자료에 근거한 설명이다. [M01 p.5] [M01 p.6]

개념 연결: [[concepts/object-oriented-programming|Object-Oriented Programming]]

### Class(클래스), Instance(인스턴스), Instantiate(인스턴스 생성하기)

Class는 Object를 만들기 위한 **blueprint(설계도)** 또는 **template(틀)**이다. 하나의 Class로 여러 Object를 만들 수 있으며, 그렇게 만들어진 각각의 Object를 Instance라고 부른다. Instantiate는 그 Instance를 만드는 행위를 가리킨다. 설계도, 만들어진 대상, 만드는 행위를 구별하는 것이 출발점이다. [STT 01:51] [STT 02:44] [STT 03:40] [M01 p.7] [M01 p.17]

**자료 그림 읽기 — 틀과 결과물.** M01 p.7의 붕어빵 틀은 Class, 틀에서 만들어진 개별 결과물은 Object의 비유이다. 옆의 자동차 그림도 하나의 `Car` Class와 여러 자동차 Object를 구분한다. 그림에서 읽어야 할 것은 “같은 틀을 사용했다”와 “동일한 하나의 대상이다”가 다른 주장이라는 점이다. 여러 Object가 공통 구조를 가진다고 해서 모두 같은 Object가 되는 것은 아니다. [M01 p.7]

<details>
<summary>M01 p.7 — 원본 자료 페이지</summary>

![M01 p.7 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/4.oop/page-007.png)

</details>

자료의 기본 문법은 다음과 같다. [M01 p.9]

```java
class Car { }

Car myCar = new Car();
```

첫 줄은 `Car`라는 Class를 정의한다. 두 번째 줄의 `new Car()`는 그 Class의 Instance를 생성한다. `myCar`는 생성된 Object를 가리키는 변수이며, 뒤에서 설명할 reference(참조)를 통해 그 Object에 접근한다. `Car`라는 Class의 이름, `myCar`라는 변수의 이름, 생성된 Object 자체를 서로 구별해야 한다. [M01 p.9] [M01 p.36]

강의는 같은 Class에서 만들어진 Instance들이 공통 **Attributes(속성)**와 **Methods(메서드)**를 갖는다고 설명한다. 이 두 종류를 함께 **Members(멤버)**라고 부른다. 여기서 공통이라는 말은 어떤 상태와 동작을 갖는지에 대한 구조가 같다는 뜻이다. 각 Instance에 저장된 값까지 항상 같다는 뜻은 아니다. [STT 02:44] [M01 p.17]

**후취득 자료 보충:** M02 p.5는 하나의 Car blueprint와 Audi·Nissan·Volvo Objects를 나누고, p.7은 `Car newCar = new Car();`로 Instance 생성을 보여 준다. STT의 `attributes or variables in methods or functions`에서 `in`, `the app that's creating an instance`에서 `app`는 불확실한 원문으로 남긴다. 자료의 attributes **and** methods 및 Instantiate 정의를 원래 발화의 교정본으로 제시하지 않는다. [STT 01:51] [STT 03:40] [M02 p.5] [M02 p.7]

**기출 적용 — 현재 접근 가능.** 파일명상 2025-1 중간고사로 분류된 복기본의 1번은 Class와 Object를 설명하도록 요구한다. 이 요구에는 Class를 설계도로, Object를 그 설계도로 만든 개별 대상으로 설명하고 둘의 관계를 연결하는 접근이 적절하다. 위 코드에서는 `Car` 정의와 `new Car()`에 의한 생성을 나누어 설명하면 된다. 용어를 각각 번역하는 데 그치거나 Class와 Object를 같은 것으로 설명하는 것이 함정이다. 해당 자료는 공식 원본으로 확인된 시험지가 아니라 복기본이며, 학기·담당교원 정보는 파일명에 의존한다. 현재 강의자료와 담당교원 표기도 다르므로 이번 수업의 출제 성향으로 일반화하지 않는다. [EX:cp_2025_1_midterm_q01 p.1]

개념 연결: [[concepts/class-and-object|Class와 Object]]

### Attributes: Object의 상태를 저장하고 구별하기

Attributes는 Object의 외형, 상태, 그 밖의 성질을 나타내는 변수이다. 자동차의 폭이나 색처럼 “이 Object가 현재 어떤 상태인가?”에 답하는 정보를 담는다. 강의의 일부 설명 문구는 불명확하지만, Attributes가 상태나 성질을 나타낸다는 개념은 자료와 일치한다. [STT 02:44] [M01 p.8]

**후취득 자료 보충:** M02 p.6의 Car 그림은 Attributes로 `fuel`, `speed`, `color`, Methods로 `refuel()`, `accel()`, `break()`를 묶는다. 코드에서는 `int speed = 10`이 새 Object의 상태이고 `newCar.speed`를 읽으면 `10`이다. 그림의 `break()`는 인쇄된 표기 그대로이며 추가 Player API 요구가 아니다. STT의 `an object that works like me`는 불명확한 표현으로 유지한다. [STT 02:44] [M02 p.6]

<details>
<summary>M02 p.6 — 원본 자료 페이지</summary>

![M02 p.6 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/lab03.v2/page-006.png)

</details>

자료는 다음과 같이 `Car`에 두 Attributes를 선언한다. [M01 p.10]

```java
class Car {
    int width = 10;
    String color = "red";
}
```

`width`는 정수 값을, `color`는 문자열 값을 갖는다. 선언과 동시에 값을 지정했으므로 이 예시에서 새로 만든 `Car`의 초기 상태는 `width`가 `10`, `color`가 `"red"`이다. Object의 Attributes에 접근할 때는 **dot syntax(점 표기법)**를 사용한다. [M01 p.10] [M01 p.11]

```java
Car car = new Car();

System.out.println(car.width); // 10
car.width = 5;
System.out.println(car.width); // 5
```

`car.width`는 `car`가 가리키는 Object의 `width`를 뜻한다. 첫 출력은 초기값을 읽고, 대입문은 그 Object의 상태를 바꾸며, 두 번째 출력은 변경된 값을 읽는다. Class 정의 전체를 고치는 작업이 아니라 이미 만들어진 특정 Object의 값을 바꾸는 작업이다. [M01 p.11]

Attributes를 선언하면서 값을 지정하지 않으면 자료에서 설명하는 **default values(기본값)**가 적용된다. M01 pp.12–13의 `Foo` 예시는 다음 값을 보여 준다. [M01 p.12] [M01 p.13]

| Attribute의 자료형 | 자료에서 확인되는 기본값 |
| --- | --- |
| `boolean` | `false` |
| `int` | `0` |
| `float` | `0.0` |
| `String` 등 Object를 가리키는 Attribute | `null` |

`char`는 자료에서 값이 비어 보이는 표기와 출력으로 제시된다. 이를 유효한 빈 문자 리터럴 `''`로 옮겨 쓰면 안 된다. **해당 표기의 정확한 의미는 검증 필요**로 남긴다. 또한 이 부분은 Class의 Attributes에 관한 설명이므로, 모든 지역변수에도 같은 규칙을 그대로 적용한다고 확대해서 읽지 않는다. [M01 p.12] [M01 p.13]

서로 다른 Object는 각자의 Attribute 값을 갖는다. 자료에서 두 `Car`를 생성한 뒤 두 번째 Object의 `color`만 `"blue"`로 바꾸면 첫 번째 Object의 `color`는 `"red"`로 남는다. [M01 p.14]

| 실행 상태 | 첫 번째 Object의 `color` | 두 번째 Object의 `color` |
| --- | --- | --- |
| 각각 생성한 직후 | `"red"` | `"red"` |
| 두 번째 Object의 `color` 변경 후 | `"red"` | `"blue"` |

따라서 같은 Class의 Object들이 같은 이름의 Attributes를 갖는다는 것과 하나의 저장 상태를 공유한다는 것은 구분해야 한다. 여기서 다룬 것은 Object별 상태이며, Class 전체가 공유하는 `static` 상태는 별도로 설명한다. [M01 p.14] [M01 p.27]

개념 연결: [[concepts/attributes-and-methods|Attributes와 Methods]]

### Methods: 호출한 Object의 상태로 동작하기

Methods는 Object가 Attributes를 바꾸거나 어떤 작업을 수행하는 방식을 정의한다. Attributes가 상태를 표현한다면, Methods는 그 상태를 사용하거나 바꾸는 동작을 표현한다. 강의는 이 두 요소를 함께 Class의 Members로 소개한다. [STT 02:44] [M01 p.8] [M01 p.17]

자료의 `printHello()`는 호출하면 메시지를 출력하는 Method이다. [M01 p.15]

```java
class Car {
    void printHello() {
        System.out.println("Hello World!");
    }
}

Car car = new Car();
car.printHello();
```

호출 부분은 Object를 가리키는 변수, 점, Method 이름, 괄호로 구성된다. `car.printHello()`는 `car`가 가리키는 Object에 해당 동작을 요청한다. Attribute 접근과 달리 괄호가 붙는다는 점도 구별해야 한다. [M01 p.15]

같은 Method를 사용하더라도 호출 대상의 상태가 다르면 결과가 달라질 수 있다. M01 p.16의 `getSpeed()`는 자신의 Object에 있는 `speed`를 반환한다. 자료에서 `myCar.speed`는 `100`, `yourCar.speed`는 `90`으로 설정되어 있으므로 각 호출 결과도 각각 `100`, `90`이다. [M01 p.16]

```java
class Car {
    int speed;

    int getSpeed() {
        return speed;
    }
}
```

이 예시를 이해하려면 Method의 코드뿐 아니라 **어느 Object를 통해 호출했는지**를 함께 보아야 한다. `myCar.getSpeed()`와 `yourCar.getSpeed()`는 같은 구조의 코드를 사용하지만 서로 다른 Object의 `speed`를 읽는다. [M01 p.16]

이 차이를 곧바로 Polymorphism의 구현 사례라고 부르지는 않는다. 여기서는 같은 Class의 같은 Method가 서로 다른 상태를 읽었기 때문에 결과가 달라진 것이다. 서로 다른 Class가 동작을 다르게 구현한다는 설명과 구분해야 한다. [M01 p.16] [M01 p.61]

**후취득 자료 보충:** M02의 별도 Car 예시에서는 `speed = 10`이지만 `getSpeed()`의 본문은 `return 10*speed;`이다. 따라서 field 접근 결과는 `10`, Method 호출 결과는 `100`이다. M01의 `return speed;`와 다른 코드이므로 Method 이름만 보고 항상 field 값을 그대로 돌려준다고 생각하면 안 된다. STT의 `the task or the object`는 불확실한 주어 표현으로 남긴다. [STT 02:44] [M02 p.6] [M02 p.7]

### Encapsulation(캡슐화): 내부 상태를 보호하고 사용에 필요한 부분 드러내기

Encapsulation은 구성 요소의 **visibility(접근 가능 범위)**를 제한하여 Object 내부의 데이터를 보호하는 개념으로 소개된다. 외부에서 내부 구현을 모두 직접 다루지 않게 하면, 사용하는 쪽은 필요한 동작에 집중할 수 있다. 강의는 이 점을 **abstraction(추상화)**이 쉬워지는 이유와 연결한다. [STT 00:55] [M01 p.59]

**자료 그림 읽기 — Class를 나타내는 캡슐.** M01 p.59의 그림은 Variables와 Methods를 하나의 Class 안에 묶어 놓는다. 그림의 핵심은 관련 요소를 함께 담고, 내부 구현과 외부 접근 사이에 경계를 둔다는 것이다. 캡슐 그림 자체가 구체적인 접근 제어 문법이나 모든 Members의 접근 수준을 지정하는 것은 아니다. [M01 p.59]

<details>
<summary>M01 p.59 — 원본 자료 페이지</summary>

![M01 p.59 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/4.oop/page-059.png)

</details>

**이해를 위한 예시:** 어떤 장치가 내부 상태를 관리하고 외부에는 필요한 동작만 제공한다고 생각해 보자. 장치를 사용하는 쪽은 내부 변수들을 직접 조정하는 대신 제공된 동작을 요청한다. 이렇게 하면 사용자가 내부 저장 방식을 모두 알아야 할 필요가 줄어든다. 이는 Encapsulation의 동기를 설명하기 위한 예시이며, 별도의 과제 명세가 아니다. [M01 p.59]

Encapsulation을 단순히 여러 변수를 Class 안에 모아 쓰는 것으로만 이해하면 데이터 보호와 접근 범위라는 목적을 놓친다. 반대로 이번 소개만으로 구체적인 접근 제어 문법을 모두 배웠다고 보아도 안 된다. 강의는 관련 특징들을 앞으로 더 배운다고 예고하며, 자료의 로드맵도 Encapsulation을 이후 학습 주제와 연결한다. [STT 00:55] [M01 p.58]

**후취득 자료 보충:** M02 p.4도 내부 구현 은닉, 외부 접근으로부터의 data protection, abstraction을 함께 제시한다. Variables와 Methods를 Class 안의 capsule에 묶은 그림은 이 관계를 설명한다. [M02 p.4]

개념 연결: [[concepts/encapsulation|Encapsulation]]

### Inheritance(상속): 공통 특성을 재사용하고 확장하기

Inheritance는 한 Class가 다른 Class의 특성을 이어받는 관계이다. 자료에서는 **parent class(부모 클래스)**의 코드를 **child class(자식 클래스)**가 재사용하고, 필요한 새 특성을 추가할 수 있다고 설명한다. 동기는 비슷한 내용을 매번 별도로 만드는 부담을 줄이는 데 있다. [STT 00:55] [M01 p.60]

**자료 그림 읽기 — 공통 분류와 세부 분류.** M01 p.60의 계층은 `Organisms` 아래에 `Animals`와 `Plants`, 그 아래에 `Duck`·`Cat`, `Tree`·`Grass`를 배치한다. 아래쪽 항목이 위쪽의 더 일반적인 분류와 어떤 관계를 갖는지 읽으면 된다. 이 그림은 공통 특성을 이어받는 구조를 설명하는 비유이지, 이번 시간에 해당 Class들의 구현 코드를 작성했다는 증거는 아니다. [M01 p.60]

<details>
<summary>M01 p.60 — 원본 자료 페이지</summary>

![M01 p.60 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/4.oop/page-060.png)

</details>

Inheritance에서 재사용과 확장은 함께 생각해야 한다. Child class는 Parent class의 특성을 이용하면서 자신에게 필요한 특성을 더할 수 있다. “재사용한다”는 설명을 Parent class와 Child class가 항상 완전히 같은 동작과 상태를 가진다는 뜻으로 읽으면 확장의 의미를 놓치게 된다. [M01 p.60]

강의의 코드 재사용 관련 문장 일부는 전사가 불명확하다. 따라서 그 문장을 정확한 기술 설명으로 복원하지 않고, 특성 계승과 재사용이라는 확인 가능한 개념을 자료로 보충한다. 상세한 Inheritance 문법과 호출 규칙은 이번 소개의 범위로 확정하지 않는다. [STT 00:55] [M01 p.58] [M01 p.60]

불확실한 원문은 `code these codes from other parent codes`이다. 후취득 M02 p.4의 parent/child·code reuse 설명과 Organisms→Animals/Plants 계층 그림은 위 개념을 보충하지만 이 발화를 다시 쓰는 근거로 사용하지 않는다. [STT 00:55] [M02 p.4]

개념 연결: [[concepts/inheritance|Inheritance]]

### Polymorphism(다형성): 비슷한 동작을 서로 다르게 구현하기

Polymorphism은 서로 다른 Class들이 유사한 동작을 갖더라도 실제 수행 방식은 달라질 수 있다는 개념으로 소개된다. 자료는 같은 Parent class를 가진 Class들에서 같은 이름의 Methods를 서로 다르게 구현할 수 있다고 설명한다. [STT 01:51] [M01 p.61]

**자료 그림 읽기 — 같은 `animalSound()`, 서로 다른 소리.** M01 p.61은 개·고양이·오리를 같은 `animalSound()`와 연결한다. 공통된 동작의 이름은 같지만 각 동물이 내는 소리는 다르다. 이 그림에서 읽어야 할 것은 공통 동작을 이야기할 수 있다는 점과 실제 구현 결과가 달라질 수 있다는 점이다. [M01 p.61]

<details>
<summary>M01 p.61 — 원본 자료 페이지</summary>

![M01 p.61 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/4.oop/page-061.png)

</details>

**후취득 자료 보충:** M02 p.4에도 같은 Parent class, 같은 Method 이름, 서로 다른 구현이라는 설명과 개·고양이·오리의 `animalSound()` 그림이 있다. 이는 behavior variation의 개념 예시이며 dispatch의 세부 문법을 추가로 가르쳤다는 증거가 아니다. [M02 p.4]

앞의 `getSpeed()` 예시와 비교하면 차이가 드러난다. 두 `Car`가 같은 Method로 각자의 `speed`를 읽는 것은 Object별 상태의 차이이다. `animalSound()` 그림은 서로 다른 Class가 유사한 동작을 다르게 구현한다는 설명이다. 결과가 다르다는 사실만으로 두 경우를 같은 개념으로 묶지 않아야 한다. [M01 p.16] [M01 p.61]

이번 녹음은 Polymorphism의 동기를 짧게 소개한다. 어떤 Method 구현이 선택되는지에 관한 상세한 Java 규칙이나 복잡한 Inheritance 코드를 이 회차에서 모두 배웠다고 확대하지 않는다. [STT 01:51] [M01 p.58]

개념 연결: [[concepts/polymorphism|Polymorphism]]

### Constructor(생성자): Object가 만들어질 때 초기 상태 정하기

Constructor는 Object 생성 과정에서 초기화를 수행하는 특별한 구성이다. Class와 이름이 같고 **return type(반환형)**을 쓰지 않는다. 강의와 자료에는 Method에 빗댄 입문적 표현이 있지만, 노트에서는 일반 Method와 구분하여 이해한다. [STT 03:40] [M01 p.19]

초기화가 필요한 이유는 새 Object가 어떤 상태로 출발할지를 정해야 하기 때문이다. 자료의 가장 단순한 예시는 `Car()`가 생성 메시지를 출력하도록 한다. `new Car()`에 의해 Object가 만들어질 때 그 Constructor의 동작을 확인할 수 있다. [M01 p.20]

Constructor는 **Parameters(매개변수)**를 받아 Attributes를 초기화할 수 있다. 한 Class 안에 Parameters가 서로 다른 여러 Constructor를 정의하는 것도 가능하다. [STT 04:37] [M01 p.21]

M01 p.22는 `weight`와 `color`를 가진 `Car`로 이 차이를 설명한다.

```java
class Car {
    int weight;
    String color = "unknown";

    Car(int w) {
        weight = w;
    }

    Car(int w, String c) {
        weight = w;
        color = c;
    }

    void printInfo() {
        System.out.println(weight + " kg, " + color);
    }
}
```

`new Car(1500, "red")`는 두 Parameters를 받는 Constructor를 사용한다. 따라서 `weight`는 `1500`, `color`는 `"red"`가 된다. `new Car(2000)`는 한 Parameter를 받는 Constructor를 사용하므로 `weight`만 `2000`으로 설정하고, `color`는 선언에서 지정한 `"unknown"`을 유지한다. [M01 p.22]

| 생성 표현 | `weight` | `color` | `printInfo()`의 출력 |
| --- | --- | --- | --- |
| `new Car(1500, "red")` | `1500` | `"red"` | `1500 kg, red` |
| `new Car(2000)` | `2000` | `"unknown"` | `2000 kg, unknown` |

Constructor에 반환값이 없다는 설명을 보고 `void`를 붙이면 안 된다. 자료가 말하는 것은 return type 자체를 선언하지 않는다는 것이다. 또한 Object를 만든 뒤 일반 Method처럼 임의로 호출하는 구조와 구별해야 한다. [M01 p.19]

**노트의 보충 설명:** 녹음의 “생성될 때 호출된다”는 설명을 Constructor 본문이 어떤 경우에도 오직 하나만 한 번 실행된다는 보편 규칙으로 확대하지 않는다. 자료는 다른 Constructor 안에서 Constructor를 호출할 수 있다는 예외를 명시한다. 이 구분은 전사 내용을 바꾼 것이 아니라 자료에 근거하여 개념을 정확히 읽기 위한 보충이다. [STT 03:40] [M01 p.19]

**후취득 자료 보충:** M02 p.8의 `Car(int carNumber, String model)`은 `this.carNumber`와 `this.model`을 Parameters로 초기화하고 `Car initialized.`를 출력하는 예시이다. `new Car(1234, "Sonata")`에 대응하는 fields는 `1234`와 `"Sonata"`이다. 다만 인쇄된 조회 코드는 선언한 `myCar` 대신 `mayCar.model`을 사용한다. 출력 상자의 `Car initialized.`와 `1234 Sonata`는 자료가 제시한 결과이며, 이 철자 불일치를 둔 코드가 그대로 성공 실행한다는 뜻은 아니다. STT의 `special class methods`, `usually called once`, `cannot be called explicitly from the outside` 및 PDF의 단순화는 Constructor의 초기화 역할과 다음 `this(...)` 연결 예시를 함께 읽어야 한다. [STT 03:40] [M02 p.8] [M02 p.9]

개념 연결: [[concepts/constructor|Constructor]]

### `this`: 현재 Object와 같은 이름의 Parameter 구별하기

`this`는 현재 Object를 가리키는 키워드이다. 자료의 표현으로는 **self-object(자기 자신인 객체)**를 나타낸다. 현재 Object의 Attributes와 Methods를 명시적으로 가리킬 때 사용한다. 녹음에서 이 표현을 설명하는 명사 일부는 불명확하므로, 정확한 발화를 추정하지 않고 자료의 정의로 개념을 보충한다. [STT 04:37] [M01 p.23]

이름이 충돌하지 않는 경우 `this`를 생략할 수 있다. M01 p.24에서는 `this.color`와 `color`가 같은 Object의 Attribute를 가리킨다. 처음 `"red"`를 대입하고 이후 `"blue"`를 대입하면 같은 Attribute의 값이 순서대로 바뀐다. 두 개의 독립된 `color`가 만들어지는 것이 아니다. [M01 p.24]

Methods를 호출할 때도 같은 원리가 적용된다. M01 p.25의 `this.speedAndUnit()`는 현재 Object의 Method를 호출한다. 자료의 해당 문맥에서는 `this`를 생략해도 같은 Method를 호출한다. [M01 p.25]

`this`의 필요성이 가장 분명한 경우는 Parameter와 Attribute의 이름이 같을 때이다. 자료에는 다음 두 Methods가 나란히 제시된다. [M01 p.26]

```java
class Car {
    int speed = 30;

    void cantChangeSpeed(int speed) {
        speed = speed;
    }

    void changeSpeed(int speed) {
        this.speed = speed;
    }
}
```

첫 번째 Method의 `speed = speed`에서 양쪽 이름은 모두 Parameter를 가리킨다. 같은 Parameter에 자신의 값을 다시 넣으므로 Object의 `speed`는 바뀌지 않는다. 두 번째 Method의 왼쪽 `this.speed`는 현재 Object의 Attribute, 오른쪽 `speed`는 전달받은 Parameter이다. 따라서 두 번째 대입은 외부에서 받은 값을 Object의 상태에 반영한다. [M01 p.26]

**이해를 위한 예시:** 위 자료 코드의 두 Methods에 각각 `70`을 전달한다고 하자. 초기 `speed`가 `30`인 Object에서 `cantChangeSpeed(70)`을 호출하면 Object의 값은 여전히 `30`이다. 이어서 `changeSpeed(70)`을 호출하면 Object의 값이 `70`이 된다. 숫자 `70`은 설명을 위해 정한 값이며 이번 녹음의 실제 호출값을 복원한 것이 아니다. [M01 p.26]

자료는 `this()`를 다른 Constructor를 호출하는 용도로도 소개한다. 다만 세부 설명은 뒤의 학습으로 미룬다. `this.speed`, `this.speedAndUnit()`, `this()`를 모두 같은 형태의 Attribute 접근으로 혼동하지 않아야 한다. [M01 p.23]

**후취득 자료 보충:** M02 p.9의 두 코드 영역은 같은 Car Class의 연속이다. `this.speed`·`this.color`는 현재 Object의 fields, `this.printSpeed()`는 현재 Object의 Method 호출이다. Parameters가 없는 `Car()`의 `this(55, "blue")`는 두 Parameters를 받는 Constructor로 초기화를 연결한다. 선언 초기값은 `100`과 `"red"`이지만 이 생성 경로를 거치면 `55`와 `"blue"`가 된다. Constructor 본문 둘이 연결되어도 만들어지는 Car가 두 개라는 뜻은 아니다. 이는 제한된 자료 예시이며, STT의 `self -confidence or the So`를 복원한 발화는 아니다. [STT 04:37] [M02 p.9]

강의자는 `this`를 이전 강의에서 다룬 내용 중 중요하다고 보아 복습에 포함했다고 설명한다. 확인되는 강조는 여기까지이며, 이를 시험 출제 가능성에 대한 예측으로 바꾸지 않는다. [STT 04:37]

**기출 적용 — 부분 연결.** 2025-1 중간고사 복기본의 5번은 `this`와 `super`를 설명하고 비교하는 요구이다. 현재 내용으로는 `this`가 현재 Object를 가리킨다는 부분을 설명할 수 있다. 그러나 문항 전체의 비교를 완성하려면 상위 Class를 다루는 `super`와 관련 Inheritance 규칙이 더 필요하다. 풀이를 준비할 때는 각 키워드가 무엇을 가리키는지를 먼저 구별해야 하며, `this`의 이름 충돌 예시만 이해했다고 문항 전체가 해결되었다고 판단하면 안 된다. [EX:cp_2025_1_midterm_q05 p.3]

개념 연결: [[concepts/this-keyword|this]]

### `static` Members: Object별 상태와 Class의 공유 상태

`static` Members는 개별 Object보다 Class에 연결되는 Members이다. 같은 Class의 여러 Object가 공통된 값을 보아야 할 때 `static` variable(정적 변수)로 설명할 수 있다. 반면 **instance variable(인스턴스 변수)**은 각 Object의 상태를 나타낸다. [STT 05:41] [M01 p.27] [M01 p.28]

**자료 그림 읽기 — 전체 개수와 개별 브랜드.** M01 p.27은 `Total number: 3`을 Class 쪽에, 서로 다른 자동차 브랜드를 각각의 Object 쪽에 놓는다. 전체 개수는 세 Object가 공유하는 정보이고, 각 자동차의 브랜드는 개별 Object의 정보이다. 그림의 화살표를 따라 “이 값은 Class 전체에 하나인가, Object마다 따로 있는가?”를 구별하면 된다. [M01 p.27]

<details>
<summary>M01 p.27 — 원본 자료 페이지</summary>

![M01 p.27 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/4.oop/page-027.png)

</details>

이번 강의의 고유 예시는 `static String owner`이다. 녹음에서는 Constructor 입력으로 이 값을 바꾸고, `"Carol"`로 변경한 값을 다른 Instance에서도 관찰하는 상황을 설명한다. 이후 Class의 `owner`를 `"David"`로 바꾸면 여러 Instance를 통해 같은 새 값을 관찰한다고 설명한다. 핵심은 같은 이름을 여러 Object에 복사해 둔 것이 아니라 Class에 속한 공유 상태를 보고 있다는 점이다. [STT 05:41] [STT 06:33]

최초 작성 당시에는 해당 예시의 원 코드가 없어 초기 문자열과 구문을 확정하지 않았다. **후취득 자료 보충:** M02 p.10은 `static String owner`, Constructor의 `this.owner = owner`, `static void printOwner()`를 제시한다. `new Car("Alice")` 다음 `new Car("Bob")`를 실행하면 공유 `owner`는 `"Bob"`이다. 이어 `car1.owner = "Carol"` 뒤 `car2.printOwner()`는 `Carol`, `Car.owner = "David"` 뒤 `Car.printOwner()`는 `David`를 출력한다. Instance를 통한 표기에서도 같은 static field를 사용하므로 서로 다른 Object의 독립된 owner를 바꾼 것으로 읽으면 안 된다. 자료는 코드를 확인하게 해 주지만 STT의 `to entrance the owner variable`, `to the main David`는 불확실한 원문으로 유지한다. [STT 05:41] [STT 06:33] [M02 p.10]

자료는 Object를 먼저 생성하지 않고 `Car.num`, `Car.printNum()`처럼 Class를 통해 `static` Members에 접근하는 예시를 제시한다. Class에 연결된 값이나 동작임을 코드에서도 드러내는 방식이다. [M01 p.28] [M01 p.29]

M01 p.30은 Object별 상태와 공유 상태를 한 코드 안에서 비교한다. Constructor는 `num`을 증가시키고, `setMile()`은 해당 Object의 `mile`을 설정하면서 전달받은 값을 `totalMile`에 누적한다. [M01 p.30]

```java
class Car {
    static int num;
    static int totalMile;
    int mile;

    Car() {
        num++;
    }

    void setMile(int mile) {
        this.mile = mile;
        totalMile += mile;
    }
}
```

자료처럼 세 Object를 생성하고 각각 한 번씩 `20`, `30`, `40`으로 `setMile()`을 호출하면 다음 상태가 된다. [M01 p.30]

| 상태의 소속 | 변수 | 결과 |
| --- | --- | --- |
| Class 공유 | `num` | `3` |
| Class 공유 | `totalMile` | `90` |
| 첫 번째 Object | `mile` | `20` |
| 두 번째 Object | `mile` | `30` |
| 세 번째 Object | `mile` | `40` |

공유 누적값은 다음 계산으로 확인할 수 있다.

$$
20 + 30 + 40 = 90
$$

이 결과는 자료에 제시된 호출 순서에 대한 결과이다. `totalMile`이라는 이름만 보고 어떤 호출 순서에서도 모든 Object의 현재 `mile` 합계가 자동으로 유지된다고 판단하면 안 된다. 실제 코드는 `setMile()`을 부를 때마다 입력값을 더한다. 의미를 판단할 때는 변수 이름과 대입·누적 연산을 함께 읽어야 한다. [M01 p.30]

개념 연결: [[concepts/static-members|static Members]]

### `static` Method의 접근 제한: 어느 Object의 값인지 지정하기

`static` Method는 특정 Object를 통한 호출 없이도 사용할 수 있다. 따라서 Object마다 값이 다른 instance variable을 이름만으로 사용하면 어느 Object의 값을 뜻하는지 결정할 수 없다. 강의의 `totalFuel()` 예시는 이 문제를 보여 준다. [STT 07:35] [M01 p.31] [M01 p.32]

다음은 자료에 실린 오류 예시이다.

```java
class Car {
    float fuel;

    static float totalFuel() {
        return fuel; // 어느 Object의 fuel인지 지정되지 않음
    }
}
```

`fuel`은 Object별 Attribute인데, `static` Method 안의 `return fuel`에는 그 값을 읽을 대상 Object가 없다. 자료는 이를 오류로 표시한다. 정상 실행 뒤 어떤 값이 출력되는 문제가 아니라 Class의 코드가 유효한지부터 판단해야 하는 문제이다. 녹음의 결과 설명은 문장 중간에서 불완전하므로, 자료의 오류 판정을 완성된 강의 발화로 바꾸지는 않는다. [STT 07:35] [M01 p.32]

**후취득 자료 보충:** M02 p.11의 Output 상자에는 `non-static variable fuel cannot be referenced from a static context`라는 오류가 실려 있다. 이것은 receiver 없이 `return fuel`을 쓴 데 대한 compile error(컴파일 오류)이며 `new Car()`를 실행한 뒤 출력되는 메시지가 아니다. STT의 `we use a non -static method called the non -static variable fuel`과 끝이 잘린 `the program will actually print out that there's no.`는 그대로 불명확하다. [STT 07:35] [M02 p.11]

**노트의 보충 설명:** 자료의 제한은 대상 Object를 지정하지 않은 직접 접근을 뜻하는 것으로 읽어야 한다. `static` 코드에서 명시한 Object를 통해 상태를 읽는 것까지 전부 금지한다는 의미는 아니다. 핵심은 `static`이라는 단어 자체보다 “어느 Object를 통해 접근하는가?”이다. [M01 p.31]

**이해를 위한 예시:** 다음은 그 차이를 보여 주기 위해 별도로 만든 작은 코드이다.

```java
class Meter {
    int value = 4;

    static int read(Meter meter) {
        return meter.value;
    }
}
```

`read()`는 `static`이지만 `meter.value`에는 값을 읽을 Object가 지정되어 있다. 여기서는 `meter`가 실제 `Meter` Object를 가리킨다는 조건으로 읽는다. 앞의 `return fuel`과 달리 Object별 상태를 어디에서 가져오는지가 분명하다. 이 예시는 현재 Lab3의 구현 코드가 아니다. [M01 p.31] [M01 p.32]

반대 방향으로, instance Method 안에서 `static` Method를 호출하는 것은 자료에서 가능하다고 설명한다. 개별 Object의 동작이 Class에 연결된 기능을 사용하는 상황이다. [M01 p.31]

**기출 적용 — 부분 연결, 후속 선수 필요.** 2025-1 중간고사 복기본의 7번은 `static` 중첩 Class에서 바깥 Object의 상태에 접근하는 오류를 분석하고, 지정된 Method 본문을 유지하면서 코드를 수정하도록 요구한다. 현재 설명의 “어느 Object의 값인가?”라는 질문은 오류 분석의 출발점으로 연결된다. 다만 원문에는 중첩 Class의 의미와 수정 제약이 추가되므로, 단순히 모든 변수를 `static`으로 바꾸는 일반 규칙으로 읽거나 이번 예제와 같은 문제라고 간주하면 안 된다. 문항 전체의 수정은 중첩 Class를 배운 뒤 다룰 범위이다. [EX:cp_2025_1_midterm_q07 p.4]

### Object의 `==`: 내용이 같은가, 같은 Object를 가리키는가

Object에 대한 `==`는 두 reference가 같은 Object를 가리키는지 비교한다. Object의 내용이 같다는 사실과 같은 Object라는 사실은 구별해야 한다. 강의와 자료는 이를 주소에 빗대어 설명하지만, 실제 물리적 메모리 주소의 표현이나 배치를 보장한다는 뜻으로 확대하지 않는다. [STT 08:32] [M01 p.34]

자료는 내용이 같은 두 `String` Object를 각각 만든 뒤, 첫 번째 reference를 세 번째 변수에 대입한다. [M01 p.34] [M01 p.35]

```java
String str1 = new String("hey");
String str2 = new String("hey");
String str3 = str1;

System.out.println(str1 == str2); // false
System.out.println(str1 == str3); // true
```

첫 두 줄에는 각각 `new`가 있다. 따라서 같은 `"hey"`를 담고 있어도 서로 다른 Object가 만들어진다. 세 번째 줄은 새 Object를 만들지 않고 `str1`의 reference 값을 `str3`에 넣는다. 그러므로 `str1`과 `str3`는 같은 Object를 가리킨다. [M01 p.35]

**자료 그림 읽기 — 같은 문자열이 들어 있는 두 칸.** M01 pp.34–35의 메모리 그림에는 `"hey"`가 두 번 나타난다. 두 칸의 내용이 같다는 데서 멈추지 말고, `str1`과 `str2`가 서로 다른 Object에 대응한다는 사실을 읽어야 한다. 자료가 제시하는 두 비교 결과는 차례로 `false`, `true`이다. [M01 p.34] [M01 p.35]

<details>
<summary>M01 p.35 — 원본 자료 페이지</summary>

![M01 p.35 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/4.oop/page-035.png)

</details>

**전사 주의:** 08:32의 부정 표현은 서로 다른 Object를 비교하는 상황 및 자료의 `false` 결과와 충돌한다. 그 발화의 정확한 의도는 **검증 필요**이다. 위 결과는 자료 코드와 reference 관계에 근거한 설명이며, 불명확한 발화를 이렇게 말했다고 고쳐 쓴 것이 아니다. [STT 08:32] [M01 p.35]

**후취득 자료 보충:** M02 p.12의 Car 예시에서도 각각 `new Car()`로 만든 `car1`, `car2`의 `==` 결과는 `false`이고, `car2 = car1` 뒤에는 `true`이다. 대입된 것은 reference 값이며 새 Car의 생성·복제나 Object의 물리적 이동을 뜻하지 않는다. 이제 같은 Car 예시의 자료 결과를 확인할 수 있지만 STT의 `We're not using the double equality. We don't get false`라는 충돌하는 부정 표현은 고치지 않는다. [STT 08:32] [M02 p.12]

이 예시에서 피해야 할 오개념은 문자열 내용이 같으므로 `==`도 참일 것이라고 판단하는 것이다. 먼저 Object가 몇 개 만들어졌는지, 각 변수가 어느 Object를 가리키는지 확인해야 한다. [M01 p.35]

개념 연결: [[concepts/object-reference|Object reference]]

### Object reference의 `=`: 새 Object 생성과 reference 대입 구분하기

Object를 가리키는 변수에 `=`로 다른 reference를 대입하면, 같은 Object를 가리키는 변수가 늘어날 수 있다. 대입만으로 Object 자체가 복제되거나 이동하는 것은 아니다. [STT 08:32] [M01 p.36]

자료의 예시는 다음과 같다.

```java
Car myCar = new Car();
Car car1 = myCar;
Car car2 = myCar;
Car car3 = car1;
```

첫 줄에서 하나의 `Car` Object를 만든다. 이후 세 줄은 기존 reference 값을 대입한다. 따라서 변수는 네 개이지만 이 코드에서 생성된 `Car` Object는 하나이다. `car3`도 `car1`을 통해 같은 reference 값을 받으므로 다른 변수들과 같은 Object를 가리킨다. [M01 p.36]

**자료 그림 읽기 — 변수 개수와 Object 개수.** M01 p.36의 메모리 그림에는 `Car` Object가 하나만 표시된다. 그림을 읽을 때 변수 이름을 셈하는 것과 Object 생성을 셈하는 것을 분리해야 한다. 이 예시에서는 `new`가 한 번만 실행된다는 점이 Object 수를 판단하는 근거이다. 이를 어떤 코드에서도 `new`라는 글자의 개수만 세면 된다는 규칙으로 확대하지는 않는다. [M01 p.36]

<details>
<summary>M01 p.36 — 원본 자료 페이지</summary>

![M01 p.36 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/4.oop/page-036.png)

</details>

이 설명은 앞의 Object별 Attributes와 모순되지 않는다. 서로 다른 두 Object는 각자의 상태를 가지지만, 두 변수가 같은 Object를 가리킨다면 그 둘은 같은 상태를 보는 것이다. “변수 이름이 다르다”는 이유만으로 서로 다른 Object라고 판단하지 않아야 한다. [M01 p.14] [M01 p.36]

08:32에서 Object의 주소가 바뀐다고 표현한 부분도 노트에서는 reference 변수의 대입으로 구분해 읽는다. Object 자체가 다른 위치로 이동했다는 실행 사실을 전사에서 확정할 수는 없다. [STT 08:32]

### Call-by-Value(값 전달): 바깥 변수와 안쪽 Parameter는 별개

Call-by-Value는 Method를 호출할 때 값을 복사하여 전달하는 방식이다. 강의는 정수 값을 전달한 뒤 Method 내부에서 받은 값을 바꾸어도, 호출한 쪽의 변수는 그대로 남는 상황을 설명한다. [STT 09:27] [STT 10:14] [M01 p.38]

여기서 중요한 구별은 변수의 이름보다 변수가 속한 위치이다. 호출한 쪽의 변수와 호출된 Method의 Parameter는 서로 다른 변수이다. 같은 이름을 사용하더라도 하나의 저장 공간을 공유하는 것이 아니다. [M01 p.37] [M01 p.39]

자료의 `swap()`은 이 차이를 드러낸다. 다음 Method는 전달받은 두 정수의 값을 내부에서 교환한다. [M01 p.37]

```java
static void swap(int a, int b) {
    int temp;
    temp = a;
    a = b;
    b = temp;
}
```

자료의 호출부에서 `a = 2`, `b = 3`을 준비하여 `swap(a, b)`를 호출하면, Method 진입 시 복사된 Parameters도 처음에는 `2`, `3`이다. 이후 `temp`가 `2`를 보관하고, 내부 `a`가 `3`, 내부 `b`가 `2`가 된다. 그러나 이 대입들은 호출한 쪽의 `a`, `b`를 대상으로 하지 않는다. [M01 p.37] [M01 p.39]

**자료 표 읽기 — 반환 직전의 두 영역.** M01 p.39의 메모리 표는 `main`과 `swap` 영역을 분리한다. 같은 `a`, `b`라는 이름이 두 영역에 등장하므로 영역을 생략하고 숫자만 추적하면 잘못된 결론에 도달한다. [M01 p.39]

<details>
<summary>M01 p.39 — 원본 자료 페이지</summary>

![M01 p.39 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/4.oop/page-039.png)

</details>

| 반환 직전의 위치 | `a` | `b` | `temp` |
| --- | --- | --- | --- |
| 호출한 `main` | `2` | `3` | 해당 없음 |
| 호출된 `swap` | `3` | `2` | `2` |

따라서 자료의 `main`에서 출력되는 값은 `2 3`이다. `swap` 내부에서 값 교환이 일어났다는 사실과 호출자의 두 변수가 교환되었다는 주장은 다르다. [M01 p.37] [M01 p.39]

녹음에서는 `number`라는 정수 변수로 같은 원리를 설명하지만 최초 STT만으로 수치와 코드 전체를 확정하지 않았다. 위 `swap`은 별도의 M01 자료 예시이다. **후취득 자료 보충:** M02 p.13에서는 caller의 `number = 10`을 `changeNumber(int number)`에 전달하고 Method 안의 Parameter에 `100`을 대입한다. 바뀐 것은 복사된 Parameter이므로 caller가 출력하는 값은 `10`이다. 자료로 이 코드와 수치를 확인해도 STT의 지시 대상이 불명확한 `the local variable itself will not be changed`나 `click the value number`를 교정한 발화로 만들지는 않는다. [STT 09:27] [STT 10:14] [M01 p.37] [M02 p.13]

개념 연결: [[concepts/call-by-value|Call-by-Value]]

### Object 전달: reference 값의 복사와 Object 상태 변경

Object를 Method에 전달했을 때는 reference 값이 복사된다. 복사된 reference가 원래와 같은 Object를 가리키므로, Method 안에서 그 Object의 Attributes를 바꾸면 호출한 쪽에서도 변경을 관찰할 수 있다. 이것이 정수 Parameter 자체를 바꾸는 경우와 다른 관찰 결과를 만드는 이유이다. [STT 11:07] [STT 11:52] [M01 p.42]

**노트의 보충 설명:** 강의와 자료는 이 부분을 Call-by-Reference(참조 전달)라는 이름으로 소개한다. 그러나 Java의 전달 방식은 reference 값까지 포함하여 **값 전달**로 이해해야 한다. 자료 p.42도 전달되는 reference의 값을 생각하면 Call-by-Value로 볼 수 있다는 보충을 포함한다. 원래 변수 자체를 Method의 Parameter와 하나로 연결하는 방식으로 해석하지 않는다. [STT 10:14] [M01 p.42]

이번 강의의 예시는 `Car`의 `speed`를 `changeSpeed` 안에서 바꾸는 상황이다. 전달된 reference가 같은 Car를 가리켜 field 변경이 호출자에게 보인다는 설명을 보존한다. 정확한 선언과 수치는 최초 STT만으로 확정하지 않았다. **후취득 자료 보충:** M02 p.14의 Car는 `speed = 50`으로 시작하며 `changeSpeed(Car car)`가 `car.speed = 100`으로 바꾼 뒤 caller의 `car1.speed` 출력은 `100`이다. 복사된 reference로 같은 Object를 변경한 결과이다. [STT 11:07] [STT 11:52] [M02 p.14]

M02 p.14의 `Passing an object is done by Call-by-Reference`는 Java의 전달 규칙을 정확히 표현하지 못한다. Java는 reference 값도 값으로 전달한다. Parameter가 같은 Object를 가리키므로 그 field를 바꿀 수 있지만 caller의 reference 변수 자체를 전달받는 것은 아니다. 이는 M01 p.42의 값 전달 보충과 연결한 설명상의 교정이며, STT의 `the variable called memory space`, `takes the Car class as an input`, `memory reference points of the class`를 수정한 발화가 아니다. [STT 10:14] [STT 11:07] [STT 11:52] [M01 p.42] [M02 p.14]

자료의 `IntHolder`는 정수 하나를 Attributes로 가진 Class이다. [M01 p.40]

```java
class IntHolder {
    int value;

    IntHolder(int value) {
        this.value = value;
    }
}
```

앞서 배운 Constructor와 `this`가 함께 사용된다. Constructor의 Parameter `value`를 현재 Object의 `this.value`에 넣는다. 이후 자료의 `swap()`은 Parameters 자체가 아니라 각 Object의 `value`를 바꾼다. [M01 p.40] [M01 p.41]

```java
static void swap(IntHolder a, IntHolder b) {
    int temp = a.value;
    a.value = b.value;
    b.value = temp;
}
```

서로 다른 두 `IntHolder`의 초기 `value`가 각각 `2`, `3`이라면 다음 순서로 진행된다.

1. `temp`에 첫 번째 Object의 값 `2`를 보관한다.
2. 첫 번째 Object의 `value`에 두 번째 Object의 값 `3`을 대입한다.
3. 두 번째 Object의 `value`에 보관한 값 `2`를 대입한다.

결과적으로 호출한 쪽에서 두 Object의 Attributes를 읽으면 `3 2`를 관찰한다. Object를 가리키는 호출자 변수들을 서로 교환한 것이 아니라, 두 Object 내부의 정수 값을 바꾼 것이다. [M01 p.41] [M01 p.43]

**자료 표 읽기 — 바뀐 것은 Object 안의 `value`.** M01 p.43은 두 Object의 `value`가 각각 `3`, `2`가 된 상태와 `swap`의 지역변수 `temp = 2`를 보여 준다. 앞의 정수 `swap` 표와 비교할 때는 대입 대상이 Parameter 자체인지, reference를 따라간 Object의 Attribute인지 확인해야 한다. [M01 p.39] [M01 p.43]

<details>
<summary>M01 p.43 — 원본 자료 페이지</summary>

![M01 p.43 — 원본 자료 페이지](https://jhlee1020lee.github.io/2026-fall-study-hub/static/page_cache/computer_programming/4.oop/page-043.png)

</details>

| 대입의 대상 | 대입 예 | 호출한 쪽에서 관찰할 핵심 |
| --- | --- | --- |
| 정수 Parameter 자체 | `a = b` | 호출자의 정수 변수는 그대로이다. |
| 같은 Object의 Attribute | `a.value = b.value` | 해당 Object의 상태 변경이 보인다. |

여기서 “복사된다”는 말을 Object 전체가 복사된다는 뜻으로 읽으면 안 된다. 복사되는 것은 reference 값이다. 또한 Parameter에 다른 reference를 넣는 행위와, 그 reference로 접근한 Object의 Attributes를 바꾸는 행위도 구별해야 한다. [M01 p.36] [M01 p.42]

이 `IntHolder` 예시는 현재 개념을 보충하는 M01 pp.40–43의 자료이다. 뒤의 별도 `More on swaps` 절을 이번 실습에서 다뤘다는 근거로 확장하지 않는다.

### Memory Management(메모리 관리)와 Garbage Collector(가비지 컬렉터)

Java의 Memory Management는 자료에서 동적이고 자동적인 과정으로 소개된다. 새 Object를 만들 때 필요한 메모리가 할당되고, 더 이상 사용되지 않는 Object의 메모리는 Garbage Collector가 회수하는 역할을 맡는다. [STT 11:52] [STT 12:50] [M01 p.44]

이 설명에서 두 단계를 구별해야 한다. 하나는 Object가 만들어질 때 필요한 공간을 마련하는 일이고, 다른 하나는 더 이상 필요하지 않은 Object가 사용하던 공간을 회수하는 일이다. 자료는 이를 C에서 명시적으로 할당하고 해제하는 경우와 대비하여 설명한다. [M01 p.44]

자동으로 처리된다는 말은 프로그래머가 Object 생성 표현을 작성할 필요가 없다는 뜻이 아니다. 앞의 예시들은 여전히 `new Car()`와 `new IntHolder(...)`를 사용한다. 자동화되는 메모리 관리와 프로그램이 언제 어떤 Object를 만들지 정하는 일은 구별해야 한다. [M01 p.36] [M01 p.40] [M01 p.44]

**이해를 위한 예시:** 여러 변수가 같은 Object를 가리키는 앞의 `Car` 예시에서, 변수 하나를 더 이상 사용하지 않는다는 사실만 보고 Object 전체가 불필요해졌다고 결론 내릴 수는 없다. 다른 변수가 여전히 같은 Object를 가리킬 수 있기 때문이다. reference 변수 하나의 상태와 Object 전체의 사용 관계를 나누어 보아야 한다. [M01 p.36] [M01 p.44]

강의는 정확한 회수 시점이나 알고리즘을 제공하지 않는다. 따라서 어떤 대입문 직후 즉시 메모리가 회수된다고 주장하거나, 특정 시점의 회수를 코드의 정상 동작 조건으로 삼지 않는다. [STT 12:50] [M01 p.44]

**후취득 자료 보충:** M02 p.15도 Object memory allocation과 unused Objects의 memory 회수를 자동 관리로 소개한다. STT의 `you do not have to allocate any memory for objects explicitly`는 자동 관리에 관한 입문적 표현이지 프로그램에서 Object 생성 자체를 생략하라는 뜻이 아니다. Lab3의 health가 0이 되거나 Fight가 끝나는 것은 simulation의 상태이며, 그 순간 Player Object가 없어지거나 Garbage Collector가 즉시 실행된다는 명세는 없다. [STT 11:52] [M02 p.15] [M02 p.23]

**기출 적용 — 역할 설명은 연결되며, 엄밀한 조건은 추가 학습 필요.** 2025-1 중간고사 복기본의 3번은 Garbage Collector의 역할과 `new`로 생성한 Object가 수거 대상이 되는 조건을 함께 묻는다. 현재 범위에서는 자동 메모리 관리와 사용되지 않는 Object의 공간 회수라는 역할을 설명할 수 있다. 조건 부분을 엄밀히 답하려면 프로그램에서 해당 Object로 도달할 수 있는 reference 관계를 판단하는 기준이 더 필요하다. 접근할 때는 “수거 대상이 되는 조건”과 “실제로 회수되는 시점”을 구분해야 하며, 둘을 같은 것으로 쓰는 것이 함정이다. 이 노트는 원문 전체에 대한 공식 정답을 제공하는 것으로 보지 않는다. [EX:cp_2025_1_midterm_q03 p.2]

개념 연결: [[concepts/garbage-collection|Garbage Collector]]

### Lab3: OOP 복습을 fighting game에 적용하는 범위와 작업 안내

Lab3는 두 플레이어가 대결하는 프로그램에 Object·Class·Attributes·Methods를 적용하는 실습이다. 녹음은 OOP 복습과 게임 구현 안내를 두 부분으로 구분하며, 12:50 무렵 Garbage Collector에서 과제 안내로 넘어간다. 두 플레이어는 round마다 차례로 공격하거나 회복하고, 마지막 round 뒤 승자를 정한다. [STT 00:00] [STT 12:50] [STT 13:47]

최초 STT에서는 `Player`, `Fight`, 실행용 main class의 역할을 확인했지만 실행용 Class의 철자와 전체 진행 명세까지 확정하지 않았다. **후취득 공식 자료 보충:** 9월 19일 확보한 M02 p.18과 M03는 정확한 이름을 `Player`, `Fight`, `Main`으로 확인해 준다. 아래 Fight/Main 설명도 이 후취득 자료에 근거한다. 9월 17일 녹음의 끝은 여전히 17:39의 미완성 Player 설명이며, 이후 명세가 실제 발화되었다고 단정하지 않는다. 전환부의 `the 93 for the option for each program`, `the third program class`, `required to enter the three related classes`, `the first problem in the...`는 불확실하거나 중단된 원문으로 남긴다. [STT 12:50] [STT 13:47] [STT 17:39] [M02 p.18] [M03 p.1] [M03 p.2] [M03 p.3]

**Project setup(프로젝트 준비):** eTL의 Lab3에서 skeleton ZIP을 받고, 압축을 푼 폴더를 IntelliJ project로 열어 지정된 source files에 작성한다. 이 작업 순서는 STT와 후취득 PDF가 함께 뒷받침한다. M03의 `src`에는 `Main.java`, `Fight.java`, `Player.java`가 있다. [STT 13:47] [M02 p.17] [M03 p.1] [M03 p.2] [M03 p.3]

**Submission contract(제출 규칙):** 후취득 M02 p.29에는 성적에 반영되는 과제를 **10월 1일 오후 6시 29분까지 eTL에 제출**하라고 적혀 있다. `src` 폴더가 없으면 만들고, 그 안에 `Main.java`, `Fight.java`, `Player.java`를 넣는다. `src` 폴더를 ZIP으로 압축하고 본인의 학번·이름을 사용한 `20XX-XXXXX_{name}.zip` 형식으로 이름을 정해 업로드한다. 이는 자료에 적힌 행정 안내이며 녹음에서 복원한 마감 발화가 아니다. [M02 p.29]

**Time 안내와 round 제한의 구분:** M02 p.3은 각 구현 단계에 `0:10`, p.16은 `0:15`, pp.22·26·28은 `15mins`를 표시한다. 서로 다른 실습 시간 안내를 simulation의 실행 시간이나 round 수로 바꾸어 읽지 않는다. 게임의 최대 round 수는 별도 Fight 명세의 기본값 `100`이다. [M02 p.3] [M02 p.16] [M02 p.22] [M02 p.24] [M02 p.26] [M02 p.28]

### `Player`: 관리할 상태와 제공할 동작 나누기

`Player`는 플레이어의 식별 정보와 health를 관리하는 Class이다. 식별 정보와 health는 Attributes이며 공격·피해 반영·회복·생존 판단·행동 선택은 Methods이다. 최초 STT의 `The health attribute should be and`는 자료형 설명이 잘린 문장이다. 후취득 자료에서 `int`를 확인하더라도 이 발화를 채우지는 않는다. `we will play the player class`, `attributes for the third class`도 불확실한 원문으로 유지한다. [STT 14:47] [STT 15:41]

**후취득 공식 자료의 Field contract(필드 명세):** 다음 선언은 M02 pp.20–22와 M03 Section 3에서 확인된다. `Random`은 `java.util.Random`이며 `random` field에는 명시적 access modifier(접근 지정자)가 없다. [M02 p.20] [M02 p.22] [M03 p.3]

| 선언 | 역할과 초기 상태 |
| --- | --- |
| `private String userId` | 플레이어의 ID를 저장한다. PDF Constructor 예시에서 입력 ID를 넣는다. |
| `private int health = 50` | 현재 health이며 field 선언에서 초기값 50을 지정한다. |
| `Random random` | 해당 Player의 random 동작에 사용할 난수 생성 Object를 가리킨다. PDF Constructor 예시는 seed로 초기화한다. |

`private`로 선언한 ID와 health를 Fight/Main의 외부 코드가 직접 수정하는 구조로 읽지 않는다. 상태를 관리하는 Player와 그 동작을 요청하는 Fight의 책임을 나누는 것이 앞의 Encapsulation 개념과 연결된다. 조회가 필요하면 추가 Methods를 만들 수 있다는 STT의 허용을 활용할 수 있지만 특정 getter 이름·개수는 필수 명세로 정해지지 않았다. `random`을 명시적인 `private` 또는 `public` field로 잘못 옮겨 적지도 않는다. [STT 15:41] [M02 p.20] [M02 p.22] [M03 p.3]

**Method contract(메서드 명세):** 14:47의 목록에는 네 이름이 나오며 15:41에서 `getDamaged`를 포함한 다섯 Methods로 보완된다. 후취득 skeleton의 정확한 선언은 다음과 같다. [STT 14:47] [STT 15:41] [M03 p.3]

| skeleton 선언 | 책임 |
| --- | --- |
| `public void attack(Player opponent)` | 상대 Player에게 공격을 가한다. |
| `private void getDamaged(int damage)` | 지정된 피해량을 해당 Player의 health에 반영한다. |
| `public void heal()` | 해당 Player를 회복한다. |
| `public boolean isAlive()` | 현재 health로 생존 여부를 판단한다. |
| `public char getTactic()` | 공격 또는 회복을 선택하여 문자로 알린다. |

`void`인 세 동작은 값을 반환하는 계약이 없고, `isAlive`와 `getTactic`은 각각 참·거짓과 행동 문자를 반환한다. `attack`의 `opponent`와 `getDamaged`의 `damage`는 각각 변경할 상대와 반영할 수치라는 서로 다른 입력이다. M02 p.19의 `userID`는 설명용 표기이고 실제 field 철자는 `userId`이다. 같은 페이지의 `attach()`는 p.21 및 skeleton의 `attack(Player opponent)`와 대조하면 오타이다. PDF의 `Boolean`과 skeleton의 `boolean` 차이는 아래 생존 판단에서 구분한다. [M02 p.19] [M02 p.21] [M03 p.3]

개념 연결: [[concepts/attributes-and-methods|Attributes와 Methods]], [[concepts/class-and-object|Class와 Object]]

### `Player` Constructor와 Seeded random sequence(시드가 정하는 난수열)

**후취득 공식 자료 보충:** `Player(String userId, int randomSeed)`는 ID와 정수 seed를 입력받으며 return type과 명시적 access modifier가 없다. M02 p.22의 예시는 `this.userId = userId`로 현재 Player의 field에 입력을 넣고 `new Random(randomSeed)`로 만든 Object를 `random`에 저장한다. `health = 50`은 field 선언의 초기화이다. 앞에서 배운 Constructor·`this`·reference 대입이 각기 초기화, 동명이름 구별, Object 연결을 맡는 예시이다. [M02 p.22]

PDF의 예시와 실제 skeleton 상태는 구별해야 한다. M03에는 field 선언과 Constructor signature가 있지만 Constructor 본문은 `// Problem 1.1`로 남아 있다. 다섯 Method 본문도 미구현 상태이다. 따라서 PDF의 초기화 예시가 ZIP에 이미 구현되어 있다고 읽으면 안 된다. [M02 p.22] [M03 p.3]

**Seed 개념 보충:** seed는 초기 health나 round 수가 아니라 난수 생성기의 시작 상태를 정하는 입력이다. M02는 Player의 `random`을 seed와 함께 사용하도록 하고 Main에서 두 정수 seeds를 받도록 한다. 각 Player가 자기 난수 생성 Object를 보유한다는 구조와 Class 전체가 하나의 static 난수 상태를 공유하는 구조를 혼동하지 않는다. [M02 p.20] [M02 p.22] [M02 p.27]

이 구조를 이해할 때 난수를 하나의 고정된 값보다 **호출에 따라 이어지는 sequence(수열)**로 생각하면 된다. 같은 `Random` 구현에서 같은 seed로 시작하고 같은 Methods·인자를 같은 순서로 호출하면 같은 수열을 재현할 수 있다. 반대로 seed만 같아도 호출의 종류나 순서가 달라지면 같은 simulation 결과까지 보장하지 않는다. 매 동작에서 같은 seed로 생성기를 다시 만들면 진행 중인 수열을 이어 쓰는 것과 달리 시작 상태로 돌아간다. 이는 seed 사용의 개념 설명이며 자료에 없는 seed별 출력값이나 유일한 난수 호출 구현을 제시한 것은 아니다. [M02 p.20] [M02 p.22]

예를 들어 health 상한 때문에 최종 health가 그대로여도 난수 호출을 했는지 생략했는지는 다음 수열 위치에 영향을 줄 수 있다. 따라서 같은 health 결과만으로 두 구현의 이후 난수 사용까지 같다고 판단하지 않는다. 자료의 hint와 필요한 상태 조건을 구분하며 완성된 Constructor·Method 구현은 이 설명에 포함하지 않는다. [M02 p.21] [M02 p.22]

### health: 초기값, 하한, 상한, 종료 의미

health는 Player의 현재 상태이며 동작 이후에도 허용 범위를 유지해야 한다. STT 15:41은 초기 health `50`, 음수 금지, `0`일 때 해당 플레이어의 패배와 게임 종료를 직접 설명한다. STT 17:39의 `initial value of the system`은 상한을 초기값과 연결하지만 숫자 `50`을 다시 명확히 발화하지는 않는다. 최초 STT에서 상한 50은 문맥상 해석이었다는 차이를 보존한다. [STT 15:41] [STT 17:39]

**후취득 공식 자료 보충:** M02 p.21은 상한을 `50`으로 명시하고, M02 p.22와 M03는 `private int health = 50`을 제시한다. 이제 자료상 초기값·자료형·상한을 직접 확인할 수 있다. 다음 기호는 상태 조건을 설명하기 위한 표기이며 과제의 변수명을 지정하지 않는다. [M02 p.21] [M02 p.22] [M03 p.3]

$$
h_0 = 50,\qquad 0 \le h \le h_0 = 50
$$

**Boundary reasoning(경계값 판단):** health가 `2`일 때 damage `5`를 그대로 빼면 `-3`이므로 허용 상태가 아니다. health가 `49`일 때 healing 양 `3`을 그대로 더한 `52`도 허용되지 않는다. 산술적으로 선택한 변화량과 실제로 저장 가능한 상태를 구분해야 한다. 경계에서 멈추도록 제한하는 설명용 해석에서는 각각 `0`과 `50`이 되며, 실제 변화량은 선택량 `5`·`3`보다 작은 `2`·`1`이다. 이는 하한·상한을 만족하는 상태 변화를 설명한 것이며 특정 경계 처리 코드만을 필수 구현으로 지정한 것은 아니다. [M02 p.21]

health `0`은 생존 판단과 Fight 종료를 연결하는 simulation의 상태이다. `isAlive` 자체가 전체 실행을 종료한다거나 Player의 reference를 지우고 즉시 memory를 회수한다는 뜻은 아니다. 기술 명세의 확인 근거는 후취득 M02/M03이며 사용자의 강의 날짜 확인을 이 수치와 동작에 대한 기술 확인으로 바꾸지 않는다. [M02 p.15] [M02 p.21] [M02 p.23] [M03 p.3]

### `attack`과 `getDamaged`: 상대에게 가하는 동작과 피해 반영 구분하기

`attack`의 대상은 공격한 Player 자신이 아니라 상대 Player이다. STT 16:37은 상대 health를 `1`부터 `5` 중 선택한 정수만큼 감소시킨다고 설명하지만 해당 문장에는 무작위 선택이라는 표현이 명시되지 않는다. 이 STT상의 한계는 유지한다. **후취득 공식 자료 보충:** M02 p.21은 `public void attack(Player opponent)`와 **random integer 1–5, 양 끝 포함**을 명시한다. 따라서 무작위 선택은 이제 자료로 확인되는 명세이다. [STT 16:37] [M02 p.21] [M03 p.3]

설명용 damage를 $d$라고 하면 다음 범위이다.

$$
d \in \{1,2,3,4,5\}
$$

`private void getDamaged(int damage)`는 전달받은 지정량을 해당 Player의 health에 반영한다. `attack`이 상대에게 가하는 동작이라면 `getDamaged`는 피해를 받는 Object의 상태 변경을 담당한다. 별도의 무작위 damage를 다시 고르는 책임으로 읽지 않는다. 양쪽 설명에 health의 non-negative 조건을 함께 적용해야 하며, 선택량이 남은 health보다 크다고 음수 상태를 저장할 수는 없다. [M02 p.21] [M03 p.3]

**이해를 위한 예시:** A가 공격하고 B가 피해를 받으면 동작을 시작한 Object는 A이지만 health가 바뀌는 Object는 B이다. B의 남은 health와 선택된 damage를 함께 보아야 상태 조건을 판단할 수 있다. 이 설명만으로 `attack`에서 `getDamaged`를 호출하는 유일한 내부 절차가 정해지는 것은 아니다. [STT 16:37] [M02 p.21]

**Hint와 명세의 구분:** 후취득 M02 p.21의 hint는 `random.nextInt()`이다. 이름만으로 호출 결과가 곧바로 1–5라는 뜻은 아니며, 최종 선택량이 명세의 구간에 들어가는지 확인해야 한다. 별도의 동일 확률 요구나 필수 API 규칙을 추가하지 않는다. STT는 hint를 필요에 따라 사용해도 된다고 말하며 `getDamaged`를 도움을 위해 포함했다는 취지를 설명한다. 불명확한 `top -of -mention method`는 복원하지 않는다. M03의 두 Method 본문은 `Problem 1.1` 상태이다. [STT 16:37] [M02 p.21] [M03 p.3]

### `heal`: 증가량과 health 상한을 함께 읽기

`heal`은 해당 Player의 health를 무작위 정수 `1`부터 `3`만큼 증가시키는 동작이다. 이 부분은 STT에도 무작위 선택으로 제시된다. **후취득 공식 자료 보충:** M02 p.21과 M03는 Parameter와 반환값이 없는 `public void heal()`을 확인해 주며, PDF는 증가량 1–3의 양 끝 포함과 health 상한 `50`을 명시한다. [STT 16:37] [M02 p.21] [M03 p.3]

설명용 증가량을 $r$이라고 하면 다음 범위이다.

$$
r \in \{1,2,3\}
$$

선택량과 허용 상태는 함께 판단해야 한다. health `49`와 선택량 `3`의 단순 합은 `52`이지만 이를 최종 상태로 저장하면 상한을 어긴다. 앞의 경계에서 멈추는 해석에서는 `50`이 되어 실제 증가량은 `1`이다. 이미 health가 `50`이면 양의 회복량을 선택해도 상한을 만족하는 상태는 더 커질 수 없다. 이를 “회복량의 선택 범위에 0이 있다”는 뜻으로 바꾸지는 않는다. [M02 p.21]

STT 17:39의 `initial value of the system`은 그대로 두며, 자료에서 확인한 숫자 `50`을 그 문장의 복원된 발화로 넣지 않는다. 구체적인 경계 처리 절차, 별도의 동일 확률 조건, 유일한 random API 구현은 명세로 추가하지 않는다. Skeleton 본문은 `Problem 1.1`로 남아 있다. [STT 17:39] [M02 p.21] [M03 p.3]

### `isAlive`: health에 대한 참·거짓 판단

`isAlive`는 health가 `0`보다 크면 `true`, 그렇지 않으면 `false`를 반환한다. 상태를 바꾸는 동작과 구별되는 predicate(조건 판단)이다. 설명용 기호 $h$로 쓰면 다음과 같다. [STT 17:39] [M02 p.21]

$$
\operatorname{isAlive}(h)=
\begin{cases}
\mathrm{true}, & h>0\\
\mathrm{false}, & h\le 0
\end{cases}
$$

health의 정상 범위가 음수가 아니라는 조건과 함께 읽으면 유효 상태에서 `false`가 되는 경계는 `0`이다. health `1`은 `true`, `0`은 `false`이므로 `0`을 살아 있는 상태에 포함시키면 명세와 다르다. 수식의 $h<0$ 부분이 잘못된 음수 health를 허용한다는 뜻도 아니다. [STT 15:41] [STT 17:39] [M02 p.21]

**후취득 자료의 Type 차이:** STT의 `a Boolean`만으로는 선언 철자를 확정하지 않았다. M02 p.21은 `public Boolean isAlive()`, 실제 M03는 `public boolean isAlive()`이다. `Boolean`은 wrapper Class, `boolean`은 primitive type이므로 정확한 선언 표기가 다르다. 여기서는 실제 skeleton의 `boolean`을 확인된 코드 계약으로 읽되 Wrapper 심화 내용을 이번 발화로 확대하지 않는다. 참·거짓의 생존 조건은 일관되지만 Method 본문은 `Problem 1.1`로 비어 있다. [STT 17:39] [M02 p.21] [M03 p.3]

`isAlive`가 `false`를 반환하는 일과 Fight가 simulation을 끝내는 일은 서로 연결되는 별도 책임이다. 생존 Method 안에서 전체 게임을 종료하는 코드까지 수행해야 한다고 추정하지 않는다. 후취득 자료에서 전체 종료 여부의 판단은 Fight의 `isFinished` 계약으로 제시된다. [M02 p.21] [M02 p.25]

### `getTactic`: 행동 선택과 후취득 자료의 반환 명세

`getTactic`은 공격 또는 회복을 선택하는 Method이다. 선택한 행동을 실제로 수행하는 `attack`·`heal`과 먼저 책임을 구별한다. STT 17:39에는 공격에 `with a chance of 70`, 회복에 `with a chance of 30%`가 나오므로 최초 STT의 공격 70%는 문맥상 해석이었다. 마지막 `If a player decides to attack or turn the character,`는 불명확한 채 중단되어 반환 문자를 확정할 수 없었다. [STT 17:39]

**후취득 공식 자료 보충:** M02 p.21은 `public char getTactic()`, attack `70%`, heal `30%`, 공격 선택 시 `'a'`, 그 외 회복 선택 시 `'h'`를 명시한다. M03도 `char` return type을 확인한다. 따라서 현재 자료상 반환 계약은 완전하게 읽을 수 있지만, STT의 `70` 뒤에 `%`를 삽입하거나 중단된 문장을 복원한 것은 아니다. `char`는 행동을 나타내는 한 문자의 값이며 ID 문자열이나 공격 damage와는 다른 정보이다. [M02 p.21] [M03 p.3]

PDF의 hint는 `random.nextFloat()`이다. 확률 70%/30%는 각 선택의 비율에 대한 요구이며 열 번 선택하면 반드시 일곱 번 공격한다는 고정 횟수 규칙이 아니다. 선택 확률, 반환 문자, 선택한 행동의 실행을 나누어 읽어야 하며 Method가 `'a'`를 반환했다는 사실만으로 이미 상대 health를 감소시켰다고 판단하지 않는다. Hint는 필수 API 규칙이나 완성된 분기 구현으로 바꾸지 않는다. Skeleton 본문은 `Problem 1.1`로 남아 있다. [STT 16:37] [M02 p.21] [M03 p.3]

마지막 `17:39`는 해당 STT 구간의 시작 시각이다. 녹음의 시작과 마지막 문장 모두 불완전하다는 한계는 후취득 자료로 사라지지 않는다. 다음 Fight/Main 명세는 **9월 19일 확보한 공식 자료 보충**이며 9월 17일 녹음의 누락된 뒷부분을 들었다는 설명이 아니다. [STT 00:00] [STT 17:39]

### `Fight`: 상호작용·round 상태와 Constructor

**후취득 공식 자료 보충:** Fight는 두 Player 사이의 interactions(상호작용)와 round 진행을 관리한다. Player가 각자의 상태와 동작을 담당한다면 Fight는 어느 Player가 언제 행동하고 언제 게임을 끝낼지 연결하는 책임을 갖는다. 기본 게임은 round `1`부터 최대 `100`까지이며, 어느 한쪽 health가 먼저 0이 되면 그 전에 끝난다. [M02 p.23]

| Field 선언 | 초기 상태와 의미 |
| --- | --- |
| `int timeLimit = 100` | 최대 round 수이다. 100초 또는 100분이 아니다. |
| `int currRound = 0` | 아직 첫 round를 진행하기 전의 초기 상태이다. 실제 첫 round 번호는 1이다. |
| `Player p1` | 먼저 행동할 Player를 가리킨다. |
| `Player p2` | 두 번째로 행동할 Player를 가리킨다. |

이 네 fields와 `Fight(Player p1, Player p2)` Constructor에는 명시적 access modifier가 없다. PDF p.26과 M03 Section 1의 Constructor는 입력 references를 각각 `this.p1`, `this.p2`에 저장한다. 앞의 reference 대입과 같은 원리로 기존 Player를 연결하며, 이 Constructor 안에서 새 Player 두 개를 생성하거나 복제하는 코드는 아니다. M02 p.24의 Players를 생성한다는 설명은 Main의 생성 책임과 함께 읽어야 한다. [M02 p.24] [M02 p.26] [M02 p.27] [M03 p.1]

Player Constructor와 달리 Fight의 reference 저장 Constructor는 실제 skeleton에 제공되어 있다. 이후 `proceed`, `isFinished`, `getWinner`의 본문은 `Problem 1.2`로 남아 있으므로 Constructor가 있다는 사실과 게임 진행이 완성되었다는 판단은 구분한다. [M03 p.1]

### `Fight.proceed()`: 한 round의 순서와 출력 계약

**후취득 공식 자료 보충:** `public void proceed()`는 한 round를 진행한다. 먼저 1부터 시작하는 현재 round 번호를 `Round <Round_Number>` 형식으로 출력한다. 이후 p1의 `getTactic()` 선택에 맞는 `attack` 또는 `heal`을 수행하고 p2의 차례로 넘어간다. 이 순서는 두 행동을 동시에 적용하는 규칙이 아니다. [M02 p.25] [M03 p.1]

**중간 상태가 다음 행동을 결정한다.** p1의 공격으로 p2의 health가 0이 되었다면 p2는 어떤 행동도 하지 않는다. Attack뿐 아니라 heal도 생략하므로 같은 round에서 회복하여 다시 행동하도록 해석하면 안 된다. 반대로 p2가 살아 있으면 자신의 tactic에 따른 행동을 수행한다. 이것이 “각 round에서 번갈아 행동한다”는 일반 설명에 추가되는 명시적 예외이다. [M02 p.25]

한 round의 행동 이후에는 두 Player의 ID와 health를 아래 형식의 두 줄로 출력한다. Angle brackets(꺾쇠괄호) 안은 실제 값으로 대체할 자리이며, 출력할 고정된 ID 문자열이 아니다. [M02 p.25]

```text
<Player1_userID> health : <Player1_health>
<Player2_userID> health : <Player2_health>
```

따라서 출력 순서는 round 번호, p1→살아 있는 p2의 행동, 두 Player의 health이다. `currRound`의 초기값 `0`을 첫 출력 번호로 그대로 쓰거나 행동 전 health를 최종 상태처럼 출력하면 계약과 달라진다. Private 정보의 조회가 필요해도 특정 getter 이름을 명세에 추가하지 않는다. Skeleton에는 signature만 제공되고 이 절차의 구현은 남아 있다. [STT 15:41] [M02 p.24] [M02 p.25] [M03 p.1]

### `Fight.isFinished()`: 조기 종료와 마지막 round 완료

**후취득 공식 자료 보충:** Fight는 어느 한 Player의 health가 0이 되었거나 마지막 round가 끝나면 종료된다. 두 조건을 모두 만족할 필요가 없다. 기본 최대치는 100 rounds이며 종료되지 않은 경우 `false`, 종료된 경우 `true`를 돌려준다. PDF p.25는 `public Boolean isFinished()`, M03는 `public boolean isFinished()`로 표기한다. `isAlive`와 마찬가지로 실제 skeleton은 primitive `boolean` 계약이며 본문은 미구현이다. [M02 p.23] [M02 p.25] [M03 p.1]

**Boundary reasoning(경계값 판단):** 두 Player가 모두 살아 있다는 조건에서 round 99를 완료한 상태는 round 제한만으로 종료되지 않고, round 100을 완료하면 끝난다. Round 100을 시작하기도 전에 “번호가 100이 될 예정”이라는 이유로 끝내거나 round 101까지 진행하는 해석은 마지막 round 완료 조건과 다르다. 어느 round에서든 한쪽 health가 0이면 100까지 기다릴 필요가 없다. 초기 `currRound = 0`, 첫 round 번호 `1`, 마지막 round의 완료를 구별해야 한다. [M02 p.23] [M02 p.24] [M02 p.25]

이는 simulation의 종료 판단이며 memory 회수 시점에 관한 조건이 아니다. 반복 진행의 책임은 Main에, 한 round 진행의 책임은 `proceed`에, 종료 판정의 책임은 `isFinished`에 나뉜다. [M02 p.15] [M02 p.25] [M02 p.27]

### `Fight.getWinner()`: health 비교·동점 규칙·반환 대상

**후취득 공식 자료 보충:** `public Player getWinner()`는 health가 더 많은 Player를 승자로 반환한다. Health가 같으면 **p2가 이긴다**. 자료는 p1이 먼저 행동한다는 이유로 이 동점 규칙을 설명한다. 따라서 동점을 무승부나 p1 승리로 처리하면 명세가 달라진다. [M02 p.25] [M03 p.1]

비교할 때는 승자 선택과 반환값의 자료형을 함께 확인해야 한다. 종료 시 두 health가 같다면 반환 대상은 p2이며, 반환값은 승자의 `userId` 문자열 자체나 참·거짓이 아니라 **Player Object를 가리키는 값**이다. Main은 이 승자의 ID를 최종 출력에 사용한다. Skeleton의 비교 분기는 아직 `Problem 1.2`로 남아 있다. [M02 p.25] [M02 p.27] [M03 p.1]

### `Main`: seed 입력에서 최종 승자 출력까지

**후취득 공식 자료 보충:** `public static void main(String[] args)`는 simulation 전체 실행을 관리한다. `Scanner`로 `int` 입력 두 개를 받아 random seeds로 사용하고, ID가 `Gryffindor`와 `Slytherin`인 두 Players 및 Fight Instance를 생성한다. 입력값 두 개는 health나 최대 round 수를 정하는 값이 아니다. Player의 Constructor 입력과 Fight의 reference 연결을 구분하여 읽어야 한다. [M02 p.22] [M02 p.26] [M02 p.27] [M03 p.2]

Main은 Fight가 끝날 때까지 진행하고, 종료 뒤 승리한 Player의 ID를 사용하여 `<userID> is the winner!` 형식으로 출력한다. 한 round의 행동 순서와 health 출력은 `proceed` 계약, 종료 여부는 `isFinished` 계약, 승자 선택은 `getWinner` 계약으로 연결된다. 이 흐름을 구현하려면 앞의 Object·Method·Constructor 외에 `Scanner` 입력, 조건 판단, 반복문 개념도 필요하다. 이는 과제 명세를 이해하는 데 필요한 선수 개념이지 누락된 녹음에서 모두 설명되었다는 주장이 아니다. [M02 p.25] [M02 p.27]

M02 p.27의 Attributes 항목은 비어 있고 p.28의 제목에는 Constructor가 들어가지만, 제시된 Main 코드에는 `main`만 있다. 이를 근거로 별도의 필수 Main field나 Constructor를 만들지 않는다. M02의 `TODO: problem3`와 M03의 `Problem 1.3`은 작성할 부분으로 남아 있으며, 이 노트는 입력·반복문·출력을 연결한 완성된 제출 코드를 제공하지 않는다. [M02 p.27] [M02 p.28] [M03 p.2]

## 강의 흐름과 연결

### 이전 강의에서 실습으로 이어지는 질문

이번 녹음은 이전 강의에서 배운 내용을 복습한다고 밝히며, Object와 Class → Constructor → 메모리 상태를 실습의 준비 단계로 제시한다. 이전 노트는 [[courses/computer_programming/lectures/2026-09-15-lecture-05|9월 15일 강의 노트]]와 [[courses/computer_programming/lectures/en/2026-09-15-lecture-05|September 15 Lecture Notes — English]]에서 함께 읽을 수 있다. 여기서는 이전 노트의 내용을 새로 추정하지 않고, 이번 녹음에서 실제로 다시 다룬 연결을 기준으로 삼는다. [STT 00:00]

앞의 본문을 읽은 뒤에는 다음 세 질문으로 흐름을 묶을 수 있다.

1. **무엇을 만들고 무엇을 저장하는가?** Class로 Object를 만들고, Attributes에 상태를 저장하며, Methods로 동작을 정의한다.
2. **어떤 상태가 바뀌는가?** `this`, `static`, reference, 값 전달을 구분하여 대입이 영향을 주는 대상을 찾는다.
3. **누가 상태·한 round·전체 실행을 맡는가?** Player의 상태와 행동, Fight의 순서·종료·승자 판단, Main의 입력·반복·출력을 연결한다. 공식 명세와 skeleton의 미구현 본문을 구분한다. [M02 p.21] [M02 p.25] [M02 p.27] [M03 p.1] [M03 p.2] [M03 p.3]

이 순서에서 Lab3는 별개의 문법 모음이 아니다. 앞에서 배운 Class·Attributes·Methods를 실제 요구사항에 대응시키는 적용 단계이다. 녹음은 미완성 Player 설명에서 끝난다. 전체 Fight/Main 명세는 9월 19일 후취득한 공식 자료로 보충하며, 그 내용을 9월 17일의 실제 발화로 확정하지 않는다. [STT 12:50] [STT 14:47] [STT 17:39] [M02 p.23] [M02 p.25] [M02 p.27]

### 시간순 coverage table

자료 열의 M01 페이지는 해당 개념을 설명하는 **자료 보충**이다. 그 페이지의 모든 코드와 그림이 이번 실습에서 직접 설명되었다는 뜻은 아니다. 학습 결과의 R 번호는 일반 회상, P 번호는 새로 만든 연습에 대응한다.

| 시간 | 자료 | 핵심 질문 | 학습 결과 |
| --- | --- | --- | --- |
| 00:00 | [STT 00:00], [M01 p.3], [M01 p.4], [M01 p.5], [M01 p.6] | 프로그램을 작업 순서와 상호작용하는 Object의 관점에서 어떻게 바라보는가? | OOP의 관점과 복습 후 게임 적용이라는 수업 구성을 설명한다. R01·P02 |
| 00:55 | [STT 00:55], [M01 p.58], [M01 p.59], [M01 p.60] | 내부 상태 보호와 코드 재사용은 어떤 동기에서 필요한가? | Encapsulation과 Inheritance의 목적을 구분하며 상세 문법은 후속 범위로 남긴다. R02–R03·P02 |
| 01:51 | [STT 01:51], [M01 p.7], [M01 p.61] | 유사한 동작의 다른 구현과 Class–Object 관계는 무엇인가? | Polymorphism의 개념 소개와 Class의 설계도 역할을 구분한다. R04–R05·P01–P02 |
| 02:44 | [STT 02:44], [M01 p.8], [M01 p.10], [M01 p.11], [M01 p.14], [M01 p.15], [M01 p.16], [M01 p.17] | 공통 Members를 갖는다는 것은 같은 값을 공유한다는 뜻인가? | Attributes와 Methods를 나누고 Object별 상태와 호출 결과를 설명한다. R06–R07·P03 |
| 03:40 | [STT 03:40], [M01 p.9], [M01 p.19], [M01 p.20] | Instance 생성과 초기화는 어떻게 연결되는가? | Instantiate와 Constructor를 구분하고 Constructor의 이름·반환형 규칙을 설명한다. R05·R08·P01·P03 |
| 04:37 | [STT 04:37], [M01 p.21], [M01 p.22], [M01 p.23], [M01 p.24], [M01 p.25], [M01 p.26] | 여러 초기화 방식과 같은 이름의 Parameter를 어떻게 처리하는가? | Constructor 선택과 `this`의 역할을 설명한다. R08–R09·P03 |
| 05:41 | [STT 05:41], [M01 p.27], [M01 p.28], [M01 p.29] | `static` 상태는 어디에 속하는가? | Class의 공유 상태와 Object별 상태를 구분한다. R10·P04 |
| 06:33 | [STT 06:33], [M01 p.30] | 한 번 바꾼 공유 값이 다른 Instance에서도 보이는 이유는 무엇인가? | 녹음의 `owner` 예시를 공유 상태로 설명한다. 후취득 [M02 p.10]의 코드로 값을 대조하되 불확실한 발화는 보존한다. R10·P04 |
| 07:35 | [STT 07:35], [M01 p.31], [M01 p.32] | `static` Method에서 어느 Object의 값을 읽는가? | 대상 Object 없는 직접 접근의 오류를 설명한다. R11·P04 |
| 08:32 | [STT 08:32], [M01 p.34], [M01 p.35], [M01 p.36] | 내용의 같음, Object의 동일성, reference 대입은 어떻게 다른가? | `==`와 `=`를 구분하고 충돌하는 전사 표현을 검증 필요로 남긴다. R12–R13·P05 |
| 09:27 | [STT 09:27], [M01 p.37], [M01 p.38], [M01 p.39] | Parameter를 바꾸면 호출자의 변수도 바뀌는가? | 호출자 변수와 복사된 Parameter를 분리한다. R14·P05 |
| 10:14 | [STT 10:14], [M01 p.38], [M01 p.42] | 정수 전달과 Object 전달의 관찰 결과가 왜 다른가? | Java가 reference 값도 값으로 전달한다는 보충 설명을 적용한다. R14–R15·P05 |
| 11:07 | [STT 11:07], [M01 p.40], [M01 p.41] | Method 안의 상태 변경이 밖에서 보이는 이유는 무엇인가? | 복사된 reference를 통해 같은 Object의 Attributes를 바꾸는 과정을 설명한다. R15·P05 |
| 11:52 | [STT 11:52], [M01 p.42], [M01 p.43], [M01 p.44] | Object 상태 변경과 자동 메모리 관리는 어떻게 구분되는가? | 상태 변경의 대상과 메모리 관리의 역할을 각각 설명한다. R15–R16·P05–P06 |
| 12:50 | [STT 12:50], [M01 p.44] | 복습은 어디에서 끝나고 Lab3 안내는 어디에서 시작되는가? | Garbage Collector 설명과 세 Class를 사용하는 게임 안내의 경계를 파악한다. R16–R17·P06–P07 |
| 13:47 | [STT 13:47] | 과제 준비 순서와 게임의 큰 구조는 무엇인가? | skeleton 다운로드·압축 해제·IntelliJ project 열기·소스 작성과 제출, 두 플레이어의 교대 진행을 정리한다. R17·P07 |
| 14:47 | [STT 14:47] | `Player`가 관리하는 상태와 제공하는 동작은 무엇인가? | 식별 정보·health·random 관련 Attribute와 Methods의 책임을 나눈다. R18·P07 |
| 15:41 | [STT 15:41] | health의 시작 상태와 종료 조건, 전체 Method 목록은 무엇인가? | 초기값 50·음수 금지·0에서 패배와 종료, 다섯 Methods 및 추가 조회 Method 허용을 보존한다. R18–R19·P07–P08 |
| 16:37 | [STT 16:37] | 공격·피해 반영·회복은 누구의 상태를 얼마만큼 바꾸는가? | `attack`, `getDamaged`, `heal`의 책임과 확인된 범위를 구분한다. R20–R21·P08 |
| 17:39 | [STT 17:39] | 상한·생존 판정·행동 선택에서 확인된 것과 빠진 것은 무엇인가? | 초기값 상한, `isAlive` 조건, `getTactic`의 불완전한 확률·반환 설명을 구분한다. R19·R21–R23·P08–P09 |

### 후취득 공식 자료와 학습 연결

다음은 고정 본문에서 도출한 자료 보충의 흐름이다. 모든 행은 **9월 19일 확보한 자료**에 근거하며 녹음의 후속 시간대로 배치하지 않는다.

| 자료 범위 | 연결할 설명과 회상 | 적용 연습 |
| --- | --- | --- |
| [M02 p.6] [M02 p.7] [M02 p.8] [M02 p.9] | Field와 Method 결과의 차이, Constructor와 `this(...)` 연결, 인쇄 오류 구별. R07–R09 | P03·P16 |
| [M02 p.10] [M02 p.11] [M02 p.12] [M02 p.13] [M02 p.14] | 공유 owner, compile error, identity, primitive와 reference 값 전달. R10–R15 | P04–P05 |
| [M02 p.17] [M02 p.18] [M02 p.29] | 세 Class의 준비·제출 규칙. R17 | P07 |
| [M02 p.20] [M02 p.21] [M02 p.22] [M03 p.3] | Player 선언, health 경계, tactic, ID·seed 초기화와 미구현 본문. R18–R24 | P07–P10 |
| [M02 p.23] [M02 p.24] [M02 p.26] [M03 p.1] | Fight가 기존 Player references를 연결하고 round 상태를 관리한다. R25 | P11 |
| [M02 p.25] [M03 p.1] | 한 round의 순서·출력, 종료 조건, 동점 규칙을 각각 구분한다. R26–R28 | P12–P14 |
| [M02 p.27] [M02 p.28] [M03 p.2] | Main의 seed 입력과 생성·진행·승자 출력 책임. R29 | P15 |

### 개념 연결 지도

- [[concepts/object-oriented-programming|Object-Oriented Programming]] → [[concepts/class-and-object|Class와 Object]] → [[concepts/attributes-and-methods|Attributes와 Methods]]: 프로그램의 대상을 정하고 상태와 동작으로 표현한다.
- [[concepts/class-and-object|Class와 Object]] → [[concepts/constructor|Constructor]] → [[concepts/this-keyword|this]]: 새 Object의 초기 상태를 정하고, 현재 Object와 Parameter를 구별한다.
- [[concepts/attributes-and-methods|Attributes와 Methods]] ↔ [[concepts/static-members|static Members]]: Object별 상태와 Class 전체의 공유 상태를 나누어 읽는다.
- [[concepts/object-reference|Object reference]] → [[concepts/call-by-value|Call-by-Value]] → [[concepts/garbage-collection|Garbage Collector]]: 같은 Object를 가리키는 관계, 전달되는 값, 메모리 관리의 역할을 차례로 구분한다.
- [[concepts/encapsulation|Encapsulation]], [[concepts/inheritance|Inheritance]], [[concepts/polymorphism|Polymorphism]]: 내부 보호·재사용·동작 다양화라는 동기를 연결한다. 이번 소개를 상세 구현 규칙의 학습 완료로 확대하지 않는다. [M01 p.58]

### 그림과 표를 다시 읽는 기준

**`static` 그림에서는 값의 소속을 읽는다.** M01 p.27의 전체 자동차 수는 Class 쪽에, 개별 브랜드는 각 Object 쪽에 놓여 있다. 그림을 다시 볼 때는 숫자와 브랜드를 암기하기보다, 새로운 Attribute가 주어졌을 때 어느 쪽에 놓아야 하는지 판단한다. 이 구분이 Lab3에서도 각 플레이어의 health를 개별 상태로 이해하는 출발점이 된다. [M01 p.27] [STT 15:41]

**메모리 표에서는 대입 대상과 영역을 읽는다.** M01 p.39에서 호출자의 `a`, `b`는 `2`, `3`이고, `swap` 안의 같은 이름 Parameters는 `3`, `2`이다. M01 p.43에서는 두 Object 안의 `value`가 `3`, `2`로 바뀐다. 두 표의 차이는 교환이라는 동작 이름에 있지 않고, Parameter 자체와 Object의 Attribute 중 무엇에 대입했는지에 있다. [M01 p.39] [M01 p.43]

이 그림과 표는 본문 설명을 점검하는 M01의 자료 예시이다. 후취득 M02에는 `owner`, `number`, `Car.speed`의 대응 코드가 있다. 공유 owner 변경, caller의 `number = 10` 유지, 같은 Car의 `speed`가 `50`에서 `100`으로 바뀌는 과정을 각각 읽는다. 자료 코드를 확인한 사실은 불명확한 STT의 단어를 복원했다는 뜻이 아니다. [M02 p.10] [M02 p.13] [M02 p.14]

## 핵심 요약

- **Class(클래스)와 Object(객체):** Class는 공통 구조를 정하고, Instance는 그 구조로 만든 개별 Object이다. 공통 Attributes·Methods를 갖는다는 사실과 같은 상태를 공유한다는 사실은 다르다. [M01 p.7] [M01 p.14] [M01 p.17]
- **Attributes와 Methods:** 상태와 동작을 함께 읽는다. 같은 Method도 호출 대상의 상태에 따라 결과가 달라질 수 있으며, 이것만으로 Polymorphism의 구현 사례가 되지는 않는다. [M01 p.8] [M01 p.16] [M01 p.61]
- **Constructor와 `this`:** Constructor는 특별한 초기화 구성이며 반환형을 쓰지 않는다. `this`는 현재 Object를 가리켜 같은 이름의 Parameter와 Attribute를 구분한다. `this()`는 다른 Constructor 호출에 관한 별도 용법이다. [M01 p.19] [M01 p.23] [M01 p.26]
- **`static` Members:** 공유 상태는 Class에, 개별 상태는 Object에 연결된다. `static` Method의 문제를 판단할 때는 값을 읽을 대상 Object가 지정되었는지 확인한다. [M01 p.27] [M01 p.31] [M01 p.32]
- **Object reference와 값 전달:** `==`는 같은 Object를 가리키는지 비교하고, reference 대입 자체는 Object 복제가 아니다. Java는 reference 값도 복사해 전달하므로, Parameter 재대입과 Object 상태 변경을 구분해야 한다. [M01 p.35] [M01 p.36] [M01 p.42]
- **Memory Management와 Garbage Collector:** Object 생성에 필요한 메모리 할당과 사용되지 않는 Object의 메모리 회수는 서로 다른 역할이다. 자동 관리가 Object 생성 표현의 불필요함이나 즉시 회수를 뜻하지 않는다. [M01 p.44]
- **OOP의 세 동기:** Encapsulation은 내부 보호와 추상화, Inheritance는 재사용과 확장, Polymorphism은 유사한 동작의 다른 구현으로 연결된다. 상세 문법은 이번 소개만으로 확정하지 않는다. [M01 p.58] [M01 p.59] [M01 p.60] [M01 p.61]
- **Player와 seed — 후취득 공식 자료 보충:** `userId`, 초기 health `50`, Player별 `Random`이 상태를 구성한다. health는 `0–50`, random damage는 `1–5`, random healing은 `1–3`이다. `getTactic`은 70%/30%로 `'a'`/`'h'`를 선택하며 행동 실행과 구별된다. Seed와 동일한 호출 순서를 함께 보아야 수열 재현을 설명할 수 있다. PDF의 초기화 예시와 미구현 skeleton을 구분한다. [M02 p.20] [M02 p.21] [M02 p.22] [M03 p.3]
- **Fight — 후취득 공식 자료 보충:** 기존 Player references를 저장하고 round 1–100을 관리한다. p1 뒤에 살아 있는 p2가 행동하며, 한쪽 health가 0이 되거나 마지막 round가 완료되면 끝난다. `getWinner`는 health가 큰 Player를 반환하고 동점이면 p2를 고른다. [M02 p.23] [M02 p.24] [M02 p.25] [M02 p.26] [M03 p.1]
- **Main과 제출 — 후취득 공식 자료 보충:** 두 `int` seeds 입력 → Players와 Fight 생성 → 종료까지 진행 → 승자 ID 출력으로 이어진다. PDF의 제출 안내는 10월 1일 오후 6시 29분까지 eTL에 세 Java 파일을 담은 `src` 폴더의 ZIP을 제출하는 것이다. 전체 명세의 확보와 과제 구현의 완성은 구별한다. [M02 p.27] [M02 p.29] [M03 p.2]
- **Source limits(근거의 한계):** 위 수치·선언은 9월 19일 확보한 자료 근거이다. `70`, `initial value of the system`, 잘린 반환 문장 등 9월 17일 STT의 불확실성은 그대로 남긴다. [STT 17:39]

## 회상·연습문제

### 1단계: 개념과 강의 범위 회상

#### R01. OOP는 프로그램을 어떤 관점으로 보는가?

자료의 Coffee Machine 예시를 이용하여 Procedural Style과의 차이를 설명하라.

<details><summary>정답</summary>

OOP는 프로그램을 상호작용하는 Object들의 모음으로 본다. Procedural Style의 커피 예시는 작업 순서를 나열하고, Object-Oriented Style은 관련 기능을 Coffee Machine에 묶는다. 어느 쪽이든 실행 순서는 필요하다. Object는 물리적 물건뿐 아니라 계좌 같은 개념적 대상이나 소프트웨어 내부 구조도 나타낼 수 있다. [M01 p.3] [M01 p.4] [M01 p.5] [M01 p.6]

</details>

#### R02. Encapsulation의 목적을 두 가지로 설명하라.

<details><summary>정답</summary>

구성 요소의 접근 가능 범위를 제한하여 Object 내부 데이터를 보호하고, 사용하는 쪽이 내부 구현을 모두 알지 않아도 되도록 추상화를 돕는다. Class 안에 변수를 모아 놓았다는 사실만으로 이 목적이 모두 충족되는 것은 아니다. 이번 소개에서는 상세 접근 제어 문법까지 확정하지 않는다. [STT 00:55] [M01 p.59]

</details>

#### R03. Inheritance는 재사용과 확장을 어떻게 연결하는가?

<details><summary>정답</summary>

Child class가 Parent class의 특성과 코드를 이어받아 재사용하면서 필요한 새 특성을 추가할 수 있다. 둘이 항상 완전히 같은 상태와 동작을 가져야 한다는 뜻은 아니다. 분류 계층 그림은 이 관계의 자료 비유이며, 실제 구현 문법 전체를 설명한 것으로 읽지 않는다. [M01 p.60]

</details>

#### R04. Polymorphism과 서로 다른 두 `Car`의 `getSpeed()` 결과 차이는 어떻게 구분되는가?

<details><summary>정답</summary>

자료의 `getSpeed()`는 같은 Class의 같은 Method가 각 Object의 다른 `speed` 값을 읽는 경우이다. Polymorphism 소개는 서로 다른 Class들이 유사한 동작을 다르게 구현할 수 있다는 설명이다. 결과가 다르다는 관찰 하나만으로 두 상황을 같은 개념으로 분류하지 않는다. [M01 p.16] [M01 p.61]

</details>

#### R05. Class, Instance, Instantiate, Members의 관계는 무엇인가?

<details><summary>정답</summary>

Class는 Object의 설계도이고, Instance는 그 Class로 만든 Object이며, Instantiate는 Instance를 만드는 행위이다. Members는 Attributes와 Methods를 함께 부르는 말이다. `Car`라는 Class, `myCar`라는 reference 변수, `new Car()`로 만들어진 Object를 구별한다. [M01 p.7] [M01 p.9] [M01 p.17]

</details>

#### R06. 공통 Attributes를 가진 두 Object는 값을 공유하는가? 초기값은 어떻게 읽어야 하는가?

<details><summary>정답</summary>

서로 다른 Object의 instance Attributes는 각자의 값을 가진다. 하나의 `color`를 바꾸었다고 다른 Object의 `color`까지 바뀌지 않는다.

자료는 명시적 초기화와 기본값을 구분하고, 초기화하지 않은 Attributes의 예로 `boolean`의 `false`, `int`의 `0`, `float`의 `0.0`, Object를 가리키는 Attribute의 `null`을 보여 준다. 이 설명을 모든 지역변수에 확대하지 않는다. `char`의 비어 보이는 표기는 유효한 빈 문자 리터럴로 확정하지 않고 검증 필요로 남긴다. [M01 p.10] [M01 p.11] [M01 p.12] [M01 p.13] [M01 p.14]

</details>

#### R07. Method 호출을 읽을 때 코드 본문 외에 무엇을 확인해야 하는가?

<details><summary>정답</summary>

어느 Object를 통해 호출했는지 확인해야 한다. `car.printHello()`는 점과 Method 이름, 괄호를 이용한 호출이다. `getSpeed()`처럼 자신의 Attribute를 읽는 Method는 호출 대상의 상태를 사용하므로, 자료의 두 호출 결과는 각각 `100`, `90`이다. [M01 p.15] [M01 p.16]

후취득 M02의 별도 Car에서는 `speed = 10`이고 `getSpeed()`는 `10*speed`를 반환하므로 field와 Method의 결과는 `10`, `100`이다. 같은 이름이라도 Method 본문을 읽어야 하며 M01의 `return speed`와 혼동하지 않는다. [M02 p.6] [M02 p.7]

</details>

#### R08. Constructor의 이름·반환형·Parameters에 관한 규칙은 무엇인가?

<details><summary>정답</summary>

이름은 Class와 같고 반환형 자체를 쓰지 않는다. 따라서 `void`를 붙이는 것이 아니다. Parameters를 받아 초기화할 수 있고, 서로 다른 Parameters를 갖는 여러 Constructor를 둘 수 있다.

자료에서 한 Parameter를 받는 `Car` Constructor는 `weight`를 설정하고 `"unknown"`인 `color`를 유지한다. 두 Parameters를 받는 Constructor는 둘 다 설정한다. 다른 Constructor에서 Constructor를 호출할 수 있다는 보충도 있으므로, 초기화 과정에서 언제나 Constructor 본문 하나만 실행된다는 규칙으로 확대하지 않는다. [M01 p.19] [M01 p.20] [M01 p.21] [M01 p.22]

후취득 M02 p.8은 `carNumber`·`model` 초기화 예시를 싣지만 `myCar`와 `mayCar.model`이 불일치한다. 출력 상자가 있다고 인쇄된 코드의 성공 실행을 확정하지 않는다. [M02 p.8]

</details>

#### R09. `speed = speed`, `this.speed = speed`, `this()`는 어떻게 다른가?

<details><summary>정답</summary>

같은 이름의 Parameter가 있는 문맥에서 `speed = speed`는 Parameter 자기 대입이다. `this.speed = speed`는 현재 Object의 Attribute에 Parameter 값을 넣는다.

`this`는 현재 Object의 Method를 명시할 때도 쓰이며, 이름 충돌 등이 없는 자료의 문맥에서는 생략할 수 있다. `this()`는 Attribute 접근이 아니라 다른 Constructor 호출을 위한 용법이다. 세부 호출 규칙은 자료에서도 후속 설명으로 남겨 둔다. [M01 p.23] [M01 p.24] [M01 p.25] [M01 p.26]

후취득 M02의 제한된 예시에서는 `Car()`가 `this(55, "blue")`로 두 Parameters Constructor를 호출한다. 생성된 Car 하나의 fields는 `55`, `"blue"`이며, Constructor 본문 둘의 연결이 Object 두 개의 생성을 뜻하지 않는다. `this.printSpeed()`는 Method 호출이다. [M02 p.9]

</details>

#### R10. 녹음의 `owner` 값이 여러 Instance에서 함께 바뀌어 보이는 이유는 무엇인가?

<details><summary>정답</summary>

`owner`가 Class에 연결된 하나의 `static` 상태이기 때문이다. Object마다 독립적인 문자열을 바꾸는 상황으로 해석하면 안 된다. 최초 STT만으로 확정하지 못했던 코드는 후취득 M02 p.10에서 확인된다. `Alice`, `Bob` 순서로 생성한 뒤 공유 owner는 `Bob`, `car1.owner = "Carol"` 뒤 `car2.printOwner()`는 `Carol`, `Car.owner = "David"` 뒤 `Car.printOwner()`는 `David`이다. 이는 자료 확인이며 불확실한 STT의 동사를 복원한 것이 아니다. [M02 p.10]

자료의 별도 예시에서는 세 Object를 생성하면 `num`이 `3`이고, `setMile()`에 `20`, `30`, `40`을 한 번씩 전달하면 `totalMile`은 `90`이다. 이 누적 코드는 호출 때마다 입력을 더하므로 항상 현재 개별 값들의 합을 자동 유지한다고 일반화할 수 없다. [STT 05:41] [STT 06:33] [M01 p.29] [M01 p.30]

</details>

#### R11. `static totalFuel()`의 `return fuel`은 왜 문제인가?

<details><summary>정답</summary>

`fuel`은 Object별 Attribute인데, 해당 접근에는 어느 Object의 `fuel`인지 지정되어 있지 않다. 자료는 이를 오류로 표시한다. 정상 실행 후 출력값을 묻는 문제로 처리하면 안 된다.

이 제한을 `static` 코드에서 Object 상태에 접근하는 모든 경우의 금지로 확대하지 않는다. 실제 Object를 가리키는 변수를 통해 `meter.value`처럼 대상을 지정하는 경우와 구분해야 한다. instance Method에서 `static` Method를 호출하는 방향은 자료에서 허용된다고 설명한다. [M01 p.31] [M01 p.32]

</details>

#### R12. 내용이 같은 두 `new String("hey")`의 `==` 결과는 무엇인가?

<details><summary>정답</summary>

각각 생성한 두 Object를 비교하면 `false`이다. `str3 = str1`로 같은 reference 값을 대입한 뒤 `str1 == str3`를 비교하면 `true`이다. 내용 비교와 동일 Object 여부를 구별한다.

이 결과는 자료 코드의 결과이다. 08:32의 충돌하는 부정 표현을 확정된 발화로 고친 것이 아니며, 그 발화의 정확한 의도는 검증 필요이다. [M01 p.34] [M01 p.35] [STT 08:32]

</details>

#### R13. reference 변수가 네 개이면 Object도 네 개인가?

<details><summary>정답</summary>

그렇지 않다. 자료의 `myCar = new Car()` 이후 `car1 = myCar`, `car2 = myCar`, `car3 = car1`은 모두 하나의 Object를 가리킨다. reference 대입은 그 자체로 Object 생성·복제·이동이 아니다. [M01 p.36]

</details>

#### R14. 정수 Parameters를 교환하는 `swap()`은 호출자의 변수도 교환하는가?

<details><summary>정답</summary>

자료의 코드에서는 교환하지 않는다. 값이 복사되어 전달되므로 `swap`의 Parameters와 호출자의 변수는 별개이다. 반환 직전 내부 `a`, `b`, `temp`는 `3`, `2`, `2`이지만, 호출자의 `a`, `b`는 `2`, `3`으로 남는다. [M01 p.37] [M01 p.38] [M01 p.39]

</details>

#### R15. Object의 Attribute 변경이 호출자에게 보이는 것은 Java의 전달 방식을 어떻게 설명하는가?

<details><summary>정답</summary>

복사된 reference 값이 같은 Object를 가리키므로, Method 안에서 그 Object의 Attribute를 바꾸면 호출자도 변경을 본다. Object 전체가 복사되거나 호출자 변수 자체가 Parameter와 하나가 된 것은 아니다.

자료의 `IntHolder` 예시는 두 Object의 `value`를 바꾸어 `3`, `2`를 만든다. 녹음의 Call-by-Reference라는 표현에는, Java는 reference 값도 값으로 전달한다는 노트의 보충 설명을 함께 적용한다. [STT 11:07] [STT 11:52] [M01 p.40] [M01 p.41] [M01 p.42] [M01 p.43]

</details>

#### R16. 자동 Memory Management에서 할당과 회수는 각각 어떤 역할인가?

<details><summary>정답</summary>

Object를 생성할 때 필요한 메모리가 할당되고, Garbage Collector는 사용되지 않는 Object의 메모리를 회수한다. 자동이라는 설명은 `new` 같은 생성 표현이 필요 없다는 뜻도, 특정 문장 직후 즉시 회수가 보장된다는 뜻도 아니다. [STT 11:52] [STT 12:50] [M01 p.44]

</details>

#### R17. Lab3의 큰 구조, 준비 순서와 제출 규칙은 무엇인가?

<details><summary>정답</summary>

OOP 복습 뒤 두 플레이어의 fighting game에 적용한다. STT는 세 Class와 round별 교대 동작을 소개한다. 준비 순서는 eTL에서 skeleton ZIP 받기 → 압축 해제 → IntelliJ project로 열기 → 해당 source files 작성이다. [STT 12:50] [STT 13:47]

후취득 공식 자료는 Class 이름을 `Player`, `Fight`, `Main`으로 확인한다. 기본 최대치는 100 rounds이며 조기 종료와 동점 규칙은 R27–R28에서 구분한다. 실습 시간 안내의 `0:10`, `0:15`, `15mins`는 이 round 수가 아니다. [M02 p.3] [M02 p.16] [M02 p.18] [M02 p.23] [M02 p.25]

PDF의 제출 규칙은 **10월 1일 오후 6시 29분까지 eTL 제출**이며 성적에 반영된다. `src` 폴더에 `Main.java`, `Fight.java`, `Player.java`를 넣고 폴더를 ZIP으로 압축한다. 본인의 학번·이름으로 `20XX-XXXXX_{name}.zip` 형식을 사용한다. 이는 9월 19일 확보한 자료 안내이며 녹음에서 복원한 마감 발화가 아니다. [M02 p.29]

</details>

#### R18. `Player`의 확인된 상태와 다섯 Methods는 무엇인가?

<details><summary>정답</summary>

상태는 플레이어의 문자열 식별 정보, health, 여러 Methods에서 사용할 random 관련 Attribute이다. Methods는 `attack`, `getDamaged`, `heal`, `isAlive`, `getTactic`이다. 필요한 정보를 얻기 위한 추가 Methods를 만들 수 있다는 안내도 있다.

후취득 자료의 선언은 `private String userId`, `private int health = 50`, `Random random`이며 `Random`은 `java.util.Random`이다. `random`에는 명시적 access modifier가 없다. `attack`, `heal`, `isAlive`, `getTactic`은 public, `getDamaged`는 private이다. 추가 조회 Methods는 허용되지만 특정 getter 이름은 요구하지 않는다. PDF p.19의 `attach()`는 p.21과 skeleton의 `attack`과 구별한다. STT의 잘린 `The health attribute should be and`는 그대로 남긴다. [STT 14:47] [STT 15:41] [M02 p.19] [M02 p.20] [M02 p.21] [M02 p.22] [M03 p.3]

</details>

#### R19. health에서 직접 확인되는 값과 문맥상 연결되는 값은 무엇인가?

<details><summary>정답</summary>

초기 health `50`, 음수 금지, health가 `0`이면 해당 플레이어가 패배하고 게임이 종료된다는 조건은 15:41에서 확인된다. 17:39의 초기값을 넘지 않는다는 설명을 연결하면 상한을 `50`으로 읽을 수 있다.

최초 STT에서 상한 50은 두 설명을 연결한 해석이었다. 후취득 M02 p.21은 상한 `50`을 명시하고, M02 p.22와 skeleton은 초기 선언 `private int health = 50`을 확인한다. 현재 자료상 범위는 `0 ≤ health ≤ 50`이다. 마지막 발화나 사용자의 기술 확인으로 이를 대신 설명하지 않는다. Health 0은 simulation의 패배·종료 조건이며 즉시 memory 회수 조건이 아니다. [STT 15:41] [STT 17:39] [M02 p.15] [M02 p.21] [M02 p.22] [M03 p.3]

</details>

#### R20. `attack`과 `getDamaged`의 책임은 어떻게 다른가?

<details><summary>정답</summary>

`attack`은 상대의 health를 `1`부터 `5` 사이에서 선택한 정수만큼 줄이는 동작이다. `getDamaged`는 해당 플레이어의 health를 지정된 양만큼 줄이는 역할이다.

최초 STT는 공격량의 무작위 선택을 명시하지 않았지만, 후취득 PDF는 `public void attack(Player opponent)`의 random integer `1–5`(양 끝 포함)를 명시한다. `private void getDamaged(int damage)`는 받은 지정량을 자신의 health에 반영하며 따로 피해량을 다시 뽑는 책임이 아니다. `random.nextInt()`는 hint이며 별도의 동일 확률 요구·필수 API·유일한 내부 호출 구조를 추가하지 않는다. 두 경우 모두 health의 하한을 지켜야 한다. [STT 16:37] [M02 p.21] [M03 p.3]

</details>

#### R21. `heal`에서는 증가량만 확인하면 충분한가?

<details><summary>정답</summary>

충분하지 않다. `heal`은 random integer `1–3`(양 끝 포함)를 선택하지만 최종 health는 상한을 지켜야 한다. 후취득 PDF는 상한 `50`, PDF와 skeleton은 Parameters·반환값이 없는 `public void heal()`을 확인한다. Health `49`에 선택량 `3`을 그대로 더한 `52`는 허용되지 않는다. 선택량과 실제 증가량을 구분하며 동일 확률·필수 random API·유일한 경계 처리 코드는 추가하지 않는다. STT의 `initial value of the system`은 수정하지 않는다. [STT 16:37] [STT 17:39] [M02 p.21] [M03 p.3]

</details>

#### R22. `isAlive`의 판단 경계와 PDF·skeleton의 선언 차이는 무엇인가?

<details><summary>정답</summary>

health가 `0`보다 크면 `true`, 그렇지 않으면 `false`이다. 허용되는 health가 음수가 아니라는 조건 안에서 `false`가 되는 경계는 `0`이다.

생존 판단과 전체 Fight의 종료 판단은 별도 책임이다. 후취득 PDF는 `public Boolean isAlive()`, 실제 skeleton은 `public boolean isAlive()`이다. `Boolean`은 wrapper Class, `boolean`은 primitive type이며 실제 코드 계약은 skeleton의 선언으로 읽는다. Body는 여전히 `Problem 1.1`이다. [STT 15:41] [STT 17:39] [M02 p.21] [M02 p.25] [M03 p.3]

</details>

#### R23. `getTactic`에서 확인된 책임과 미확정 정보는 무엇인가?

<details><summary>정답</summary>

확인된 책임은 공격 또는 회복의 선택이다. 확률 표현에는 공격 쪽 `70`, 회복 쪽 `30%`가 있으며, 공격 쪽 퍼센트 단위는 빠져 있다. 공격 70%·회복 30%는 문맥상 해석으로 구분한다.

이는 최초 STT-only 판단이다. 후취득 PDF는 `public char getTactic()`, attack `70%`이면 `'a'`, heal `30%`이면 `'h'`를 명시하며 skeleton도 `char`를 확인한다. `random.nextFloat()`는 hint이고 완성된 분기는 제공되지 않았다. 10회 선택에서 반드시 7회 공격한다는 규칙이 아니며 `'a'` 반환만으로 공격이 실행된 것도 아니다. Fight/Main 명세 역시 후취득 자료 보충이다. 원문의 `70`과 미완성 마지막 문장은 그대로 보존한다. [STT 17:39] [M02 p.21] [M02 p.25] [M02 p.27] [M03 p.3]

</details>

#### R24. Player Constructor의 입력·초기화와 seed의 역할은 무엇인가?

<details><summary>정답</summary>

후취득 PDF의 `Player(String userId, int randomSeed)` 예시는 ID와 seed로 만든 Random을 설정한다. Health `50`은 field 초기값이다. Constructor에는 return type과 명시적 access modifier가 없으며 실제 skeleton의 본문은 `Problem 1.1`로 남아 있다. PDF 예시가 ZIP에 이미 구현되어 있다고 판단하지 않는다. [M02 p.22] [M03 p.3]

Seed는 health나 round 수가 아니라 난수 생성의 시작 상태를 정하는 입력이다. 같은 Random 구현·같은 seed·같은 Methods와 인자의 호출 순서가 수열 재현의 조건이다. 같은 seed만으로 전체 simulation 결과가 같다고 보장할 수 없다. 매 동작마다 재생성하는 것은 진행 중인 수열을 이어 쓰는 것과 다르다. [M02 p.20] [M02 p.22]

</details>

#### R25. Fight의 네 fields와 Constructor는 무엇을 연결하는가?

<details><summary>정답</summary>

`int timeLimit = 100`은 최대 rounds, `int currRound = 0`은 시작 전 상태이다. 첫 실제 round는 1이다. `Player p1`, `Player p2`는 기존 Player를 가리킨다. Constructor는 입력 references를 `this.p1`, `this.p2`에 저장하며 Player를 새로 만들거나 복제하지 않는다. Fields와 Constructor에는 명시적 access modifier가 없다. 이 Constructor는 skeleton에 제공되어 있으나 세 Methods 본문은 `Problem 1.2`이다. [M02 p.23] [M02 p.24] [M02 p.26] [M03 p.1]

</details>

#### R26. 한 round에서 p2가 행동하지 않는 조건과 출력 순서는 무엇인가?

<details><summary>정답</summary>

`proceed()`는 `Round <Round_Number>`를 먼저 출력하고 p1의 tactic에 따른 행동을 진행한다. P1의 공격으로 p2의 health가 0이면 p2의 attack과 heal을 모두 생략한다. 살아 있으면 p2가 자신의 tactic에 따라 행동한다. 두 동작을 동시에 적용하는 규칙이 아니다. [M02 p.25]

이후 `<Player1_userID> health : <Player1_health>`, `<Player2_userID> health : <Player2_health>`를 차례로 출력한다. 첫 round 번호는 1이며 출력 health는 행동 후 상태이다. 필요한 조회 Method의 이름은 명세로 추가하지 않는다. [M02 p.25] [M03 p.1]

</details>

#### R27. Fight는 언제 끝나며 PDF와 skeleton의 반환형은 어떻게 다른가?

<details><summary>정답</summary>

어느 한 Player의 health가 0이거나 마지막 round가 완료되면 끝난다. 두 조건을 모두 요구하지 않는다. 둘 다 살아 있으면 기본 설정에서 round 99 완료 후에는 계속하고, 100 완료 후에는 끝난다. `timeLimit`는 seconds가 아니다. PDF는 `Boolean isFinished()`, skeleton은 primitive `boolean isFinished()`이며 본문은 미구현이다. 이 종료는 즉시 Garbage Collection을 뜻하지 않는다. [M02 p.15] [M02 p.23] [M02 p.24] [M02 p.25] [M03 p.1]

</details>

#### R28. 동점 규칙과 getWinner의 반환 대상은 무엇인가?

<details><summary>정답</summary>

Health가 큰 Player가 승자이고, 같으면 p2가 이긴다. 자료의 이유는 p1이 먼저 행동한다는 것이다. `public Player getWinner()`는 승자를 가리키는 Player reference를 반환한다. ID 문자열·참거짓·무승부 표시를 반환하는 계약이 아니다. Main이 그 승자의 ID를 최종 출력에 사용한다. [M02 p.25] [M02 p.27] [M03 p.1]

</details>

#### R29. Main의 입력·생성·진행·출력 책임과 선수 개념은 무엇인가?

<details><summary>정답</summary>

`public static void main(String[] args)`에서 Scanner로 두 `int` seeds를 입력받고 `Gryffindor`, `Slytherin` Players와 Fight를 생성한다. Fight가 끝날 때까지 진행한 뒤 승자 ID로 `<userID> is the winner!`를 출력한다. Seed 입력이 health나 round 제한을 정하는 것은 아니다. [M02 p.27]

한 round는 `proceed`, 종료 판단은 `isFinished`, 승자 선택은 `getWinner`가 맡는다. 구현에는 Constructor·reference·Method 외에 Scanner 입력, 조건 판단, 반복문이 필요하다. PDF의 빈 Attributes 항목이나 p.28 제목만으로 별도의 Main field·Constructor를 요구하지 않는다. Skeleton의 `Problem 1.3`은 미구현이며 이 흐름은 누락 발화가 아닌 후취득 공식 명세이다. [M02 p.25] [M02 p.27] [M02 p.28] [M03 p.2]

</details>

### 2단계: 실제 기출의 요구에 적용하기

**Class–Object 설명 요구:** 2025-1 중간고사 복기본의 1번은 두 개념을 설명하도록 요구한다. 현재 범위에서는 두 정의를 제시하고, 하나의 Class로 여러 개별 Object를 만들 수 있다는 관계를 덧붙이는 접근이 가능하다. 단어 번역만 나열하지 않고 설계도와 생성 결과를 구분해 보라. [EX:cp_2025_1_midterm_q01 p.1]

<details><summary>정답</summary>

답안의 출발점은 “Class는 Object를 만들 공통 구조를 정한다”와 “Object는 그 구조로 만든 개별 대상이다”이다. 여기에 같은 Class의 Object들이 같은 Members 구조를 갖더라도 각자의 상태를 가질 수 있다는 설명을 연결한다. 다음 P01은 이 설명 요구에 작은 코드 판별을 더한 새 연습이다. 기출에 해당 코드가 있었다는 뜻은 아니다. [M01 p.7] [M01 p.14] [M01 p.17]

</details>

`this` 비교, `static` 중첩 Class 오류 수정, Garbage Collector의 엄밀한 수거 조건을 묻는 문항은 현재 개념과 일부 연결되지만 요구 전체가 같지는 않다. 아래 연습에서는 그 추가 개념을 배운 것으로 가정하지 않으며, 재검토 범위는 마지막 복습 계획에서 구분한다.

### 기출 스타일 기반 예상·변형 문제

#### 연습 P01

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** Class, Object, Instance, Instantiate의 관계. [STT 01:51] [STT 03:40] [M01 p.7] [M01 p.9] [M01 p.17]
- **요구 능력:** 정의를 코드의 Class 선언·변수·생성 결과에 대응시키기.
- **선수 개념:** Class와 `new`의 기본 의미.
- **접근 상태:** 현재 접근 가능.
- **스타일 근거:** Class와 Object를 설명하는 요구를 가져왔다. 아래 코드 판별은 별도로 추가한 변형이다. [EX:cp_2025_1_midterm_q01 p.1]

**이해를 위한 예시:** 다음 Class 선언과 실행 조각을 읽어라. 실행 조각은 적절한 Method 안에서 한 번 실행한다고 가정한다.

```java
class Lamp { }
```

```java
Lamp left = new Lamp();
Lamp right = new Lamp();
```

1. Class, reference 변수, 생성된 Object를 각각 구별하라.
2. 두 Object가 같은 Class에 속한다는 것과 같은 하나의 Object라는 것은 왜 다른가?
3. `Instantiate`라는 말에 해당하는 동작을 찾아라.

<details><summary>풀이와 채점 포인트</summary>

`Lamp`는 Class이고 `left`, `right`는 생성된 Object를 가리키는 변수이다. 두 생성 표현이 각각 실행되므로 이 조각에서는 서로 다른 두 Object가 만들어진다.

두 Object가 같은 Class에 속한다는 것은 공통 구조를 따른다는 뜻이다. 하나의 동일한 Object라는 뜻은 아니다. 각 `new Lamp()`의 실행이 Instance 생성, 즉 Instantiate에 해당한다.

**채점 포인트:** Class·변수·Object 구별 1점, 공통 구조와 개별 대상 구별 1점, 두 생성 동작 식별 1점. 빈 Class라는 이유로 Object가 생성되지 않는다고 답하면 안 된다.

</details>

#### 연습 P02

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** OOP의 대상·상호작용 관점과 Encapsulation, Inheritance, Polymorphism의 동기. [STT 00:00] [STT 00:55] [STT 01:51] [M01 p.3] [M01 p.4] [M01 p.58] [M01 p.59] [M01 p.60] [M01 p.61]
- **요구 능력:** 설계 설명에서 관련 개념을 고르고, 그 이유와 현재 설명의 한계를 밝히기.
- **선수 개념:** Object, Class, Attributes, Methods.
- **접근 상태:** 현재 접근 가능. 상세 접근 제어·Inheritance 구현 문법은 요구하지 않는다.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

**이해를 위한 예시:** 도서 대여 서비스를 설명하는 다음 요구를 읽어라.

1. 책뿐 아니라 대여 기록도 프로그램의 대상으로 표현한다.
2. 입력 확인·기록 갱신 같은 순서와 함께, 어떤 대상이 어떤 동작을 맡는지 정한다.
3. 사용하는 쪽이 내부 저장 방식을 직접 다루지 않고 필요한 동작을 요청하게 한다.
4. 공통 특성을 이어받아 재사용하면서 새 특성을 추가한다.
5. 관련된 여러 Class에서 유사한 동작을 서로 다르게 구현한다.

각 문장을 OOP 관점 또는 세 특징과 연결하라. 마지막으로 “OOP에서는 작업 순서가 필요 없다”는 결론이 타당한지 설명하라.

<details><summary>풀이와 채점 포인트</summary>

1번은 Object가 물리적 대상에 한정되지 않는다는 관점이다. 대여 기록처럼 개념적인 대상도 표현할 수 있다. 2번은 Object와 동작의 책임을 함께 보는 OOP의 구성 관점이다.

3번은 내부 구현과 외부 사용 사이의 경계를 두려는 Encapsulation의 동기이다. 4번은 재사용과 확장을 연결하는 Inheritance, 5번은 유사한 동작의 다른 구현을 설명하는 Polymorphism에 대응한다.

OOP에서도 입력 확인 후 갱신하는 등의 실행 순서는 필요하다. 자료의 Coffee Machine 역시 작업을 수행한다. 관련 동작을 어디에 묶는지와 실행 순서가 존재하는지는 별개의 질문이다.

**채점 포인트:** 물리적·개념적 대상 구별 1점, 세 특징과 이유의 연결 각 1점, 작업 순서에 관한 오개념 교정 1점. 이 설명만으로 구체적인 Java 구현이 정해졌다고 주장하지 않는다.

</details>

#### 연습 P03

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** Attributes 초기화, Object별 상태, Methods, Constructor, `this`. [STT 02:44] [STT 03:40] [STT 04:37] [M01 p.12] [M01 p.14] [M01 p.16] [M01 p.22] [M01 p.26]
- **요구 능력:** 초기화와 대입의 대상을 추적하고 호출 결과를 설명하기.
- **선수 개념:** 정수·문자열 값, 대입, Method 호출.
- **접근 상태:** 현재 접근 가능.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

**이해를 위한 예시:** 다음 코드는 Lab3와 별도로 만든 상태 표시기이다.

```java
class Gauge {
    int level;
    String color = "gray";
    boolean ready;

    Gauge(int level) {
        this.level = level;
    }

    Gauge(int level, String color) {
        this.level = level;
        this.color = color;
    }

    void wrong(int level) {
        level = level;
    }

    void set(int level) {
        this.level = level;
    }

    int read() {
        return level;
    }
}
```

실행 조각은 적절한 Method 안에서 한 번 실행한다.

```java
Gauge a = new Gauge(6);
Gauge b = new Gauge(9, "blue");
a.wrong(14);
a.set(4);
```

1. 생성 직후 각 Object의 `level`, `color`, `ready`를 적어라.
2. `a.wrong(14)` 직후와 `a.set(4)` 직후의 `a.read()` 결과를 구하라.
3. 마지막 `b.read()` 결과와 그 이유를 설명하라.
4. 두 Constructor에 `void`를 붙여도 같은 의미인가?

<details><summary>풀이와 채점 포인트</summary>

생성 직후의 상태는 다음과 같다.

| Object | `level` | `color` | `ready` |
| --- | --- | --- | --- |
| `a`가 가리키는 Object | `6` | `"gray"` | `false` |
| `b`가 가리키는 Object | `9` | `"blue"` | `false` |

첫 Constructor는 `level`만 설정하므로 선언에서 정한 `color`가 남는다. 두 번째 Constructor는 두 Attributes를 모두 설정한다. `ready`는 명시적으로 초기화하지 않은 `boolean` Attribute이므로 `false`이다.

`wrong()`의 두 `level`은 모두 Parameter를 뜻하므로 Object 상태는 바뀌지 않는다. 따라서 그 직후 `a.read()`는 `6`이다. `set()`은 현재 Object의 Attribute에 대입하므로 그 뒤에는 `4`이다. `b`는 별개의 Object이므로 `b.read()`는 계속 `9`이다.

Constructor에는 반환형을 쓰지 않는다. `void`를 붙인 선언을 같은 Constructor로 취급할 수 없다.

**채점 포인트:** 초기 상태 표 2점, 두 대입 대상 구별 2점, `b`의 독립 상태 설명 1점, Constructor 반환형 구별 1점.

</details>

#### 연습 P04

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** `static` 공유 상태, instance 상태, 명시한 Object를 통한 접근. [STT 05:41] [STT 07:35] [M01 p.27] [M01 p.30] [M01 p.31] [M01 p.32]
- **요구 능력:** 공유값과 개별값을 따로 추적하고 실행 전 오류를 판별하기.
- **선수 개념:** Constructor, `this`, 대입과 누적, Method 호출.
- **접근 상태:** 현재 접근 가능.
- **유형:** 일반 연습 — 관련 기출 근거 없음. 중첩 Class의 오류 수정 문제를 재현하지 않는다.

**이해를 위한 예시:** 아래 Class에 대해, 이전 Object 생성이나 호출이 없는 상태에서 실행 조각을 한 번 실행한다.

```java
class Ticket {
    static int count;
    static int total;
    int value;

    Ticket(int value) {
        this.value = value;
        count++;
    }

    void put(int value) {
        this.value = value;
        total += value;
    }

    static int read(Ticket ticket) {
        return ticket.value;
    }
}
```

```java
Ticket a = new Ticket(0);
Ticket b = new Ticket(0);
a.put(4);
b.put(6);
a.put(1);
```

1. 마지막 `Ticket.count`, `Ticket.total`, `a.value`, `b.value`를 구하라.
2. `Ticket.total`은 마지막 두 Object의 `value` 합과 같은가?
3. `Ticket.read(a)`가 읽는 값은 무엇인가?
4. 별도로 `static int bad() { return value; }`를 Class에 추가하면 왜 문제가 되는가?

<details><summary>풀이와 채점 포인트</summary>

Constructor가 두 번 실행되므로 `count`는 `2`이다. `put()`은 매번 전달값을 공유 `total`에 더한다.

$$
\text{total}=4+6+1=11
$$

반면 각 Object의 `value`는 최근 대입값을 저장하므로 `a.value`는 `1`, `b.value`는 `6`이다. 현재 두 값의 합은 `7`이며 누적값 `11`과 다르다.

`Ticket.read(a)`는 `a`가 가리키는 Object를 명시하여 `value`를 읽으므로 결과가 `1`이다. 가정한 `bad()`의 `return value`에는 어느 Object의 Attribute인지 지정되어 있지 않으므로 컴파일 오류가 발생한다. 이를 실행 후 임의의 값이 나오는 상황으로 처리하지 않는다.

**채점 포인트:** 공유값과 개별값 계산 2점, 누적합과 현재 합 구별 1점, 명시한 Object 접근 설명 1점, 오류의 원인과 단계 구별 1점.

</details>

#### 연습 P05

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** reference 대입, `==`, 정수의 값 전달, reference 값 전달과 Object 상태 변경. [STT 08:32] [STT 09:27] [STT 11:07] [M01 p.35] [M01 p.36] [M01 p.38] [M01 p.42]
- **요구 능력:** 변수와 Object를 분리한 상태 추적.
- **선수 개념:** Constructor, Attributes, Parameters, 대입.
- **접근 상태:** 현재 접근 가능. `More on swaps`의 별도 심화 예제를 요구하지 않는다.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

**이해를 위한 예시:**

```java
class Box {
    int value;

    Box(int value) {
        this.value = value;
    }
}

class Tools {
    static void touch(int n, Box box) {
        n = 8;
        box.value = 8;
        box = new Box(20);
        box.value = 21;
    }
}
```

다음 실행 조각은 적절한 Method 안에서 한 번 실행한다.

```java
int n = 3;
Box a = new Box(3);
Box c = new Box(3);
Box alias = a;
Tools.touch(n, a);
```

1. 호출 뒤 `n`, `a.value`, `alias.value`, `c.value`는 각각 무엇인가?
2. `a == c`와 `a == alias`의 결과는 무엇인가?
3. `touch()` 안에서 `box`에 새 reference를 대입했는데도 `a`가 그 새 Object를 가리키지 않는 이유는 무엇인가?

<details><summary>풀이와 채점 포인트</summary>

호출 시 정수 값과 reference 값이 각각 복사된다.

| 확인 대상 | 호출 뒤 값 |
| --- | --- |
| 호출자의 `n` | `3` |
| `a.value` | `8` |
| `alias.value` | `8` |
| `c.value` | `3` |
| `a == c` | `false` |
| `a == alias` | `true` |

내부 `n = 8`은 복사된 Parameter만 바꾼다. 첫 `box.value = 8`은 아직 `a`와 같은 Object를 가리키는 reference를 통해 그 Object의 상태를 바꾼다. `alias` 역시 같은 Object를 가리키므로 `8`을 관찰한다.

이후 `box = new Box(20)`은 내부 Parameter에 새 reference 값을 넣는다. 호출자의 `a`에 대입하는 문장이 아니므로 `a`는 기존 Object를 계속 가리킨다. 마지막 `21`은 새 Object의 값이다. `c`가 가리키는 별개의 Object는 변경되지 않는다.

**채점 포인트:** 네 상태값 2점, 두 동일성 비교 1점, 값 복사·Object 변경·Parameter 재대입의 세 단계 설명 3점. 실제 물리적 주소값을 가정할 필요는 없다.

</details>

#### 연습 P06

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** 자동 메모리 할당과 Garbage Collector의 역할, reference 관계에 대한 과도한 추론 피하기. [STT 11:52] [STT 12:50] [M01 p.36] [M01 p.44]
- **요구 능력:** 자료가 뒷받침하는 설명과 보장하지 않는 설명을 구분하기.
- **선수 개념:** Object 생성, 여러 reference가 같은 Object를 가리키는 관계.
- **접근 상태:** 현재 접근 가능. 엄밀한 수거 대상 판정 규칙 전체는 요구하지 않는다.
- **유형:** 일반 연습 — 관련 기출 근거 없음. 기출의 수거 조건 요구 전체를 단순화한 재현 문제로 취급하지 않는다.

다음 설명을 판단하고 이유를 적어라.

1. Java가 메모리를 자동 관리하므로 프로그램에서 Object 생성 표현을 작성할 필요가 없다.
2. Object 생성에 필요한 공간을 마련하는 일과 사용되지 않는 Object의 공간을 회수하는 일은 구분할 수 있다.
3. 같은 Object를 가리키던 변수 하나를 더 이상 사용하지 않으면, 다른 변수의 상태를 보지 않고 즉시 회수를 확정할 수 있다.
4. 제공 자료만으로 특정 대입문 다음 순간의 회수 시점을 정할 수 있다.

<details><summary>풀이와 채점 포인트</summary>

1번은 부적절하다. 자료의 예시는 `new`로 Object를 생성하면서 메모리 관리는 자동으로 이루어진다고 설명한다.

2번은 적절하다. 할당과 회수는 서로 다른 역할이다.

3번은 부적절하다. 다른 변수가 여전히 같은 Object를 가리킬 수 있다. 변수 하나의 사용 여부만으로 Object 전체의 사용 관계와 즉시 회수를 확정할 수 없다.

4번도 부적절하다. 자료는 정확한 회수 시점이나 알고리즘을 제공하지 않는다.

**채점 포인트:** 각 판단과 이유 1점씩, 총 4점. 단순히 “자동이므로 알아서 된다”는 답은 역할과 보장 범위를 구분하지 못한다.

</details>

#### 연습 P07

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** Lab3 운영 안내, 세 Class, Player Members, 후취득 공식 자료의 선언과 제출 규칙. [STT 12:50] [STT 13:47] [STT 14:47] [STT 15:41] [M02 p.17] [M02 p.18] [M02 p.20] [M02 p.21] [M02 p.29] [M03 p.3]
- **요구 능력:** 상태·동작·작업 절차를 분류하고 STT와 자료의 확인 범위를 구분하기.
- **선수 개념:** Class, Attributes, Methods, 선언의 type과 access modifier.
- **접근 상태:** 현재 접근 가능. 본문의 선언 범례를 사용하며 과제 구현은 요구하지 않는다.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

다음 과제 준비 메모를 **9월 19일 후취득한 공식 자료까지 포함하여** 점검하라.

> 두 플레이어가 등장한다. Player는 식별 정보와 health를 관리한다. attack, heal, isAlive, getTactic으로 Method 목록은 완전하다. 현재 자료에서 health의 정확한 자료형과 실행용 Class 이름을 확인할 수 있다. 추가 정보를 조회하는 Method는 만들 수 없다.

1. 맞는 내용과 수정할 내용을 구분하라.
2. 빠진 Method, random Attribute의 역할과 명시적 access modifier 유무를 적어라.
3. 프로젝트 준비 순서와 PDF의 제출 마감·파일 구성·ZIP 이름 형식을 적어라.
4. 자료로 확인된 항목 세 가지와 여전히 미구현인 부분을 구분하라.

<details><summary>풀이와 채점 포인트</summary>

두 플레이어와 Player의 상태 관리 책임은 맞다. 목록에는 `getDamaged`가 빠졌고 추가 정보 조회 Methods는 허용된다. 특정 getter 이름이 필수라는 뜻은 아니다. 후취득 자료에서 health는 `int`, 실행용 Class는 `Main`으로 확인된다. STT의 잘린 type 설명을 완성한 발화는 아니다.

`Random random`은 Player의 random 동작에 사용할 Object를 가리키며 명시적 access modifier가 없다. `private`인 `userId`·`health`와 구분한다. [M02 p.20] [M02 p.22] [M03 p.3]

준비는 skeleton ZIP 받기 → 압축 해제 → IntelliJ project로 열기 → source files 작성이다. PDF 기준 제출은 **10월 1일 오후 6시 29분까지 eTL**이며 성적에 반영된다. `src` 안에 `Main.java`, `Fight.java`, `Player.java`를 두고 폴더를 ZIP으로 압축하여 본인의 학번·이름으로 `20XX-XXXXX_{name}.zip` 형식을 사용한다. [M02 p.17] [M02 p.29]

자료로 확인된 항목의 예는 `int health`, `Main`, `char getTactic()`의 `'a'`/`'h'` 계약이다. 반면 Player Constructor·다섯 Methods, Fight의 세 Methods, Main의 실행 본문은 skeleton에서 미구현이다. Fight Constructor의 reference 저장은 제공되어 있다. 명세의 확보가 구현 완료를 뜻하지 않는다. [M02 p.21] [M03 p.1] [M03 p.2] [M03 p.3]

**채점 포인트:** 누락 Method·추가 조회 허용 각 1점, random 역할·modifier 각 1점, 준비 순서 1점, 마감·파일 구성·ZIP 형식 각 1점, 확인 항목 세 가지 1점, 미구현 본문과 제공된 Fight Constructor 구별 1점. 총 10점.

</details>

#### 연습 P08

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** health의 초기값·범위·종료 의미와 `attack`, `getDamaged`, `heal`, `isAlive`의 책임. 후취득 공식 자료의 random 범위와 상한도 사용한다. [STT 15:41] [STT 16:37] [STT 17:39] [M02 p.21] [M02 p.22] [M03 p.3]
- **요구 능력:** 관찰 결과가 확인된 요구와 양립하는지 판정하기.
- **선수 개념:** 상태 변경, 정수 범위, 비교와 참·거짓 값.
- **접근 상태:** 현재 접근 가능. 경계 처리 코드나 완성된 과제 해답은 요구하지 않는다.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

다음은 실제 실행 기록이 아닌 **이해를 위한 가상 점검 결과**이다.

1. 공격을 시작한 플레이어 자신의 health만 감소했다.
2. `attack`의 감소량으로 `6`을 선택했다.
3. `getDamaged`를 “지정된 피해량을 해당 플레이어에게 반영하는 동작”으로 분류했다.
4. `heal`에서 증가량 `3`을 선택하고, 기존 health `49`에 더한 `52`를 최종 health로 저장했다.
5. health가 `0`인 상태에서 `isAlive`가 `true`를 반환했다.
6. `attack`과 `heal`의 모든 가능한 양은 같은 확률로 나온다고 확정했다.

각 결과를 점검하라. 최초 STT에서는 상한 50이 문맥상 해석이었고, 후취득 M02 p.21에서는 직접 명시된 값임을 구분하라. Random 명세와 별도의 동일 확률 요구도 나누어 판단하라.

<details><summary>풀이와 채점 포인트</summary>

1번은 `attack`이 상대의 health를 감소시킨다는 설명과 맞지 않는다. 2번의 `6`은 확인된 `1`부터 `5` 사이의 범위를 벗어난다.

3번은 확인된 책임에 맞는다. 다만 이 책임 설명만으로 `attack`이 반드시 어떤 Parameters로 `getDamaged`를 호출해야 하는지까지 정해지지는 않는다.

4번에서 증가량 `3`은 범위에 속하지만 `49 + 3 = 52`는 후취득 PDF의 상한 `50`을 어긴다. 선택량과 허용 최종 상태를 함께 확인해야 한다. 이는 특정 경계 처리 코드만을 정답으로 지정한 것이 아니다. [M02 p.21]

5번은 `health > 0`일 때만 `true`라는 조건에 어긋난다. health `0`은 패배와 게임 종료 조건으로도 설명된다. 음수 health 역시 별도의 상태 범위 요구에 어긋난다.

6번의 별도 동일 확률 요구는 확인되지 않는다. 최초 STT의 attack 문장은 무작위를 명시하지 않았지만, 후취득 PDF는 attack `1–5`와 heal `1–3`을 모두 random으로 규정한다. 이 차이를 보존하면서 명세에 없는 분포 조건을 추가하지 않는다. [STT 16:37] [M02 p.21]

**채점 포인트:** 변경 대상·감소량·지정량 역할 구별 2점, 회복량과 상한 동시 점검 1점, 생존 경계와 종료 의미 1점, random 명세·별도 동일 확률 요구의 구별 1점.

</details>

#### 연습 P09

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** getTactic의 선택 책임, 최초 STT의 불완전한 표현과 후취득 공식 자료의 확률·반환 계약. [STT 17:39] [M02 p.21] [M03 p.3]
- **요구 능력:** STT 직접 확인·문맥상 해석·후취득 자료 확인을 구분하고 반환값의 의미를 설명하기.
- **선수 개념:** attack·heal, 반환값, char, 확률과 고정 횟수의 차이.
- **접근 상태:** 현재 접근 가능. 난수 생성 코드나 과제 분기는 요구하지 않는다.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

1. “행동을 선택한다”, “회복 30%라는 표현이 있다”, “공격 70%의 단위까지 STT에 있다”를 각각 판단하라.
2. 후취득 자료에서 확인한 정확한 반환형·반환 문자·확률을 적어라. 자료 확인으로 마지막 STT 문장을 완성해도 되는가?
3. getTactic이 `'a'`를 반환하면 이미 상대 health가 감소했는가? 10회 선택에서 공격이 반드시 7회인가?
4. `random.nextFloat()` hint와 skeleton의 미구현 본문을 구분하라.

<details><summary>풀이와 채점 포인트</summary>

1번의 앞 두 설명은 STT로 확인된다. 공격 쪽 원문은 `70`이고 퍼센트 단위가 없어 최초 70% 해석은 문맥에 근거한다. 중단된 마지막 문장은 그대로 남긴다.

후취득 PDF는 `public char getTactic()`, 공격 70%이면 `'a'`, 회복 30%이면 `'h'`를 명시한다. Skeleton도 char를 확인한다. 이는 자료상의 계약이며 그 문자들이 녹음에서 발화되었다는 증거가 아니다. [M02 p.21] [M03 p.3]

선택과 실행은 별도 책임이므로 `'a'` 반환만으로 health 변화가 일어난 것은 아니다. 70%는 선택 확률이지 매 10회를 7회 공격으로 맞추는 규칙이 아니다. Hint는 사용할 수 있는 안내이고 필수 API나 완성된 분기 구현이 아니다. Skeleton 본문은 `Problem 1.1`로 남아 있다.

**채점 포인트:** STT의 직접 근거와 해석 구별 2점, char·두 문자·확률 계약 2점, 누락 발화 미복원 1점, 선택/실행·확률/고정 횟수 구별 각 1점, hint/미구현 구별 1점. 총 8점.

</details>

#### 연습 P10

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** ID·seed 초기화, field 초기값, 난수 수열과 호출 순서, PDF 예시와 skeleton 구분. [M02 p.20] [M02 p.22] [M03 p.3]
- **요구 능력:** 초기화 책임과 재현 조건을 설명하기.
- **선수 개념:** Constructor, this, reference, 순차 실행.
- **접근 상태:** 현재 접근 가능. 본문의 seed 설명과 아래 가상 조건을 사용한다.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

후취득 PDF를 읽은 사람이 “Player Constructor는 health를 seed로 설정하며 ZIP에도 완성되어 있다”고 말했다. 이를 고쳐라. 이어 과제와 별개인 가상 생성기는 같은 seed·같은 호출로 항상 `2, 4, 1, …`을 낸다고 **문제에서 가정**한다. 한 생성기를 유지하며 두 번 호출할 때와 매번 같은 seed로 새로 만들고 첫 값만 얻을 때의 두 결과를 비교하라. 이 수열은 Java Random의 실제 seed별 출력이 아니다.

<details><summary>풀이와 채점 포인트</summary>

Health 50은 field 초기값이다. PDF Constructor 예시는 ID와 seed를 사용한 Random을 초기화하지만 실제 ZIP의 Constructor body는 비어 있다. Seed를 health로 대입하는 명세가 아니다. [M02 p.22] [M03 p.3]

가상 생성기를 유지하면 첫째·둘째 값인 `2, 4`, 매번 재생성하면 첫 값으로 되돌아가 `2, 2`이다. 같은 seed라도 호출 순서·종류·인자가 달라지면 같은 simulation 결과까지 보장하지 않는다. 두 생성기가 같은 수열을 낸다고 동일한 Object인 것도 아니다.

**채점 포인트:** health/ID·Random 초기화 구별 1점, PDF/ZIP 구현 상태 구별 1점, 두 결과 각 1점, 동일 seed만으로 충분하지 않은 이유 1점. 총 5점.

</details>

#### 연습 P11

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** Fight fields, 초기 round와 첫 round, Constructor의 reference 저장. [M02 p.23] [M02 p.24] [M02 p.26] [M03 p.1]
- **요구 능력:** 상태 단위와 Object 연결을 판별하기.
- **선수 개념:** Constructor, reference 대입, instance field.
- **접근 상태:** 현재 접근 가능.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

서로 다른 Player Objects A·B는 이미 생성되어 있다. 제공된 Fight Constructor에 A·B를 전달하고 아직 round를 진행하지 않았다. `timeLimit`, `currRound`, `p1`, `p2`의 의미를 설명하라. “Constructor가 Player 두 개를 복제한다”, “첫 출력은 Round 0이다”, “100초 후 끝난다”를 각각 판단하라.

<details><summary>풀이와 채점 포인트</summary>

`timeLimit = 100`은 최대 rounds, `currRound = 0`은 시작 전 상태이다. 첫 실제 출력 번호는 1이다. `p1`은 A, `p2`는 B를 가리킨다. Constructor는 전달된 references를 저장하므로 Player를 복제하지 않는다. A·B가 두 개 더 생기는 것도 아니다. 시간의 seconds와 round 횟수는 다른 단위이다. [M02 p.24] [M02 p.26]

**채점 포인트:** 두 정수 fields의 값·단위 2점, 두 reference의 대상과 복제 아님 2점, 첫 round 1 구별 1점. 총 5점.

</details>

#### 연습 P12

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** proceed의 순서, p2 행동 생략, 행동 후 health 출력. [M02 p.21] [M02 p.25] [M03 p.1]
- **요구 능력:** 주어진 중간 상태로 다음 행동과 출력을 추적하기.
- **선수 개념:** attack, heal, isAlive, 정수 뺄셈, 조건 판단.
- **접근 상태:** 현재 접근 가능. 난수 값은 문제에서 지정한다.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

가상 점검에서 이제 round 4를 진행한다. p1의 ID는 A, health는 8이고 p2의 ID는 B, health는 2이다. P1은 attack을 선택하고 damage 2를 준다. “P2도 같은 round 안에서 heal 3을 받아 살아난다”는 제안이 계약에 맞는지 판단하고 proceed의 출력 세 줄을 적어라. 실제 seeds나 과제 실행 결과를 재현하는 문제가 아니다.

<details><summary>풀이와 채점 포인트</summary>

P1 행동 뒤 B의 health는 `2 − 2 = 0`이다. 이 중간 상태에서 p2의 모든 행동을 생략하므로 heal도 실행하지 않는다. A의 health는 8이다. [M02 p.25]

```text
Round 4
A health : 8
B health : 0
```

**채점 포인트:** damage 적용과 A/B 상태 구별 1점, heal까지 생략하는 이유 1점, round 먼저·행동 후 두 health의 순서와 형식 2점. 총 4점.

</details>

#### 연습 P13

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** 종료의 두 조건, 마지막 round 완료, Boolean/boolean 차이. [M02 p.23] [M02 p.24] [M02 p.25] [M03 p.1]
- **요구 능력:** 서로 독립인 경계 사례의 종료 여부와 근거를 판단하기.
- **선수 개념:** 비교, 참·거짓, 논리 OR, round 완료의 의미.
- **접근 상태:** 현재 접근 가능. 구현 코드는 요구하지 않는다.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

기본 최대치 100 rounds에서 아래는 서로 독립인 **round 완료 후** 상태이다. 각 종료 여부와 근거를 적어라. PDF와 skeleton의 isFinished 반환형도 비교하라.

| 완료한 round | p1 health | p2 health |
| --- | --- | --- |
| 99 | 7 | 8 |
| 100 | 7 | 8 |
| 12 | 0 | 8 |
| 12 | 7 | 0 |

<details><summary>풀이와 채점 포인트</summary>

순서대로 `false, true, true, true`이다. 첫째는 둘 다 살아 있고 마지막 round가 끝나지 않았다. 둘째는 100 완료, 셋째·넷째는 한쪽 health 0으로 끝난다. 두 종료 조건을 동시에 요구하면 조기 종료를 놓친다. Round 100을 시작하기 전에 끝내거나 101까지 진행하는 해석도 다르다.

PDF의 `Boolean`은 wrapper Class이고 실제 skeleton은 primitive `boolean`이다. 종료 판정은 simulation의 상태이며 즉시 Garbage Collection을 보장하지 않는다. [M02 p.25] [M03 p.1]

**채점 포인트:** 네 판정과 근거 각 1점, 마지막 round 완료/조기 종료의 OR 관계 1점, type 차이 1점. 총 6점.

</details>

#### 연습 P14

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** getWinner의 health 비교, 동점 p2 규칙, Player 반환. [M02 p.25] [M02 p.27] [M03 p.1]
- **요구 능력:** 승자 선택과 반환값의 자료형을 구분하기.
- **선수 개념:** 정수 비교, reference, p1 선행 순서.
- **접근 상태:** 현재 접근 가능.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

각각 round 100을 마친 별개의 가상 경기에서 p1·p2 health가 `(17, 9)`, `(8, 12)`, `(6, 6)`이다. 각 승자와 getWinner가 반환할 대상을 설명하라. 마지막 경우에 ID 문자열 `"draw"`를 반환하자는 제안을 평가하라.

<details><summary>풀이와 채점 포인트</summary>

승자는 순서대로 p1, p2, p2이다. 앞 두 경우는 health의 크기, 마지막은 동점 규칙에 따른다. P1이 먼저 행동하므로 자료는 동점 승리를 p2에게 준다. 각 반환값은 선택한 Player를 가리키는 reference이다. `"draw"`는 동점 규칙에도, Player 반환 계약에도 맞지 않는다. Main에서 사용하는 승자의 ID 문자열과도 구분한다. [M02 p.25] [M02 p.27]

**채점 포인트:** 세 승자 각 1점, 동점 이유 1점, Player 반환과 String 구별 1점. 총 5점.

</details>

#### 연습 P15

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** Main의 seed 입력, 생성·진행·출력 책임, 빈 skeleton. [M02 p.22] [M02 p.25] [M02 p.27] [M02 p.28] [M03 p.2]
- **요구 능력:** 실행 계획의 책임 배분과 근거 없는 요구를 식별하기.
- **선수 개념:** Constructor, reference, Method 호출, Scanner 입력·조건·반복의 기본 의미.
- **접근 상태:** 현재 접근 가능은 명세 판독에 한정한다. 전체 구현에는 위 입력·반복 개념이 필요하다.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

가상 실행 메모를 점검하라. “Main이 받은 두 int는 각 Player의 초기 health이다. Main은 Players와 Fight를 준비하고, proceed를 한 번 부른 뒤 언제나 승자를 출력한다. Main 전용 field와 Constructor도 p.28 제목 때문에 필수이다.” 수정할 이유를 적어라. 별도로 종료 후 반환된 승자가 ID `Slytherin`인 Player라는 조건에서 최종 출력 한 줄을 적어라. 입력 seeds로 승자를 계산하는 문제는 아니다.

<details><summary>풀이와 채점 포인트</summary>

두 int는 seeds이다. Players의 지정 ID는 `Gryffindor`와 `Slytherin`이며 health 초기값은 별도 field의 50이다. Players와 Fight 생성이라는 책임은 맞지만 진행은 종료될 때까지 이어져야 한다. 한 round는 proceed, 종료 판정은 isFinished, 승자 선택은 getWinner가 맡는다. Main field 목록은 비어 있고 예시에는 main만 있으므로 제목에서 별도 필수 Constructor를 만들 수 없다. Skeleton의 실행 본문도 미구현이다. [M02 p.22] [M02 p.25] [M02 p.27] [M02 p.28] [M03 p.2]

주어진 승자 조건의 출력은 `Slytherin is the winner!`이다. 이를 특정 seed의 실제 결과로 일반화하지 않는다.

**채점 포인트:** seeds/health 구별 1점, 생성 책임·지정 ID 1점, 종료까지 반복과 세 Methods의 책임 2점, 필수 field/Constructor 추정 배제 1점, 최종 출력 1점. 총 6점.

</details>

#### 연습 P16

**새로 만든 연습 — 실제 기출·공식 정답이 아님**

- **범위:** Constructor 연결, this의 세 용법, field 값과 Method 계산의 차이. [M02 p.6] [M02 p.7] [M02 p.9]
- **요구 능력:** 초기화 순서와 계산 결과를 분리하기.
- **선수 개념:** instance field, Constructor Parameters, 곱셈, Method 호출.
- **접근 상태:** 현재 접근 가능.
- **유형:** 일반 연습 — 관련 기출 근거 없음.

과제와 별개인 가상 Meter를 다음처럼 정의했다고 가정한다. Field `level`의 선언 초기값은 9이다. Parameters가 없는 Constructor는 `this(4)`로 int Constructor에 연결하고, int Constructor는 `this.level = level`만 수행한다. `read()`는 `3 * level`을 반환한다. `new Meter()`를 한 번 실행하면 생성된 Meter 수, 초기화 후 field, read 결과는 무엇인가? `this.level`, `this.read()`, `this(4)`의 차이도 설명하라.

<details><summary>풀이와 채점 포인트</summary>

Meter는 하나이다. 선언 초기값 9 다음에 연결된 Constructor가 field에 4를 넣으므로 최종 level은 4이고 read 결과는 `3 × 4 = 12`이다. Method가 field를 그대로 반환한다고 이름만 보고 판단할 수 없다. `this.level`은 현재 Object의 field, `this.read()`는 Method 호출, `this(4)`는 같은 Object의 다른 Constructor로 이어지는 호출이다. Constructor 본문 둘을 거쳐도 Object 두 개가 생긴 것은 아니다.

**채점 포인트:** Object 수와 이유 1점, 최종 field와 계산 각 1점, this의 세 용법 각 1점. 총 6점.

</details>

### 다음 복습 계획

먼저 R01–R16과 P01–P06으로 Object·변수·대입 대상을 구분한다. P16으로 후취득 M02의 Constructor 연결과 Method 계산을 점검하고, R17–R23과 P07–P09로 Lab3의 선언·행동 계약을 읽는다. 이어 R24–R29 → P10–P15 순서로 seed·초기화, reference 연결, 한 round, 종료, 승자, Main의 책임을 적용한다. 틀린 답에는 어느 상태·순서·근거 수준을 혼동했는지 표시한다.

기출 재검토는 수업 범위와 필요한 선수 개념을 기준으로 나눈다. 이는 개인의 숙달 여부에 대한 판정이 아니다.

| 구분 | 문항 | 지금 연결할 수 있는 부분 | 이후 필요한 확인·학습 |
| --- | --- | --- | --- |
| 현재 접근 가능 | 2025-1 중간 1번, [EX:cp_2025_1_midterm_q01 p.1] | Class와 Object의 정의 및 관계 설명 | 설명에 설계도·개별 대상·공통 구조의 관계가 들어가는지 점검 |
| 부분 연결 | 2025-1 중간 3번, [EX:cp_2025_1_midterm_q03 p.2] | Garbage Collector와 자동 메모리 관리의 역할 | 프로그램에서 Object로 도달할 수 있는 reference 관계를 판단하는 엄밀한 기준, 수거 대상 조건과 실제 회수 시점의 구별 |
| 부분 연결 | 2025-1 중간 5번, [EX:cp_2025_1_midterm_q05 p.3] | 현재 Object를 가리키는 `this` | `super`와 상위 Class 관련 규칙을 배운 뒤 전체 비교 |
| 후속 선수 필요 | 2025-1 중간 7번, [EX:cp_2025_1_midterm_q07 p.4] | 어느 Object의 상태에 접근하는지 묻는 오류 분석의 출발점 | 중첩 Class의 의미와 지정된 Method 본문을 유지하는 수정 제약 |

전체 후보에는 이번에 소개된 단어와 일부 겹치더라도 제네릭, 상세 호출 선택 규칙, 컬렉션, 예외 처리, C++의 포인터·참조 등 추가 개념을 요구하는 문제가 있다. 이런 후보를 현재 연습의 근거로 억지로 연결하지 않는다.

후취득 M02/M03와 선언·반환 규칙·진행 조건을 대조한다. 새 목표에도 일반 연습을 유지하며 Lab3 명세를 기출 스타일의 근거로 사용하지 않는다. 연습은 작은 상태·책임 판단이며 미구현 skeleton 전체의 제출 해답이 아니다.

## 출처와 검증 상태

2026-09-20 공개 안내: Lab 3 보충 내용의 출처와 연습문제 검토를 마쳤습니다. 9월 19일 받은 공식 PDF·skeleton으로 9월 17일 녹음의 부족한 부분을 보충했으며, 누락된 발화를 복구한 것은 아닙니다. [[courses/computer_programming/transcripts/2026-09-17|개인정보를 가린 보정 STT]]를 함께 볼 수 있습니다.

### 일차 강의 근거

- **STT:** 사용자 확인에 따른 2026년 9월 17일 Computer Programming 실습의 익명화·정규화 전사. 근거 시각은 각 전사 구간의 시작 시각이다.
- **[M01] 이전 OOP 자료:** [4 oop.pdf](https://jhlee1020lee.github.io/2026-fall-study-hub/materials/computer_programming/4.oop.pdf), 총 62쪽. 이번 개념과 대응하는 PDF 3–44쪽과 58–61쪽을 자료 보충으로 사용한다. [M01 page cache manifest](https://jhlee1020lee.github.io/2026-fall-study-hub/page_cache/computer_programming/4.oop/manifest.json).
- **[M02] 후취득 공식 Lab3 자료:** [Lab03 v2.pdf](https://jhlee1020lee.github.io/2026-fall-study-hub/materials/computer_programming/Lab03.v2.pdf), 총 30쪽. 불완전한 9월 17일 녹음 이후 **9월 19일 확보**했다. [M02 page cache manifest](https://jhlee1020lee.github.io/2026-fall-study-hub/page_cache/computer_programming/lab03.v2/manifest.json). PDF와 cache 링크는 제공된 verified navigation에 근거한다.
- **[M03] 후취득 공식 비공개 skeleton:** 9월 19일 확보한 ZIP의 선언과 미구현 상태를 참조한다. **PDF가 아니며** `[M03 p.1]` = Section 1 `src/Fight.java`, `[M03 p.2]` = Section 2 `src/Main.java`, `[M03 p.3]` = Section 3 `src/Player.java`이다. M01/M02의 번호만 표지를 포함한 PDF 쪽수이다.
- **범위 제외:** M01의 `More on swaps`에 해당하는 45–56쪽을 이번 실습의 실제 강의 범위로 포함하지 않는다. 전체 자료의 존재가 모든 페이지의 수업 진행을 입증하지는 않는다.

녹음의 `static owner`, 정수 `number` 전달, `Car.speed` 변경에 대응하는 코드는 후취득 M02 pp.10–14에서 확인된다. M01의 Coffee Machine·IntHolder는 별도 보충 예시이다. M02의 Player·Fight·Main 명세와 M03의 선언·미구현 본문은 자료 근거이며 녹음의 빠진 뒷부분을 복원한 내용이 아니다. P01–P16은 별도로 만든 연습이고 실제 과제 실행이나 기출 재현이 아니다. [M02 p.10] [M02 p.13] [M02 p.14] [M02 p.21] [M02 p.25] [M02 p.27]

강의자가 `this`를 복습에 포함하며 중요하다고 언급한 점은 전사에서 확인된다. 이 언급을 출제 빈도나 이번 시험의 확률·우선순위로 바꾸지는 않는다. [STT 04:37]

### 전사 불확실성과 노트의 보충 설명

| 항목 | 노트에서의 처리 |
| --- | --- |
| 08:32의 `==` 결과 관련 부정 표현 | 정확한 발화 의도는 검증 필요. 서로 다른 두 생성 Object의 비교 결과는 M01 코드와 reference 관계로 설명한다. |
| Call-by-Reference라는 소개 | 발화 복원과 구분하여, Java는 reference 값도 값으로 전달한다는 노트의 보충 설명을 적용한다. |
| `static` Method의 접근 제한 | 어느 Object인지 지정하지 않은 직접 접근의 문제로 설명한다. 명시한 Object를 통한 접근까지 전면 금지하지 않는다. |
| Constructor의 입문적 표현 | 일반 Method와 구분하는 특별한 초기화 구성으로 설명한다. 다른 Constructor 호출 가능성을 보존한다. |
| 주소에 빗댄 reference 설명 | Object 동일성과 reference 관계를 설명하는 표현으로 읽는다. 실제 물리적 주소 배치를 보장하지 않는다. |
| 자동 메모리 관리 | 생성 시 할당과 Garbage Collector의 역할을 설명한다. 즉시 회수나 정확한 시점을 주장하지 않는다. |
| health의 상한 | 최초 STT에서 50은 문맥상 해석이었다. 후취득 [M02 p.21]은 상한 50을 직접 명시한다. `initial value of the system`이라는 원문은 보존한다. |
| `getTactic`의 공격 비율과 반환 | STT의 `70` 뒤 단위와 마지막 문장은 불완전하다. 후취득 [M02 p.21]은 70%/30%, char와 `'a'`/`'h'`를 명시한다. 이를 복원 발화로 바꾸지 않는다. |
| PDF와 skeleton의 type 차이 | PDF의 isAlive·isFinished는 `Boolean`, skeleton은 `boolean`이다. 실제 코드 선언과 PDF 표기를 각각 보존한다. [M02 p.21] [M02 p.25] [M03 p.1] [M03 p.3] |
| 자료의 오류·단순화 | `mayCar.model`, `attach()`를 올바른 선언과 구별하고 Call-by-Reference 표현에는 reference 값도 값으로 전달한다는 설명을 적용한다. [M02 p.8] [M02 p.14] [M02 p.19] |

이러한 보충은 불명확한 발화를 정확히 복원했다는 뜻이 아니다. 녹음 시작은 문장 흐름 중간이며, 마지막 17:39 구간도 문장 중간에서 끝난다. 17:39를 녹음 종료 시각이나 과제 설명의 완결 시각으로 해석하지 않는다. 새 음성 청취나 전체 누락 구간 점검을 완료했다는 주장도 하지 않는다.

Lab3 전용 PDF와 비공개 skeleton은 9월 19일 후취득한 공식 자료로 확보되어 있다. 식별자·자료형·반환 문자·제출 안내·Fight/Main 명세는 자료에서 확인하되 미구현 본문은 그대로 구분한다. 기술 수치와 선언의 근거는 M02/M03이며, 사용자의 확인은 강의 날짜 9월 17일에 관한 것이다. [M02 p.21] [M02 p.25] [M02 p.27] [M02 p.29] [M03 p.1] [M03 p.2] [M03 p.3]

### 기출 연결 검토

**기출 연결 검토:** 전체 과목 후보에서 현재 강의와 직접 맞는 Class–Object 설명 문항을 연결했다. Garbage Collector, `this`, `static` 관련 문항은 각각 엄밀한 수거 조건, `super`, 중첩 Class와 수정 제약이라는 추가 선수 개념이 있어 부분 연결 또는 후속 학습 대상으로 구분했다. 이 부분 연결을 현재 연습의 완결된 기출 스타일 근거로 사용하지 않았다. 나머지 목표는 강의에 근거한 일반 연습으로 유지했다.

실제로 연결한 문항의 출처는 다음과 같다.

| 인용 | 사람이 읽을 수 있는 출처 | 이 노트에서의 사용 |
| --- | --- | --- |
| [EX:cp_2025_1_midterm_q01 p.1] | 파일명상 2025년 1학기 중간고사 복기본, 1번 | Class–Object 설명 요구와 P01의 스타일 근거 |
| [EX:cp_2025_1_midterm_q03 p.2] | 같은 복기본, 3번 | Garbage Collector 역할의 부분 연결과 후속 수거 조건 학습 |
| [EX:cp_2025_1_midterm_q05 p.3] | 같은 복기본, 5번 | `this` 부분 연결, `super` 비교 유보 |
| [EX:cp_2025_1_midterm_q07 p.4] | 같은 복기본, 7번 | Object 지정 문제의 연결과 중첩 Class 학습 이후 재검토 |

위 자료는 공식 시험 원본으로 확인된 문서가 아닌 복기본이다. 연도·학기·담당교원 정보는 파일명에 의존하며 독립적으로 확인되지 않았다. 현재 강의자료와 담당교원 표기도 달라 이번 수업의 출제 성향으로 일반화하지 않는다. 연결한 네 문항은 같은 시험 묶음에 속하며, 네 종류의 시험이나 반복 출제 빈도를 뜻하지 않는다.

공식 답안은 제공되지 않았다. 연습의 해설은 명시한 조건과 대입 순서에 따라 도출한 설명이며 공식 정답을 재현한 것이 아니다. 기출 원문 문제지·답안·비공개 추출물의 다운로드 링크는 포함하지 않는다.

**Review status(검토 상태): 검토 완료.** 보충 설명과 연습문제의 출처·풀이를 별도로 검토했다.
