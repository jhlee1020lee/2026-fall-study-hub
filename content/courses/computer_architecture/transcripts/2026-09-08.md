---
title: "2026-09-08 · Computer Architecture · 보정 STT"
course: computer_architecture
date: 2026-09-08
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
---

강의 녹음을 바탕으로 보정한 STT의 공개용 사본입니다. 음성인식 오류와 불명확한 발화가 남아 있을 수 있습니다. 수강생 이름·연락처와 일부 개인 자기소개를 가렸고, 타임스탬프·화자 순서는 보존했습니다.

원문 보정본 SHA-256: `938b9987d4ba6f117c54b3e35bb042b2adb66a84ed4f3d9f0d7e45b623eca885`

`[[unclear]]` 등 표시는 원문에 남은 불확실성입니다. 코드·수식과 발화 표기를 그대로 읽을 수 있도록 본문을 텍스트로 제공합니다.

````text
00:43 화자 1
Thank you for coming today. Let's start today's lecture. Starting today, we're going to discuss about the actual RISC-V instruction set architecture, and there is an announcement related to the homework assignment. This is a tentative schedule for our homework assignments. It will consist of homework assignments, and the first one will start at the September. And as a policy, AI is not allowed in your homework assignment, but just to be honest, we cannot check all of your homework assignments, right? Like our TAs are around four or five, I don't know the exact number, but we have four or five TAs.

01:40 화자 1
So and then you guys are like 170 or 180, so it is impossible for us to check all of your homework assignments. So instead of checking for AI use in your homework assignments, we're going to make some questions on your exam related to your homework assignments. So I believe that questions will be very easy if you do your homework assignments by yourself. Don't need to worry about the difficulties [[unclear: about the mechanism]]. Okay, so.

02:27 화자 1
And the schedule for midterm and final will be announced on Thursday, and your midterm and final will be held in the lecture schedule, so it will be on Tuesday or Thursday, this time. And [[unclear: it will turn at night]], so your midterm and final will be [[unclear: for other two hours because then]], and the schedule will be announced on Thursday. This Thursday, and [[unclear: I will amalgate to each other]]. So now let's just look at the recap of what we covered last time, because some of you didn't come.

03:13 화자 1
[[unclear: You first read the lectures]]. So in computer architecture, we have two kind of high-level concepts. The first one is instruction set architecture, and the second one is microarchitecture. And the instruction set architecture is like specifications of your processors, and the microarchitecture is implementation of these specifications. And in this course, we discussed about the instruction set architecture first. And all of the contents are related to RISC-V instruction set architecture.

03:55 화자 1
And the instruction set architecture is kind of a programmer-visible state, and it is also called, called architectural state. And then what is it? So it is kind of architectural—I would say it is a kind of things that you [[unclear: couldn't see / could see]] in the processor as a programmer. For example, registers are included in the instruction set architecture, memory is included in the instruction set architecture, and the format of instructions are included in the instruction set architecture. So if—for some developers, instruction set architecture is not important. For example, if you implement the program using Python, and Python hide everything.

04:46 화자 1
Away from you, so you don't have to think about it. But if you implement something in systems, for example, if you implement the operating system or a compiler, then instruction set architecture is important. Right, because let's say if you write a C program that do some very basic arithmetic operations, then your compiler have to translate the very simple C program to machine instructions. Then you have to know about the format of the machine instructions or format of the data. Or if you get to implement the operating system, then operating system has to do some context switching, which is a operation in operating system that switches the running process from something to another.

05:32 화자 1
You will learn about the context switching in operating systems course, but to do that you have to store the current values of registers to memory and then retrieve some values for another process to actual registers. So if you get to do something, [[unclear: it says something, it certainly is there's a point]]. So at a high level, summary, the architectural state or the instruction set architecture is the contract that—how—the contract that defines how software and hardware interact with each other.

06:19 화자 1
So today we're going to learn about the format of data and instructions. You could think instruction set architecture as a kind of the repertoire of the instructions or operations of computer. For example, it includes the arithmetic operations, it includes the data movement operations, [[unclear: with a virtual operations]], and different computers have a different [[unclear: value]]. I want to turn this on. You know how to turn this on? Which one is better for you? This is better or turn off is better? This is better, that's better, okay?

07:20 화자 1
Yeah, let's just keep going. So different computers have different instruction set architectures, which means that if your desktop has an Intel processor, then Intel processor has its own instruction set architecture, which is called x86-64, and AMD also shared the x86-64, and ARM has another, the other different instruction set architecture for ARM processors. But they share many common aspects. All of them have arithmetic operations and control flow operations and data movement operations or memory operations. So in this course we choose RISC-V, which is the—which is invented by UC Berkeley, but it is not just the educational instruction set architecture. It is used in the industry for embedded processors.

08:20 화자 1
AI accelerators. So even though the instruction set architecture we're going to learn is not what you are actually running on your computer, they share the same common aspects across the multiple different instruction set architectures. And early computers had a very simple instruction set architecture because at that time a program on a computer was very simple. But later computers had got more complex, and the instruction set architecture also got more complex, and then became simple again, some—for the sake of the performance [[unclear: at the cost for sentiments]].

09:14 화자 1
And these things are defined in instruction set architecture, like the first one is the data format and size here. The data format represents—the data format specifies the format of numbers, not just like—not like data structure in C or array in C. It says about the numbers. For example, if you have 32-bit machines, then the size of the number is like 1 byte or 2 byte or 4 bytes, and if you have 64-bit machines, then the size of the number can be 64 bit. So the kind of things are defined in the instruction set architecture. And as I said, the programmer-visible state will be included in the instruction set architecture, and instructions [[unclear: of both]] we're going to learn about today's lecture.

10:09 화자 1
And some other things like instruction-to-binary encoding and how to interact with the outside world, that is I/O operations, we learn about it after the midterm. And some security-related features of a processor. So many things are included in the instruction set architecture, but in this course the first three things are main contents for this course. That is the very common aspect of instruction set architecture. And it covers every, all the details of [[unclear: social circumstances]].

10:54 화자 1
But [[unclear: it was a fun]], right, [[unclear: not people saying about not saying something]]. So as I said, the RISC-V instruction set was invented by UC Berkeley as an open instruction set architecture, which means that it is free. So let's say if you want to implement your own processor using x86 instruction set, instruction set architecture, then you have to pay Intel because they have kind of patents, I don't know, they have the right for the instruction set architecture. And if you want to implement ARM processor, then you have to pay ARM, but RISC-V is free, so you can use it.

11:44 화자 1
To implement your own processors, that's the reason why it gets popular these days. And so this is our running example. Running—this is the instruction set architecture that we're going to use as a running example. So in your homework assignments you're going to implement or design your own processor that can run the RISC-V program, like a very simple RISC-V program.

12:31 화자 1
Contents that we learned last week, and let's start with the most basic category of instructions in RISC-V. Today we're going to discuss about—we're going to learn about instructions and the format of instructions and the format of data. So the first one is the most simple instructions, which is called arithmetic operations. In RISC-V, arithmetic operations all follow a very simple and strict format. So as you can see there, arithmetic operations like add or subtract have the same format that has the three operands. The first.

13:18 화자 1
First operand is the destination, and the second and third operand are sources, and all the three operands are registers, which means that in RISC-V arithmetic operations can operate only on registers. So if you have instruction like add a, b, c, then all of the a, b, c operands are registers. And the semantic of the add a, b, c is straightforward. It just adds two values from registers b and c and store the results of the add operations to the register a. In, in RISC-V, all arithmetic operations have the same form, and it's because of the design principle, [[unclear: like constant]].

14:14 화자 1
It's like, yeah, I just read it, like, simplicity favors regularity. That means that because all of the arithmetic operations have the same format, the design of this processor gets simpler, which means that when we design a processor, then we have to put everything, like every logic, into the small rectangle chip, right? We implement everything as a logic gate in the processor. So if the formats are very diverse, and you have to implement all of them, you have to implement all of them, you have to implement handling logic for all of them, then you have to implement a lot of logic gates in a chip.

14:56 화자 1
But in RISC-V, all of the arithmetic operations have the same format, which means that you have—you have to implement a small number of handling logics in the chip. So the design of a processor gets simpler, and simpler design has advantages over complex one because you can increase the frequency more easily. If your processor is complex, then increasing a frequency will cause some problems like a heat problems or electricity leakage problem. But in RISC-V, logic is simple, which means that it is easier to increase frequency. You can implement a high-performance processor more easily at lower cost. So that's the design principle behind—the first design principle behind the RISC-V design.

16:02 화자 1
But the downside of the simple design is that we cannot express all arithmetic operations in one single instruction, right? For example, in this C program, like in your C program, let's say you implement that kind of arithmetic operations. So that is pretty common, right? This is just two add operations and one subtract operations, one subtract operations. But in RISC-V, all arithmetic operations have the same—all arithmetic operations have two source operand, which means that you cannot express this as a single machine instruction. So to implement this one in RISC-V instructions, the compiler has to translate the C instruction into three RISC-V machine instructions, right?

16:55 화자 1
I think this is straightforward, but let me explain it. So the first machine instruction is add operations. It handles the first term of the C statement. The semantic is simple. It gets values from two registers g and h and do the add operation and store the result of the add operation into a register which is called t0, as intermediate results of the add operation. And the second one is also very similar. It gets values from i and j and stores the result of the add operations to [[unclear: t0 / t1]], and then with the two registers t1 and t0 as intermediate operands.

17:48 화자 1
The third instruction will subtract the operands with the t0, t1. So this is the only—t0, t1 and g, h, i, j, f are virtual register names, which means that these names are not real. The names are virtual. We will see the actual names of registers later, but don't think this as actual names of the registers in RISC-V processor. As I explained, all arithmetic operations, all arithmetic instructions operate on registers. I.

18:43 화자 1
This is [[unclear: the first system first in semi]]. I think some of you have heard about register. Register is kind of a small and fast storage in the processor, like—and the number of registers are fixed in the arithmetic—what is that—instruction set architecture. So RISC-V has 32, 64-bit register file, which means that here a register file is a set of registers, and the register file has a 32 registers. Each register stores 64-bit value.

19:28 화자 1
Because it is very small, it is used for data that is frequently accessed. For example, if your program access data only once during the entire lifetime, you have to store that value in register, you waste the register. But if your program access one data like very frequently, billion times per second, then you want to put the data into register because it is very fast. Well, and that is just a conventional term: 64-bit data, doubleword, and 32-bit data is called word.

20:16 화자 1
But in processor, in architecture, smaller is always—not always, but smaller is often faster, which means that register file is [[unclear: traceable]]. As you can see, the number of bytes that can be stored in the register file is 32 times 8 bytes, which is very small. I think it is 128 bytes. Because it is very small, it is very fast, and we will learn about cache later. And cache is a little bit larger than registers, so it is a little bit slower than registers, but it is smaller than main memory, so cache is faster than main memory. And the main memory is smaller than storage devices like SSD or hard disk.

21:13 화자 1
So main memory is a little bit faster than storage devices, so that is the common hierarchy of a storage in your computer. In RISC-V we saw that the number of register is fixed. It is 32. We have 32 registers, and these are actual register names defined in instruction set architecture. So as you can see there, register name is x0, x1 and x2, blah, blah, blah, to x31. Each register has a common purpose or common role. For example, x0 always represents a number zero.

22:09 화자 1
And x1 will be—we will learn about the calling convention later, but I will just explain it all. But x1 usually stores the [[unclear: term value / return address]] of a function, and x2 stores the stack pointer. Now if you don't know about the stack, we will learn about it later. So as you can see in this table, all of the register has the common role. But I think at this point you don't have to remember all of them. The point is that each register has a conventional role, and the compiler will pick a register, pick a proper register for each when the compiler generates code.

23:03 화자 1
But x0 is an exception. You cannot change the value of x0 in RISC-V program, RISC-V processor. x0 always stores the value of zero. That is one exception, but all the remaining registers can store arbitrary values, but conventionally they are used in a specific way.

23:31 화자 1
So now let's go back to the previous statement, like this one is the example, like arithmetic operation example. So before—now we know the actual names of the register in RISC-V processor, right? So let's suppose these C variables map to these registers. For example, the C variable f maps to register x19, 19, and g maps to x20, blah, blah, blah, and j maps to x23. If a compiler maps variable to registers in that way, then the compiled RISC-V machine instructions or assembly instruction will look like this. Like we have the same three instructions.

24:24 화자 1
And subtract, but the operands will look like this. These are actual registers. So if you pick a RISC-V compiler and then you compile this statement, then you will see instructions like this. So this means that the first one will get the values from x20 and x21 and do the add operation and stores the value to x5, and so on. I'll skip the remaining. But we know that the data that your program has to deal with is larger than 128 bytes. It is very small, tiny, which means that we have to use memory as a secondary storage.

25:22 화자 1
So for that we will use memory operands, or load and store instructions. So in computer, main memory is used for composite data or larger data, like if you—your program has arrays or structures or some kind of dynamic data, dynamically allocated data, then all of them goes to the memory.

25:49 화자 1
Then sometimes we have to do some arithmetic operations or operations on them. Then we have to load values from memory into registers and do some operations on registers and store results from register to memory. So this is the reason why we—if you remember that we discussed about that the RISC-V instruction set architecture is a [[unclear: fundamental architecture]], or it is also called the load-store architecture. It means that we load values from memory to registers and do something and store values from registers to memory. And I believe you are familiar with this kind of concept, but memory is byte-addressable, which means that each memory location has each one address and each location stores one byte.

26:46 화자 1
So for example, if your memory has one gigabyte size, then each byte has its one address, and your program access that location with the address, with the specific address. And our RISC-V is a little—I think this concept might be confusing for students who are not familiar with the computer architecture, so let me explain about that. So in computer architecture, there are generally two types of endianness. The first one is big endian, and the second one is little endian.

27:33 화자 1
Before we think about big endian and little endian, let's think about numbers, like two numbers. Here we have the example of two numbers. The first one is 0xFF000000. It is very large number. And the other number is just one, but they are represented in 32 bit.

27:58 화자 2
Hi.

27:59 화자 1
These are [[unclear: times]]. This one is just a number one, and I don't know what the exact number of this, but it's large number, large unsigned number. Then, then numbers are represented in four bytes, right, and the memory has—then we have to split these numbers into four part, one byte for one memory address. If you split this one into four bytes, then FF goes to here, and zero zero, zero zero, then zeros go to here, the remaining three bytes.

28:45 화자 2
So.

28:46 화자 1
If you represent number in that way, then this part is called [[unclear: most significant bits]], like in Korea 이 뭘까요? This is [[unclear: most significant bit]], and this is the [[unclear: least significant bit]]. So if you store this number into here, then zero will—if you represent this number in this way, then zero will goes to here, and zero one goes to here. Then let's think about how to store that numbers into memory. Then we have two different ways. The first one is a big endian, and the second one is a little endian.

29:35 화자 1
Big endian stores [[unclear: it was a simple bit / the most significant bit]] in lower address. So let's think about this red rectangle. Then the address zero—this one is address zero, and this one is address one, and this one is address two, and this one is address three. Then the most significant—the [[unclear: bit]] goes to address zero in big endian, and the second most [[unclear: bit]] part goes to address one, and so on. So that is big endian. And in little endian, the way of storing that number is different. [[unclear: this is significant]]. [[unclear: This significant bit]] goes to address zero, so that part goes to address zero, and the second part, [[unclear: second bit, significant bit]] goes to address one, and so on.

30:36 화자 1
So this is the two different ways of storing multibyte numbers in memory.

30:44 화자 1
And in RISC-V and for—in modern computer architectures, little endian is more popular. So if your computer has an Intel processor, then Intel processor has the little-endian integer number. And if your processor is ARM, it is also a little endian. Big endian is [[unclear: suffering]]. It is very hard to find a big-endian computers these days, but it is used in older computers, and it is used in network. So I'm explaining about this because this is important in network. For example, in network, if you implement a program for only your computer, then you don't have to really think about the endian in both cases, because whatever you do, the way of storing number is identical for—across the program. But if you implement a network program, then two machines might have different endianness, so you have to think about how to communicate with the other, with another machine.

32:01 화자 1
Because of these things. But in this course we are not going to discuss about network, so just remember that in RISC-V little endian is our way of storing numbers. Anyway, let's go back to memory operand example. In previous statement example we have only register operation, right? We had f, g, i, j, I don't know. But in this example, like in this C example, we have the array is called A, and we implement arithmetic operations like this. So we want to get the value from a ninth element of the array A and add the value with the value stored in h and store.

33:02 화자 1
Result value into the array A, the 13th element of the array A. So because arithmetic instructions in RISC-V take registers, we first need to get the value from main memory to register. So in this example, let's say h maps to the register x21, and base address of A maps to register x22. Here base address means that the starting address of the array. You know that the array is just a large chunk of memory, and basically that chunk to represent each element of the array, right?

33:54 화자 1
And in this example, suppose the size of each element is 8, right? So let's say if you have a 64 bytes array, then you have the number of elements. So if you compile that statement to RISC-V machine instructions, then the instructions will look like this. Here the first one loads the value from that here and stores the value to here. And you can see that the format there is—the format of the second part looks a little bit weird, right? 64, what is this?

34:43 화자 1
Semantically this is 64 plus x22, and semantically this gets a value from [[unclear: 60 plus 2 plus that]]—semantically this gets a value from 64 plus x22. So this is the kind of syntactic sugar for array access. We know that x22 is not syntactic sugar, but that is important because of the array, because of—we know that x22 is the base address of the array, right? Then the element that we want to access is located at the offset 64, because each element, the size of each element is 8, and the index of the array is 8, which means that the offset of that element is 8 times 8.

35:36 화자 1
So this is—the number 64 comes from here and here. So we do multiplication with 8 and 8, and it goes to here, and x22 is the base address of the array. So this instruction will get the number from 64 plus x22 and stores the value to x9. After that we have two numbers stored in registers, then we are ready to do the arithmetic operations, and we do the add operation here and store the result to here. Here the 96 is the same. 96 comes from the number 12 and 8, so we do the multiplication with 12 and 8, and it goes to here.

36:37 화자 1
So in assembly languages or in machine code, you will often see this kind of representation, because it is very convenient for array access or structures, so you will see this kind of representation very often.

36:59 화자 1
So when we do arithmetic operations, then there are largely two types of operands. The first one is register, and the second one is memory. As we discussed before, registers are faster to access than memory, so, so that's the reason why compiler must use registers for variables as much as possible, which means that a compiler must put a value that is frequently accessed across the lifetime of a program to registers.

37:45 화자 1
So a compiler tends to use a memory only if it runs out of registers or it knows that the variable is very rarely. So that is the reason why a compiler is important. So if your compiler does not optimize your program, then the performance of your program will degrade a lot, like it will slow down like two or three, three times, for example. So compiler has an important role in computing ecosystem. So I recommend you to take the compiler course later. And actually the design principle behind the RISC-V is also related to compiler, like the developer of the RISC-V developer.

38:45 화자 1
The developer of RISC-V processor believes in the compiler, and they believe that the compiler will optimize their program because of—even if my processor has simple instructions, we do not have many arithmetic operations, but they believe that a compiler will optimize their program with a lot of instructions in the—very efficient. Actually, we're going to learn about how to optimize that kind of machine instructions. Like I think it will be after the midterm, but we're going to learn about how to—oh, actually I say something wrong. We're going to learn some kind of other optimizations that is implemented in hardware. Even if we have the same instruction sequence like this, the performance of the instruction sequence will differ.

39:47 화자 1
According to the microarchitecture, so we're going to learn about hardware optimization, but compiler also do the optimization independently. As I said, I will recommend you to take the compiler later. So up to now we discussed about the dynamic operand, like a register operand, like memory operand. But sometimes we want to use the constant operand, and it is called immediate operand in computer architecture. So constant data can be used in your program very often, right?

40:32 화자 1
If you implement the C program with the for loop, then the index will increment by one, and if you have a pointer, then the pointer may increase by four, four or eight. So constant operand is used very frequently. So we have special types of arithmetic operations like this. It operates on register and constant variable. It makes the common case very fast and efficient. Then the common case includes the for loop in your C program or the array iterations in your C program. So because we use immediate value here, we can avoid a load instruction that.

41:30 화자 1
Loads a value from memory to register. We don't have to store [[unclear: double foreign, for every eye and eye person]]. So, so far we discussed, we learned about the basics of arithmetic operations. We learned about how they look like, but it is about instructions. But sometimes we need—not sometimes, but that is about instructions. We need to encode data in memory. In computer, everything is just a number, zero and one, and we have to aware of interpreting zero and one to compose some real numbers.

42:28 화자 1
So now let's talk about it. So let's start with the simplest case, which is called—which is the unsigned integer number, unsigned integer numbers. So in unsigned integer numbers, in the case of unsigned integer numbers, we [[unclear: reconcreate the binary a]]. We interpret the binary representations in this way. So the number x is represented in this way, and here n represents the size of the number. As I told you, instruction set architecture defines the size of the numbers. Your 64-bit machines.

43:11 화자 1
Represent number with 64 bits, and your 32-bit machines represent—your 32-bit machines represent the number with 32 bit. So here the number n represent the size of the—and this is the representation of unsigned integer number. So its range starts from 0 to this number. For example, let's say this is the 64-bit number, so there are a lot of—and it ends with 1, 0 or 1. Then we can interpret it in this way, and we will get the results as this number. And it's very straightforward, right? This is just a very simple binary number, 이진법 in Korean, and this is—most of computers are 64 bits, right? And using 64 bits the range of the number is 0 to this.

44:22 화자 1
That means that even though this is a very large number, we cannot represent like a very [[unclear: infinite]] number in instruction set architecture. So if you work on some scientific applications in Python or something, like some other languages, then the way of representing that kind of big number is implemented on top of instruction set architecture. It implements a different way of implementing the kind of very large number, larger than this number. And it was about the positive number, I mean non-negative number, but in computer we have negative numbers, right? So we need to have a way to represent—we have to have a way to represent the negative numbers, and it is called a two's complement, signed two's complement.

45:18 화자 1
What is the thing? It is called two's-complement signed integers. So to represent a negative numbers, the number x is represented in this way. I think it would be helpful to think about your C program with the integer and unsigned integer. In your C program, you can represent the number with unsigned integer or integer, right? The unsigned integer will map to this way, and your integer variable will map to this way. So the way of interpreting the number is different according to the type of your variable. And because the only difference between this and this is the sign of the.

46:13 화자 1
The leftmost term in the equation. Here we have a minus sign there. For unsigned number we don't have it. So the range of the number is different, and for the negative number the range could be this. For example, if we have a number in this way in your memory, then it will be interpreted, and then it will result in minus four. So if you implement a C program and your variable stores number minus four, then in memory that minus four is stored in this way. So if you actually see the data that are stored in memory, then you can see numbers like this. And using 64 bit, the range of the numbers is like this.

47:17 화자 1
And two's-complement signed integer has some advantages. For example, we could determine the sign of the number very easily, which means that the leftmost bit represents the sign of the number. For example, if you see this kind of numbers, then you can see that the leftmost bit—you can see that if the leftmost bit is one, then number is negative, and if the leftmost bit is zero, then the number is non-negative. Right, in the previous example this is negative four because this bit is one.

48:05 화자 1
And in these examples this is non-negative because this number is zero, and this is negative because the leftmost bit is one. So this is a very convenient way, a convenient feature of two's-complement number. And so other features or properties of two's-complement numbers is that we can construct negative numbers from positive numbers very easily.

48:31 화자 1
For example, if we have a number plus two, then we can get minus two by complement plus two and add one. Here the complement means that—complement means that we flip every bit of the number. So to construct a negative two from positive two, we flip all bit in positive two. So we will get this number. We flip zero to one and we flip one to zero, and then we add number one to that flipped number, and this number will represent a negative two.

49:22 화자 1
I explained about the sign because it is very important in computer architecture. Let's say you know that in C program the size of the integer numbers can vary. If you use the character variable, then the size of the number is one. If you use the short, then the size of the number is two bytes, and if you use integer number, integer variable, then the size of the number is four bytes, and let's say long long is eight bytes. So if you implement the C program without thinking about the size of the numbers, then it will cause the problem. For example, this is number two, but this is negative two. But if you don't think about the signedness of the negative two.

50:21 화자 1
And the compiler will—the compiler may translate this number into something, so some number that you don't intend to use. For example, this is minus two in four bytes, but if a compiler put zeros for the remaining bits—I'm so tired. There were so many things to do as a professor. I didn't expect this. Anyway.

51:05 화자 1
Really, you guys don't know about how many [[unclear: birds / works]] we have to do as a professor. Anyway, let's say if you have a variable, four-byte integer number like this, and pass this number to a function that accepts eight-byte integer number, then if you don't think about the signedness of the number, then compiler may—okay, let's think. I want to say, let's take a break, but—so if you pass this number to a function that accepts a eight-byte number, then a compiler may fill this part with zero. That is not the correct way of translating four bytes into eight bytes.

52:03 화자 1
The number will be different. The number will become different. So that's the reason why I explained about the signedness for this long time. Actually, that is one common source of vulnerabilities in your programs, like if you don't think about the size of the numbers, and then you can think about [[unclear: the kids part of]] [[unclear: I do want to do that check]].

52:56 화자 1
아니 지금 진짜 너무한거 같아 그렇지. Anyway, the signedness is one of the common source of the vulnerabilities in your C program because depending on the type of variables, the compiler choose the instructions, like machine instructions. Choose a wrong type of some variable, and the compiler will choose a wrong machine instructions. For example, like in RISC-V here we have two variants of the—two variants, variations of the instructions. The first one is lb, and it is sign-extended loaded byte, which means that this one will fill one to this part. lbu, it is for zero-extended.

53:56 화자 1
Loaded byte, which means that if you use this one, then the processor will fill this part with zeros. And a compiler choose the two variations depending on the types of your integer numbers—what's that—the types of the—if these are the types of the variables in your C program. So when you write a C program, be aware of the signedness and the size of the numbers. It is important, but it introduces a lot of the vulnerabilities in Chromium or Firefox.

54:39 화자 1
So actually, that is—representing numbers in memory is kind of easy, like we have two ways of representing numbers, and that is just binary numbers. But I explained that—we discussed about that instructions are also represented as data in memory. Everything is zero and one in memory, and instructions also—instructions are also stored as zero and one in the memory, which means that we need a way to interpret zero and one to specific instructions. So we're going to learn about them. We are going to learn about them.

55:32 화자 1
In RISC-V, every instructions are encoded in 32-bit numbers. For example, this number is—this number represents the instruction up there, and the size of that number is 32 bits. So in RISC-V every instructions are represented in 32 bits. That is common in RISC machines. For example, if you use ARM or ARM processors, like this one is ARM processor, then the instructions are represented in 32-bit numbers. But if you use CISC machines like Intel, then the size of the instructions are different. It can vary.

56:21 화자 1
But for some instructions in Intel, the size is one byte, but for some instructions in Intel, the size of the instructions may be like six bytes. So in Intel the size of the instructions can vary, which means that the processor design gets complicated than the RISC machines like RISC-V. And from now on we're going to use hexadecimal number like this. I'm not going to explain about the hexadecimal number because I believe you all know about it, right? 16진수 in Korea. We're going to use hexadecimal number, [[unclear: signal strength]], for now.

57:13 화자 1
And these are instruction types that we're going to learn in this course. The first one is R-type. It is for arithmetic operations, and the second one is I-type, and so on. So we're going to learn one by one. Look at the R-type instruction first. R-type instructions are arithmetic operations like this and this one. We need to have a way to represent these instructions in hexadecimal number, and the format of the R-type instruction—the format of the R-type instruction look like this. We have 32 bit, and each bit—the 32 bits are splitted into five fields or six fields. And as you can see here, the register numbers go here and here and here.

58:11 화자 1
We know that arithmetic instructions have the three operands, right? Two for sources and one for destination. As you can see here, the x9 is the destination register, so the number 9 goes to here, and the number 20 and number 21 is the source register numbers, so that numbers goes to here. And the remaining field will determine the type of arithmetic operations. For example, like in arithmetic operations, when we talk about the arithmetic operations, then the type of the arithmetic operation may be add operations or subtract operation or logical bitwise operation or something like that. So these fields will determine the type of the arithmetic operation. Actually, you will implement this in your homework assignment.

59:09 화자 1
So this is just an example, and it's represented in 0 for funct7 and 0 for funct3 and 51 for opcode. So all—using all the bits together, we could represent the arithmetic operation like this, and it goes to [[unclear: the subjection of the]], like this. As we discussed before, we need some memory operands, right? We need a way to get the value from memory, and we need a way to store the value to memory. I-format instruction is the instruction that loads a value from the memory.

01:00:08 화자 1
As you can see here, the I-type operation, like the load operation, also takes three operands. The first one is the destination, and the second one is the immediate number, like it is represented—it is used to represent the offset in the array, and the third one is the base address of the array. So I-type operation also has the three operands, like the destination register goes there, the first—the base address of the array goes to rs1 over there, and the immediate value is used to represent the offset of the array, offset inside the array. Here there is some important point, as you can see in these two different types of instructions.

01:00:58 화자 1
The location of the destination registers are the same here. In the R-type instruction, register, destination register goes here. And in I-type instruction, the destination register also goes here. That means that when we design a processor, we don't have to implement a different logic to get the destination register for R-type and I-type instructions because we could use the same logic to get the destination register for R-type and I-type, because the location of the destination registers, register is the same. And it is the same for here, the first register, let's say first source register.

01:01:48 화자 1
rs1 is located in that position in R-type instruction, and this one is located in the same location in I-type instruction. It means that we don't have to implement two different logics to get the source register for R-type and I-type instructions. We can share the same handling logic for rs1 in R-type and I-type operations.

01:02:23 화자 1
So that is—I keep saying that RISC-V is simple, and the design of the RISC-V is simple, and it is possible because of this kind of the—how to say that—the common format or strict format in instruction set architectures. And this is the store, S-format instruction. S-format is to store value from the registers to the memory, and as you can see here rs1 is also located at the same position.

01:03:10 화자 1
But the difference between I-type instruction and R-type instruction, S-type instruction, is that immediate value is split into two parts in S-type instructions. Like as you can see here, the store operation also has the three operands. The first one is the source register of the store, the first one is the source register, and these are destinations. To get the destination with the immediate value and the base of the destination.

01:03:54 화자 1
But for the processor, but the perspective of the processor, all the numbers are input. The value does not go to register. It goes to main memory, which means that the processor—the processor—the registers are input to the processor. The processor will read the values from the register and do something with the inputs from—stored in the register, which means that the two registers are input for the S-type operations. And, and for the sake of the simplicity, the instruction set architecture defines the position of two source registers.

01:04:49 화자 1
In the same way of R-type operations, like this. So rs2 is located here, and rs1 is located here, and then we have to put immediate value in that format. But we don't have enough space for the immediate value, so we split the immediate value and put the first part in the upper seven bits over there, and we put the lower five bits here. So that is the reason why we split the immediate value into two parts. We want to put two source registers in the same positions with R-type operation.

01:05:42 화자 1
And in RISC-V, add and subtract are not the only arithmetic operations. We have other arithmetic operations like logical arithmetic operations and so on, I think. But actually I wanted to [[unclear: start / stop]] the lecture here, so yeah, [[unclear: let's start with the lecture]]. I will start from here on Thursday, so okay, let's just stop here. Actually, I don't have enough time to explain all of the [[unclear: permits for versions]] [[unclear: and I guess time of versions about]].

01:06:45 화자 1
And I think—and on Thursday we're going to learn about the remaining [[unclear: IPV origin / arithmetic operations]] and [[unclear: British origin / branch operations]].
````
