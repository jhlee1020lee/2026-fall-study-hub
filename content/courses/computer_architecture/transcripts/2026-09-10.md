---
title: "2026-09-10 · Computer Architecture · 보정 STT"
course: computer_architecture
date: 2026-09-10
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
---

강의 녹음을 바탕으로 보정한 STT의 공개용 사본입니다. 음성인식 오류와 불명확한 발화가 남아 있을 수 있습니다. 수강생 이름·연락처와 일부 개인 자기소개를 가렸고, 타임스탬프·화자 순서는 보존했습니다.

원문 보정본 SHA-256: `351f02a28c41010f15e54a8edb844df20d49bd55b378c3c007829b1ead1f23ad`

원본의 음성 한계 기록: `{"recovery_status": "source_limited", "verbatim_complete": false, "source_limit_items": 43, "inline_unclear": 41}`

`[[unclear]]` 등 표시는 원문에 남은 불확실성입니다. 코드·수식과 발화 표기를 그대로 읽을 수 있도록 본문을 텍스트로 제공합니다.

````text
00:44 화자 1
Okay, then now let's just I remember today's lecture thank you for coming today, obviously I told you that I'm going to announce [[unclear: the speech of schedule]], but it has not been determined yet. Like that schedule that I will be interested, sorry for that today now let's keep talking about the instructions of architecture on risk. This week is to get the basic of the RISC-V instruction set architecture and starting from this week we're going to learn about how to understand the performance of a CPU and after that we will learn about the design of this CPU.

01:40 화자 1
I don't like explaining all the details of instruction set architecture and assembly languages, but because it is very tedious and boring, but today is the last lecture we learn about this kind of assembly languages. Sorry for touring contents, but we need to understand some pages of the assembly language. Anyway, as a quick recap of the last lecture, we were learning about the instructor assembly language and we started from the arithmetic operations. In RISC-V arithmetic operations take registers as operands, two registers are sources with input for the arithmetic operation.

02:36 화자 1
One register is the destination so with the arithmetic operations regional compiled the CEO instruction like that into multiple possibly related And because registers are very small, we need a memory so we learn about the memory and Because memory is byte addressable, we need a range to put multiplyte data in degree so we learn about two different ways to put multiplyte data in degree the first one is big endian and the second one is little endian. In modern computer sectors, little endian is a kind of de facto standard. intel processors use little endian, R processors use little endian.

03:32 화자 1
Most of processors you can think of use a little endian, but historically there were some machines that you big endian in network, like if you implement the network applications, then you need to make it compatible with multiple different architectures, right, you don't want. To make it working only for Intel processors or whatever so if you think about effort and this kind of thing is important but in this person we only think about the little endian. And to use the memory. To use data store memory, we need instructions to load data from memory and to store data to memory.

04:26 화자 1
And these two types of instructions there for that load instruction and store instruction and then we can do some more complex portions because now we have a large storage Register is very faster than memory in computer architectures most of the time a smaller is faster registers are very small, which means very fast. So because of that, we have a chance to optimize the performance of processor, I mean performance of program by pulling data that is more frequently accessed in registers instead of putting that data to memory.

05:12 화자 1
[[unclear: That is the role of a compiler The compiler is important for your program to be more faster so you can try to compile your program with a different compiler for example, if Let me show you this one. So if you compile your C program with that, could you see that by made a middle, there is a dashboard zero, which means that zero sets for none? Kind of dot and that large for kindergarten sets for optimization so if you compile your C program with that command then the compiler will not optimize your program.]]

06:12 화자 1
[[unclear: And the default is this so you can compare the performance of the program to compile primers with these two different functions. And you will see the importance for the significance of a compiler in the perspective of performance only that is not the focus of this course I'm just saying it.]]

06:40 화자 1
[[unclear: And immediate operations is another kind of immediate operations there was an immediate operations and Big MDL and a little MDL MDL MDL is a way to put modify data into memory like how to put this into memory and beyond besides the MDL is we need a way to interpret that number, that text number into our Our data types for example, this might be an unsigned number and this might be a side number and according to the types of debt number the value will be different and those complement side integer physics I mean this is how to Interpret x value into your data type so if the data is assigned number then you can get the Value of that representation with that equation and if the data is data must be completed in as a sign number then you can use this equation to get the The number that is representatively the ex number.]]

08:00 화자 1
[[unclear: And the signance of your data is determined by your program, for example, if you declare a variable as an assigned int, then the compiler or a processor will think that that number, that text representation will Should be interpreted as an unside number like this and if you declare your variable as a just p then your compiler and the processor will interpret that representation as a side number so processor will use these equations to get the variable And the spoken amount of time integer has some cool features to contract.]]

08:50 화자 1
And in von Neumann architecture, everything is data, so instruction is also data and that means that we need a way to encode instructions as a byte. These kind of numbers also can be interpreted as an instruction. And we learn about some common types of instructions like there are R-type instructions which stand for arithmetic operations and I-type is for Loading instruction and some arithmetic instruction with the immediate value and S-type is for store instruction and we will learn about the remaining today. As you can see here, the R-type format, this R-type instruction can be reported there's a number in this way.

09:51 화자 1
That was the contents that we covered on Tuesday, so say we will talk about some more instructions, like more RISC-V assembly instructions. Let's start with the logical operations, logical operations are another types of first clinical portions, but instead of doing some So some calculations that we are familiar with, this is a bitwise operations, which means that we manipulate bits in representation directly. I believe you know about these kind of representations if you implement the C program can You might see these type of things or not, I don't know, and these things are big approaches, we're going to look at this.

10:52 화자 1
So the first bit operations that we're going to learn is shift operations, shift operations are different, I mean, it is fast, so we use it very often anyway, RISC-V We can shift the bits of a register left or right, which means that here, if, let's say we have a number 1 and in binary representation it is 001. And if we shift this in that number left by one, then the result will be this [[unclear: 100 10]], which is equal to number two. So basically there are two types of bit shifted operations, the first one is shift left.

11:47 화자 1
And the second list is a shift right, so shift left is to as the next sense it is to just shift beat in the register to be in a land direction and how much the We have another operant, which is I on there, so we shared it in the register by I. And in this case, the I is just one, the left vertical person is equal to multiply that number by [[unclear: two to nine]].

12:34 화자 1
What's the [[unclear: power of die]] and shift right operation is the opposite way it moves the beat in the register to the right direction and it is just equal to Divide operations.

12:49 화자 2
Okay.

12:50 화자 1
We divide the number by the [[unclear: power of time]]. So as you can see here, [[unclear: this is also kind of R-type operations]], so it shares its age format with the R-type operations here. The left rightmost, several bits are opcode and the five bits after that is destination register as well so as you can see here. After shift operation is a kind of variation of I-type operation sorry, I said work. It is kind of the I-type operations but the immediate field is divided into here these fields are common in shift operations but this part is divided into two.

13:48 화자 1
And there are some more logical operations whether these are very waiting so let's scheme of those demands. There were popular bitwise operations, the first one is AND and the second is OR and the third is XOR, exclusive OR I believe you make your part of it right, so AND operation is just to do AND operation for everything. For example, if we have an AND operation in this, like in this assembly instructions, then it means that we want to do bit y AND operation with the x, n and x.

14:33 화자 1
11 and store the results into X9 As you can see in this figure, the inputs are x10 and x11 so We do with the input, we do the bitwise end of questions for everything, so if there are bits that are Both there are one in both registers and the results will be one in the output register so you can see here if you look at the red curve here, red pass of there. These two bits are one because they are one in x10 and x11, but these two bits are zero because in x ten these bits are zero and for every other bit it is the same.

15:26 화자 1
And OR operation is similar, but the semantic is a bit different in OR operation. The result B is what? If any of the bits in the input registers is one, for example, if you look at these bits, then they are one because it extends. Those things are one, so it does not matter if this meeting as eleven is one or zero and the remainder of the same, the same rule applies to the remainder. I believe you know about NN sor and Sor, right, so sor is.

16:27 화자 1
So XOR stands for exclusive OR and the result is zero if the bit in two input registers are the same, for example, this one is zero because these bits are what? These are not alike, but maybe this is 0 because this is 1 and this is 1 and this is 1 and this is 1 because the bits in the input registers are different, which means that here we have 1, but here we have 0, so they are not equal. So the output register, the BT, the output register is what and the same applies to both the other, both the remaining bits in the output registers so that is the bitwise equations which means that We apply to a group for every date in the input registers and if I register this.

17:26 화자 1
Bitwise logical operations are R type operations so in your homework assignment you will implement these operations with this kind of lower R type operations. Yeah, so that was the remaining R-type operations that we covered this course, which means that. But if you remember, I told you that we have three kind of common operations like, right, the first one is arithmetic operations and the second one is the envelope and the third one is control flow operations.

18:10 화자 1
So R type operations are for arithmetic operations and S and I operations are for data movement operations and the remaining the last one is the control flow operations so from now on we're going to look at the control flow operations. So control flow operation is kind of you are really familiar with the control flow operation it is a kind of if the statement in your seat program or for or while in your seat program, this one if we write I see if the statement in this way that we need a way to interpret this, we need a way to represent that is interesting one to be assembly related. Operations are represented by condition, operations are represented as a credit instruction.

19:07 화자 1
For example, if we have an info segment like this, if RS1 equals to RS2, then to subtain. So it is called branch equal and in a cycle languages it is represented like this and this one is similar like it is branch not equal rs one if Rs one is not equal to Rs two and something. To implement that project, we use this possible value. Before looking at what's the languages, let's look at the code depend to understand how your what is this?

20:05 화자 1
[[unclear: There are many things that I don't like about this school, but the worst thing is that this school is in mountain. Know about this or not, I don't know if you guys know about this or not, but the record department is located at what is that Kumun?]]

20:26 화자 2
어

20:28 화자 1
[[unclear: The way on the NASAM day, which means that our houses are in the mountain, which means that we always are tutoring with the mosquitoes. But this year is better than last year last year we had a lot of mosquitoes I saw some students that some students who took my course last year and you may remember that I was like catching mosquitoes during the lecture.]]

21:02 화자 2
But.

21:03 화자 1
This year, I think I don't have to do that. In that sense, Kaist is better, Kaist is not involved. Let's go back to the lecture so we're going to look at the ecosystem that is C to understand how those kind of control flow operations are in one set ecosystems are translated into the Control flow operations so let's say we have a same structures like this [[unclear: if I equal to j, then f is bad. Otherwise f is two minus H]]. The control flow of the C program can be represented in this way.

22:00 화자 1
So if you write the program and compile it into RISC-V more assembly, you may see something like this. So the first one is the branch not equal, which means that if these two registers are different, then the program goes to here else. But instructions that is tagged with the S. So if these are different and program both to here, then the program will compute this subtractor version with the tube to input registers and stores into X19.

22:49 화자 1
And if the two registers are the same then the program will compute the add operation and store the results into the register for past 19 and in this case IFA are equal then the problem is Just keep this part right, we don't have to do the subtract operations so whatever happens, the program jumps to exit here. To avoid the unnecessary subtract operation. You can see that this one completion of branch uses one register as the.

23:37 화자 1
Comparison operate you can see that we use x0 and here it is also x0 which means that that can be translated into If x zero is equal to x zero, we jump to x n so x zero is always equal to x zero, right, so this is unconditional jump and it doesn't really pull in this constant fit in which five x zero always stores zero. And this is another example of a C program that can be compiled into RISC-V it is the while statement like [[unclear: while same I equal this is equal equal. While I say I equal to k, then I cost equal one]].

24:29 화자 1
So if you write it like this and compile it into RISC-V or sample images, then you will see instructions like this. I don't want to explain in the every single line of acceptable instructions, but this is the first time we so while we look in acceptable languages, when we explain it so the first The first four instructions are to check the condition in the while loop first we get the offset of this same I like.

25:16 화자 1
I is stored in x22 and we shift left by 3, which means that we compute I times to the power of 3, so it is identical to this one and that stores the results into the results of code x10. And the ATPAN is used to get the address of this one save high development of this save. So this address of the same is stored in X25 so we get the address of the same I by adding x10 to x25 and stores the address of the same I into X10.

26:04 화자 1
Here and load the value of the same eye into register code x9 and in RISC-V. The not equal operation always take the register operands so we can compare to register like this x9 and x24 so if they are not equal then we jump to the exit exit is here but if they are equal then we continue so we The operation here with this particular operation. And the other one happens we want to go back to the beginning of the loop right, that is the semantic of the while statement so we use another unconditional branch operation here so we go back to the first instruction of the while statement so that is the semantic of the assembly languages.

27:01 화자 1
So assembly languages are assembly programs are really previous or going to read, but if you really carefully, then you will realize that this meting of the assembly code will be very similar to the structure of your C program. And because we were discussing about the control flow, let's talk about some important concept in the program like the program management it is called a basic block so basic block is A sequence of instructions then. No embedded branches and it does not embed a branch target, for example.

27:52 화자 1
In this case, the fact you may realize that the first four instructions will be always executed together when we jump to the first instructions, then no matter what happens, we execute the remaining three instructions until this one. So these four instructions are these four instructions composed for what was that? We pull that foreign structure as a basic block so in this figure like this rectangle is a instruction, you could think each rectangle is a instruction and if your program jumps to this part Then no matter what happens, your program goes to the end of the basic block and jumps to the other so basic block is important because we can optimize this part.

28:47 화자 1
Independently, although what happens, we always execute these four instructions together and that execution will not be interfered by other parts of the program, which means that if Execution of that program instruction is identical to be optimized code and the correctness of your program is preserved so a compiler identifies basic blocks for your program and try to optimize that basic blocks to improve your program. And also a processor design of the processor also tried to optimize the execution of basic blocks or I think there are some optimization for the execution of the basic block.

29:38 화자 1
Anyway, that is not related to East Transfector so let's go back to the east transfector so these are For equality check, right, when you see here, these two branch instructions are for equal and equal, which means that sometimes we need another type of branch conditions, branch instructions for But larger than or greater than or smaller than the kind of thing.

30:08 화자 1
So we have more branch operations here you can see examples the first one is branch less than and it takes three operands the first two branch are equal however the open document The two operands are input and the third operand is [[unclear: the destination of the registration]] so the submitting of this one is that if RFS one is less than Rs two Then the program jumps to as what and this one is similar. It's a greater than or equal to GE sets program and or equal to so to customize what this structure is that if Rs is Rs one is Greater than or equal to Rs two and productoms two. So this one will be greater than this requirement structure.

31:10 화자 1
You can follow this separating structures. Easily. And when your science is always important when you deal with integer numbers and in the case of branch instruction it is also important so let's think about it so here we have two numbers, two registers that One stores one more and the other stores zero, zero and at the end of the representation there is one. So here we can see that design, this is true because if both numbers are interpreted as signed numbers, then this one will be true because this one is negative number.

32:08 화자 1
If you remember the last, the upper most of the. Most significant bit of the number represent the sign of the number so if this is 0, then it is non-negative number but if this is 1, then it's negative number so you can see that x22 is a negative number and x23 is a non-negative number. But if most are unsigned numbers, then this one is obviously larger than this one, this one is very large number. So this is determined by your program, which is that if your program declare that both variables are unsigned numbers, then a compiler and the processor will translate.

32:57 화자 1
Set numbers in this way and if your program clear set numbers as a signed number then this will happen so if you write a C program, please be aware of the assignments of your numbers. 어 Of the branch instructions in you can think that kind of branch instructions as Control flow inside the function but in your program you have another type of branching control flow operations that is a function call your program always have more than one function so let's talk about that.

33:42 화자 1
To do the function code in C program, these things will commonly happen, which means that the caller function will pass function parameters to the callee function and callee function to do some operations based on the parameter argument and the So local data and has the result to the caller function and return to the caller function so every instruction architecture needs a way to implement these kind of things and in RISC-V instructions I mean in RISC-V processor This will be translated into this procedure, which means that if parameters are placed in registers [[unclear: starting from x0 to X17]] and there is an instruction to transfer the control to the procedure.

34:41 화자 1
And the procedure callee function will do something and callee function will replace the results into a register to pass the results to the caller function and return to the caller. So we will look at these procedures. So for the function call, we could think of two instructions, two assembly instructions, the first one is jump and link, the second one is jump and link register, the first one is for function call and the second one is for return. So for example, if your main program calls a function called books, then this one will be worse. This one will be used for the function and this one is used for return.

35:36 화자 1
So the semantic of the first one is that your program, your processor put the address of the following instruction that would be return address of the function in X1 there we put x1 as the first of this instruction, right? So return address of the function will be stored in x one register and the processor will jump to the target like this one and This x1 will be used as a return address in just a link register instruction. So as I told, this one is this one towards the return address of the function so the semantic of this instruction is that jump to this address.

36:33 화자 1
And stores the return address of the I mean stores the next instruction of this one [[unclear: into x two]]. So typically you put x zero as the first operand of the jump a link register instruction because in reason why x zero always stores zero, which means that we cannot change the value of the register x zero so whatever happens, this one is lower and nothing happens. If you ignore the first operating that instruction, that instruction means that it jumps to the address. Which is [[unclear: identical to the internal threshold contract]].

37:21 화자 1
Understand to understand how that works but I think it is good to see the code example this one is a very simple code example right there is ultimate operation that we saw before. [[unclear: Equal g plus h minus I plus a]] since very simple lower okay, I think now there are mosquitoes I got this. I think last year I got mosquitoes, like ten mosquitoes, I don't know, during lectures.

38:13 화자 1
[[unclear: I think I should bring what was that should bring epic killer what is the English temporary epic killer, the instant killer or whatever this train I have to bring. I can see many foreigner international students are there, there are international students and then more you guys are okay with the mosquitoes like are you fine living in Musk Mountain? Anyway, let's go back to the lecture so there is a simple C function there and it does a simple architect operation.]]

39:03 화자 1
If you compile this is the programming to certain languages then you will see something like this it's very complicated and don't and tedious to read I know that let's break it now to understand how the function call works. So instead of let's focus on the middle part first, that is the arithmetic operations that we saw before, right, the first example that we saw at the beginning of this lecture is just three arithmetic operations.

39:39 화자 1
To compute this one and now this example, this one is the return value of this function so Instead of just finishing the three expressions, we put the results into x ten in RISC-V x ten is used as a return value of the function x10, x11 is used as a return value of the function but So we have only one return value of the value of x20 is through the register x10 so the caller of the function will read x10 to get the return value of the function. And the remaining part is a bit complicated.

40:28 화자 1
And we're going to look at before that here we have a jump and link register instruction at the end of the function, which means that [[unclear: The return value of the function is store data register s one]] and after speeding all of the instructions a processor will jump to the address store data register and this one stores the polar the address of the caller function. Effectively, that is the equal diverse. And beyond that there are many other instructions, right, and the purpose of that instruction is to implement something like something called calling convention so as you can see in this example, we use three registers X18 to X20.

41:26 화자 1
And what if x z registers are used in the caller function, for example, if this function is called by the main function and the main function use the register like the three registers in its implementation. The values stored in these registers will be lost and the main function if the main function, these debt values and the main function will bulk. We lose the value of the register.

42:04 화자 2
So

42:07 화자 1
So in this region we need a way to store or we need to preserve the values of the registers so that part these parts are to preserve the values of the registers, which means that we store the values of the registers x18 to x20 to memory. Oh, yeah, we can see here here we have a stack on the memory, the stack here, the stack is just a stack you learned in the data structure course. Like, you know that right, the stack has a top and we put the data on top and we pop the data from the time so it is just a stack and the top of the stack is located at about in this example.

42:58 화자 1
So, when this function is called, the function preserves the values of the register x18, x19 and x20 in the stack, and when it returns to the caller, it restores the values of the registers. From the state.

43:20 화자 2
So

43:21 화자 1
Before doing the operation in the function, the state is empty, but The function first preserve the variance of register [[unclear: x88 to exchange]] in the stack is when it uses. Restore operations in this way so it preserves the data and after doing the automate operation it restores the value from the set so In that way, the function will not lose the register that will be needed by the caller.

44:09 화자 1
So now let's connect that behavior with the sourcing called calling convention so calling convention is the kind of contract between the caller and the colic contract, for example. Not after we have to pay, as we discussed in the example. Sometimes the callee function needs to preserve the values of the register because of caller in this caller may be used that layer the calling convention is a kind of contract that which registers are preserved in equaling and which registers are preserved by the caller. So as you can see this example, these registers, I guess.

44:55 화자 1
X what was the register x 18 to x 20 is saved by callee, which means that if calling was to use debt registers and the callee function should preserve the values of the registers. And there are some other registers like x five, x six and seven and you can see this table, it is preserved by caller, which means that callee function is free to use that registers. And the caller function should preserve the various if it needs to use the various layer. So these kinds of things are kind of contract between calling convention so your compiler will translate your C program to satisfy calling convention.

45:52 화자 1
This is important, for example, when you use a library function, your program consists of some libraries, right, you don't have to implement the program by yourself and you will use some functions like a follow that is implemented by others. And your program can communicate with the library functions because most implementations, like your programs and the library programs, library implementations satisfy calling convention. If one of them do not satisfy the calling convention, then they will not communicate. In a current way so your program will not contract so this is a kind of important concept in low level implementation but most of the time you don't have to think about calling convention because the compiler will do this beyond review.

46:54 화자 1
And in this example we discussed about only return which means that this function does not call any other function now let's look at another example that calls another function in this example we have a recursive function recursive function which is this per factorial. As you can say this, as you can say this, the extent is used as an argument and return value. So the first argument of the function is passed through the register x ten, and the return value of the function will be passed through the register x ten, which means that the argument will be overwritten by another function.

47:40 화자 1
For example, in this example, you can see that the function recursively called itself here, right, but after calling this function, the Value of n will be lost because it is stored in register x ten, but return value of this part will be also stored in register x ten, but we need an argument here. So this function is to preserve the value of the x ten register. So when you compile this into assembly instructions that you will see this kind of very long and something that I don't want to read very tediously.

48:33 화자 1
But if you look at these codes that have very healthy way, then you will realize that this one is really the structure of this one is really similar to this one, but it's way lower and more complicated. So let's look at the first part the first part is for the case that kind is less than one so as you can see here. The function does the similar thing is towards the values of the register that will be used later. As I told you, x10 will be used later after calling this function, we will do the multiplication with n and the result of the function right, so this function preserves the value of the x10 in the site. And after that it goes the.

49:31 화자 1
Factorial operations, It computes in minus 1 and stores the result of n minus 1 to register x5 and compare the x5 with 0 to check if it is greater than more important zeros. So if it is greater than over, equal to zero, it comes to l one over here. And if this one like a m minus one is less than zero, then it continues. [[unclear: This part is the health statement in C program. This one we don't work.]]

50:20 화자 1
So return one means that we want to store the value, the number one into the register x ten and return to the function return to the caller function so this part is the return one. Reading assembling in structures is really. And for this course, after this week we will not see assembly instructions in this way we don't have to read this kind of long instructions.

51:06 화자 1
Which is very painful so please admit for these lectures, please read this kind of assembly linkages and understand how they work I mean understand images, think about how processor work with registers. And the second part. [[unclear: Happens all the time our lecture is lower than one hour, but the background one does not class for one hour.]]

51:55 화자 1
So the same part is for the third instruction, like the S part, s instruction is the factory function C, but I don't want to read them because it is too painful to me, so please read them and understand them as a homework. Like I said, it is homework, but I will not check whether you do this or not, but just I want you to understand how these kind of assembly, these structures work with registers. And if you have problems to understand how they work, [[unclear: please email A or please email A]] and make a help you to understand this kind of machine instructions.

52:55 화자 1
Okay, there is this. Let's talk about the memory layout a little bit actually you will learn about the memory layout more detail in system programs. But let's talk about it very quickly, as I told you on Tuesday for last week, instructions are just the data. As you can see in this case, memory layout, [[unclear: there is a data section, I mean there is a state data and above that there is a state data]] and above that some kind of dynamically allocated data where and all of them are located together. Here, one important feature of the memory layer in modern architecture is that state grows in this way, it grows with boundaries.

53:58 화자 1
So we call the pop, the pop, but the pop goes. Downwards so if the stack is empty and the top is at the top of the figure but if we put data understand, then the top grows downwards and the top moves downwards. That is what kind of computing picture or characteristic of other architecture so please be aware of this example shows that other At the beginning of the function stack is empty so there is nothing but if we put some data on the stack and the stakeholder grows downwards and it will be located here and when you pop data from the stack then data will be run and stack.

54:55 화자 1
Further this one is the sequare will be located here so to implement the stack. In architecture, we have two registers, the first one is a stack pointer SP stands for stack pointer and FP stands for frame pointer, so stack pointer points to the power of the stack and FPA points to the arm of the stack. For example, let's say if your program has a main function like the main function calls another function which is called food and maybe this one is the memory layout of the main function before pulling the function food.

55:44 화자 1
But when you call the function book, then frame pointer moves to here to point to the model of the stack for your function book and stack pointer grows from this location. The level range is starting from frame pointer to stack pointer is a stack frame for the function pool. So this portion of memory pointed by FP and SP store data for the local function tool, that is this data is only for the function. In this step frame, there are many locally located data. You know that there are largely two types of data, the first one is locally allocated data and the second one is globally allocated data.

56:43 화자 1
Here locally allocated means that stack variable if your program like if your functions declare a variable like inside a function, then that variable will go to here. Yeah, and you can see that here we have local arrays and structures so that data would go there and because of the public convention we save some other data like arguments of the function here if the function has A lot of arguments, let's say if your function has 20 arguments, then the 20 arguments cannot be fit into here because they are just eight registers.

57:36 화자 1
So 20 is larger than, say right and the remaining elements will be stored on the stack and [[unclear: the database of the stack will be database of the function will be stored on the stack at some Say police, say the registrar tools]], okay, so separate stores, a lot of things for the function to work correctly. And this one is related to a question from the students of Tuesday, actually, I answered, let me. I'm really good at catching up here.

58:31 화자 1
I mean, registers are fixed size, which means that all registers are fixed, are not worked on. Most registers in RISC-V has [[unclear: sixty four five, sixty four six five]] it does not align with the memory site right in memory we can store one byte and we can store two bytes or four bytes or eight bytes or over. So there were curations of load and store instructions here, [[unclear: byte is just zero bytes, hot word is two bytes and one is zero bytes]].

59:15 화자 1
And depending on the size of the data you store the memory you can choose corporate variations for load and store corrections so if you use this one then the processor will load a single bind from a memory and Stores, let's single-like it to the register, but there are remaining parts of the register. the register stores 8 bytes and we'd go to just 1 bytes. The processor will feel the remaining part with zero or sign of the data it also depends on the time of the

59:53 화자 2
These questions.

59:55 화자 1
So this one is assigning ten with rolling instruction, which means that if the data is negative number, like a single mite is negative number, then a process of law fills the remaining part with what? And if the data is a positive number, running a number, it's a processor will be the meaning parts with zero. And [[unclear: this one is used for assigned numbers]], so this one will fill zeros to the remaining parts of the region.

01:00:29 화자 2
Yes.

01:00:30 화자 1
[[unclear: Word does not have the periods of the Titans]] because. We do not have to feel memory, we do not have to feel memory locations depending on the size of the. These things are used to deal with a different side of the number for example, strcpy is pretty familiar function, right, we use this really frequently and you know that character, the type character is just a number and the size of the character is one. So in this example, you can realize that the data pointed by X and Y are stored in memory, but you have to load that data into registers to do some operations like arithmetic operations.

01:01:31 화자 1
So if you compile this, this program, then you will get assembly instructions like this again, this is very low and tedious, I don't want to read it, but You have to win it. But in this lecture, let's focus on the two instructions at line number six and eight, the line number six lost a single byte from memory like this is this meaning of this one is

01:02:03 화자 2
Like this.

01:02:04 화자 1
[[unclear: We load a single byte from yi and store the value to x six, the reducer x six. This one and. Real sentence. 한국 컴퓨터에서 얻을 것 같아 Because character has a signed character and unsigned character so I think this one should be LP instead of every use but let's say this is an unsigned number, unsigned character.]]

01:02:56 화자 1
Memory point by this one and stores did single line into the register x6 and x6 has the empty seven bytes so the processor will fail zero to that seven bytes and later That single light will be stored in that single light stored in X6 will be stored in two memory points by X7 here we have a single light store operations. [[unclear: The IA doesn't matter, it doesn't matter LDU and LD has the semantically same operation]] so it doesn't matter in the register X6 will be stored in memory point by X7.

01:03:52 화자 1
This one, the semantic of this assembly instruction is identical to this one so this example shows how we deal with the different various sizes of the numbers and please check out the code system by yourself. And sometimes we need a 32-bit constant like I think we need to explode to implement the branch instruction, I mean, the format of the branch instruction is like this. We have two source registers and immediate value but as you can see here, immediate value, the size of immediate value is plus and how does this instruction work?

01:04:40 화자 1
This instruction works in this way like we have address of the current instruction and the current address of the branch is computed in this way we have the address of the current instruction and we And immediate value times two to the address of current, for example. For example, if you look at this example, you see that here we have an English word exit, but In machine instructions, everything should be encoded into binary, they should be encoded into numbers, but xi is not numbers right then how do we store the exit into binary numbers in processors?

01:05:39 화자 1
We use a relative offset between the target address and the current instruction. For example, here we have, let's say this address of this instruction is 100 and the address of this instruction is 200, then we can compute the difference between the two instructions, right, we compute the difference between 200 and 100 and we get 100 and the difference So instead of writing exit in binary. We write down 100 as a difference between 50 100 divided by two, so we write the 50 as the third operand of the branch instruction.

01:06:29 화자 1
So this one represents that we have a difference between the target address and the current instruction as the immediate value. That immediate value will be translated into the difference between the target address and the currency instruction and we add that difference into the address of the currency instruction that is the way of getting the target address in branch instruction. So sometimes that decides up because as you say this example, it is format, the immediate value has a 12 bit, the size of the immediate value is 12 bit. Your program can be very large, for example, like your program can be reinstalled and your program can be chromium, which may say [[unclear: the size of the program is way larger than 20 bits]].

01:07:22 화자 1
in that case we need a way to construct large number [[unclear: larger than 20 bits]]. So third of this customs. This one, like the last one, is used to construct the virus constant.

01:08:10 화자 1
[[unclear: So the last type of the instruction that we saw in the table is to construct a large constant number and the behavior we have a As I'm going to start here, Lui and LD constant and the behavior of how to construct a large number is that right? We cast a 12 bit concerned to read at the value of the register rd. Here we have a constant number and the size of the constant number is let almost that.]]

01:09:07 화자 1
[[unclear: 12 bits the size of the const number is 12 bits so we copied that 12 bits const number to bit story 1 to 12 of the destination register so In the vegetation register, we have 12 bits of data and then we have some other arithmetic operational instructions to fill the remaining a little bits of the lower. So in that way we can construct a 32 bit of a data and this instruction will extend 31 bit to the upper 32 bit. So in risk five, constructing large number is a little bit complicated than you think we need multiple instructions, including]]

01:10:05 화자 2
This.

01:10:06 화자 1
Instructions as the multimetic operations and data movement operations to consider the right number and sometimes we need it because of for example, in case of these branch instructions. So let me say you want to jump very long from long way from the current instruction, then you have to construct the light number and use the jump and link register instruction. Here this instruction was used in return instruction we used this as a return instruction in our examples, but this can be used to Also, this can be also used to jump to very long way from the current instruction because it takes the register as an input.

01:11:03 화자 1
It jumps to the address store database so combining this one with some multiple automatic aversions, we can construct large constant number and use that large constant number as an input of the Sometimes think about this instruction to some target that is very far from the current instruction. And that is the basic of the instructions cover the discourse and let's talk about the synchronization a little bit synchronization is used to synchronize multiple strategies running in multiple processors.

01:11:56 화자 1
So these days every computer has a multiple processors, right, your processor, your program is never be a single processor computer ever again. And sometimes they need to synchronize to get to read each other, which means that there is a shared data and your problem is a way to enforce some orders between multiple cores. Synchronization operations are used to synchronize that kind of instruction, but that kind of processors, for example, sometimes you want to implement the atomic swap operations between register memory you will learn about locks in system programming but let me talk about it briefly so there is a negative structure called lock and the lock is to lock a portion of memory.

01:12:57 화자 1
A lock is used to lock the data structure stored in there so to implement a lock data structure using the synchronization that synchronization operations are used to implement a lock of lock data structure. As you ferment a lot data structures, you need hardware support because.

01:13:23 화자 2
Speech.

01:13:24 화자 1
[[unclear: Assembly instruction is kind of atomic, so it cannot be interfered by another processor]], but lab operations can Be a little bit couple that spend a single instruction and you don't want to be compared by another process. For example, this is the example of a lot construction and you want to make this part as a single atomic operation so you don't want to get interfered by another process. So to implement this, you RISC-V to provide two synchronization instructions, the first one is load reserved and the second one is the store conditional. So load reserved is similar to load operation, it loads values from memory, but it does additional operations beyond just loading a memory, loading data from memory.

01:14:24 화자 1
It also put some reservations and memorials, which means that it declares that I want to keep this data uninterfered by another processor. It just put a reservation on that memory location and later these two are used in a pair if a processor puts reservation on memory location and later it checks the It later detects whether the reservation is preserved in that memory location. The store conditional is the kind of store operation like in store data is memory, but only if the reservation is still there, the reservation will be done if another processor will override the data.

01:15:16 화자 1
In that case, this store conditional will fail and the processor will recognize that if the store is failed or not. So these examples implement a simple data structure that will be used in kind of information [[unclear: the first one is less than is luck]]. So how many of you the second example will be kind of homework so try to understand the second example I will send the first one so the first one is the atomic swap [[unclear: you want to slot two registers]].

01:16:08 화자 1
In an atomic way, which means that it wants to load the value from memory and store that value to register and store data that is storing another register to memory, but that should happen atomic growth. Is that the important that Atomics was a processor called reservation of the memory while owning the data and To use the store conditional to check if the reservation is still there of memory.

01:16:49 화자 1
And [[unclear: if the reservation is one]], that means that the data is changed by another processor, it goes back to the first instructions to do it again and if reservation is there, it means that there is no other There is another low other processors that interfere between these two instructions so the swap operation happens atomically so that is this method of the atomic swap and the second example is a lock but I'm skipping the second example. There are many things that we didn't talk about like floating point is another thing that is defined in instructions architecture and but so far we covered basic of the instructions architecture and the remaining course will be Only related to the contents that we cover.

01:17:44 화자 1
So, yes, I say for today's lecture and I think for listing today's lecture.
````
