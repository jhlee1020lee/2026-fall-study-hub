---
title: "2026-09-03 · 프로그래밍의 원리 · 보정 STT"
course: principles_of_programming
date: 2026-09-03
tags: [lecture-transcript]
source_kind: corrected_transcript
privacy_redacted: true
verbatim_complete: false
---

강의 녹음을 바탕으로 보정한 STT의 공개용 사본입니다. 음성인식 오류와 불명확한 발화가 남아 있을 수 있습니다. 수강생 이름·연락처와 일부 개인 자기소개를 가렸고, 타임스탬프·화자 순서는 보존했습니다.

원문 보정본 SHA-256: `d75cd3508870585d4916a27cc4f5ba6cc066906617152beb20a888e64bbe484c`

`[[unclear]]` 등 표시는 원문에 남은 불확실성입니다. 코드·수식과 발화 표기를 그대로 읽을 수 있도록 본문을 텍스트로 제공합니다.

````text
01:23 화자 1
지금 우리가 배울 게 4개 part로 되어 있습니다. 프로그래밍의 원리인데, functional programming하고 imperative programming, Part 1과 4가 대비되는 개념이고 Part 2와 3이 대비되는 개념이에요. OOP하고 type class를 배울 텐데, [[unclear]]가 제일 어렵기 때문에 마지막에 하고요. 순서대로 어떤 차이인지 먼저 하나씩 보겠습니다. Imperative programming하고 functional programming의 차이는 program을 실행하는 원리가 좀 달라요.

02:10 화자 1
Imperative 방식으로 program이 실행될 때는 memory라는 state가 있어요. 하나의 computation은 memory에서 value를 읽어 와서 뭔가 계산하고 다시 memory에 쓰는 거예요. 계속 state를 읽고, 계산하고, 쓰고, 다시 읽고 계산하고 쓰는 과정을 반복하도록 기술하는 게 imperative program입니다. 그런 computation을 sequence로 순서대로 쓰고 반복하는데, 실제 반복 장치가 없으면 내가 code를 100줄 짰을 때 computation이 100번 일어나고 끝나요.

03:08 화자 1
그러면 1시간 동안 도는 program을 짜려면, 예를 들어 1시간 동안 computation이 1조 번 일어난다고 치면 내가 1조 line을 짜야 돼요. 말이 안 되잖아요. 그렇죠? 그래서 이건 본질입니다. 내가 조금 짜고 많이 시키려면 반복시켜야 하거든요. 여기서는 loop라는 형태로, while loop 같은 code로 같은 code를 state를 바꿔 가면서 계속 실행합니다. Code는 그대로지만 memory state가 바뀌었잖아요. 그래서 같은 code를 실행시키더라도 바뀐 state에서 다른 계산을 하게 됩니다. 그리고 일반적으로 기계가 하는 일에 가깝습니다. 실제 CPU가 이렇게 해요. 아, 제가 코로나 일정이 있어 가지고 오늘 찾아보니까 지금 감염성은 아예 없대요.

04:05 화자 1
감염성은 없는데 끝나고 나서 계속 면역계가 반응하거나, 아니면 안에 죽어 있는 코로나바이러스가 자꾸 자극을 가합니다. 그래서 어쨌든 이건 기계가 하는 것에 가까워요. 기계가 원래 하는 일이 뭐예요? Memory에서 value를 읽어 와서 계산하고 다시 memory에 쓰고, 다음 instruction으로 가면 또 memory에서 읽어 와서 계산할 수 있고, 그냥 기계가 하는 일과 같은 수준의 얘기잖아요. 기계가 동작하듯이 기술하는 거예요. 그래서 장점은 efficient code를 쓰기가 좋아요.

04:51 화자 1
실제로 내가 쓴 그대로 CPU에서 일이 일어나고 거의 일대일 대응이 되기 때문에, CPU와 computer resource를 얼마나 쓰고 속도가 얼마나 걸릴지 예측할 수 있어요. Code를 더 효율적으로 돌아가게 잘 짜면 efficient code를 쓸 수 있겠죠. 실제 CPU에서 돌아가는 것을 내가 control할 수 있는 거예요. 반면 functional programming은 좀 더 사람에게 맞춰져 있어요. Low level로 기술하는 게 아니라 더 high level로, 기계가 생각하는 방식이 아니라 사람이 생각하는 방식으로 기술하는 겁니다. Computation을 memory state에서 읽고 계산하고 쓰는 단위로 생각하는 게 아니라, function이라는 단위로 어떤 계산을 하는지 생각해요.

05:44 화자 1
그러니까 위의 imperative program에서는 program이 가진 information이 memory에 들어 있어요. 앞선 instruction이 다음 instruction에 information을 전달하려면 memory에 저장시키는 거예요. 그러면 다음 instruction이 memory에서 value를 읽어 information을 받고 또 update합니다. Loop를 돌 때도 여기 while loop를 보면 `sum`의 내용이 계속 바뀌잖아요. Memory 내용이 바뀌어서 이 instruction이 수행하는 계산이 달라지거든요. 이전에 계산한 결과가 다음에 사용되고, 앞 iteration에서 계산한 것이 다음 iteration에서 사용되면서 computation이 계속 일어나는데, information의 전달이 다 memory를 통해서 일어나요. 그런데 function에서 information의 전달은 argument를 통해 일어납니다. 어떤 function `f`가 있으면 `f`의 argument에 3을 주면 3에 대해 계산하고, argument에 4를 주면 같은 code가 4에 대해 계산하잖아요.

06:44 화자 1
Code는 똑같잖아요. 그런데 3을 주면 같은 code가 3이라는 information에 대해 계산하고, 4를 주면 4라는 information에 대해 계산하잖아요. 같은 code로 다른 data에 대해 계산시킬 수 있는 거죠. Loop의 경우에는 같은 code인데 memory에 들어 있는 내용이 바뀌는 거예요. Loop를 한 번 iterate할 때마다 `sum`이 처음에는 0이었다가 다음 값으로 바뀌고, 같은 code가 다른 data에 대해 계속 반복되는 게 중요한 거잖아요. Loop를 통해 memory 내용을 바꾼 거예요. Functional programming에서는 같은 function code를 3에 대해서 시키고 4에 대해서 시키는 식입니다. Value를 바꿔 가며 시키고 싶으면 argument value를 바꾸는 거예요. Computation은 function을 계속 실행하면서 일어나는데, 계속 바뀌는 value가 argument value입니다.

07:42 화자 1
그래서 여기에는 memory라는 개념이 없죠. Value의 전달인데, 이 function이 value를 받은 후에 다른 function을 부르면 그 function에 value를 전달하는 식이거든요. Function을 계속 실행하면서 function에서 다른 function으로 argument value가 계속 pass되면서 바뀌잖아요. 그걸 통해서 다른 data에 대해 계산합니다. 그래서 기본적으로 functional programming에는 memory라는 개념이 없고 value가 argument를 통해서 계속 전달됩니다.

08:12 화자 1
그래서 계산이 되고, 반복은 recursive function, recursion을 사용합니다. `f`가 자기 자신을 부르거나 `f`가 `g`를 부르는 식이죠. 실제 computer에서 어떻게 돌아가는지 resource를 control하기는 좀 더 어려워요. High level로 쓰기 때문에 이게 resource를 정확히 얼마나 쓰는지 분석하기가 좀 어렵습니다. 여기 summation을 하는 code로 양쪽 계산 원리의 차이를 보겠습니다. 이 summation은 0부터 `n`까지 더하는 code예요. `n`은 user에게서 argument로 주어지고, 0부터 `n`까지 더합니다. 위에는 C code처럼 짠 거고요.

09:09 화자 1
이게 imperative인데, 먼저 `sum`은 memory location의 이름이에요. `sum`에 0이라는 value를 넣어 initialize합니다. 그다음 `i`라는 memory location에 처음 argument로 받은 `n` value를 복사해요. `sum`은 0이고 `i`는 `n`으로 setting되어 있습니다. 이제 while loop에서 `i`가 0보다 큰지 판단해요. 처음에 10이 주어졌으면 당연히 0보다 크겠죠. 크면 body를 수행하고 다시 돌아와 condition을 판단해서, 또 만족하면 수행합니다. 이렇게 하다가 condition이 false가 되면 loop를 나가는 거죠. `i`가 10이면 `sum`에 10을 더해 다시 `sum`에 저장하고 `i`를 9로 줄입니다. 그다음에는 `sum`에 9를 더해 다시 저장하는 식으로 진행되죠.

10:08 화자 1
그러니까 `sum`에 10, 9, 8, 7, 6을 차례로 더해서 결국 0부터 `n`까지 더한 value가 저장됩니다. 마지막으로 `i`가 0이 되면 loop를 exit하고, 끝나고 나면 `sum`이라는 memory location에 내가 원하는 결과가 있습니다. 이건 익숙한 방식이라 아마 잘 이해할 거고요. 밑은 functional program입니다. Functional program은 function을 정의하고 그 function을 apply하는 식입니다. 이 function `sum`은 `n`이라는 value를 argument로 받습니다. `sum(10)`을 하면 계산이 끝났을 때 0부터 10까지 더한 결과가 나오는 거예요. 이 함수는 `n`이 0 이하면 0이 되고, 그렇지 않으면 자기 자신을 다시 부릅니다. `sum(10)`이면 `sum(9)`를 부르고, 그 결과에 처음 받은 `n`, 즉 10을 더해 return하는 거예요.

11:08 화자 1
그러면 `sum(10)`은 55가 나오고 `sum(100)`은 5050이 나옵니다. 그런데 보면 둘이 다르죠. 위에는 memory에서 무슨 일이 일어나는지까지 기술하고, 밑은 그냥 수학적 정의 같잖아요. `sum(n)`은 `n`이 0 이하면 0이고, 아니면 `sum(n-1)`을 다시 불러 계산한 뒤 그 value에 `n`을 더합니다. 그래서 차이가 많이 나죠. 같은 일을 하는데 style이 많이 다르죠. 밑은 전부 function으로 computation을 기술하는 거예요.

11:51 화자 1
그래서 좀 더 사람이 생각하는 level로 씁니다. 이게 설명만 하는 게 아니고 실제로 수행 가능해요. `sum(100)`을 계산하라고 하면 5050이 나왔어요. 여기서 5050이 실제로 계산돼서 나온 거잖아요. 그러면 0부터 123까지 더하면 얼마나 나올까요? 7626이라고 합니다. Code를 보면 좀 더 수학적인 정의 같잖아요.

12:46 화자 1
`sum(n)`은 `n`이 0 이하면 0, 아니면 `n + sum(n-1)`입니다. 그리고 이것은 function을 계산해서 적용하는 방식입니다. 그래서 어떤 식으로 function이 계산되는지는 앞으로 배울 겁니다. 이렇게 하면 이게 어떤 computation을 할 수 있는 거죠. 이거는 어느 게 더 자연스러운지는 사람마다 다른데, C programming이나 이런 걸 많이 하는 사람은 앞의 것이 훨씬 자연스럽고 조금 쉬운데 밑처럼 쓰는 것은 잘 이해가 안 되고 어렵다는 생각도 있고, 반대도 있어요. 여러분은 어때요? 이렇게 쓰는 게 자연스럽게 보이죠? [[unclear: 13:42–13:51의 짧은 후속 발화는 원본과 독립 전사 모두 문장이 무너져 정확히 확정할 수 없음]]

13:43 화자 1
제가 하려는 말은, 이게 어쨌든 다른 style이고 실제로는 무조건 밑처럼 쓴다거나 무조건 위처럼 쓰는 게 좋은 것은 아니고 목적에 맞게 섞어서 쓰는 게 좋다는 겁니다. 그래서 둘 다 원리를 이해하고, 이걸 잘 섞어서 적절하게 목적에 맞게 써야 합니다. 그런데 이제 programming style을 고르는 게 기본 programming 원리입니다. 이런 원리를 이해하면 여러분이 손으로 안 쓴다고 했잖아요. 그러면 AI한테 시킬 수가 있는 거예요. AI한테 그냥 시켰더니 얘는 무조건 imperative로 갔어요. 그런데 내가 원리를 이해하기로는 이런 부분은 functional로 하는 게 낫다, 그러면 AI한테 이쪽은 functional로 하라고 시킬 수 있잖아요.

14:41 화자 1
물론 AI가 똑똑해져서 나보다 더 잘 판단해서 할 수도 있겠지만, 어쨌든 아직은 그런 guide를 우리가 줘야 되는 것 같고요. 그다음에 아니면 걔가 뭔가 코드를 짰는데, 당연히 걔 입장에서는 섞어 가지고 imperative하고 functional을 섞어서 짰어요. 그런데 내가 imperative만 이해하고 functional을 이해 못하는 사람이면 걔가 짠 코드를 이해할 수 없겠죠. 그래서 걔가 짠 코드를 이해하려면 둘 다 이해해야 돼요. 어떤 이유에서든 이런 기본적인 원리는 다 잘 이해하고 있어야 돼요.

15:42 화자 1
그래서 어차피 요새 거의 다 둘 다 잘 지원하고, 다 섞어 쓸 수가 있어요. 그런데 보통 위쪽의 Java나 JavaScript, C++, Python, Rust 같은 언어들은 imperative를 훨씬 많이 쓰고 필요할 때 functional로 쓰는 게, 그쪽 사람들이 전통적으로 그런 경향성이 있는 거고요. OCaml이나 SML, Lisp, Scheme 같은 것들은 주로 대부분 functional로 짜고 필요할 때 imperative를 섞어 쓰는 겁니다. 그러니까 imperative 중심적인 language도 functional feature를 다 지원하고, functional 중심적인 language도 imperative를 기본적으로 지원합니다. 그리고 둘 다를 균형 맞춰서 잘 지원하는 것이 Scala예요. Haskell은 좀 특별한 경우인데, 얘는 완전히 purely functional language예요.

16:38 화자 1
Imperative를 주로 안 하는데, purely functional language 안에서 imperative 느낌의 programming을 하는 방법이 있습니다. 이게 monadic programming이라고 하는데, 그런 style이고 마치 imperative programming을 하는 느낌을 줄 수가 있어요, 안에서. 그런데 technically는 memory 같은 것 없이 purely functional로 하는 것처럼 생각하면 됩니다.

17:22 화자 1
이게 computation을 하는 원리의 두 가지 큰 paradigm이라면, OOP랑 type-oriented programming—제가 지어낸 말이고 type class programming을 말하는 거죠—은 어떤 방식으로 computation을 수행하느냐의 개념이 아닙니다. 우리가 program을 짤 때 program이 커지면 수백만 줄 단위로 커지거든요. 그걸 묶어 가지고 abstraction을 할 수 있어야 돼요. Module이라고 말할 수 있죠.

18:18 화자 1
우리가 함수로도 abstraction을 하거든요. 함수는 어떤 computation을 abstraction시킬 수 있어요. 함수를 쓰는 사람이 함수 코드를 다 읽고 쓰는 게 아니라, “이 함수는 무슨 일을 한다”라고 abstraction해서 얘기할 수 있잖아요. 그런 식으로 함수들을 다시 묶고 데이터를 묶어 가지고 하나의 abstraction 단위를 만들어 줍니다. 그래서 OOP나 type class는 programming을 할 때 abstraction을 어떤 식으로 하는지에 대한 방법입니다. 두 가지 mainstream으로 OOP라는 방법으로 abstraction을 하는 방법이 있고 type class라는 방법으로 abstraction을 하는 방법이 있어요. 그렇지만 code reuse도 있잖아요. Abstraction은, 내가 코드를 이해할 때 100만 줄을 한 번에 한 줄 한 줄 이해할 수 있는 사람은 없잖아요. 불가능하단 말이에요. AI는 할 수 있을지 모르겠지만 인간의 두뇌는 너무 작아 가지고, 100만 줄을 한 줄 한 줄 다 이해하면서 전체를 볼 수 있는 사람은 없어요.

19:11 화자 1
그렇기 때문에 큰 단위 단위로 abstraction을 해서, 이 코드를 그대로 보는 게 아니라 “이건 무슨 일을 하는 거지?” 이런 식으로 abstraction하고, 또 그걸 묶어 가지고 abstraction해서 “이건 크게 봐서 이걸 하는 거지” 하는 식으로 추상화해야만 그 system을 이해할 수 있거든요. 그래서 큰 system이 있으면 이만큼은 뭘 하고, 이만큼은 뭘 하고, 얘들이 이런 식으로 interact해서 전체적으로 뭘 한다, 이런 식으로 high level로 이해할 수 있어야만 전체 system을 이해할 수 있죠. 그래서 abstraction이 첫 번째 중요한 거고요. 두 번째는 같은 코드를 여러 번 쓰는 게 아니라, 같은 일을 하는 걸 한 번만 쓰고 그걸 reuse해서 계속 비슷한 일을 할 때 generalized code가 있어서 그걸 계속 활용해 비슷한 일을 할 수 있게 하는 code reuse에 관한 거예요.

20:05 화자 1
그래서 어떻게 보면 programming에서 진짜 중요한 게 이 두 개와 연관이 됩니다. 연관이 되는데, 어쨌든 code reuse라고 할게요. 보통 사람들이 쓰는 말로 “Don't repeat yourself”라고 들어 본 사람들 있죠? 그러니까 짰던 코드를 copy and paste해서 또 쓰지 말라는 겁니다. Copy and paste를 피한다는 것은 최대한 generalized된 코드를 짜면 그걸 가지고 specialize해서 이렇게도 쓰고 저렇게도 쓸 수 있거든요. 그런데 generalize하지 않으면 이 일을 하는 코드를 copy and paste해서 조금 바꾸고, 다시 copy and paste해서 조금 바꾸는 식이 되잖아요. 이걸 generalize하면 하나만 짜 가지고 parameter만 바꾸면서 이 일도 하고 저 일도 할 수 있거든요.

21:00 화자 1
그래서 코드를 어떻게 하면 generalize할까, 그리고 어떻게 abstraction을 잘할까에 관한 방법론이 OOP이고 type class예요. 이 두 개를 배울 건데, 일단 먼저 딱 이야기하면 OOP는 틀렸고 type class는 맞습니다. OOP가 type class보다 나은 점은 하나도 없어요. 100 대 0으로 무조건 type class가 모든 면에서 나아요. OOP는 이미 있는 거죠. 사실 태어나지 않았으면 인류가 더 좋았을 텐데, 불행하게 잘못된 방법론이 생겼습니다.

21:56 화자 1
그래서 역사를 보면 불행한 일들의 역사죠. 왜냐하면 OOP가 90년대에 유행할 때 당시에 학계에 있는 분들이 제대로 된 답을 못 줬어요. 사람들이 OOP를 쓰기 시작했고, 그러다가 드디어 2000년대 들어서 사람들이 답을 찾았어요. Type class라는 엄청 아름답고 멋진 방법론을 찾은 거예요. 그런데 이미 너무 늦었죠.

22:53 화자 1
왜냐하면 OOP가 대세가 되고 세상을 지배하고 있어서 더 이상 어떻게 할 수가 없어요. 실제로 Rust라는 언어 아시죠? 이제 좀 들어 봤죠? Rust는 가장 최근에 나온 언어로서 좋은 것만 모아 놓은 거예요. 세상에 좋다는 것만 다 모아 놓은 게 Rust예요. Rust에는 OOP가 없습니다. Type class만 있습니다. Rust를 design한 사람들은 이제 뭐가 맞는지 아는 사람들이니까 type class만 넣은 거죠. 일단 module system을 지원한다고 했는데, 사실상 목적은 이거예요.

23:45 화자 1
Abstraction과 code reuse, 코드를 추상화하고 그다음에 재활용성을 높이는 게 목적입니다. OOP는 어차피 Part 2, 3에서 자세하게 이야기할 겁니다. 그런데 대충 아는 사람, 지금 2학년인가 3학년인가, 그래도 C++나 Java를 써 봤으면 대충 경험이 있을 거잖아요. 여기서는 data와 method를 합쳐 가지고 object라는 abstraction 단위를 만드는 겁니다. 그리고 code reuse는 inheritance라는 최악의 아이디어를 써서, 그렇죠? Inheritance라는 인류 최악의 발명을 써서 code reuse를 합니다. 그렇지만 많은 사람들이 아마 OOP가 좋은 거라고 알고 있어요. 왜냐하면 OOP밖에 없는 사람에게는 없는 것보다 낫거든요. 아예 아무것도 없는 것보다 OOP라도 있는 게 abstraction도 할 수 있고 reuse도 할 수 있으니까, 없는 것보다는 나아요.

24:46 화자 1
그러니까 OOP가 좋은 거다, OOP로 짜야 된다, 이런 말을 하고 와요. 모든 면에서 type class보다 나은 게 없는데 “OOP밖에 없어서 어쩔 수 없이 쓴다” 그렇게는 말할 수 있고, “없는 것보다는 낫다. 어쩔 수 없이 쓴다” 그렇게 말할 수 있어요. 그래서 이제 정답을 배울 겁니다.

25:16 화자 1
어떻게 보면 같은 목적을 향하는데 조금만 다르게 생각하면 훨씬 더 아름다운 방법이 있는데, 우리가 1900년대에는 지능이 낮아 가지고, 그렇죠? 그래서 우리가 왜 Part 1, 2, 3에서 Scala를 쓰냐? Scala는 imperative와 functional을 다 잘 지원하고 OOP랑 type class를 둘 다 잘 지원해요. 그리고 Java와 compatible해서 Scala code에서 Java library를 다 쓸 수 있거든요. 그런데 Rust는 왜 쓰냐? 마지막에 imperative programming을 배우는데, imperative programming은 아까 처음에 말한 C 같은 거잖아요. 여러분이 잘 아는 거예요.

26:15 화자 1
그래서 그 부분은 아예 강의를 안 합니다. 어차피 잘 알고, 어려운 것도 아니고 C나 C++, Java에서 다 아는 거라서 그건 이야기하지 않아요. Rust가 imperative programming language거든요. 완전히 C 같은 건데 거기다가 ownership type이라는 개념을 추가했어요. 그 ownership type이라는 게 되게 fancy하고 좋은 개념이거든요. 그래서 imperative programming을 하지만 사실은 ownership이라는 개념을 배우는 겁니다. Rust의 ownership type입니다. 이게 뭐냐면, imperative programming을 할 때 아까 safe하지 않을 수 있다고 했잖아요. Efficient하게 짜기는 쉽지만 low level로 짜다 보니까 실수하기가 너무 쉬워요. 그래서 memory를 free한 걸 또 free한다든가, 아니면 free한 걸 사용한다든가, memory bound를 넘어서 access한다든가 이런 실수들을 할 수 있겠네요.

27:08 화자 1
그런데 Rust에서는 그런 일을 type checker가 다 detect해서, 효율적으로 짜면서도 안전한 게 보장돼요. Rust type checker를 통과하면 그런 error가 없다는 게 보장되거든요. 그러니까 efficient code를 짜면서도 safe code를 짤 수 있는 개념을 도입한 겁니다. Rust가 ownership type이라는 걸 통해서 그런 걸 실현하거든요. 결국 Rust의 ownership type이 무엇인지를 배우는 겁니다. Imperative language를 안전하게 만들었고요. Ownership type은 Rust만 지원해요. Rust만 ownership type을 지원해요. 어떻게 보면 엄청나게 advanced된 idea거든요.

28:03 화자 1
그래서 ownership type은 Rust에만 있기 때문에 당연히 Rust를 써야 되고요. 그런데 그것 말고도 Rust는 거의 모든 기능에서 좋은 것만 가지고 있어요. 예를 들면 아까 말씀드렸듯이 OOP는 아예 없으니까 지원하지 않아요. Exception도 지원하지 않아요. 그런 안 좋은 idea를 지원하지 않습니다. 그러면 exception이 없는데 exception에 해당하는 error 처리를 어떻게 하느냐? 그것도 예쁘게 할 수 있어요. 아까 monadic programming과 비슷한 건데 error monad라는 idea를 써서 exception에 해당하는 걸 할 수 있습니다. 어쨌든 좋은 function은 다 들어 있습니다. Rust에 없다, 그러면 안 좋은 거예요. 그렇게 보면 됩니다.

28:59 화자 1
이게 수업하기 전에 전체적인 맛보기였고요. 지금 질문이 있을까요? 이제 functional programming부터 이론을 배워 봅시다. 아까 functional programming code는 봤잖아요. 실행해 봤고요. 그런데 그게 어떤 원리로 돌아가는지는 아직 잘 모르잖아요. 그런 원리를 배울 텐데, 우선 functional programming에서 type하고 value라는 개념이 있습니다. Type은 일반적인 imperative language에도 다 있고 C에도 type이 있잖아요. 그런데 functional language라고 해서 꼭 type이 있는 건 아니에요.

29:54 화자 1
Functional language 중에도 typed language가 있는데, 여기서는 어쨌든 type을 기본적으로 가정하고 functional language를 설명하겠습니다. Type은 어떤 value들의 집합이에요. Integer type이라면 type은 set을 나타내는 거거든요. 예를 들어 Scala에서 `Int`는 32-bit integer를 표현하니까, 거기에 들어가는 value는 -2147483648부터 2147483647까지입니다. 그런 integer value들의 집합이에요. `Double`은 64-bit floating-point value들의 집합이고, 이런 식으로 type은 value들의 집합이라고 합니다.

31:07 화자 1
일단 계산의 결과인 것, 예를 들어 3은 더 이상 계산할 방법이 없잖아요. 그렇죠? 그런데 3하고 `3 + 4`는 같지 않잖아요. `3 + 4`는 value가 아니고 expression이에요. 왜냐하면 계산할 게 남았잖아요. 계산하면 value 7이 나올 거예요. 이건 7이라는 value가 되는 거예요. `3 + 4`는 계산식을 표현한 expression이고, 이 계산식을 실행해서 결국 value가 되는 거예요. Value라는 건 더 이상 계산할 게 없는 최종 결과입니다. Expression은 우리가 computation을 기술하는 표현식이에요.

32:05 화자 1
그것은 computation하고 나면 결국 value가 될 거예요. 알겠죠? 그래서 우리가 program을 쓴다는 것은 expression을 쓰는 거예요. 실행한다는 것은 expression을 계산해서 결과 value로 만드는 거예요. Expression은 계산해서 value가 될 수 있는 표현식을 말합니다. 그걸 표현하기 위해서는 여러 component를 조합하는데, value 자체도 expression이라고 할 수 있어요.

33:05 화자 1
왜냐하면 3이라고 해도 이건 계산이 끝난 expression이거든요. 그래서 value, 그다음에 아직 안 봤지만 name, primitive operation 같은 걸 섞어 가지고 계산식을 만들 수 있는데요. 그건 좀 이따가 보겠습니다. 계산식은 내가 짠 code인데, 그 자체가 value일 수도 있고 아직 value가 아닐 수도 있어요. 그걸 실행해서 계산이 끝나면 value가 되는 겁니다. 그다음에 name이라는 개념이 나오는데, name은 특정 expression에 이름을 붙이는 거예요. 예를 들어 어떤 `3 + 7` 같은 식이 있는데 내가 이 식을 열 번 써야 돼요. 계속 그 식을 쓰다 보면 손가락이 아프잖아요. 그러면 이름을 붙일 수 있어요.

34:00 화자 1
`n`이라고 이름을 붙여 놓고 `n + n + n + n + n + n + n + n + n + n`이라고 쓰는 거죠. 그냥 expression에 이름을 붙이는 거예요, 그게 너무 길면. 그래서 기본적으로 name이라는 것은 어떤 복잡한 식에 짧게 이름을 붙이는 겁니다. Name binding이라는 것은 “이 이름은 이 expression을 가리킨다”라고 선언하는 겁니다.

34:52 화자 1
그래서 Scala로 이렇게 쓰면 `a`는 `1 + (2 + 3)`을 가리키는 이름, `b`는 `3 + a * 4`를 가리키는 이름입니다. 이름이 없으면 저 `a` 자리에 `1 + (2 + 3)`이라고 써야 되잖아요. 그런데 이름이 있으면 그냥 그 이름을 쓰면 같은 식을 표현하게 됩니다.

35:16 화자 2
그렇죠.

35:17 화자 1
식을 효율적으로 쓰기 위해서 이름을 붙이는 거예요. 현실에서도 똑같잖아요. 여러분에게 이름이 있는 이유가 뭐예요? 이름이 없으면 어떻게 되겠어요? 누군가를 identify해야 되잖아요. 이름이 없으면 “안경을 쓰고 무슨 옷을 입고 무슨 신발을 신고 뭘 하는 누구야”라고 불러야 될 거 아니에요. 너무 힘들고 비효율적이죠. 이름이 있으면 이름만 부르면 딱 누군지 결정되잖아요. 여기서도 `1 + (2 + 3)`이라고 계속 부르는 건 너무 힘들잖아요. 이름을 붙여 놓으면 “`a`”라고 하면 되고, `a`는 `1 + (2 + 3)`입니다.

36:02 화자 2
네

36:03 화자 1
그래서 일단은 그런 의도예요. 실제로 여기서 한번 제가 해 볼게요. 여기서 내가 `a`라고 하면 `a`를 계산하라는 뜻이 되는 거예요. `b`로 합시다. `b`를 계산하면 뭐가 나와요? 계산되나요? 27이죠. 그렇죠? 이건 `b`라는 expression을 실행해서 value로 만들라는 뜻이에요. 실행하면 저 식이 다 풀어져 가지고 `3 + (1 + (2 + 3)) * 4`처럼 계산돼서 최종적으로 27이 되는 거예요.

37:02 화자 1
지금까지 배운 것은 식을 만들 때 어떤 value를 직접 식에 쓸 수도 있고, 어떤 식을 name에 binding한 후에 그 name을 쓸 수도 있다는 겁니다. 이 식을 binding한 후에 그 식을 쓰는데 name도 포함했단 말이죠. 그다음에 primitive operation을 쓸 수가 있습니다. 지금까지 배운 걸로 expression을 쓰는 방법은 이거예요. Value를 쓰거나, 더하기나 곱하기 같은 primitive operation을 쓰거나, name을 어떤 expression에 binding하고 그 name을 쓰는 식으로 expression을 만들 수 있어요. 앞으로 expression의 종류가 점점 늘어날 겁니다. 저게 Scala expression의 전체 space는 아니고 더 많은 기능이 있으니까 배울 건데, 일단 지금까지는 가장 기본적인 것만 배웠습니다. Expression을 만드는 방법을 이야기했고요. 그다음에는 이 expression을 실행하는 방법입니다. 아까 식을 실행했잖아요.

38:01 화자 1
실행이 어떻게 되는지, 실행이 일어나는 과정을 원리로 얘기해 보겠습니다. Evaluation rule이라고 할 수 있는데, 어떤 식을 계속 계산해서 value로 만드는 게 evaluation입니다. 사실 이게 영어긴 한데요. 잘 보면 value라는 단어가 있잖아요. Value로 만드는 거야, 이렇게 이해할 수 있겠죠. 그래서 evaluation은 결국 expression을 value로 만드는 겁니다. 그 과정의 rule은 지금까지 구조에서는 간단해요. Expression을 value로 만드는 strategy가 있고요. 먼저 expression은 tree, AST니까 tree 형태로 나오잖아요. 아까 `5 + b`를 계산한다고 해 보죠. `5 + b`라는 식을 쓰고 이걸 계산하면 뭐가 나오죠?

39:02 화자 1
32가 나오죠. 어떻게 32가 되는지 볼게요. 먼저 이 AST라는 것은 이렇게 생겼다고 보면 되잖아요. 위에 더하기가 있고 양쪽에 operand가 있을 거예요. 그러니까 top에 있는 것이 value일 수도 있는데, value면 그냥 끝나는 거예요. 더 계산할 게 없는 겁니다. 아니면 어떤 expression의 top에는 name이 있거나 primitive operation이 있어요. 먼저 top의 case를 봤을 때 top이 value면 더 이상 할 게 없는 거고, 그렇지 않으면 name이거나 operator인 경우를 봅니다.

40:02 화자 1
Name인 경우, 내가 계산해야 되는 것이 name이면 그 name을 그 정의로 바꿔치기해요. 그러고 나서 실행하는 거예요. 아까 `b`라면 `b`의 정의인 `3 + a * 4`로 바꾸는 거죠. Operator인 경우에는 왼쪽 operand를 먼저 계산해서 value로 만듭니다. 왼쪽 tree를 value가 될 때까지 계속 진행하고, value가 되면 그다음 오른쪽 tree에도 이 rule을 계속 적용해 value로 만들어요. 양쪽이 다 value가 되면 그다음에 operator의 의미에 따라서 두 value를 계산해 새로운 value로 만드는 거예요. 이게 전부예요. 어떻게 보면 이 rule이 recursively 적용되는 겁니다.

41:01 화자 1
그러면 한번 구체적으로 해 봅시다. `5 + b`면 위에 있는 게 operator잖아요. 그럼 rule에서 operator case로 가야 되죠. 왼쪽을 먼저 계산하고 그다음에 오른쪽을 계산합니다. 왼쪽을 보면 5죠. 5의 root에 있는 게 value잖아요. 그렇죠? 그럼 계산이 끝나요. 왼쪽 expression을 계산하려고 했더니 이미 value예요. 계산할 게 없으니까 돌아오고, 그다음 오른쪽을 계산해야 합니다. 이제 `b`를 계산해야 돼요. `b`의 root에 있는 것은 name이잖아요. 그러면 name을 정의로 바꾸라는 rule로 가죠. 이름을 정의로 바꾸면 `3 + a * 4`입니다.

42:02 화자 1
`3 + a * 4`죠. `b`가 이걸로 바뀌었어요. 그럼 rule을 적용해서 다시 계산해 보자고요. 위에가 더하기죠. 왼쪽으로 가서 실행해야 되는데 왼쪽은 이미 value 3입니다. 그럼 오른쪽의 `a * 4`를 실행해야 되죠. 그 안에서 왼쪽을 보면 top에 있는 것이 name `a`잖아요. Name이면 rule에 의해서 다시 expression으로 바꿔야 돼요. `a`가 뭐였죠? `1 + (2 + 3)`이죠. 이걸 실행해서 value로 만들어야 돼요. Rule을 또 apply해 보면 top에 있는 게 더하기죠.

43:00 화자 1
왼쪽으로 가서 value가 될 때까지 실행해야 되는데 이미 value 1이에요. 돌아와서 오른쪽의 `2 + 3`을 value가 될 때까지 실행해야 돼요. Rule을 apply해 보면 root에 operator가 있죠. 왼쪽으로 가면 이미 value 2이고, 오른쪽으로 가면 이미 value 3입니다. 그러면 돌아와서 더하기라는 operation을 이 두 value에 적용하는 거예요. 더하기를 2와 3에 적용하면 어떻게 돼요? 부끄러워서 나중에는 대답을 안 한단 말이에요. 계속 하다 보면 그런 거 있잖아요. 가수들 공연장에 갔는데 노래하다가 관중들한테 일어나서 따라 부르라고 하고 마이크 주고 난리 치잖아요. 약간 그런 거예요.

43:57 화자 1
그래서 5죠. 그럼 다시 오른쪽이 value가 됐잖아요. 이제 `1 + 5`에서 왼쪽도 value이고 오른쪽도 value니까 이 operator를 두 value에 적용해요. 1에 5를 더하면 뭐가 되죠? 6이 됐죠. 그러면 `a * 4` 입장에서는 왼쪽이 이제 value 6이 됐고, 오른쪽 4도 이미 value예요. 두 value에 곱하기를 적용하면 뭐죠? 24. 그러면 이것도 value가 됐죠. 다시 돌아와서 `3 + 24`를 하는 거예요. 그러면 27이 되죠. 이게 아까 `b`를 계산하는 과정이잖아요. 제가 옆으로 옮겨 놓고 한 건데 사실 여기서 시작한 거였거든요. 그게 27이라는 거죠. 그렇죠?

44:53 화자 1
아까 `5 + b`에서 이걸 하고 있는 거죠. 이제 오른쪽 `b`도 value 27이 됐죠. 양쪽이 value니까 더하기를 하면 뭐가 되죠? 32가 되고, 그래서 전체가 32가 돼요. 그러면 이 전체 evaluation이 끝나고 value 32가 나오는 거예요. 더 이상 evaluation할 게 없죠. 여기 보면 32가 나왔죠. 이 과정을 거쳐서 계산이 일어난 거예요. 알겠죠? Function 없이 기본적인 primitive operator만 가지고 있는 expression은 이렇게 계산됩니다. 아직 function이 안 나왔어요. 중요한 것은 function이 메인이긴 한데, function의 body도 결국 expression이 되어야 하잖아요. 이런 expression을 실행하기 위해서 이 rule을 만들었습니다.

45:52 화자 1
기본적인 primitive operation은 이런 식으로 계산합니다. 반복적으로 expression이 있으면 value로 만들고 또 위로 올라가서 expression을 value로 만드는 과정입니다. Evaluation이라는 말도 expression을 value로 만드는 과정이라고 이해하면 됩니다.

46:26 화자 1
어원이 정말 그런지는 모르겠는데, 어쨌든 evaluation은 expression을 value로 만드는 과정입니다. 그다음에는 드디어 function이 나올 거예요. 지금까지는 primitive operation이 어떻게 계산되는지를 봤고, 이제 function이 어떻게 계산되는지를 볼 겁니다. 그러기 전에 if expression은 아주 간단해요. `if condition then branch else branch`가 있으면 condition을 evaluate해서 true면 then branch를 evaluate하고, false면 else branch를 evaluate합니다. 그러고 나면 아까 summation이 어떻게 계산되는지도 알 수 있을 거예요.

47:50 화자 1
그다음에 function을 이해하고 나면 `sum`을 드디어 실행할 수 있는 거니까, `sum`이 어떻게 실행되는지도 알 수 있습니다. 그래서 function을 봅시다. Function에도 이름을 줄 수가 있어요. 아까 name binding을 확장한 겁니다. 잘 보면 `f`의 `x`가 parameter잖아요. 그다음에 여기 expression을 쓰는데, 이 expression에서 parameter의 이름을 쓸 수가 있어요. Function은 function 이름을 먼저 쓰고요.

48:47 화자 1
그다음에 function이 argument로 받는 value의 이름을 선언하고 그 type을 씁니다. 이 function `f`는 `Int` type의 value를 받을 건데, 지금은 그 value를 모르잖아요. 나중에 실제로 function을 apply할 때 value가 주어질 겁니다. 3일지 4일지 모르고 나중에 주어질 건데, 지금 정의할 때는 무슨 value인지 모르죠. User가 나한테 어떤 value를 줄지 모르잖아요. 그러니까 `x`라는 name을 써요. 뭔지는 모르겠지만 `x`라고 해 놓고, 여기에는 return type의 expression을 씁니다. Return type의 expression이라는 것은 이 expression을 실행했을 때 return type의 value가 나오는 expression이에요.

49:28 화자 1
`x`가 `Int` type의 value이고 `a`는 우리가 앞에 정의한 name입니다. `f`라는 이름을 function에 binding하는데, `x`라는 value를 받으면 `x + a`를 계산하는 function에 `f`라는 이름을 binding하겠다는 뜻이에요. `f`라는 이름에, `x`를 받아서 `x + a`를 계산하는 function expression을 binding하는 겁니다. Function expression은 parameter가 있고 expression이 있는 거예요. `x`라는 parameter에 대해서 `x + a`를 계산하는 식이기 때문에 약간 open된 식이에요. 지금은 계산이 불가능해요.

50:27 화자 1
왜냐하면 `x`가 안 주어졌기 때문에 지금은 계산할 수 없지만, 나중에 `x`가 주어지면 계산할 수 있는 식이에요. 정보가 더 주어지면, argument value가 주어지면 계산할 수 있는 그 식에 `f`라는 이름을 binding합니다. 아까 제가 name이라는 게 단순히 이미 존재하는 걸 가리킨다고 이야기했어요. `1 + (2 + 3)`이 있는데 그걸 계속 쓰기 귀찮으니까 `a`라고 이름 붙이자, 이렇게 이미 존재하는 걸 짧게 표현하기 위해서 name을 붙인다고만 이야기했죠. 사실 name은 더 본질적인 역할을 해요. 여기 function에서 배우는 겁니다. Function에서 name은 지금 주어지지 않고 미래에 주어질 value에 대한 expression을 쓸 수 있게 하는 이름이에요. 여기서 name은 이미 존재하는 걸 가리켜서 짧게 만드는 개념만은 아니고요. 무슨 말인지 알겠죠?

51:23 화자 1
그래서 여기서 name의 역할은 아주 본질적입니다. Name이 없으면 functional programming이라는 개념을 만들 수가 없는 거예요. 저 name은 function이라는 computation을 만들어 내기 위해서 필요한 본질적인 이름입니다. 어쨌든 function expression에 이름을 붙이는 것은 이렇게 `f`라고 function의 이름을 붙이는 거고, 실제 function이 의미하는 것은 `x`라는 value를 받아서 `x + a`를 계산하는 겁니다.

52:20 화자 1
그럼 아까 실제 `f`를 써 볼게요. 이 `a`는 앞의 `a`, `1 + (2 + 3)`을 말하는 겁니다. 우리가 `f`만 계산한다고 하면 의미가 있나요? `f`를 계산하려면 `f`만 가지고는 안 되겠죠. Function을 계산시키려면 argument가 있어야 하죠. 그러면 argument로 10을 줍시다. `f(10)`이면 이제 이걸 계산한다는 게 말이 되겠죠. `f(10)`을 계산하면 10에 `a`를 더하니까 16이 되겠네요. 한번 계산해 보자고요. 16이 됐죠. 이제 계산한다는 것은 그 function을 argument에 apply했을 때의 계산을 말하는 거예요. Function을 argument에 apply하면 어떻게 계산되는지, 그 의미를 보겠습니다.

53:20 화자 1
그래서 아까 evaluation rule을 또 확장한 거예요. Tree를 봤더니 function application이 top에 있을 수도 있잖아요. 그림으로 그리면 여기에 `app`이라고 표현돼요. `app`은 function application을 의미해요. `f`를 10에 apply하는 것이 `f(10)`이에요. 우리가 괄호 syntax로 쓰지만 내부적으로는 abstract syntax tree로 표현되거든요. 여기 application operator가 있어요. Application operator는 function과 argument, 두 개를 받아서 이 function을 argument에 apply합니다. 아까 rule을 확장해서, 내가 계산하려는 expression의 top operator를 보니까 function application인 경우를 추가하는 거예요.

54:12 화자 1
“Replace the function application by the expression of the function.” Function application이 있고 argument expression이 있잖아요. Argument를 계산해서 value로 만든 후에, function application을 `f`의 body expression으로 바꿔요. 여기 같은 경우 body expression은 `x + a`잖아요. 그다음에는 body에서 parameter, 그러니까 `x`를 실제 argument value로 바꾸는 거예요. 여기 `f`에서 `x`가 parameter인데 이제 argument value를 아니까 그 value로 바꿉니다. 그리고 그 expression을 실행합니다.

55:06 화자 1
그러니까 function application은 function의 body expression으로 바뀌는데, 아까 몰랐던 parameter `x`는 이제 argument value를 아니까 그 value로 바뀌고, 그 expression을 실행하는 거예요. 그럼 `10 + a`를 실행할 수 있겠죠. `a`를 다시 `1 + (2 + 3)`으로 바꾸고 계산하면 6이 되고, `10 + 6`은 16이 되겠죠. 일반적으로 function을 argument에 apply하면 argument를 실행해서 value로 만들고, function 이름을 look up해서 그 function의 body expression으로 바꿔치기합니다. Body expression에서 parameter에 해당하는 name을 실제로 계산된 argument value로 바꿔치기해요. 그러고 나서 다시 그 expression을 계산하는 거죠. 이해되나요? 자연스러운 과정이죠. 그래서 아까 `5 + f(f(3) + 1)` 같은 것도 이 rule을 계속 적용하면 계산할 수 있습니다.

56:03 화자 1
방금 `f(10)`에 대해서 한 것처럼 하면 됩니다. 그럼 `sum`은 어떻게 실행됩니까? 여기서는 evaluation을 간단히 하기 위해 10도 100도 아니고 2만 합시다. `sum(2)`를 하면 3이 나오죠. `sum(2)`가 어떻게 계산되는지 한번 봅시다. `sum(2)`도 사실 function application이고, 여기 `sum(2)`라고 쓸게요.

56:59 화자 1
우선 argument expression을 value가 될 때까지 계산할 거예요. 그런데 이미 2라는 value죠. 그러면 function application을 `sum`의 body로 바꿉니다. 그리고 그 body에 있는 parameter name을 argument value 2로 바꾸는 거예요. 아까 `sum`의 정의가 뭐였죠? `if n <= 0 then 0 else n + sum(n-1)`이죠. Function application을 이 body로 바꿨어요. 그다음에 parameter `n`을 2로 바꿔야 돼요. `n`이 function expression에서 parameter name이고 이제 argument가 2인 것을 아니까, `n`을 전부 2로 바꿔야 되죠.

57:49 화자 1
여기 `n`을 2로 바꾸고, 또 여기 2로 바꾸고, 여기 2로 바꿉니다. 그다음 다시 실행하자고요. 아까 if expression에서는 condition을 value가 될 때까지 evaluate한다고 했죠. 여기 condition의 operator는 less than or equal이고 왼쪽과 오른쪽이 둘 다 value잖아요. 2가 0보다 작거나 같은지를 test하는 operator죠. 실행하면 뭐가 돼야 돼요? False죠. 그러면 condition이 false로 바뀌는 거예요. 사실 한 step씩 써야 되는데 제가 몇 번씩 copy and paste하기 힘들잖아요. 이렇게 할게요. 이제 if가 false면 아까 이 expression이 어느 쪽으로 간다고 그랬죠? Then branch는 무시하고 else branch를 실행합니다. 그러면 이게 `2 + sum(2-1)`로 바뀝니다.

58:49 화자 1
이제 이걸 실행하려면 더하기를 계산해야 되잖아요. 왼쪽은 value이고 오른쪽은 value가 아니니까 오른쪽을 value로 바꿔야 되겠죠. `sum(2-1)`은 function application이니까 argument expression을 value가 될 때까지 실행해야 합니다. `2 - 1`을 실행하면 1이 나오죠. 이제 argument가 value 1이 됐어요. 그럼 `sum(1)`을 evaluate해야 되죠. 아까와 똑같이 function application을 `sum`의 body, 즉 `if n <= 0 then 0 else n + sum(n-1)`로 바꿉니다. 똑같은 구조가 또 나오죠. 그렇지만 차이는 argument value가 다르다는 겁니다. 이번에는 `n`을 뭘로 바꾸죠? Argument가 1이죠.

59:47 화자 1
그러니까 body의 `n`을 1로 바꿔요. 같은 code가 반복해서 실행되는데 information이 바뀌었잖아요. Argument value가 다른데, 그걸 통해서 다른 계산이 계속 일어나는 거예요. 아까 while loop에서는 같은 code가 계속 실행되지만 memory state가 바뀌어 가지고 다른 information에 대해 계산하고 다른 계산을 하게 됐죠. 여기서는 argument value가 바뀝니다. 그러면 1이 0보다 작거나 같으냐 하면 또 false죠. 따라서 else branch로 가서 `1 + sum(1-1)`이 됩니다. 전체는 `2 + (1 + sum(1-1))`이 되겠죠. 또 이 expression을 계산해야 하잖아요. 더하기의 왼쪽은 이미 value이고 오른쪽은 value가 아니니까 실행해야 합니다. 오른쪽의 function application에서 argument를 보니 아직 value가 아니어서 또 실행해야 되죠.

01:00:48 화자 1
`1 - 1`을 evaluate하면 0이 되고, `sum(0)`을 evaluate하면 아까와 같이 `sum`의 body가 또 나오겠죠. `if n <= 0 then 0 else n + sum(n-1)`으로 똑같이 나옵니다. 그런데 이번에는 차이가 있겠죠. `n` 자리에 0을 쓰면 여기 0, 여기 0, 여기 0으로 바뀝니다. 이제 condition을 실행하면 0이 0보다 작거나 같은가요? True죠. 그럼 condition이 true로 바뀌고 then branch가 실행됩니다. 그러면 `sum(0)` 전체가 0으로 바뀌죠. 전체 expression은 `2 + (1 + 0)`이 되는 거예요. 그다음 이걸 계속 계산해야 되죠.

01:01:42 화자 1
`1 + 0`은 1이고 다시 `2 + 1`은 3이고, 그래서 이 과정을 통해 3이 됩니다. 이해됐나요? 여기 `sum(2)`를 계산해서 3이 나왔어요. 그런데 `sum(100)`은 이걸 100번 하는 거죠. 100번 해서 5050이 나오는 거예요. 제가 100을 쓰지 않은 이유를 알겠죠? 그러면 칠판이 부족해 가지고 아마 수업 끝날 때까지도 못 끝낼 수 있거든요. 이해됐나요? 이게 functional programming으로 실행되는 원리예요. 이렇게 이해할 수가 있어요. Memory라는 개념 없이도 계산을 표현할 수 있잖아요. 일단 그 과정을 제가 쓴 거고, 이렇게 substitution을 통해서 이해할 수 있습니다.

01:02:42 화자 1
그래서 value의 전달을 통해서 계산이 계속 일어납니다. Value를 전달해서 memory에 쓸 내용 없이 계산하는 거예요. 아까 Haskell이라는 언어는 이런 programming만 지원해요. Memory 개념이 아예 없이 순수하게 function만 가지고 programming하게 하는데, Haskell로 어느 정도까지 program을 짤 수 있느냐 하면 [[unclear: OS·browser·window manager 사례의 정확한 범위는 발화에서도 확신하지 않음]]. 어쨌든 웬만한 건 다 짤 수 있고 효율도 나쁘지 않게 짤 수 있어요. 이런 식으로만 computation을 기술해도 우리가 표현할 수 있는 모든 computation을 다 표현할 수 있어요.

01:03:34 화자 1
그런데 여러분은 이런 데 익숙하지 않고, 주로 memory가 있고 memory에 value를 쓰고 그걸 누군가 공유하고 거기에 value를 덮어쓰고 읽는 것에 익숙하잖아요. 그런데 이런 식으로만 programming해도 계산 가능한 모든 걸 계산할 수 있습니다. 지금 한 10분 넘었네. 지금까지는 계산해서 value가 됐잖아요. 그런데 계산해도 value가 안 되는 게 있어요. 계속 계산하는데 안 끝나는 거예요. 무한으로 빠지는 겁니다. 계속 무한히 계산하는 것도 가능합니다. 실제 while loop를 써도 그렇죠. 예를 들어 while 조건을 계속 true로 하면 무한히 돌잖아요. C code에서도 안 끝나는 program을 짤 수 있죠. 마찬가지로 여기에서도 안 끝나는 code를 짤 수 있어요.

01:04:33 화자 1
예를 들면, 밑의 `def loop: Int = loop`는 잠시 두고 다른 예를 생각해 봅시다. 무한히 안 끝나는 걸 한번 생각해 볼까요? Function `g`가 자연수를 받아 자연수를 내는데 안 끝나게 하려면 어떻게 짤 수 있을까요? 여기 뭐 생각나는 것 없어요? `g(x)`가 `g(x)`를 부르게 하는 거예요. 아까 `sum`을 정의할 때 `sum` 자기 자신을 썼잖아요. `g(x)`가 다시 `g(x)`를 불러도 type은 맞거든요. `g`가 `Int`를 받아서 `Int`를 내는 function이니까 `g`에 `x`를 주면 다시 `Int`를 내는 거잖아요.

01:05:32 화자 1
이렇게 짜고 `g(0)`을 하면 아마 안 끝날 거예요. 뭐야? 얘들아 미안한데, 이 server를 내가 지금 실제로 점유하고 있는 거잖아요. 어? 끝났다. Timeout으로 알아서 끝난답니다. 여기 warning도 있고 실행 환경 자체에 timeout이 있는 것 같아요. 아니, 그런데 끝내는 명령은 있어야 되는데 수업 시간에 매번 이걸 기다릴 수가 없잖아요. 이렇게 안 끝나는 걸 짤 수 있어요.

01:06:27 화자 1
이게 어떤 거냐면, value가 되는 것은 끝나는 expression이고 무한히 도는 expression도 가능해요. 이건 뭐냐면 name인데…

01:06:39 화자 3
Name도 아까 function에는 argument가 있었잖아요. 그런데 argument가 없는 것도 function…

01:06:44 화자 1
똑같이 동작할 수 있어요. 얘는 argument가 없는데, `loop`라는 게 어떤 integer expression을 가리키는 name이라고 했잖아요. 그런데 걔가 또 recursively 자기 자신을 쓸 수 있어요. `loop`라는 name은 `Int` type이라고 하면서 body에 `loop`라고 쓰면 type은 맞잖아요. `loop`도 `Int` type인데 여기에 integer expression이 나오는 거니까 이것도 허용됩니다. 그래서 이건 어떻게 보면…

01:07:05 화자 3
인자가 없는

01:07:05 화자 1
함수라고 이해해도 돼요. Argument가 없는 함수. 그냥 `loop`를 부르면 뭔가 실행되는 계산식이에요. 그런데 이걸 실행하면 어떻게 되냐면, 아까 rule에 따르면 여기 name이죠. Name이 있으면 그 name의 body로 바꿔치기해요. Name의 body가 `loop`죠. 그럼 또 name이 있죠. 이걸 또 body로 바꿔치기해요. 그것도 `loop`죠. 계속 무한대로 반복됩니다. 실제로 여기 써 보면 `def loop: Int = loop`인데 정의는 잘 되죠. 그런데 `loop`를 실행하면 무한히 빠집니다. `loop`가 되고, 다시 `loop`, `loop`, `loop`가 됩니다. `loop`를 unfolding해서 expression을 쓰면 또 `loop`가 계속 나옵니다. 어쨌든 이렇게 무한히 빠지는 식이 가능합니다.

01:08:01 화자 1
그다음 slide 17은 다음에 하고, 지금은 여기까지만 합시다. Expression은 반드시 value가 되는 게 아니라 value가 될 수도 있고, 그냥 무한히 evaluation되는 경우도 있어요. 다음 시간에 이어서 나가겠습니다.

01:08:25 화자 2
[[unclear]]
````
